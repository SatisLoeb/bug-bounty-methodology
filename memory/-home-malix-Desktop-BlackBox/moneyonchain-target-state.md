---
name: moneyonchain-target-state
description: "Money on Chain / RIF on Chain (Immunefi, Rootstock) — intake+threat-model state, seams, gates, routing"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1b59875a-6245-4df4-8481-5dbfa8461a7f
  modified: 2026-08-21T09:03:54.274Z
---

Money on Chain / RIF on Chain — Immunefi, **Rootstock mainnet** (chainId 30), dual-token stablecoin (MoC=RBTC legacy single-collateral + ROC=RIF multi-collateral, shared MoCState/MOC/oracle). **Max $10k · KYC · 10-day-executed recevability rule** (le gate dominant). Scope+dossier: `~/Desktop/BlackBox/moneyonchain/{scope.md,TARGET-DOSSIER.md}` (53 contrats adresses complètes extraites 2026-08-21).

**Intake 2026-08-21 = CONDITIONAL GO (fresh surface only, invfuzz-first).** Saturation SPLIT : legacy MoC core (#1–8) HIGH/dup-dense → RE-SOURCE away ; surface 2026 = payable (ROC multi-collateral #14–22, fee-flow reverse-auctions #40–47, migration oracle UniswapV3 #10, core-v2 repo added 2026-07-08).

**Artefacts reachability EXÉCUTÉS (cast, Rootstock) :** `MoCState.getMoCPriceProvider()` == UniswapV3OracleUSD #10 (`0x1168…86ECBA`) → **Seam-1 wiring CONFIRMÉ** ; `getMoCPrice()`=6.635e16 (~$0.066 live) ; `globalCoverage()`=6.99e18 (~699%, solvable) ; BTC provider == #9 CoinPairPrice.

**Seam #1 (oracle #10) — invfuzz EXÉCUTÉ 2026-08-21 = NULL-COÛTEUX mesuré** (registre `moneyonchain/INVFUZZ-RESULT.md`). 5 chemins de vol tués par artefact cast/source : (1) spot-manip RÉFUTÉ par source — `getSqrtTwapX96` appelle `pool.observe([120,0])`, vrai TWAP 120s, `require(twap>0)`, PAS de slot0 ; (2) freeze via observe-OLD MORT — cardinality 2500, observe([3600,0]) OK ; (3) diff math (token-index confusion + neg-tick rounding manquant) = correct-pour-config déployée, ≤0.01% = informational ; (4) MOC ≠ collatéral → getMoCPrice ne nourrit que fee-discounts + floors reverse-auction (borné) ; (5) **reverse-auctions #42-47 = circular-oracle slippage-floor** (triggerOrders permissionless, floor = même pool WRBTC/MOC via priceProvider) mais NON-PROFITABLE : floor ancré 1h-TWAP sur les auctions financées (#42/#43), slippage 3-5%, notional capé maxAmountToSwap ~$766-1320, extraction ≤$23-40/trigger < coût manip ; seul instance short-TWAP (#47 oracle#10 120s+5%) a balance 0 ; isValid discardé mais prix jamais 0.

**Seam #4 (governance) — power EXÉCUTÉ 2026-08-21 = NULL-COÛTEUX mesuré** (registre `moneyonchain/GOV-RESULT.md`). OMoC gov = VotingMachine #51 (owns Governor #11, `executeChange` onlyOwner→owner==VM, aucune bypass admin) ; vote weight = staked MOC lock full-balance 7j via `stakeAvailable` modifier (surplus=0 pendant lock → withdraw REVERT → pas de recycle/flash-multiply) ; double-vote incremental (revert) ; flash-loan cassé par lock+DelayMachine ; denom = MOC.totalSupply() live non-shrink ; params déployés sains (5/20/30/50%, 7j, minStake 100k) PAS misconfig ; quorum=20%×198M=~$2.6M via pool $180k = 51%-attack OOS+infaisable. 7 chemins tués par artefact source+cast. Résidu = unregister prevote-grief (Low, non poursuivi) + proxy-upgrade (Admin #13, admin-OOS).

**FAN-OUT COMPLET 9-agents Fable-5 2026-08-21 (synthèse `moneyonchain/FANOUT-SYNTHESIS.md` + résultats A1-A9) = MEASURED-FORTRESS sur toute la surface SC, 0 Critical/High submittable.** Chaque surface tuée par artefact cast/source : A1 buckets ROC (mint/redeem symétrique, coverage RIF 1052%/DOC 100%, rebalancer dormant liqEnabled=false), A2 queues (FIFO, exec-price+bound, exec-fee→recipient fixe), A3 tokens (mint/burn authz→contrats protocole, 0 EOA), A4 splitters (split() → recipients gouvernance, ~sub-$500 dust, 0 event 10j), A5 rewards+coiner (arbre fixe, dispose gated), A7 staking/delay/vesting (id∈owner, share-math floor, solvency OK), A8 legacy MoC v1 (floors→protocole, coverage 707%, dup-fortress), A9 price-providers (collat = publication/Medianizer PAS DEX → S-A MORT ; upgrade gouvernance-gated). **CONVERGENCE : tous les résidus Critical (rebalancer-slippage A1, staleness A2, freshness-mask A9) réduisaient à "unprivileged peut-il manipuler l'oracle OMoC ?" → A6 a prouvé NON (seat=250k MOC + publication sig-majorité ≥6/10 → Sybil/51%=OOS) → surface Critical CLOSE.**

**Medium oracle reward≠stake = TUÉ by-design (spec-check workflow 5-agents, HIGH conf, DONT_SUBMIT).** 4 sources first-party concordantes : OMoC README "distributed proportionally to the POINTS collected", moneyonchain.com "payment proportional to their collected points / Rewards = Points Won / Total Points" = exactement l'impl `points_i·pot/totalPoints` ; stake documenté = ticket d'entrée + siège-sélection + FRÉQUENCE-de-tour ("larger stake → elected more often"), JAMAIS base de reward ; split lexical délibéré (Supporters="according to stake" vs Oracles="proportional to points") ; RoundManager:398 "// Distribute according to points/TotalPoints ratio" ; SMOKING GUN = variante stake-cappée `TasksRunner._distributeRewards` existe mais NON-déployée pour le price-oracle → intentionnel. Impl==spec, 0 déviation ; le spread 0.00×–4.69× = conséquence voulue de récompenser l'uptime. Soumettre = perte de réputation (by-design). Le libellé Immunefi Medium paraphrase le design voulu, pas une déviation ; pas d'exploit non-Sybil.

**VERDICT FINAL 2026-08-21 : Money on Chain / RIF on Chain = MEASURED-FORTRESS, 0 submittable (Critical+High+Medium tous morts par artefacts exécutés + doc-check). CLOSE → RE-SOURCE.** Résidu documenté non-submittable = freshness-mask A9×A2 (`getLastPublicationBlock≡block.number` système, pas de trigger unprivileged ; monitor = deploy câblant un feed collat AMM ou flippant useMaxLastPublicationBlock). Réouvrir si : nouveau marché/bucket ROC, feed collat AMM-based, drift post-audit, ou activation micro-liquidation (isMicroLiquidationAvailable=true). Voir [[deployed-code-not-head]], [[rootstock-flyover-target-state]] (autre programme). Fable-5 fan-out = 9 agents tous artifact-backed+disciplinés, 1 intervention (steer A6 linchpin) ; spec-check = workflow 5-agents adversarial-judge. Coût total ~1.5M tokens subagents.
