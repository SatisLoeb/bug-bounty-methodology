---
name: horizen-target-state
description: Horizen ZEN staking (Immunefi horizen) — BOTH scopes measured near-fortress, 0 payable (Web + SC done)
metadata: 
  node_type: memory
  type: project
  originSessionId: bc465f84-7337-4d09-aead-c6f01b71fc91
  modified: 2026-08-22T10:01:55.205Z
---

Horizen ZEN staking — Immunefi `horizen`. Fresh program (live 15-Jul-2026), **$101k paid** (actively rewarding — but almost certainly SC-side). TWO scopes: Smart-Contract + Web&App. I audited **WEB&App ONLY** (operator scoped it; SC not done).

2026-08-22: fan-out 6 agents (ran on **claude-opus-4-8** — sec-content downgraded from the opus-5 pin, cf [[subagent-model-routing]]), whole web surface. **Near-fortress, 0 payable.**

Architecture: Next.js 16 STATIC EXPORT (output:"export", NO server runtime → server-side web impacts N/A: no SSRF/middleware/RSC), React 19, ethers v6, MetaMask. Config baked at build from Cloudflare Pages env (no runtime override): CONTRACT_STAKER=0x6BF7CF29a8bcE11Aa62Cf593d165C244fA4d3E31 (deployed ZenStaker = ScopeLift/UniStaker fork + StakerPermitAndStake), CONTRACT_TOKEN=0xb06EC4ce262D8dbDc24Fac87479A49A7DC4cFb87, RPC=caldera testnet, SUBGRAPH=goldsky, CHAIN_ID=2651420. RewardAccumulator=0x06f5555fee73EDdc385b6d76FE00DB2D96ccDaE8. Frontend src: HorizenOfficial/staker-services frontend/ @ pinned a404746a693adca207c8c31f3e21fc3762fbb7b8. Subgraph mapping: HorizenOfficial/staker/subgraphs @ ab92502e9da98784dfe3bd3ef933d4e9345ff628 (= the scope's `...9345ff628`; FRESH/UNAUDITED — the 7 staker audits [ToB/C4rena/Cantina/Sherlock×2/Offbeat×2] cover Solidity only).

6 veins measured-null (executed + cross-corroborated):
- **Wallet-tx integrity**: address-substitution CLOSED (baked==deployed-bundle==real ZenStaker); chain-id GATED (isCorrectChain===CONFIG.chainId, write UI blocked off-chain, wrong-chain reverts). Deposit-id seam (PR #27/#28 churn) = subgraph-influenceable BUT every AMOUNT from on-chain getDepositsInfo (subgraph stakedAmount/earningPower discarded); a foreign depositId REVERTS ("not owner", executed eth_call) via deployed Staker.sol `_revertIfNotDepositOwner` + claimReward claimer/owner-gate; injecting a foreign id needs OOS Goldsky compromise → griefing/display-spoof NOT theft. approve = EXACT amount to baked staker (not infinite).
- **Config-substitution (the Critical)**: NO runtime override — grep executed: no URLSearchParams / location.search|hash / window.name / postMessage / document.referrer anywhere; static-export literal in deployed bundle. Airtight (Agents 1/5/6 independently).
- **Signature-phishing**: dApp requests ZERO signatures. permitAndStake in ABI but DEAD CODE (ZEN is a LayerZero OFT w/o EIP-2612 permit → always plain approve); signTypedData/personal_sign in bundle = ethers v6 vendor internals not app calls. delegatee=self (v1 not surfaced), surrogate non-voting (N/A).
- **DOM injection**: zero dangerouslySetInnerHTML/innerHTML in app (pinned+bundle); subgraph schema all numeric/hex (only String fields = Deposit.id[decimal] + Activity.kind[hardcoded literal]); no free-form string on-chain (all 6 events address/uint256-only); explorer hrefs trusted-base-prefixed (data lands after https://).
- **Subgraph-drift**: amounts on-chain (subgraph discarded); id-set has chain-log fallback (fetchDepositIdsFromChain) + owner-gate; id-collision impossible (contract-monotonic depositId).
- **Subdomain/secrets**: 43 subdomains (certspotter; crt.sh down) all live/claimed incl. crown-jewel Cloudflare Pages; no source maps; only public NEXT_PUBLIC config; strong CSP (frame-ancestors none/object-src none; script-src 'unsafe-inline' but NO sink = OOS missing-header-w/o-impact).

Non-payable residuals: useDeposits skips the owners field from getDepositsInfo (cosmetic display-spoof of another deposit, backstopped by revert); CSP unsafe-inline (no sink); a display-spoof via OOS indexer.

VERDICT: WEB scope = RE-SOURCE (near-fortress; minimal well-built static dApp — the code is defensively designed: deposit-id churn defense, chain-id gating, exact-approve, no-signature design). Reopen triggers: dApp surfaces DELEGATION or re-enables permitAndStake (both staged in the ABI already) → re-run signature/EIP-712-domain analysis; or a new dangling *.horizen.io CNAME.

**HIGHER-EV PIVOT (not done — operator scoped web-only):** the SC scope is where the $101k + real surface is. RewardAccumulator (0x06f5555f…) is NEW code (NOT in the 7 UniStaker audits); ZenStaker's DelegationSurrogate + reward-accumulation flow are Horizen-specific modifications on top of the audited UniStaker base. That's the payable target if pursuing Horizen — a separate SC engagement.

**SC SCOPE — DONE 2026-08-22 = MEASURED NEAR-FORTRESS, 0 clean payable** (fan-out 15 agents, ALL verified `claude-opus-5` 584/584 model-records zero-fallback — auth-header held on Opus-5 via Workflow-tool inheritance, cf [[authz-framing-subagent-prompts]]; workspace scratchpad staker/ @ ab92502). Differential PROVEN: `Staker.sol` byte-identical to withtally/staker v1.0.1 (b5b6f98) EXCEPT 2 events → `indexed` (owner). All extensions/notifiers/surrogates/IdentityEPC identical. BinaryEligibilityEPC diverges but NOT deployed. Deployed config (broadcast + live RPC): ZEN-on-ZEN (stake==reward==0xb06EC4ce), IdentityEPC wired (earningPower==balance, no oracle), maxBumpTip=0, MAX_CLAIM_FEE=0/claimFee=0, RewardAccumulator timeWindow=30d==REWARD_DURATION & whitelistEnabled=FALSE (all 3 entrypoints permissionless). Live admin/owner = Safe 0x1Afb144aaD0aE02f3Bb04C1eae4AC6020a727A21 (rotated AGAIN post-broadcast, off 0x9B264B21).

Killed with EXECUTED artifacts (forge PoCs + on-chain reads), every candidate REFUTED/DUP/OOS: (D2) same-token safe by SURROGATE-ISOLATION — principal lives in per-delegatee surrogates, Staker's own balance = reward-only, rewards explicit-notify not balanceOf-derived → immune to the yeet same-token cluster (#41521/#42723/#41831); (D3) empty-pool stranding REAL & reproduced (250/1000 ZEN) BUT **DUP: Cantina 3.1.3 + SECURITY.md item 7 declared known-issue** = the alchemix#31141/zerolend#29047 pattern, pre-excluded; dust-grief flush = dust-ESCAPABLE (anyone tops to 2,592,000 wei) not a brick; forced-InsufficientRewardBalance freeze REFUTED; precision-theft REFUTED numerically (scaled 1e36 value preserved, C4-QA[11] dup); (D1) reentrant double-flush needs a token HOOK, ZEN=plain OFT no hook → dead; grid-snap 1s-apart double-notify OOS; (D5) **entire StakerOnBehalf EIP-712 surface UNDEPLOYED — ZenStaker inherits only (Staker, StakerPermitAndStake), NOT StakerOnBehalf** (confirmed 3 ways incl compiled ABI 46 fns, none *OnBehalf); permitAndStake dummy-permit = SECURITY.md item 4 verbatim + ZEN no EIP-2612; L-9/Cantina-3.3.1 dup+undeployed; multicall pseudo-onBehalf REFUTED (OZ Multicall delegatecalls self, no ERC2771); (D6) bumpEarningPower UNGATED but INERT (IdentityEPC always reverts Unqualified) + maxBumpTip=0; claim-fee skim impossible (MAX_CLAIM_FEE=0). H-1/H-2/L-9 Sherlock fixes all PRESENT at pinned commit.

**SECURITY.md = 8 pre-declared known-behaviors** that pre-exclude nearly every avenue (item1 permissionless-accumulator, item2 Phase1-config, item3 admin-Safe, item4 permit-swallow, item5 same-block-zero, item6 delegation-bookkeeping, item7 empty-pool-stranding, item8 indexed-event) — item1 carve-out KEEPS in-scope only: loss/incorrect-attribution of principal-or-rewards, permanent bricking, extraction>contributed, theft — fan-out found NONE surviving. Only residual = deployer EOA 0x9B264B21 retains isRewardNotifier=true post-rotation (likely INTENTIONAL 2nd notifier per deploy-script REWARD_NOTIFIER_ADDRESS; not attacker-reachable = Informational). VERDICT SC = RE-SOURCE. Reopen on: mainnet deploy (chain 26514, re-pin commit) w/ non-Phase1 config (whitelist logic, non-identity EPC, StakerOnBehalf wired, maxBumpTip>0), or a RewardAccumulator variant with a sweep/rescue or timeWindow≠REWARD_DURATION.

UPSHIFT2 SEAM-HUNT (2026-08-22, web-only per operator): Phase-0 seam statement = the two highest-value upshift2 seams are STRUCTURALLY ABSENT (no backend/static export → no off-chain→on-chain orchestration [F1]; no server authz → no product↔authz/BOLA [web2]; all authz on-chain=SC). Pass-4 chain-construction (the pass the discipline fan-out can't do) drilled the un-swept residual and killed all 5 cross-discipline chains with executed artifacts (display-spoof→action = owner-gate + no fn emits owner≠msg.sender + no URL/postMessage sink; wallet-RPC re-point = chainParams all baked; tx-effect = surrogate identical-bytecode/stakeMore preserves config/claimReward pays msg.sender; mirror-invariant = getDepositorFullSummary re-checks owner on-chain). VERDICT: MEASURED NO-WEB-SEAM (seam-verified, not felt). Only theft-capable vector = generic malicious-clone (report-only-unrated, no live lookalike found, inherent to any client-side dApp — not submittable as a code defect). Workspace: ~/Desktop/BUGS/horizen-zen-staking-web-audit/. Confirms RE-SOURCE-web; the real seam is SC (operator's other terminal).
