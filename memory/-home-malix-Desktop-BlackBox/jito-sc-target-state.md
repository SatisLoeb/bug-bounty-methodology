---
name: jito-sc-target-state
description: "Jito Immunefi Smart-Contract side — interceptor mesuré-clos (F1 executed-kill), restaking drift green-lightable, mev picked-clean."
metadata: 
  node_type: memory
  type: project
  originSessionId: b9d8aaf3-0cf4-4fb9-8c66-a824e509a28b
  modified: 2026-08-12T08:03:54.753Z
---

Jito bug bounty (Immunefi) **Smart Contract** category (distinct de la vue Blockchain/DLT = [[jito-firebam-target-state]]). Impacts : Critical vol-de-fonds/freeze permanent, High vol-de-revenue/yield, Medium griefing. Mêmes exclusions (BAM/tiers-trusted discrétionnaire, misconfig, unit-test-only, adresse privilégiée sans escalade, best-practice **hors scope**) ; oracle-manip/flash-loan EN scope. 6 assets / 3 codebases. Évalué 2026-08-12.

**Allocation mesurée** (clones blobless dans /home/malix/Desktop/BlackBox/jito-*) :
- **Restaking** (vault_program 2999 / vault_core 5200 / restaking_program 1717 / restaking_core 1512 = ~11.4k lignes) — **forteresse, MESURÉ-CLOS 0 payable** (workflow w0dslah7f, 7 veines adversariales). 3 audits (Ottersec Oct-2024, Offside Nov-2024, Certora v1+**v2 = review manuel** pas formel malgré le titre ; baseline audit ecbe19a→fix `3fdcd88`). Certora v2 findings TOUS clos : H-01 zero-supply/inflation→virtual shares (PR150), H-02 over-unstaking→crank tracking (PR163), M-01 token-prog validation (PR175), **M-02 Token2022 = WON'T FIX** (mais gardé : `load_token_mint` pin `owner==spl_token` à init + chaque entrée ⇒ fee-on-transfer/rebasing rejeté, RV6 tué), M-03 fee-bounds, L-01/L-02. **Drift post-audit `3fdcd88`→HEAD = 2 changements logiques seulement** (+ rename spl_token→spl_token_interface, msg!, const-extract) : (a) fee-waiver `calculate_burn_summary` si staker==fee_wallet (value-neutral, non-atteignable non-priv : enqueue pin `load_signer(staker)`+`ticket.staker=staker.key`, out routé vers `ATA(ticket.staker)`) ; (b) hardening cross-vault ticket `VaultStakerWithdrawalTicket::load` (nouvelle vérif, pas un affaiblissement). 7 veines tuées, cité. **2 résidus freeze/liveness réglés** : Résidu B (min(2) vs gap >2 epochs → freeze VRT) CLOS par vérif statique d'exhaustivité du gate `check_update_state_ok` (tous les mutateurs de buckets VRT/delegation_state gated ⇒ 0 accumulation pendant un gap ⇒ min(2) n'orpheline rien) ; Résidu A (reserve under-count fee-waiver) borné au ticket du fee-wallet, self-heal. Pas de PoC nécessaire.
- **Stake Deposit Interceptor** — 2656 lignes, **native** (validation de comptes MANUELLE, jito_bytemuck), 1 seul audit étroit (Certora coinbase v0.3), déployé `5TAiuAh3...` (TVL cbSOL). = LEAD, **audité (workflow w8m180w8p, 6 hypothèses adversariales).**
- **mev-programs** (tip-payment/tip-distribution/priority-fee) — le plus vieux/scruté, 0 surface fraîche. **Skip.**

**INTERCEPTOR = MESURÉ-CLOS, 0 payable.** IH2-IH6 bien gardés (cités) : change-owner exige stored-owner signer + re-derivation PDA (`:416-436`) ; deposit `lst_amount`=delta vault mesuré (`:325-354`) ; hopper drain authority-gated net-zéro (`:775-912`) ; whitelist program-id lu depuis la SDSA PDA-validée + `contains(signer)` (`:650-660`) ; init/update authority-gated, discriminateur+PDA double-bloquent la type-confusion. Fee-math défensive (u128 checked, div_ceil, cappée).

**F1 = seul candidat, EXECUTED-KILL.** `process_claim_pool_tokens` ne valide PAS `token_program` (`processor.rs:459`) alors que `init` le fait (`:98`) → CPI arbitraire avec la SDSA PDA en signer propagé (`transfer_tokens_cpi:1258`). Classe canonique (sealevel #5). **Réfuté par PoC exécuté** (3 prog BPF, validator 4.0.1 local) : voir [[solana-cpi-frame-scoped-resolution]] — résolution de programme CPI **frame-scoped**, le token_program malveillant reçoit `[vault,mint,dest,SDSA(signer)]` (aucun exécutable) ⇒ ne peut PAS atteindre le vrai SPL-Token ⇒ signature propagée inerte ⇒ **pas de drain**. F1 = defense-in-depth/QA, **hors scope** (best-practice exclu). Non payable, non reportable ici.

**Leçon** : ne pas concéder ni sur-claim un CPI-arbitraire Solana sans exécuter la résolution frame-scoped ([[measure-before-asserting-in-reports]], [[recevability-gate-before-poc]]).

**HAND-TRACE thief-mode (2026-08-12, sans hypothèse, après feedback opérateur « lis comme un voleur pas comme un roman ») confirme le mesuré-clos des deux programmes.** Faits structurels tirés du trace (pas dans les résumés d'audit) : (1) **le slashing est INERTE** — `delegation_state.slash()` n'est appelé NULLE PART, aucune instruction de slash dans le vault program ; les slasher-tickets sont des comptes de relation opt-in sans exécution ⇒ toute la classe « slash vole le principal du staker » est morte, `tokens_deposited` ne baisse que par `burn` légitime. (2) Invariant prouvé **`additional_assets_need_unstaking ≤ Σ staked`** (car `amount_requested ≤ tokens_deposited` et `available = tokens_deposited − staked`) ⇒ `close` toujours complétable ⇒ pas de freeze permanent. Custody = vault ATA (in=`mint_to` classic-SPL-pinned + gardes self-deposit ligne 87 / rounding-donation ligne 97 ; out=`burn` borné `tokens_deposited−total_security` + `delegate_token_account` rejette supported_mint) et ticket-ATAs (recorded==transferred à enqueue, donation→program fee). `restaking_program` = gestion de relation SANS custody, admin-gated ; seuls handlers token = `operator/ncn_delegate_token_account` (SPL approve sur les propres tokens de l'op/ncn, admin-gated ; note : manque la garde supported_mint du vault mais inoffensif, pas de custody staker). SlotToggle = warmup/cooldown 2-epochs propre. **Surface fund-theft ÉPUISÉE pour un chasseur solo à ce HEAD.**
