---
name: royco-target-state
description: Royco (Immunefi $250k) — surface 1 (Concrete async vault) measured-closed; surface 2 (live tranche waterfall) under audit.
metadata: 
  node_type: memory
  type: project
  originSessionId: ea902d31-6f05-473c-8d8a-d124b6463c66
  modified: 2026-08-12T06:36:00.568Z
---

Royco = Immunefi bug bounty, max $250k, KYC+PoC. Non-custodial yield tranching (Senior/Junior/SLP). Local scope extract: `/home/malix/Downloads/foundry-v2-royco-cef721c0b1097d1b` (Instascope v2, per-chain `chains/<slug>/`). RPC egress works from BlackBox (publicnode for state, drpc for ≤10k-block getLogs, blockscout keyless v2 for source/logs). Foundry+solc present; vault compiles under `FOUNDRY_PROFILE=contract_ConcreteAsyncVaultImpl_1b5c`.

**TWO surfaces, not one.**

**Surface 1 — srRoyUSDC `0xcD9f` = a Concrete Earn V2 async ERC-4626 vault (Blueprint Finance), NOT bespoke Royco.** ~$11M. Its `factory()` = `0x0265d73a…` (a Concrete factory), NOT RoycoFactory. Cached-accounting (donation/inflation neutralized). Only the Makina strategy `0xc5Fe` is registered; MultisigStrategy `0xd3F8` is NOT attached (its `totalAllocatedValue()` reverts stale — landmine pointed at nothing). **Measured-CLOSED 2026-08-12:** 20-agent fan-out (0 confirmed-payable of 13) + my own executed proof that async epoch settlement conserves at price≠1 (gain 1.062 & loss 0.754, per-user claim == exact floor, total ≤ reserve, 3-wei residual dust) and double-claim reverts. Non-payable residue only: the `IStrategyTemplate` "MUST NOT revert" violation (`BaseStrategy.totalAllocatedValue` has `whenNotPaused`) — real but unreachable (MS detached) + admin-recoverable via `toggleStrategyStatus` (no yield accrual); and the dead `performanceFeeHighWaterMark` slot (fee ratchet, dust magnitude, to a whitelisted splitter → OOS). See [[measure-before-asserting-in-reports]], [[deployed-code-not-head]].

**Surface 2 — the ST/JT waterfall IS deployed (I first mis-called this "no market" — a drpc scan silently rate-limit-failed; blockscout is authoritative).** RoycoFactory `0x7cC6` (OZ AccessManagerUpgradeable) emitted `MarketDeployed` (topic `0x4e3cae2d`) at **block 25,624,357**: ST=`0xc6f9a91e…`, JT=`0x91f00dee…`, Kernel=`0x48fe2f6a…` (`Identical_ERC20_ST_JT_ChainlinkToAdminOracle_Kernel`), Accountant=`0xeb8ce87d…`, underlying=`0x827Ce7E8…` (Morini StockMarketTRBasisTrade vault, 18dec). Verified source pulled to `scratchpad/royco-market/`. Live: ~$850k, ST/JT balanced ~4.37e23 each, coverage OK, PERPETUAL, self-liq bonus 0.25%. Master invariant = **NAV conservation** `stRawNAV+jtRawNAV == stEffectiveNAV+jtEffectiveNAV`. Theft-shaped veins: ST self-liquidation bonus (JT→ST transfer, utilization-neutral cap), NAV-attribution floor-rounding (residual→JT), share-pricing boundaries (totalSupply/totalAssets==0), coverage math (`maxJTWithdrawalGivenCoverage`). Recevability nuance: tranche deposit/redeem are `restricted` (LP whitelist) — LP-vs-LP mechanism theft = payable USER theft; admin/oracle/MEV = OOS. **Measured-CLOSED 2026-08-12, 0 payable** (5-finder waterfall fan-out `w5arfgeul` + my analytical closure). Key kills: NAV conservation asserted on both sync paths (`RoycoAccountant:232,586`); every inter-tranche conversion Floors against the mover (`IdenticalAssetsOracleQuoter:163,168`, `UtilsLib.scaleAssetClaims`); coverage Ceil-rounded + enforced post-ST-deposit/post-JT-redeem. **Self-liq bonus (the prime JT→ST vein) is HARD-CAPPED at `desiredBonusNAV = 0.25%·redeemedNAV` (`RoycoKernel:842,851` `min(min(desired,jtEff),maxUtilNeutral)`) — any error in the utilization-neutral cap can only SHRINK the bonus, never inflate it; and it's dormant (needs utilization≥liquidationUtilization>WAD, i.e. a real oracle-driven loss, non-summonable).** Only non-payable residue: ~1-2 wei/sync attribution dust routed to JT (symmetric, non-amplifiable — checkpoints reset delta to 0 each op). Fork-PoC of utilization-neutrality remains available if execution-grade certainty wanted, but the hard-cap makes it low-value.

**Reopen triggers:** new `MarketDeployed` on `0x7cC6` (ETH/ARB/AVAX); a 2nd strategy attached to srRoyUSDC or `0xd3F8` becoming registered; Concrete vault impl upgrade; kernel `setConversionRate`/YDM/oracle change.
