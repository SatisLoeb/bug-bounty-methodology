---
name: olas-autonolas-target-state
description: "Olas/Autonolas Immunefi target — scope, economics, and marketplace money-path measured-clean"
metadata: 
  node_type: memory
  type: project
  originSessionId: cc2cefa9-e9ba-4bcd-b45f-c8437d50f007
  modified: 2026-08-21T08:30:44.627Z
---

Olas (Autonolas) Immunefi — https://immunefi.com/bug-bounty/autonolas/scope/ (payload updatedDate 2026-08-20). Solidity SC only, 8 chains, **max $5k** (Critical=10% funds-at-risk capped $5k; High≤$2k; Medium=$1k; Low OOS). PoC-code required, KYC L3. Repos: valory-xyz/autonolas-{governance,registries,tokenomics,marketplace}. Full scope dump: ~/Desktop/BlackBox/OLAS-SCOPE.md + olas_scope.json.

**Recevability gates (kill dups FIRST):** 4 living `Vulnerabilities_list_*.md` (95 known issues total: gov 24, reg 23, tok 27, mkt 21) + everything in audits/docs/test/ + code COMMENTS pinned at tags (gov v1.2.5, reg v1.3.3, tok v1.4.3, mkt v0.4.2) + anything visible in ANY public commit/PR/branch at submit time. Deploy anchor: read the Olas custom proxy slot keccak256("PROXY_...") NOT EIP-1967 (that returns 0). Deployed impls == scope-listed impls (verified on ETH).

**Carve-outs:** StakingNativeToken permanently OOS (all chains). SubscriptionProvider OOS. 4 Wormhole families OOS (Celo→OP-stack). UniswapPriceOracle Celo deploy OOS. registries-family (18 contracts) OOS for "permanent freezing" AND "griefing". Deep note: [[wsb-sweep-blackbox-outcome]]-style thoroughness.

**Marketplace money-path = MEASURED-CLEAN 2026-08-21 (0 payable).** Deployed==repo (git diff v0.4.2..HEAD on contracts/ EMPTY). Opus hand-audit + 8-agent Fable-5 fan-out (dup-checked, adversarially verified) both → 0 confirmed / 0 needs-PoC across: cross-path double-settle, Path-B sig/nonce, fee/solvency, subscription credit, reentrancy, freeze/grief, mech-lifecycle, Door-C invariant. Why conservative: mech credit ≤ requester debit always (deliverMarketplace L766 rate cap + Path-B EIP-712 sig binds rate/nonce/mech into requestId); settlement over-collateralized (undelivered overpay = stuck orphan = known #7 Low, not insolvency); one `_locked`/tracker + CEI (mapMechBalances zeroed before native `_withdraw`); no requester-withdraw exists but that's self-inflicted; gnosis-mech base = standard Zodiac Mech (empty payable receive() verified, `exec` operator-gated, can't reach others' funds); paymentType hardcoded-immutable per impl (no tracker hijack). Residuals (non-payable): shared mapNonces cross-path footgun; native trackerBalance staleness = known #14.

 **POL LiquidityManagerETH = MEASURED-CLEAN 2026-08-21 (0 payable unprivileged).** Deployed impl 0x0171..75C1 (Sourcify exact_match, solc0.8.30, verified 2026-05-18) behind proxy 0x83Adc; audited THE DEPLOYED source (not HEAD; HEAD=+363L #26 fail-closed rewrite=public=dead). Opus hand-audit + 6-agent Fable-5 verified fan-out both 0-confirmed. DECISIVE: only permissionless state-changer = collectFees (burns OLAS / sends secondToken to immutable Treasury -> NEVER to caller; recipient hardcoded address(this); tokens[] pinned to real pair by getPool+positionId!=0; _locked spans all external calls; price-guard return DISCARDED so #26 fail-open irrelevant on permissionless path). Everything fund-moving (convertToV3/changeRanges/inc/decLiquidity/transfer*/changeImpl) = OwnerOnly. owner=0xeb2a22b2..EOA keeper (NOT Timelock; treasury=Timelock) => centralization-OOS; makes #16/#26 MEV-on-owner-ops more realistic but dead dup. Non-custodial (donations=self-loss). Proxy atomic-inits, impl no selfdestruct/delegatecall (no UUPS-impl takeover). RE-SOURCE: a release wiring a price return into a PERMISSIONLESS value transfer, or removing an owner gate on a minting path, or deploying the fresh v1.5.0 cross-check math.

 **Cross-chain staking bridge = MEASURED-CLEAN 2026-08-21 (0 payable unprivileged).** deposit-processors L1 + target-dispensers L2 (repo==deployed; OptimismTargetDispenserL2 Sourcify exact_match solc0.8.30; L2 dispensers non-proxy). Opus hand-audit + 6-agent Fable-5 verified fan-out both 0-confirmed. Bridge auth AIRTIGHT both directions: L1->L2 send = l1Dispenser-only; each leaf authenticates real L1 processor (OP msg.sender==l2Messenger && xDomainMessageSender()==l1DepositProcessor [resets to 0xdEaD outside relay]; Arbitrum msg.sender==l1Aliased [l1+0x1111..1111, bijection]; Gnosis AMB.messageSender()==l1DepositProcessor [no chainId=gov#7 but single-source ETH<->Gnosis=informational]; Polygon fxChild+rootSender==immutable l1DepositProcessor). L2->L1 sync owner-only + l2Dispenser check. OLAS exits ONLY to verifyInstanceAndGetEmissionsAmount-validated staking instances (emissions-capped) or owner(migrate/drain). redeem() permissionless but only completes _processData-queued legit deposits (queuedHashes single-site write, idempotent, chainid/addr-bound, no double-spend). THEFT ENDPOINT: createStakingInstance permissionless + implementationsCheck=false lets attacker stand up a drainable 'verified' instance -> but INERT: _processData targets come only from authed L1 msg or owner; reaching it needs a forge (blocked) or winning VoteWeighting gauge weight (veOLAS) = governance/Sybil/economics = OOS. Forge needs 3rd-party bridge compromise = OOS. Documented: #10 migrate-withheld, #11 _sendMessage, gov#7. RE-SOURCE: a release wiring an unprivileged L2 caller to supply a target list, or a multi-source AMB on Gnosis.

 **Governance = MEASURED-CLEAN 2026-08-21 (0 payable unprivileged).** veOLAS/wveOLAS/VoteWeighting/GovernorOLAS/Timelock/cross-chain gov exec. Opus hand-audit + 6-agent Fable-5 verified fan-out both 0-confirmed. CROWN-JEWEL vote-manip CLOSED: GovernorOLAS=stock OZ 4.8.3, token()=wveOLAS (guarded wrapper); wveOLAS.getPastVotes/balanceOfAt return 0 if blockNumber<account first-USER-point (veOLAS writes user points with UNDETACHED {ts,block}; the #3 week-boundary detachment is SUPPLY-points-only, so guard fully closes the inflation branch); getPastTotalSupply passthrough but its current-block interp term cancels in votes/quorum ratio -> no quorum-deflation. veOLAS bias<=amount (MAXTIME cap); non-transferable/non-delegatable; flash-lock blocked (min 1wk > votingPeriod 2.7d, past snapshot). VoteWeighting deployed-OLD delta vs HEAD (git show a22490e) = EXACTLY documented #8/#11/#18/#19/#20 (redeploy-fixed=OOS) -> 0 undocumented residual. Cross-chain gov receivers (OptimismMessenger/HomeMediator/FxGovernorTunnel) dual-gate auth (msg.sender==immutable relayer + xDomain/messageSender/rootSender==Timelock) = sound, mirror dispensers; arbitrary-exec only on Timelock-authored msg. OLAS.mint minter-only (#12), Burner self-held-only, buOLAS zero voting power+unreferenced. RE-SOURCE: only residue = #3-detachment quorum approx (documented, no attacker trigger).

 **Registries = MEASURED-CLEAN 2026-08-21 (0 payable unprivileged).** ComponentRegistry/AgentRegistry/ServiceRegistry(+L2)/ServiceRegistryTokenUtility/ServiceManager/RegistriesManager/RecoveryModule/multisig-helpers + staking (StakingFactory/Token/Base/Verifier). Opus hand-audit + 7-agent Fable-5 verified fan-out both 0-confirmed. DECISIVE SCOPE: the 18-contract FAMILY is OOS for permanent-freezing AND griefing -> only theft/mint/alteration/insolvency payable; ALL 23 documented issues are Low/Info or OOS-class (#10 DoS, #23 takeover) => NO documented theft => theft was the open lane, and it's closed. Bonds: slash=multisig-gated funds->slashedFunds(not slasher); unbond/terminate=manager-gated, reentrancy-locked, refund capped-at-balance + zeroed, keyed (operator|serviceId<<160) collision-free; ERC20 deposit enforces balanceAfter-balanceBefore==amount (defeats fee-on-transfer credit-inflation); per-token pools (no cross-token drain). RegistriesManager.create permissionless=BY-DESIGN open registry; updateHash owner-gated (solmate ERC721). Staking: conservation balance==Σreward+availableRewards; claim/unstake owner-gated + CEI (reward zeroed/ServiceInfo deleted before payout); each StakingToken instance closed accounting (no shared pool). #23 mitigated in DEPLOYED bytecode by 1:1 mapMultisigServiceIds (MultisigAlreadyBound). DEPLOYED-ANCHOR NOTE: staking deployed=solc0.8.25 (Sourcify exact_match, StakingToken 0x0Dc2/StakingFactory 0xEBdd), SIMPLER than HEAD 0.8.30 (no custom-distributor); read deployed _claim/_unstake directly = conservation-sound. RE-SOURCE: new manager without _locked (re-opens #22), new custom-distributor/staking-token integration on redeploy.

**FULL COVERAGE 2026-08-21: 5/5 surfaces (marketplace + POL LM + cross-chain bridge + governance + registries incl. deployed-anchored staking) MEASURED-CLEAN for unprivileged attackers on deployed code. Olas FULLY EXHAUSTED solo.**

**Reopening triggers (if reopening):** tokenomics POL LiquidityManager (137 post-v1.4.3 commits, new V2/V3 cross-check math — BUT freshest code un-deployed, deployed impl = LiquidityManagerETH 0x0171..75C1, older); governance veOLAS/VoteWeighting/Dispenser (vote-manip Critical but VoteWeighting mid-redeploy known #8, veOLAS Curve-derived 6 view-fn known issues); cross-chain deposit-processors/target-dispensers (forged-msg theft, but trusted-bridge paths = centralization OOS). Given $5k ceiling + 4-audit density, ROI is low — see [[recevability-gate-before-poc]] / [[bounty-playbook-5-gates]] G5 materiality.
