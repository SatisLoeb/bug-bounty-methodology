---
name: decentraland-target-state
description: "DCL Immunefi — #87537 Critical PAYÉ (W1 scene-provenance bypass) ; SC both-chains + signatures-api mesuré-clos 0 payable ; Estate by-design ; next = business logic des autres services web2"
metadata: 
  node_type: memory
  type: project
  originSessionId: 208cc87d-b406-4917-a559-2619911748c4
  modified: 2026-08-12T14:30:43.775Z
---

**Programme :** Immunefi Decentraland, Smart Contract + Websites & Apps. Compte opérateur : `0x6a82c4832cc8f8633ba6b0dbc815676b1928e62d`.

## Le win payé — #87537 (Critical)
Bypass de provenance de scène **case-sensitive** dans `@dcl/platform-crypto-middleware` (`createPayload` replie le payload en minuscules, la garde compare l'en-tête verbatim). Escaladé Critical 2026-08-06. Fix DCL : migration de 5 services vers `@dcl/crypto-middleware` **5.1.0** + `trim().toLowerCase()` (+ `declaresSceneSigner` côté builder). **C'est le finding payant sur ce programme.**

## État W1/W2 (mesuré LIVE 2026-08-12)
Les 5 services (builder/worlds/marketplace/auth/signatures) **rejettent** le case-bypass (case-inversé → `Invalid chain metadata`). **signatures-api n'est PLUS le traînard** (contredit la note du 08-10, périmée) — fixé. Seul résidu 200 : le **repli légataire builder-server** (`get:/projects`, timestamp stale/absent/malformé → 200) = **lecture first-party**, fenêtre de replay trop longue, **NON payable** (déjà downgradé dans ma réponse).

## Estate finding — BY-DESIGN, ÉTAGÉ, NON soumis
"Estate sales V4 leave scene-write control with seller" (Medium, Controlled loss). PoC fork **vert** (block 25692307, deux tests). MAIS comportement **documenté** : PR #93 `decentraland/land` (2018, Nachomazzara « _doTransferFrom clean the updateOperator ») + doc permissions DCL (« revoke manually, cleared only on LAND transfer ») + subgraph officiel `LAND-permissions-graph` indexe les operators (donc pas caché). Situation Lombard → **passif, pas actif. NE PAS soumettre.** Docs de soumission dans `submissions/decentraland/estate-immunefi-*.md` (prêtes mais à ne pas envoyer).

## SC surface both-chains — thief-tracé, 0 payable (2026-08-12)
~24 contrats décortiqués flux-par-flux (mode voleur, sans hypothèse) :
- **Polygon** : CreditsManager · CouponManager · CollectionDiscountCoupon · Marketplace V4 (+ base/Signatures/Verifications/NativeMetaTx/EIP712/hashing/Aggregator) · Collection (+ custom-ERC721 = OZ 3.1.0 verbatim + ContextMixin/meta-tx) · CollectionStore · ThirdPartyRegistryV3 · RoyaltiesManager · RaritiesWithOracle · CollectionManager+FactoryV2 · Committee · ERC721Bid.
- **Ethereum** : Marketplace V4 ETH (sibling simple, `_modifyTrade` vide, pas de royalties) · Rentals (escrow signé, replay triple-index sound) · DCLController+DCLRegistrar (name reg, 100 MANA burn atomique) · Marketplace V1 · ERC721Bid ETH.
- Anomalies **non-payables** : coupon `allowedRoot`/`externalChecks` évalués contre le *marketplace* (fail-closed, feature cassée) · TPR "programmatic" self-declaration (slots inertes sans committee) · sham-trade credit→MANA (borné par le crédit signé DCL) · public `onERC721Received` (keyed msg.sender → self-scoped).
- Propriétés vérifiées : mint gardé (creator/minter + meta-tx safe signer==ecrecover+nonce+chainid), **pas de chemin de burn** wearable, provenance `isCollectionFromFactory` non-forgeable (minimal-proxy→impl fixe), committee membership `onlyOwner`, domaines EIP712 bind address+chainid (no cross-chain replay).

## signatures-api (signatures-server) — sound
W1 fixé. Logique propre : pas de SQLi (`getParameter` **throw** sur valeur non-enum ; reste paramétré `SQL`${}``, seul l'ORDER BY interpole du raw mais enum-borné) ; création liée **requester=lessor=signer=owner** (`lessorAddress=verification.auth` + `verifyRentalsListingSignature` + `areSameAddress(nft.owner, lessor)`) ; c'est un **index off-chain re-vérifié on-chain** par Rentals (traçé sound) → impact inhérent bas.

## ENGAGEMENT web2 « autres services » — marketplace-server MESURÉ-CLOS 0 payable (2026-08-12)
`dcl-web/marketplace-server` (20K LOC, package `template-server`) décortiqué en mode voleur. Surfaces à valeur réelle (non-index) toutes tenues :
- **Trades** (`ports/trades`) : index off-chain re-validé on-chain. `addTrade` lie requester=`trade.signer`=`verification.auth` (l.159) + EIP-712 full-field verify (l.178) + POST `/v1/trades` gardé (signed-fetch **required** + metadata `dcl:create-trade` + `TradeCreationSchema`). Tamper du stored → le fill on-chain échoue (self-DoS), pas de vol.
- **SQLi** : codebase durci `sql-template-strings` partout ; les 2 seuls builders raw-string (`getTradesForTypeQuery`/`WithFilters`, `.append(type)`) n'interpolent que des **enum hardcodés** — `WithFilters` a **0 caller**. ORDER BY toujours via switch-enum → clauses fixes. `buildOrderByClause`/rankings `orderBy`/`orderDirection` = littéraux serveur, jamais user. **Aucune SQLi vivante.**
- **Favorites/Lists ACL** (seul état off-chain pur, IDOR candidat) : **correctement gardé**. Permission centralisée dans `getListQuery` (owner OR default-list OR ACL-grant du perm requis) ; mutations owner-scoped (`WHERE user_address=$`) ou via `getList(requiredPermission:EDIT)` ; `createAccess` → `getList(considerDefaultList:false, no requiredPermission)` réduit à `user_address=me` ⇒ **seul l'owner grant** (pas de takeover) ; le `insertAccessQuery` non-scoped de `updateList` est rattrapé par `getListQuery` owner-scoped + rollback tx.
- **Transak** : `/orders/:id` gardé `userAddress===order.walletAddress` (pas d'IDOR) ; **Activity** : `getUserActivity(verification.auth)` = self-scoped.

**Seul lead réel = Wert signing-oracle (SOUS-PAYABLE, ne pas re-chasser).** `/v1/wert/sign` (`wert-signer-and-session-creator-handler.ts`) signe le **message entier** `{address, commodity, commodity_amount, sc_address, sc_input_data, network}` avec la clé partenaire DCL ; ne valide QUE `verification.auth===message.address===session.wallet_address` + `target∈enum` ; **aucune** validation de `sc_address`/`sc_input_data`/`commodity_amount`, aucun allowlist, route sans schema-middleware. Défait l'intégrité que la signature partenaire existe pour garantir. MAIS modèle Wert (docs vérifiées 2026-08-12) : **l'utilisateur finance sa propre fiat** → `commodity_amount` crypto → exécute `sc_input_data` ; on-chain permissionless re-enforce les prix. Impact plafonné = abus du compte partenaire Wert de DCL pour des appels fiat→on-chain arbitraires (ToS/compliance/réputation), **pas de vol, pas de perte user, pas de PII, pas de takedown** ⇒ Informational–Low sous rubrique W&A. Réélever SEULEMENT si preuve que DCL subventionne les frais OU que les 2 clés (DEFAULT vs PUBLICATION_FEES, client choisit `target`) portent des termes différents (fee-bypass cross-key).

## builder-server (29K LOC) — CŒUR business-logic mesuré-CLEAN (2026-08-12)
Modèle d'autz = `withAuthentication`(auth-chain signed-fetch) → `withModelExists` → `withModelAuthorization` (défaut `Ownable.isOwnedBy` = `count({id,eth_address})>0`, ou `checkOwnership` custom). Footgun `canUpsert = count({id})===0 || isOwner` (id inexistant ⇒ tout le monde) **contenu** : un nouvel item devient orphan owned-by-creator, toute association à une collection re-check l'ownership.
Seams à valeur réelle tous tenus :
- **Curation** (`Curation.router`) : approve/reject + assignee **committee-gated** (`isCommitteeMember`, l.343-373) ; reads/inserts via `validateAccessToCuration→service.hasAccess`. Routes en `withAuthentication` seul, autz **dans le handler**.
- **Item upsert** (`PUT /items/:idOrURN`, autz in-handler) : `upsertDCLItem` bloque add/move vers une collection non-possédée (`isDbItemCollectionOwner/Manager`, l.609/671) ; item-in-collection ⇒ own/manage la collection ; orphan ⇒ `isOwnedBy`. Items builder = drafts pré-publish (prix/rarity settle on-chain) ⇒ même un gap = défacement draft, Medium max.
- **TP paid-slot publish** (`publishTPCollection`) : route `withCollectionAuthorization` ; publisher = TP manager (l.180) ; `availableSlots` (on-chain-backed) ; **cheque signer lié au manager authed** (`signerAddress===getAddressFromSignature(cheque)`, l.217) ; consommation slots + mint = on-chain ThirdPartyRegistry, builder crée juste des ItemCuration PENDING (encore committee-gated). Pas de publish-sans-payer.
- **SlotUsageCheque** = EIP-712 `ConsumeSlots` (domain=ThirdPartyRegistry on-chain), salt-replay-guard, re-vérifié on-chain ⇒ index.

builder-server autres surfaces (Deployment owner-scoped, ThirdParty isManager-gated, virtual-TP squatting=Low griefing on-chain-supersedes) vérifiées.

## worlds-content-server (14K) — MESURÉ-CLEAN (2026-08-12)
Deploy (`POST /entities`) : pas de middleware auth, la garde est le **validator pipeline** — `validateAuthChain→validateSigner→validateEntityId→validateSignature` (canonical `@dcl/crypto Authenticator`, authChain crypto-vérifié) PUIS `createValidateDeploymentPermission` = signer **owns le world-name** (`dcl-name-checker.checkPermission` = résolution owner on-chain via `nameOwnership.findOwners`, lowercased, false si owner vide) OU a permission deployment sur toutes les parcels. `signer=authChain[0].payload` lié cryptographiquement ⇒ pas de forge. **Toutes** les mutations permissions (`postPermissions`/`put/deleteAddress`/`post/deleteParcels`/`access/communities`) passent par `checkOwnership`(owner du name) ; undeploy world+scene idem ; settings délégué au component (checkPermission, throw UnauthorizedError). Comms=access-gated, contributor=self-scoped read.

## auth-server (5K) — MESURÉ-CLEAN (2026-08-12)
Relais de login (socket `request`→`outcome`). Modèle = **requestId bearer-secret** (uuid v4) + **outcome vérifié client-side** + `rejectIfHasResponse` (first-wins, pas d'override) + expiration + code pairing 2-digits (UX, faible mais secondaire). Création request : `validateAuthChain` canonical `@dcl/crypto` + garde `isEphemeralMessage` (empêche un sign générique de minter une identité). Account-deletion (`DELETE /accounts`, irréversible) : auth en couches = origin-allowlist + DCL signed-fetch + Magic DID token **cross-checké à la même adresse** (AddressMismatch/Stale/Reused). Pas de suppression d'autrui.

## G3 LIEU-A — diff couture off-chain↔on-chain EXÉCUTÉ (2026-08-12, Protocole Chasse)
Diff ancré typehash serveur vs contrat déployé (`_a40b`). Le contrat `Asset` a 5 champs mais les typehashes signent en 2 formes : `sent`→`AssetWithoutBeneficiary` (beneficiary **NON signé**), `received`→`Asset` (beneficiary signé). ⇒ **UN seul champ agi-mais-non-signé : `sent[].beneficiary`** (destinataire de l'actif VENDU ; NatSpec = caller-set by design). Candidat High : si le serveur sert un `sent[].beneficiary` attaquant qu'un fill acheteur consomme aveuglément → NFT vole vers l'attaquant, acheteur paie. **TUÉ par artefact** : l'adaptateur `fromDbTradeAndDBTradeAssetWithValueListToTrade` mappe `sent` via `fromDBTradeAssetWithValueToTradeAsset` (SANS beneficiary) ; seul `received` porte le beneficiary. Tous les chemins fill-capables (`getTrade`, `getTradesByAddress`) strippent `sent[].beneficiary` ; la query browse l'expose mais OMET `t.signature` (pas fill-capable). ⇒ reachability-null ADOSSÉE À L'ARTEFACT. **Leçon Maxime-2** : mon raisonnement initial « la signature lie tout → self-DoS » était FAUX (la sig ne lie PAS sent.beneficiary) ; la sûreté vient de l'adaptateur une couche plus bas. Le null confortable masquait un vrai gap fermé ailleurs — exactement ce que le diff exécuté attrape.

## Wert oracle — reclassé HARNESS-SUSPENDU (pas « rouvrir si tu veux »)
La partie dure EST la porte (CP6 MULTI-REPO / G12 SUSPEND). **Sonde** : (a) lire le fill-client marketplace (decentraland-dapps, hors dcl-web) — quel `sc_address`/`sc_input_data`/`target` envoyé normalement → confirme la déviation arbitraire ; (b) CONFIG-SHAPE = les 2 clés DEFAULT vs PUBLICATION_FEES sont-elles le MÊME partner Wert ou des termes/subvention différents (events on-chain de settlement + config partner). **Capacité manquante** : accès frontend marketplace + dashboard partner Wert (out-of-band). **Déclencheur reprise** : obtenir l'un des deux ; termes différents cross-key = fee-bypass payable, sinon Info-Low confirmé. NE PAS soumettre avant sonde résolue.

## VERDICT SWEEP web2 (2026-08-12) — 0 payable, 1 couture artefact-fermée, 1 suspendu
DCL W&A = codebase mature, autz consistamment enforced. Couture off-chain↔on-chain trades = **null adossé-artefact** (pas raisonnement). Wert = **SUSPENDU** (sonde nommée). TP-name squatting = Low griefing. #87537 Critical (payé) reste LE win.

## 3 coutures passées au diff-ancré EXÉCUTÉ (workflow 6-agents + inline, 2026-08-12)
- **worlds-name ↔ ownership on-chain = NULL adossé-bytecode.** Le null reposait sur UN fait hors-repo non-exécuté (registrar lowercase le label) que le commentaire du repo CONTREDIT (`name-ownership.ts:202-207` « DCL owners are case-sensitive »). Si le registrar était case-SENSITIVE : `name-ownership.ts:222` `[0]` first-match + `orderBy:name` (ASCII G=71<g=103) = **takeover Critical de world** (registre `Genesis` ~qq MANA → deviens l'owner résolu de `genesis`). TRANCHÉ par la source déployée `DCLRegistrar_2a18/DCLRegistrar.sol:1532-1534` : `keccak256(_toLowerCase(_subdomain))` + `require(_available(...), "Subdomain already owned")` ⇒ collision case-insensitive ⇒ 1 seul NFT par nom ⇒ `[0]` = owner correct. Le commentaire vise la casse d'AFFICHAGE stockée, pas l'unicité. **Footgun latent** : `[0]` first-match n'est sûr QUE par l'invariant lowercase du registrar (idem ENS via namehash.normalize) — vivrait si migration vers registrar case-sensitive.
- **cheque builder ↔ ThirdPartyRegistryV3 = NULL adossé-artefact.** Typehash EXACT (`ConsumeSlots(string thirdPartyId,uint256 qty,bytes32 salt)` + domain « Decentraland Third Party Registry »/« 1 » ≡ TPRV3:19-20,149). Consommation on-chain = **onlyCommittee** (TPRV3:536,513) ; `_consumeSlots` (625-649) autoritatif : receipts replay-guard (643), `managers[signer]` (647), slot-cap (637), merkle `root` mintabilité committee-only (527). Le builder = requête/index off-chain ; APPROVED off-chain ne donne AUCUNE valeur mintable non-payée.
- **Wert = plancher info-low + escalade HARNESS-SUSPENDU ; le sceptique du workflow a SUR-CORRIGÉ.** Faits publics RE-VÉRIFIÉS par gh api (measure-before-asserting) : marketplace prod.json `WERT_PARTNER_ID=01J4K8B18GWQ7KVKDK5XFJHHCN` + `CONTROLLER_V2=0xbe92b49aee993adea3a002adcda189a2b7dec56c` (DEFAULT=register name-claim, L1) ; builder `WERT_PUBLISH_FEES_ENV=prod` + serveur `WERT_PUBLICATION_FEES_PRIVATE_KEY` (PUBLICATION_FEES=CollectionManager.createCollection, Polygon) ⇒ **2 comptes partenaires Wert DISTINCTS confirmés** ; `WertPayload{message,session,target}` client-fourni ; serveur signe TOUT le message avec la clé choisie par `target`, 0 binding target↔sc_address, 0 allowlist. **Le point (a) du sceptique (« partner_id jamais côté serveur ») est vrai-mais-non-pertinent** : l'attaquant fournit partner_id côté widget ET target côté serveur, les deux pointent le partner publicationFees, la clé signée MATCHE la clé publique enregistrée du partner ⇒ Wert accepte l'achat sous ce partner. **Le point (b) (« la DAO subventionne le frais MANA on-chain, pas la commission carte Wert ») est une HYPOTHÈSE, pas une preuve = c'est PRÉCISÉMENT le suspend.** LE FAIT DÉCISIF (dashboard Wert, out-of-band, capacité opérateur) : le partner publicationFees a-t-il **« sponsor processing/network fees for users » = ON** ? + y a-t-il un **allowlist sc_address/network par-partner** ? **Règle** : sponsor ON + pas d'allowlist (ou bypassable) → PAYABLE (route n'importe quel achat via le partner subventionné, DCL absorbe le frais Wert, répétable) ; sponsor ON + allowlist strict CollectionManager/Polygon → narrowed→probable info-low ; sponsor OFF → INFO-LOW (déviation code réelle = defense-in-depth, 0 perte DCL). NE PAS soumettre avant réponse dashboard.
  **PROBE LIVE EXÉCUTÉ (2026-08-12, dev marketplace-api.decentraland.zone) :** `POST /v1/wert/sign` avec identité éphémère jetable (auth-chain ADR-44 construite via @dcl/crypto), `target=publicationFees`, `sc_address=0x..dead`, `sc_input_data=0xdeadbeef` → **HTTP 200 `{signature:...}`**. Le serveur déployé signe un sc_address ARBITRAIRE sous la clé partner publicationFees, self-auth trivial ⇒ **déviation confirmée LIVE** (plus juste lue). Réponse = signature SEULE (pas de sessionId) ⇒ couche allowlist Wert non-tranchée. **Plafond d'impact INCHANGÉ** : `message.address` lié au caller (handler:24-28) ⇒ Wert exécute pour l'adresse de l'attaquant, financé par sa propre fiat ; 0xdead revert ⇒ arbitrary-exec sans victime. PoC de la DÉVIATION, pas d'une PERTE. Script PoC : `scratchpad/wert-probe/probe.mjs`. Payable reste gaté sur le sponsoring (out-of-band).

## nullguard sur « web2 measured-exhausted » (2026-08-12) → requalifié NON-CONCLUANT, puis browse-SQLi EXÉCUTÉ-null
« measured-exhausted » était une extrapolation repo-level depuis ~30% exécuté. nullguard l'a tué. CONTINUER sur la veine non-testée la plus haute = **browse-SQLi / `catalog/queries.ts` (1307 LOC, jamais lu)**. Trouvé ce que le grep ORDER-BY-only avait raté : `catalog/queries.ts:249,255` `SQL\`...category = '\`.append(filters.wearableCategory)` = **raw user-value append DANS un littéral quote** (pas paramétré). TUÉ par la garde adjacente `WearableCategory.validate()`/`EmoteCategory.validate()` = strict AJV enum-membership (confirmé source @dcl/schemas `generateLazyValidator({type:'string', enum:Object.values})` → `validate("hat' OR '1'='1")===false`). Scan exhaustif de TOUS les query-files : chaque `.append` raw = SQL-paramétré OU SQLStatement-interne OU littéral-serveur (rankings orderBy, trades type=0-caller) OU enum-validate-gated. **Aucune interpolation user-value non-gardée.** Browse-SQLi = **executed-null adossé-artefact**.

**Bilan protocole+nullguard** : 3 « clean confortables » (worlds + trade-seam + catalog-category) reposaient sur un fait non-exécuté qui, si faux, était Critical/High/SQLi ; le diff-ancré + nullguard exécutés les ont convertis en nulls adossés-artefact. 0 nouveau payable, mais les nulls sont PROUVÉS, pas lus.
**Scorecard veines** : EXÉCUTÉ-null (4) = trades-seam · cheque-seam · worlds-name · browse-SQLi(tous query-files). SUSPENDU (1) = wert (config-fee Wert dashboard). READ-STRONG-null (authz tracé complet, pas live-probé) = favorites-ACL · transak · builder item/collection-upsert · auth-server relay. NON-TOUCHÉ (basse-EV) = builder LAND/Pool/Share/S3/Forum/Analytics · worlds comms/GC/reprocess. Verdict = NON-CONCLUANT→close-mesuré, dette de couverture déclarée, AUCUN « imprenable/exhausted » nu.

**How to apply:** ne PAS rouvrir sans déclencheur. Wert = armé (config-fee Wert). worlds `[0]`-first-match = watch-item si changement de registrar. [[by-design-gate-not-just-git-dup]] [[report-no-self-devaluation]] [[measure-before-asserting-in-reports]]

## RE-AUDIT 2026-08-21 — fan-out 11 agents Fable-5 (toute la surface, tous tiers) → 0 payable, RE-SOURCE
Déclenché par scope Immunefi maj 05-août + additions "21 April 2026" (Marketplace V4 2 chaînes, Credits Manager, Coupon Manager). Ces additions = **dates d'ajout au scope, PAS code neuf** (déjà tracés 2026-08-12). 11 tranches indépendantes, tous NULL avec artefacts EXÉCUTÉS (algèbre de balance, 1 PoC Foundry, 8/8 tests fork, 54/54 sig-tests, cast on-chain 2 RPC, source déployée). Re-confirme l'état mesuré-clos avec cerveaux frais.

**DEPLOYMENT-TRUTH établie (orchestrateur, broadcast + addresses.json Polygon-mainnet) :**
- In-scope CreditsManager `0x8b3a40ca1b6f5cafc99d112a4d02e897d1fd8cc5` = **variant BASE `CreditsManagerPolygon`** (broadcast run-latest CREATE). `CreditsManagerPolygonWithMANAClaim` = **NON déployé** (aucun script/broadcast, absent addresses.json) ⇒ OOS. `RegisterNameCrossChainExecutor` idem non-déployé.
- Config constructeur live décodée : `creditsSigner`=**address(0)** + `customExternalCallSigner`=**address(0)** (hasRole(role,0)=**true** en live via drpc, jamais révoqués) MAIS **INERTE** — OZ **5.0.2** `ECDSA.recover` mappe `ecrecover→0` sur `InvalidSignature` et **REVERT** (jamais ne retourne address(0)) ⇒ le rôle-tenu-par-zéro n'est jamais matchable ⇒ pas de forge de crédit. Note d'hygiène DCL, PAS un finding. MARKETPLACE allowlist=[V3 0x540f…, V4 in-scope 0xA40b…716f] ; PRIMARY_SALES=true, SECONDARY=false ; maxManaCreditedPerHour=1000 MANA ; réserve MANA live ≈ **23 473 MANA (~$6-9k)** ⇒ plafonne tout vol Credits.
- Credits accounting = **forteresse** (invariant maître : perte réserve = exactement `creditedValue`, triple-cappé par crédit signé DCL). WithMANAClaim retire 2 gardes (call arbitraire MANA si `maxUncreditedValue==0` + skip sender-guard si `target==mana`) = would-be-Critical **SI déployé** → WATCH-ITEM, rouvrir sur déploiement.

**Vein oracle MORTE (V4 Polygon) :** feed déployé `0xA1CbF3…7512` = **Chainlink PUSH aggregator, tolérance 27s** → 0 primitive de manipulation same-block/flash-loan on-chain ; les gaps round-completeness/`rate==0` = oracle **bad-DATA = OOS**. Fee/royalty `extra` non-injectable (réécrit sur copie mémoire post-sig-verify), conservation vérifiée (100 USD→232.273 MANA exact). `sent[].beneficiary` asymmetry = null sur les 2 chaînes (payer==setter==_msgSender ; meta-tx Polygon append le _userAddress vérifié = le payeur).

**Résidus 2026-08-21 (tous NON-payables, inventoriés) :** coupon flat-discount footgun (by-design) · TPR V3 `addThirdParties` permissionless (by-design doc, committee-gated aval) + TP id-squatting (Low grief) · bid counter-reset + bid solvency place-time (Low by-design, escrow=allowance rien à drainer) · Estate XOR-fingerprint UNSAFE-fallback <19 LANDs (gated-non-exploitable, collision 2⁴⁹⁺/birthday 2¹²⁸, sunset 2026-11-26) · updateManager persiste après révoc-approval (Low, précondition⊃transfer) · EIP-1271 fresh-counters/extra-overwrite/payable-meta-tx (Info) · **NOUVEAU** onboarding email-relay `/admin/onboarding/*` sans middleware mais gated `ONBOARDING_ADMIN_ENABLED` (Info OOS-prod) · marketplace-server L1 pick-into-private-list authz-inconsistency (impact-NUL, picks attacker-scoped invisibles owner) + L2 search-500 + L3 `/v1/trades` non-paginé (Low).

**worlds `[0]` first-match name-resolution = CLOSED** (pas juste watch) : DCLRegistrar déployé impose unicité case-insensitive (`keccak256(_toLowerCase(_subdomain))`+`require(_available)`) ⇒ précondition collision FAUSSE on-chain.

**Wert = SEUL lead armé, toujours SUSPENDU** sur sponsoring Wert-dashboard (out-of-band). Re-confirmé in-band 0 escalade (repo n'a que dummy keys, 2 clés signent même message, session sans autorité on-chain). #87537 Critical (payé) reste LE win. VERDICT : **RE-SOURCE**, cible mesurée-épuisée solo.

## Wert — RE-CLASSIFIÉ 2026-08-21 par PROBE BROWSER EXÉCUTÉ (n'est plus "armé/suspendu")
Déviation `/v1/wert/sign` re-confirmée LIVE sur dev ET prod, sur les 2 clés (default + publicationFees), sc_address arbitraire → 200 + signature (control `bogusTarget`→400 = seul garde-fou = l'enum). MAIS : widget Wert rendu avec le **partner_id prod default `01J4K8B18GWQ7KVKDK5XFJHHCN` + signature prod valide** depuis une origine non-DCL (example.com) → **"URL not in the whitelist — loaded outside known domains list"**. ⇒ **Wert impose une ALLOWLIST DE DOMAINE côté partner** : la signature (même sc arbitraire) ne peut PAS être rendue/complétée depuis une origine attaquant. La weaponization naïve est **bloquée par Wert**, indépendamment du sponsoring. La question out-of-band "dashboard Wert sponsoring" est PÉRIMÉE. Nouveau (et unique) sub-lead, IN-BAND : une composition qui injecte un sc_address/amount attaquant DANS un widget qui rend sur un domaine DCL-whitelisté (XSS/embed sur *.decentraland.org, OU le frontend DCL qui passe un champ user-contrôlé dans le message — name-claim semble figé 100 MANA→CONTROLLER_V2). **Verdict Wert : Info–Low mitigé, plus un lead payable armé.** Kit reproductible : `submissions/decentraland/wert-probes/` (sign.mjs exécuté + probe_widget.html + README avec matrice).

## Wert — FERMÉ non-payable 2026-08-21 (frontend tracé in-band, plus aucun lead)
Grep frontend (marketplace webapp + decentraland-dapps) : `sc_address`/`sc_input_data` NÉS côté client par le code DCL de chaque flow, `sc_address` FIGÉ par flow (name-claim `ClaimNameFatFingerModal`→config CONTROLLER_V2 ; `sc_input_data`=encodeFunctionData('register',[ENSName,wallet.address]) fonction+contrat figés, user ne contrôle que son propre nom→sa propre adresse). Saga générique `gateway/sagas.ts` passe `data` tel quel MAIS `data` construit par DCL, pas atteignable arbitrairement sans injecter via CONSOLE navigateur = OOS explicite du programme ("requires the user to input commands in the browser console"). Plus : `message.address`=acheteur finance SA fiat → plafond = DCL absorbe le frais Wert d'un achat auto-financé, 0 vol/0 victime. **Chaîne fermée à chaque couche : (1) déviation serveur réelle, (2) Wert domain-whitelist bloque render attaquant [probe browser exécuté], (3) frontend fige sc_address par flow, (4) injection = console-OOS, (5) impact-ceiling = fee-abuse self-funded Info-Low.** Wert = note d'hygiène Info (serveur devrait valider sc_address + binder target), NON-payable. Ne plus rouvrir sauf XSS réel sur *.decentraland.org hébergeant le widget.
