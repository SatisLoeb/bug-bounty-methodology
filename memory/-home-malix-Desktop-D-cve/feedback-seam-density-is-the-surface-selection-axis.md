---
name: feedback-seam-density-is-the-surface-selection-axis
description: "The binding constraint is sourcing not rigor; select by seam-density (any layer), and access>selection when supply≈0"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: c639357a-aca4-4017-84b4-5a00b153de69
  modified: 2026-09-17T01:30:22.047Z
---

**The object-level game (find + prove + blind) is solved; the binding constraint moved to SOURCING.** The
corpse-rule that makes the playbook great is structurally BLIND to sourcing: a sourcing failure leaves no
corpse (nothing submitted → nothing dies in a gate → just a low hit-rate and silence). So the playbook
self-optimized on the only surface that produces corpses (object-level gates) and over-maintained it,
while the real bottleneck — WHERE to hunt — went unencoded. Fix = **Phase -1 Surface-Selection** (playbook
v1.6.2 §0 + gate-runner step -1).

**The revenue fact (measured, not opined):** 28 SC-core fortresses → 71% self-null, ZERO paid. The 5
paid/escalated are ALL seam-dense, none hardened-core-math. Tagged by SEAM CLASS (not by layer):
- Decentraland #87537 $18k — signer↔app (auth bypass)
- Mt Pelerin #88293 High — session↔callback (token theft)
- August/Mezo (39) — deployed-config↔code (authority/impl-drift)
- Granite #92663 escalated — periphery↔core, liq↔oracle-freshness (freeze/bad-debt)
- OZ #92486 escalated — pool↔pool, batch isolation broken on the ERROR path (freeze + cross-pool misallocation)

**Corrected axis (do NOT re-run the cargo-cult "web vs SC" binary):** pay-surface = the SEAM (the boundary
no single audit owns), wherever it is — including deep in SC. Granite + OZ are SC that pays. Dead = the
HARDENED CORE-MATH (StackingDAO/Euler/Pareto internal accounting), because that's where all the audits +
contests concentrate. This is [[feedback-edge-is-seams-not-web]] made operational. Score seam-density on
the 5-boundary taxonomy + the OZ sub-pattern (batching/pooling/settlement where per-unit isolation holds on
the happy path and breaks on the error/refund/failure branch = the untested sibling, darkside Door A).

**Access > selection when supply≈0 (measured [[venue-landscape]] 2026-09):** fresh public comps ≈ 0
(Cantina/Sherlock/C4/Immunefi farmed or closed). A selection gate can't score an empty board, so the
binding constraint is ACCESS, not choice. Phase -1 routes to the ACCESS layer: tier-migration/reciprocity
with clients who paid (Decentraland housekeeping = asset), relationship (Upshift-shape), invite-only
(Aerodrome), pre-mainnet, and drift-watch REPOINTED to known seam-dense clients' NEW deployments (not the
over-farmed pond monitored harder).

**How to apply:** run Phase -1 BEFORE Phase 0 on every prospective target. Thin-seam core-math = NO-GO
day -1 regardless of layer. Don't spend firm effort on saturated core-math because "it's what's on the
board." The next corpse to engrave is a SOURCING corpse, not a 6th gate. Related:
[[feedback-pull-impacts-in-scope-hunt-all-classes]] [[pareto-credit-nogo]] [[granite-clarity-findings]].
