---
name: granite-clarity-findings
description: "Granite Protocol (Stacks/Clarity, Immunefi $150k) = ACTIVE target, 2 novel measured findings in progress"
metadata: 
  node_type: memory
  type: project
  originSessionId: c639357a-aca4-4017-84b4-5a00b153de69
  modified: 2026-09-16T20:50:01.950Z
---

Granite Protocol (Immunefi bug-bounty/granite-protocol, Crit $100k/High $25k/**Med $2.5k**/Low $1k, KYC + PoC required, Stacks/Clarity lending). Pivoted here 2026-09-10 after [[boros-evmfork-nogo]] + [[infinifi-delta-nogo]]. This is the operator's Clarity edge and it PAYS Medium/Low + liquidations are PERMISSIONLESS (unpriv-reachable) — better EV shape than the EVM fortresses.

**TWO novel findings, PoCs re-run + confirmed passing by operator (not agent self-report), 2026-09-11:**
- **A — Renewable liquidation blackout:** 1-micro-unit borrow re-stamps `borrowed-block` (state-v1:710) → 6-block liquidation immunity (liquidator-v1:282, gap u6); renewable even while insolvent by replaying an archived in-window signed Lazer price that passes the only borrow gate (borrower-v1:52, max-LTV at caller-supplied price; pyth-adapter is-valid accepts < time-delta 1800s). → bad debt → socialize-bad-debt → LP loss. PoC: poc-borrow-liquidation-blackout (3/3). Mainnet: 60 re-arms @1800s / 10 @300s.
- **B — Bad-debt socialization double-debits borrowable-balance:** state-v1:870-882 subtracts amount from total-assets (correct) AND borrowable-balance (wrong — principal already left at borrow, :718) → LP cash physically present made permanently unlendable (immutable, no mainnet repair) → borrow brick. PoC: poc-borrowable-double-debit (1/1): 19.8% pool sterilized, borrowable→0 while cash present.

Dup: both NOVEL vs 10 audit reports (6 in repo + CA v2/v3, Cybasecurity x2). Not on KNOWN-ISSUES/OOS. ABA L-11 (1-block origin) & Cybasecurity I-06/I-09 individually acknowledge ingredients but never the composition; ABA H-03 is a DIFFERENT borrowable bug.

**Why:** first payable confirmed findings after 2 EVM no-gos — the Clarity-edge pivot worked. Both ≥Medium, argue High (LP fund loss / permanent freeze) per Immunefi impact matrix.

**How to apply:** dossier ~/Desktop/BUGS/granite-clarity/ (FINDINGS.md + 02-poc/ passing PoCs). Source scratchpad/granite-core-v1 (Clarinet: `npx vitest run tests/<poc>`; CLI `clarinet check` breaks on epoch 3.4, use vitest+clarinet-sdk). Repo HEAD uses .mock-usdc (dev); analyze deployed. pyth-lazer = 3rd-party stx-labs submodule (in-scope only by impact). BEFORE filing: confirm deployed time-delta + block-gap on-chain (Hiro API) for A's magnitude; build reports w/ immuformat+report-nerve+decentra+chill; secret gist; KYC. STILL TODO: verify 2 cut candidates (withdrawal-caps-v1:126 debt-cap denom; liquidator batch-liquidate fold swallows first error) — resume run wf_487c8ba1-b4f; triage ~20 other candidate PoCs on disk.

**UPDATE 2026-09-11 (triage complete):** live time-delta=300s on-chain (all 3 markets, Hiro) → A/D window 5min/10 re-arms. Finding D (poc-remove-collateral-stale, 1.4B sats extracted) MERGES with A (same root: is-valid stale + caller-supplied price on max-LTV gate; sinks = remove-collateral extraction + borrow re-arm) → one **High** report. B stays Medium-High. C (premium-before-principal)=soft-liq OOS. em01/em02/l01/l-03/el01=known-audit dups; market-asset-price=fixed; zero-converted-expo18=unreachable. Full triaged PoC set runs (22/25 pass). NEXT: build reports (A+D High, B) — immuformat+report-nerve+decentra+chill; KYC; secret gist.

**PLAYBOOK v1.5 VERDICT 2026-09-11:** deployed-code CONFIRMED on in-scope USDCx market (deployer SPSX722NK9V3A8D3CVQT0CDY4EBQ3E9FSDDE61FT): borrower-v1 borrow/remove-collateral gate max-LTV on caller-supplied price; pyth-adapter-v1 is-valid time-delta LIVE=300s; adapter uses pyth-lazer (scope lists PNAU pyth-oracle-v4 but USDCx uses Lazer). USDCx TVL (6dp): ~$5.0M supplied / $3.8M borrowable / **reserve only ~$1,194** → any bad debt = LP principal loss. A+D → SUBMIT High (arguing Critical `Protocol insolvency`); Immunefi has NO submission fee. B RE-TIERED down to **Medium/Low** (`Griefing`/`fails-to-deliver-returns` — no principal loss, LPs redeem via free-liquidity; NOT permanent-freezing). Gate1 soft spot for A+D: rides exogenous crash (inherent to liq findings, 'oracle manip not excluded' saves it). Next: gists + submit FAST (velocity vs dup).

**READY-TO-SUBMIT 2026-09-11 (A recadré):** Report A REWRITTEN off the OOS wall — leads on contract-logic ('borrower-v1 lets the checked account choose which genuine price its own solvency gate reads') NOT staleness; dedicated 'What this is not' section pre-empts the OOS clause 'Incorrect data supplied by third party oracles / not to exclude oracle manipulation' — genuine blob reframed as PROOF the defect is caller-selection not data; PoC blocks relabeled CURRENT vs CHOSEN price. **The differential (same genuine oracle, opposite result by caller's choice) is the load-bearing pillar — hold it against any 'stale-oracle' push at triage.** Fee ZERO on Immunefi → downside nil. Tier held High-arguing-Critical (Protocol insolvency); do NOT over-claim total insolvency beyond the bounded-per-crash bad-debt the artifact shows. Dup-risk LOW (subtle 4-contract composition, not scope-highlighted like Robinhood multiplier). Gists: A d4ec285770ff3689e1ec06fbeb24b6a7, B 3fc67d2de633cc3923df27ed03578843 (both public=false). Submission is the operator's action (account+KYC). Dossier ~/Desktop/BUGS/granite-clarity/04-report/.

**SUBMITTED 2026-09-11: #92663** (Granite A+D, Critical, Primacy-of-Impact/Protocol insolvency, by @MalikX31, gist A). Status Reported. Chain-of-custody ready to post. Hold triage lines: differential vs stale-oracle push + no over-claim beyond bounded bad debt. Finding B not yet submitted (Medium/Low, ready). Post-submission: don't chase before SLA.

**ESCALATED 2026-09-11 → PATCH-CHECK 2026-09-16 = NOT PATCHED (verified repo + on-chain).** Immunefi escalated #92663 to Granite same day (SLA: 48h ack, 336h Critical resolve → ~2026-09-25). As of 2026-09-16 our defect is fully OPEN: `pyth-adapter-v1.is-valid` untouched (still `(> timestamp (- block-timestamp (var-get time-delta)))`, NO monotonicity/last-seen — zero commits since baseline); `remove-collateral` gate still prices off the caller-supplied `verify-update` with no freshness binding; live `get-pyth-time-delta` still **u300** (Hiro call-read, deployed SPSX722…pyth-adapter-v1). The project IS actively patching OTHER Granite findings in the same files — `f3b1de6` (2026-09-14, liquidator "absorb unseizable collateral remainder") and `1219a6b` (2026-09-16, `borrow` `(> new-debt-shares u0)` = zero-share-drain #99). `1219a6b`'s new check only forces our Sink-A re-arm borrow to mint ≥1 share (trivial); the re-stamp survives. Competition is live on Granite but our defect is distinct + open. Drift-watch re-baselined granite→`1219a6b9b` with sharpened triggers (is-valid|time-delta|remove-collateral|…) so the NEXT commit on our seam = the patch of our bug = payout signal. Do NOT re-poke the thread before SLA.
