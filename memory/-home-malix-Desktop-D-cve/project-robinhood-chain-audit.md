---
name: project-robinhood-chain-audit
description: "Robinhood Chain Cantina bounty audit state: multiplier differential is a near-kill (R1 capped Low), now on JIT+Factory seams"
metadata: 
  node_type: memory
  type: project
  originSessionId: cfee37ae-4ecb-491d-b3d4-10fe89cccbc7
  modified: 2026-09-10T14:18:03.440Z
---

Robinhood Chain — Cantina bounty (ID 2acbecd1-3a5e-44fb-a324-a524c156d690, max $1M, started 8 Sep 2026, KYC). Tiers: T1 core/funds $1M, T2 oracle/pricing $250k. Program rules: NO mainnet/public-testnet probes, static source review only, no exfil, no public disclosure.

Source pulled 2026-09-09 to `/home/malix/Desktop/D-cve/robinhood/` (Blockscout robinhoodchain.blockscout.com is Cloudflare+privacy-filter gated → pull via Claude-in-Chrome: navigate to `?tab=contract`, then in-page `fetch('/api/v2/smart-contracts/<addr>')`, inject `source_code`+`additional_sources` into a `<pre>` INSIDE `<main>`, read with get_page_text). Addresses in `src/_CONTEXT.md`.

**ERC-8056 multiplier differential = DONE, near-kill** (dossier: `robinhood/DIFFERENTIAL-DOSSIER.md`; workflow wf_fe877a7c-bed, 61 agents). The multiplier is DISPLAY-ONLY on-chain; money is the off-chain Chainlink Data Streams feed reading uiMultiplier()×share-price. 26 candidates → 4 real RH-only deviations. KILLED as parity-with-canonical: stale-pending effectiveAt, no-upper-bound (canonical also only `>0`), event rename, ERC-165 IDs, rounding, overflow. Best finding R1 (pending multiplier activates during a pause while the only corrective path is pause-gated; canonical's pause-free `cancelUIMultiplierUpdate` removed) — **capped at LOW** because P2 resolved: `AccessControlsRegistry.unpause()` is a plain `PAUSER_ROLE` flip, NO timelock/delay/quorum, so the freeze is atomically recoverable. R2/R3/R4/R5 are Info/trusted-only. The ONLY path to real severity is **P1** (does the off-chain feed price mint/redeem off a LIVE uiMultiplier() while paused() set but oraclePaused() not?) — out of source reach, needs a program question. Do NOT re-run the multiplier differential.

**Now auditing (2026-09-09):** JIT + StockFactory + cross-contract registry seams (workflow wf_abdcbca1-783). Key live seam: **JIT (JustInTimeFunding, the pooled ETH/ERC20 fund contract, permissionless-with-authorizer-signature `withdraw`) uses its OWN OZ AccessControl, NOT the registry — so `registry.isBlocked`/`registry.paused` do NOT gate JIT withdrawals.** JIT sig binds address(this)+account+chainid+asset+amount+nonce+expiry (abi.encode, EIP-191 v0x45, Solady tryRecover no-low-s but nonce single-use kills malleability-replay). Factory: Create2 salt=uid, BeaconProxy empty-init-data + separate atomic initialize; beacon=registry (one contract is registry+beacon+pause+blocklist).

**Scope update (user supplied actual Tier text):** the Chainlink Data Streams aggregator + "incorrect data from third-party oracles in and of itself" are OOS, and the program EXPLICITLY states oraclePaused() is advisory/not-enforced-onchain. So **F1's value-impact-via-feed leg is confirmed NON-recevable (Q1 = OOS)** — do NOT submit F1 as drafted (`REPORT-F1-cantina.md`, superseded). In-scope Tier 2 surface = "uiMultiplier() correctness, bounds (only >0), scheduled-update mechanic" + "oraclePaused() correctness and role-gating".

**JIT = clean kill, CONFIRMED TWICE** (workflow #2 = 18 killed, + manual pass on the user's 3 signature-seam axes): (1) chainId cross-chain replay → `block.chainid` IS in getHash abi.encode; (2) nonce keyed on caller-controlled identity (dHEDGE/Leather motif) → keyed on `[nonce][account]` where account=msg.sender=signed field, no free variable; (3) authorizer/withdrawer who-what divergence → recipient=msg.sender=signed account, asset+amount signed, emergencyWithdraw only to IMMUTABLE_RECOVERY_VAULT. Only residual = authorizer-key EIP-191 cross-protocol signing hygiene (off-chain, not a contract bug). Don't re-hunt JIT signature seams.

**Submission drafted (RESERVE):** `REPORT-multiplier-correctness-cantina.md` — bundles Gap1 (pause deadlocks multiplier correction while the scheduled step still activates), Gap2 (silent supersede, no UIMultiplierUpdateCancelled per ERC-8056 SHOULD L49), Gap3 (only `>0` bound → Math.mulDiv in _update panics at large M → bricks transfer/mint/burn AND the pause-exempt adminBurn seizure). Low/Med, source-closed, zero feed dependency. BUT scope-highlighted ("bounds (only >0)" + "scheduled-update mechanic" are verbatim in Tier 2) → dup-probable. Held in reserve. NOT submitted.

**JIT-blocklist SEAM (F-A) — the corrected-lens finding** (workflow wf_28fff48a, 3 survivors=1 finding, 18 killed; dossier `JIT-BLOCKLIST-SEAM-DOSSIER.md`). The fund-holding JIT is a total ACL island: never reads registry.isBlocked/paused (the sanctions/freeze invariant that all 14 stock onlyNotBlocked sites enforce). A sanctioned account keeps an outstanding unexpired authorizer-signed WithdrawRequest payable after blockAccounts(); no targeted stop (only JIT.pause/revokeRole/emergencyWithdraw, all system-wide). Killer differential: the SAME withdraw() reverts if asset is a Stock token (onlyNotBlocked) but PAYS on ETH/external-ERC20. **Real-theft hunt DIED** (adminBurn exempt from pause+blocklist traps collateral; pre-transfer variant = off-chain F1 class). So value_class = COMPLIANCE-BYPASS not theft. **KEY WIN vs F1: gating = program-intent question, feed-INDEPENDENT** (does the blocklist bind the JIT?) — answerable, does NOT route through OOS Chainlink feed. LIVE recon: JIT holds real USDG, 300+ withdrawals, all roles granted incl. PAUSER (0x3a56) → one-tx stop exists → **T1 Low** (Medium only argued on the block-vs-JIT-pause coordination gap: different roles/contracts, uncoupled). Modal triage = "by design". Edge-class (cross-contract invariant seam), less dup-probable than the scope-named multiplier. **Forge PoC BUILT + PASSING** (`robinhood/poc-jit-blocklist/`, real JIT vendored, OZ+Solady deps, solc 0.8.33): 3 green tests — blocked account drains 50k USDG from real JIT; same withdraw reverts on a Stock asset (differential); no targeted stop (global pause only, re-drains after unpause). Report `REPORT-FA-jit-blocklist-cantina.md` drafted (report-nerve+chill, leads with the forge-proven empirical fact + intent question). NOT submitted.

**Both live candidates are Low.** Neither is the $250k/$1M path (those needed the OOS feed or a JIT theft, both dead). F-A = edge-class + feed-independent + less-dup but "by-design" risk; multiplier bundle = executed-artifact-provable but scope-highlighted/dup-probable.

**ROBINHOOD = DONE (2026-09-10), MEASURED fortress (artifact-backed, NOT reflex).** 5 workflows (~250 agents) + forge PoC + manual passes across EVERY in-scope surface: multiplier differential, JIT sig-core, JIT-blocklist-seam, factory, registry, seizure/re-mint, upgrade-safety. Last hunt (wf_c2c420b0, seizure/upgrade Tier-1, the un-hunted surfaces user flagged): 0 survivors, 11 killed on arithmetic/writer-enumeration/one-tx-recoverability, NOT reflex. Seize/re-mint asymmetry is deliberate+fail-safe (RAW in/RAW out, exempt leg is the one that must work under duress). Dossier `SEIZURE-UPGRADE-FORTRESS-DOSSIER.md`. **User corrected me: "core is hard" ≠ "pivot" — must hunt SEAMS by execution first; this stop IS earned (every surface executed, incl. the un-hunted ones), unlike the earlier reflex-pivot I got corrected on ([[feedback-edge-is-seams-not-web]]).** FINAL yield = F-A (submit-ready Low, executed vs real-money JIT, modal by-design but best on shelf) + multiplier bundle (HOLD, dup-probable, weakened — multiplier confirmed display-only no state-changing consumer) + 2 one-para hardening notes (adminBurn lacks onlyNotBlocked(msgSender) → blocklist can't contain a compromised operator key; _EIP712Name()=>name() rotates permit domain on setMetadata). Recommendation: submit F-A, hold rest, PIVOT program. Dossiers reusable if scope changes.

This is a SEAM target (fits [[feedback-edge-is-seams-not-web]]): on-chain module boundaries + off-chain feed. Playbook: front-load recevabilité+matérialité; "un kill propre vaut un finding"; no unsolicited severity concession (but P2/JIT concessions are EXECUTED-artifact-backed, not reflex). Program dup-risk BRUTAL ($1M, live since 8 Sep, ~47 findings in 2 days, multiplier is scope-named) — the veine is the SUBTLE correctness gaps, not the scope-named oraclePaused decoupling everyone sees.
