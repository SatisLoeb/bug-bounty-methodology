---
name: project-boros-onchain-core-3vein-null
description: Pendle Boros (Cantina $500k) on-chain SC core = fortress after 3 fresh veins @ HEAD 78403bf; RE-SOURCE to off-chain relayer / deployed layer / oracle integration
metadata: 
  node_type: memory
  type: project
  originSessionId: 76238c5f-a7fa-4a5f-8c3b-19dd78e66c8f
---

Pendle Boros Core (Cantina $500k, `pendle-finance/boros-core-public` @ HEAD `78403bf`, cloned /home/malix/Desktop/BUGS/boros-core-public). On-chain interest-rate-swap: Market/MarketHub (margin/liquidation), AMM (positive/negative), Router (modular: amm/auth/conditional/deposit/misc/otc), FundingRateVerifier (Chainlink/ChaosLabs/Pendle), cross-chain OFT. Hardhat (build: `yarn hardhat compile` after `yarn install`, node_modules linker). 3 audits: WatchPug, ChainSecurity, Spearbit (Aug 2025). Deployed on Arbitrum (deployments/*.json).

**COMMIT DELTA vs my June work:** `_bookMatch` is NOT dead code at this HEAD — it IS called from `MarketOrderAndOtc.sol:76` → `_coreAdd`/`_mergeNewMatchAft`. The prior "on-chain matcher is dead / matching off-chain" note does NOT hold at 78403bf; the on-chain order-book matcher is live. Surface moved.

Ran nuke barrage (85 signals, 6 silent classes; slither reentrancy detectors DISABLED in slither.config.json; aderyn crashed on hardhat parse) then 3 fresh veins — **on-chain SC core = fortress, all closed:**
- **/power (signed-intent scope):** tight. Replay guards = shared high-water-mark `signerNonce` + `isIntentExecuted[hash]` + `_markOrderExecuted[orderHash]`. All 18 EIP-712 intent types bind `root`; sig verified against root/accManager. ALL intent consumers `onlyRelayer`; direct approveAgent/revokeAgent gated `msg.sender==accManager`. Agent selectors (`_checkAgentAllowedToCall`) bounded to `_account()`, cashTransfer/ammCashTransfer intra-account. EIP712 binds chainId. Residual = semi-trusted relayer/validator/approved-agent (OOS).
- **/darkside (funding-rate verif + reentrancy):** null. FundingRateVerifier all `updateWith*` = `onlyAuthorized`; ChaosLabs authenticity delegated to trusted `riskOracle` contract (getUpdateById), replay bound by `lastUpdatedTime+period==fundingTimestamp`; economic staleness already refuted June (F-FIDX-STALE-ARB, 0.31bps<<trading cost). Reentrancy: 0 `nonReentrant` in whole codebase + scanner off = loud, but sharpest candidate `depositFromBox` external swap (`swapExtRouter`+`swapCalldata`) closed by replay-mark + self-authed callback + `netTokenReceived`=balance-delta.
- **/extract (liquidation valuation):** null at Gate-1. `_calcLiqTradeAft`: incentive `mulDown`+floor-div (rounds down), notional `mulCeil` (cost up), incentiveFactor capped `min(k, healthRatio<1)` — all legs round AGAINST the liquidator. slither `tautology` L42 = redundant uint guard.

Honest debts: no executed rejection-PoC on #1 (reading scope-null), no full fork reentrancy PoC on #2, Gate-1 dispositive on #3.

**CORRECTION — the mandatory cold-poke ([[feedback-mandatory-manual-poke-before-after-corpus]]) found what the 3 auto-veins + my premature "RE-SOURCE" missed.** Following the ONE mapped-but-unread thread (offchain-helpers/ → SwapExecutor, which I'd waved past), apparatus OFF:

**FINDING (live, deployed==repo verified, in-scope): `SwapExecutor`** — impl `0xb2633d396738d3fd815a6b3afe0d88f844fef35d` behind Transparent proxy `0xd8d82d566F251E0280F3B5C91c58C8A7bB7A1780` (Arbitrum, deployments/core.json). Etherscan V2 verified source (chainid 42161) BYTE-IDENTICAL to repo contracts/swap/SwapExecutor.sol (19 lines). `swap(address receiver, SwapData $)` is:
1. PERMISSIONLESS (external, no modifier; proxy delegates to impl for non-admin callers).
2. `_transferOut($.tokenOut, receiver, _selfBalance($.tokenOut))` — sweeps ENTIRE tokenOut balance to caller-chosen receiver (not the swap delta). Any residual (esp. leftover tokenIn from partial-consume) stolen by first `swap(tokenOut=residual, receiver=attacker)`.
3. `_safeApproveInf($.tokenIn, $.extRouter)` — INFINITE approval to caller-controlled extRouter, NEVER reset (DepositBox.approveAndCall DOES reset → asymmetry). Standing allowance drain if contract ever holds the token.

Honest bounds: live balance = 0 now → conditional-armed Medium (permissionless residual-leak-to-MEV + latent infinite-approval), consistent with MEV bots sweeping any residual instantly. NOT a protocol drain. Next: historical tx analysis of 0xd8d82d… to quantify residual accrual → severity; package report. LESSON: poke the mapped-but-unread files BEFORE any "fortress/RE-SOURCE." Etherscan V2 key is in ENV (ETHERSCAN_API_KEY / ARBISCAN_API_KEY); use `api.etherscan.io/v2/api?chainid=42161`.

**FINDING #2 (cold-poke, offchain-helper): `MultiTokenMerkleDistributor`** (contracts/distributor). `verify(user, tokens, totalAccrueds, proofs)` is PERMISSIONLESS and snapshots `verified[token][user] = totalAccrued` (checked vs CURRENT merkleRoot, no monotonic guard). `claimVerified(receiver, tokens)` pays `verified - claimed` with NO merkle proof against the current root. The `verified` snapshot SURVIVES root changes → if the operator ever DECREASES a pre-`verify`'d user's cumulative in a new root (correction/clawback/epoch reuse), that user `claimVerified`s the stale-high amount, stealing the delta from other users' funds in the distributor. Unwritten invariant (Door C): "roots only increase → verified ≤ current." Conditional-Medium (armed on root decrease). Next: confirm deployed + whether design ever decreases roots.

Poke completeness sweep DONE on all offchain-helpers/custody: CrossChainPortal.bridgeOFT/setOFTMessenger = onlyAuthorized (OOS); DepositBox withdrawTo/approveAndCall = onlyManager + approval-reset (sound); DepositBoxFactory.deployDepositBox unguarded but deterministic-benign; FundingRateOracle/AdminModule/MarketFactory gated; only OTHER infinite-approval site = MiscModule:123 forceApprove(MARKET_HUB, max) onlyAuthorized to trusted core (non-finding). SwapExecutor is the only bad infinite-approval.

**SESSION 4 (2026-07-18) — re-sourced to the on-chain MATCHER + hybrid-swap value-accounting** (the surface June wrongly dismissed as `_bookMatch` dead-code, and July's 3 veins skirted). Repo UNCHANGED (HEAD still 78403bf, last commit 2026-04-22); all 3 in-scope distributors + SwapExecutor live (codesize 1201, proxies). Re-firing nuke = identical 85 signals, refused. Read the value primitives directly: PaymentLib.calcUpfrontFixedCost=rawDivCeil on signed cost (pays more/receives less — trader-adverse), calcSettlement=mulFloor signed (adverse both sides), calcFloatingFee=rawDivUp (protocol), PositiveAMMMath mint/burn netCashIn=rawDivUp + size legs divUp-when-negative (all protocol-favorable). Matched taker pays ceil(cost), maker gets floor(cost), 0–1 wei gap retained by system NOT minted to attacker = the SAME 56wei/cycle size-independent dust extract-06-21 already closed. **Fortress re-confirmed a 4th time.** The ONE hand-undecidable residual = `SwapMathHighLevelLib.calcSwapAmountBookAMM` (SwapMath.sol:131) sweep-split notional-conservation across book+AMM straddle.

**SESSION 5 (2026-07-18, same day) — BUILT + RAN the /invfuzz fork harness → door CLOSED NULL (executed).** Harness at `/home/malix/Desktop/BUGS/boros-invfuzz-poc` (Foundry, local interfaces only — no heavy Boros lib import, via_ir cheap; remaps to boros-core-public/contracts). Direct-EOA entry CONFIRMED: `setNonAuth` (RouterAccountBase.sol:20) sets account=`AccountLib.from(msg.sender,0)` — no relayer needed. Market 2 is MATURED → used LIVE market 130 (Market 0xE704Af9C…, AMM 0x3374d585…, ammId 1300, WBTC collateral token1=0x2f2a2543…). **RPC GOTCHA: arb1.arbitrum.io/rpc + publicnode + drpc-free are NON-archive/timeout → forge fork dies on `missing trie node` for empty accts (makeAddr taker). FIX = `https://arbitrum.gateway.tenderly.co` (free archive, ~10s/run direct, no anvil needed).** Real entry for the split = `placeSingleOrder` with ammId≠0 (NOT swapWithAmm, which is AMM-only). Result across 42 REAL hybrid swaps (6 sizes×7 ticks×2 sides grid, snapshot/revert, anti-vacuity counter) + 64-run fuzz + AMM-only: **max taker-equity-gain = −7.86e10 << DUST 1e13 — taker ALWAYS loses, zero attacker-extractable mint.** Maker-grief direction closed BY CONSTRUCTION: `_bookMatch` (OrderBookUtils.sol:190-191) binds taker-charge AND maker-credit to the SAME `lastMatchedRate=getRateAtTick(curTick)` (two sides of one `totalMatched`). NB: 3-party system-conservation assert was a FALSE POSITIVE (AMM value follows its bonding-curve reserves, not linear cash+mark·size — do NOT assert on AMM net-value; taker-equity is the robust mint-detector). **Boros = fortress across 5 sessions. NO finding from the SC core. Only submittable = merkle goodwill-Low. Do NOT re-open the sweep-split — it is executed-null.**

(Pre-session-5: EV low, most-audited fn of 3-firm protocol.) Merkle report self-downgraded to Low (zero Verified events on all 3 live distributors ever → program's own 'reasonably-avoidable operational condition' OOS clause → goodwill-only).

(The earlier RE-SOURCE framing below was PREMATURE — kept for the record.) See [[project-cantina-nuke-triage-2026-07]], [[feedback-replicate-protocol-check-before-claiming-extraction]].
