---
name: project-mezo-nuke-triage
description: Mezo Cantina bounty NUKE barrage — scope reality-check + routing for musd (EVM) + mezod (Go/Cosmos)
metadata: 
  node_type: memory
  type: project
  originSessionId: 2a9b0467-80eb-4a17-8933-9632b7eb629d
---

Mezo protocol bounty (Cantina, MEZO-paid: Crit 50–500k / High 10–100k / Med 1–10k). Two in-scope repos, both cloned to `~/Desktop/ManuPoke/targets/`:
- `musd` @ 43ef4412 — EVM/hardhat, **Threshold-USD → Liquity v1 fork** (BTC-collateralized CDP stablecoin). PRIMARY EVM target.
- `mezod` @ 79b85b2d — Go, EVM-Cosmos chain (Evmos-style): `x/bridge` (native BTC bridge), `x/poa` (validator/consensus), `x/evm`, `precompile/{assetsbridge,btctoken,mezotoken,validatorpool,priceoracle}`, off-chain `bridge-worker/`.

**Scope reality-check (verified):**
- **veBTC/veMEZO = ABSENT** from both repos (grep-confirmed) despite being a named special-focus → off-repo/unreleased, can't audit.
- **Wormhole NTT = off-repo**; only in-scope hook is `MUSD.sol`/`MEZO.sol` `mintList`/`burnList` (onlyOwner-managed god-mode minters). NTT internals likely OOS.

**Barrage result:** static tools found ZERO fund-theft signal on either repo (expected). musd EVM barrage (slither+aderyn+semgrep, 522 sig) = ~380 `console.sol` noise + 2 "corroborated" both in echidna/test harness (OOS). mezod: only opengrep ran (14 low hits: hanging-goroutine + eth-rpc-trace); **golangci broke** (deprecated `--out-format`), **nilaway** needs proto-gen build. Value is 100% in negative space.

**MANUAL POKE DONE (apparatus-off, hypothesis-free) — the "crown jewel" interest-split was a TEMPLATE, not a bug.** Traced to the metal and CLEARED: (1) musd interest accounting reconciles — every principal mutation materializes interest first same-block (adjust L769/refinance L1021/open L626/redeem L366/liq L662); increaseTroveDebt L525 does addPrincipal so refinance-fee is balanced; cold-start sets lastUpdatedTime before numerator≠0; rounding-to-zero impossible (num=principal-wei×rate ≫ 3.156e11). (2) EIP-712 delegation sound — _moveTokensAndCollateralfromAdjustment: _caller pays MUSD, borrower-signed _recipient gets coll; I earlier wrote "HAND-VERIFIED SOLID" from novel-reading = the fortress-null error, now actually traced. (3) mezod bridge: AssetsLocked single ABCI caller + monotonic seq; **triparty** (non-obvious sibling mint via assetsbridge.bridgeTriparty precompile) controller=MsgSender bound + allow-list + poaKeeper.CheckOwner = trusted, compromise OOS; **bridgeOut precompile state-desync** (SaveAssetsUnlocked = raw KVStore writes before burn) DEFENDED by cachedCtxCheckpoint journal entry (state_object.go:148) that clones+restores the whole cache multistore on RevertToSnapshot. **Net: highest-value seams on BOTH repos are hardened for a solo untrusted actor.**

**CORRECTED-SCOPE VERDICT (bounty pays freeze/insolvency/gov-manip=Crit, temp-freeze>1wk=High, GRIEFING+unbounded-gas=Med — NOT theft-only; my first NO-GO was theft-scoped=ERROR).** Re-hunt (8-seam/21-agent + manual): NO Crit/High survives (bridge-in halt, EndBlock halt, musd trove-freeze/insolvency all PIERCED-sound). **One thin writeable Medium = S1 bridge-worker infinite re-enqueue** (btc_withdrawal.go:678 loop, :715 poison re-enqueue no-backoff vs :737 submission backoff): over-capacity BTC withdrawal (amount > every live tBTC wallet spendable) → prepareBTCWithdrawal fails forever → worker never idles → RPC hammer. CAVEAT (my interim "temp-freeze High" was OVER-CLAIMED): ~5s gRPC-throttled not hot-spin, FIFO-back re-enqueue interleaves → NOT a freeze of others = Medium griefing/unbounded-resource. Promote = deterministic Go unit test (fake isPendingBTCWithdrawal→true + prepareBTCWithdrawal→err, assert unbounded dequeue). **S2 poa consensus-key squatting (validatorpool submitApplication no proof-of-possession, dev-comment-flagged, test only checks vs existing-validator not pending-application) = REAL code smell but payable impact FALSE** (my interim "Medium" OVER-CLAIMED — cons key is node-local/regenerable, victim rotates key & re-onboards; no permanent DoS) → informational hardening note only. feemarket ElasticityMultiplier div-by-zero = Crit-if-reachable but param immutable post-genesis = OOS. **CORRECTED FINAL: marginal GO for ONE Medium (S1) iff program pays Mediums + scopes off-chain bridge-worker DoS; else NO-GO.** Highest-ceiling = off-repo Wormhole NTT manager.

**EXECUTED VERIFICATION (no Crit/High conceded on reasoning — ran every lane):** installed echidna 2.2.6 + solc 0.8.24. musd echidna FULL 500k config (500,291 calls, seqLen 200, 1yr time-delay: sum_of_debt/BTC_balances/troves_order/trove_properties ALL passing, 0 reproducers) + wrote test/normal/FreezeProbe.test.ts (3 pass): every trove closeable after 1yr interest (no ActivePool underflow), underwater trove always liquidatable, **supply==systemDebt diff=0 EXACT**. mezod: exhaustive abtc mint/burn enum (all counter-paired, SetBalance net-zero, BtcSupplyAssertionEnabled=true, no base-fee burn, mezotoken=amezo) + `go test` ALL PASS on x/bridge/{keeper,abci,types}, x/poa/keeper, precompile/{assetsbridge,validatorpool}. bridge-worker per-op mutex → observer interleaves → Medium not freeze. gov-voting-manip N/A (no x/gov, PoA-owner-gated, veMEZO not in-repo). All severity rows covered by executed artifact — verdict stands: no Crit/High, one thin Medium.

---
**(prior, theft-scoped) VERDICT: NO-GO for a solo untrusted actor (fortress, by executed trace not concession).** 20-agent packaging workflow (targets/mezo-package-workflow.js) traced 13 seams: 11 clean nulls w/ named saving gate, 1 split survivor. Sound (don't re-audit): musd liquidation-SP (offset min-capped, SP invariant), PCV (whitelisted recipients), oracle (price>0 in consensus-fed precompile, Decimals=18 skips unchecked scaling), governance/fee-decay (base-rate machinery DELETED, fixed-const fees), delegation; mezod bridge-authz (no sdk.Msg service), attestation (requiredVP=floor(2n/3)+1 correct, denominators before absent-voter skip), mint-atomic (supply invariant EndBlock+post-EVM-msg, PreBlocker aborts block), precompiles (from=MsgSender/CheckOwner/re-verified authz). **Manual poke CLOSED the workflow's #1 residual** (no-allowance MUSD.burn composition): caller must be burnList {TM,SP,BO}, all 6 sites burn msg.sender/self/require-gated-protocol-addr, never victim — CLOSED. **The one survivor = bridge-worker busy-loop DoS** (btc_withdrawal.go ~709-716 re-enqueue no-backoff vs ~737 submission backoff) = OFF-CHAIN worker liveness, self-funded (burn real BTC), operator-recoverable = triple-OOS, don't PoC unless program pays off-chain liveness. **mezod-poa consensus-key squatting** (validatorpool submitApplication, ed25519 no proof-of-possession, dev comment "may need validation") = reachable but no value change (owner-recoverable griefing) = informational only.

**HIGHEST-CEILING REMAINING = off-repo Wormhole NTT manager** (scoped out of every seam; the one place a bug → arbitrary MUSD mint/burn via the no-allowance primitive). If NTT ever enters scope, it IS the target. Worklist §5(manual poke)+§6(workflow+verdict) at `targets/MEZO-NUKE-WORKLIST.md`. Lessons: [[feedback-manual-poke-mandatory-bracket]] (poke killed 3 templated hypotheses + closed the burn seam agents left dangling), [[feedback-invariant-that-passes-is-not-a-finding]], [[feedback-corpus-not-100-percent-poke-finds-unnamed-surface]].
