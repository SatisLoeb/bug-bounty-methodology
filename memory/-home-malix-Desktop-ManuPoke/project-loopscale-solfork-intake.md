---
name: project-loopscale-solfork-intake
description: "Loopscale (closed-source Solana lending) /solfork intake 2026-07-21 — STRONG GO, seam = CLMM-LP-as-collateral valuation"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7a1cec50-aec0-40f4-9311-a86e2cb19f35
---

Loopscale bug bounty (self-hosted security@loopscale.com, $250k Crit / $100k High). The flagship of the 2026-07-21 solfork-niche sweep [[reference-landscape-solfork-niche-2026-07-21]]. **STRONG GO** — all day-0 gates pass: ~$78M TVL, PROVEN payer (paid 10% white-hat on its Apr-2025 $5.8M oracle exploit), permissionless no-KYC, closed-source Solana. Workspace ~/Desktop/BUGS/loopscale-2026 (STAGE0-DOSSIER.md).

**Core program `1oopBoJG58DgkUVKkEzKgyG9dvRmpgeEm1AVjoHkF78`** (NOT the truncated Lend7gmdCf… in blogs — that's stale/other). Dumped loopscale.so (3.45MB eBPF). Source NOT public. Head start: reverse-engineered IDL from npm `@exponent-labs/loopscale-deserializer` (loopscale-idl-RE.json) = 40 ix / 13 accounts / 150 errors.

**SEAM CORRECTION (key):** oracle/asset-integration/collateral-pricing = MOST-audited (Adevar×6+Highland, public reports = OOS dup wall, post-$5.8M scrutiny) — DON'T hunt it. CORE = only sec3+OShield (private, early-2025) = under-audited. **CROWN JEWEL = CLMM-LP-as-collateral valuation** (`creditbook/collateral/{orca,raydium}/manage_liquidity+transfer_position+claim_fee` + `clp/sqrt_price.rs` + `creditbook/liquidate.rs`): Loopscale takes Orca-Whirlpool/Raydium-CLMM positions as collateral, values them via own sqrt-price math, manages liquidity via CPI. Core↔AMM composition (Lieu A) no oracle-audit owns; SAME CLASS as the $5.8M exploit (mis-valued collateral) on a different path. Thesis: borrower perturbs a position's on-chain state (liquidity/sqrt-price/fees/mid-op manage) → Loopscale over-values → over-borrow or dodge liquidation → theft. Secondary: liquidate/refinance/sell_ledger math; internal API/backend ($50k off-chain, operator's proven shape). NEXT: fork loopscale.so + clone Orca/Raydium, open a CLMM-collateral loan, attack valuation for an executed over-valuation delta. Gates [[feedback-check-prior-audits-and-competitions-at-intake]] (applied — seam moved off the swept periphery).

**UPDATE 2026-07-21 — LEAD confirmed (5-agent RE, bytecode-level).** CLMM-collateral valuation seam is a REAL $250k-tier LEAD (same class as the $5.8M exploit, un-audited path). Loopscale values an Orca/Raydium CLMM position by splitting liquidity L into amount0(p)/amount1(p) at pool price p, valued at independent Pyth prices P0,P1 → V(p)=amount0·P0+amount1·P1 is CONVEX in p (min at fair, max at range boundaries) → pool manipulation only OVER-values. Pool is a borrower-supplied remaining acct (asset_index_guidance, pinned to AssetData.oracle_account); borrow_principal RE-COMPUTES on-chain (expected_loan_values=slippage-check, protocol_admin co-sign=whole-tx not param-bound → can't catch in-tx pool move). Attack: atomic [swap pool→boundary]+[borrow over-valued]+[swap back]=over-borrow/bad-debt. KILLED veins: stale-accounting desync (loan read-only in manage), oracle-substitution (pinned), fake-pool (binding enforced), permissionless-wrongful-liquidation (convexity: can't under-value). **THE CRUX (fork-resolvable in 1 measurement):** does clp/sqrt_price.rs use the pool's LIVE sqrt_price (→CRITICAL) or oracle-derived fair sqrt(P0/P1) (→NULL)? Cheap test: clone a real Loopscale loan w/ CLMM collateral + pool + Pyth, simulate liquidate_ledger (permissionless), read logged maintenence_weighted_collateral_value, then OVERWRITE the cloned pool's sqrt_price_x64 on the fork + re-simulate — if value moves at constant Pyth → CRITICAL. Deps staged: ~/Desktop/BUGS/loopscale-2026/{loopscale,raydium_clmm,orca_whirlpool,pyth_receiver}.so + LEAD-clmm-collateral-overvaluation.md. Reachability caveat: atomic-swap-bracket needs backend to co-sign a client-composed tx (also probes $50k App/Services tier).

**FORK EXEC 2026-07-21 — staged, NOT yet resolved (honest).** Attempted the decisive fork test; did NOT get the Critical/null verdict yet. Progress: 4 deps dumped + fork boots (SUN_LEN → short --ledger path). Found real template tx 47Pikjdm = manage_collateral_withdraw_orca_liquidity whose REMAINING accounts include the whirlpool Fae5dWVn → CONFIRMS the pool is read in the health-check valuation (strengthens the live-price thesis). BLOCKER: that loan (3sjNyUvF)+position(2u16YuUq) are CLOSED (final withdraw) → null; public RPC (mainnet-beta 429/publicnode 403) blocks bulk --clone (individual backoff got 14/20). REMAINING (1 bounded session + a real RPC key): getProgramAccounts(Loan disc)→find a LIVE Orca/Raydium-CLMM-collateral loan→clone via `--account <addr> <json>` files→simulate value-computing ix, mutate cloned whirlpool.sqrt_price_x64 at constant Pyth→value moves⇒CRITICAL. Full: LEAD-clmm-collateral-overvaluation.md.

**FORK-RESOLVED 2026-07-21 (Helius) — CLMM over-valuation = EXECUTED-NULL.** Ran the decisive test end-to-end. Found a LIVE CLMM-collateral loan via getProgramAccounts (Loan disc 14c34675a5e3b601, collateral@969, asset_id@1010; loan CVFTBW5X + Orca whirlpool Czfq3xZZ). Cloned its borrow_principal template onto a fork (loopscale+orca+raydium+pyth .so; fixes: event_authority stub, Pyth timestamp refresh, CU=1.4M). Program LOGS maintenence_weighted_collateral_value. RESULT: whirlpool sqrt_price ×1.5 (Pyth constant) → collateral value BYTE-IDENTICAL to 24 decimals (8923.664130450776380976025000); Pyth ×2 → value changes. **The pool's live sqrt_price has ZERO effect → valuation is Pyth-fair (composes via sqrt(P0/P1), pool only for binding check) → manipulation-resistant → the over-borrow attack does NOT reproduce.** Clean NULL-COÛTEUX with executed artifact. Loopscale core-valuation = SOUND. Un-tested slivers: transfer_position (writable loan), off-chain backend ($50k). Harness ~/Desktop/BUGS/loopscale-2026/accts + the mutate method reusable for any future Solana oracle-valuation target. LESSON: the /solfork fork test flips a strong LEAD to a definitive verdict (Critical or NULL) with ONE byte-comparison — this is exactly what solfork is for.

**CLOSED 2026-07-21 — RE-SOURCE.** Core = EXECUTED-NULL (Pyth-fair valuation). transfer_position sliver DEPRIORITIZED (not conceded): payable over-valuation angle dead by the executed proof; residual accounting-desync backstopped by the post-op health check; op rare on-chain. Operator's call (correct): B = proving-absence on a twice-backstopped sliver = the comfortable-NULL the chase protocol forbids ("effort goes to GENERATING theft paths, not proving absence"; "money is made at sourcing"). Don't re-open. NEXT = Metric (Sherlock #1279, genuinely-fresh $150k escrowed) per [[reference-landscape-solfork-niche-2026-07-21]]. Off-chain backend ($50k web) = optional only.
