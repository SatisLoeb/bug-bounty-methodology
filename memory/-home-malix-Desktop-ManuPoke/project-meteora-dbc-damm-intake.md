---
name: project-meteora-dbc-damm-intake
description: "Meteora DBC/DAMM-v2/DLMM $500k bounty — fortress-with-thin-edge; GO on v0.2.0 post-audit drift + migration seam, NOT core math"
metadata: 
  node_type: memory
  type: project
  originSessionId: a811a6bc-6075-48c3-a7de-7c7a1c1f6aa1
---

Meteora bug bounty (OOOSEC/Immunefi, up to **$500k Critical**), 3 in-scope Solana programs. Only deployed bytecode at the given Program IDs is in scope; must repro on a local mainnet fork. Repos cloned at `~/Desktop/ManuPoke/meteora-2026/{dbc,dammv2}` (DBC @ 3b540e9 "release 0.2.0" 2026-06-03; DAMM v2 = cp-amm). DLMM has NO source link in scope (program-ID `LBUZKhRxPF3XUpBCjp4YzTKgLccjZhTSDM9YuVaPwxo` only).

**INTAKE GATE (per [[feedback-check-prior-audits-and-competitions-at-intake]]):** FORTRESS-but-thin-edge, NOT a null.
- Audits repo `github.com/MeteoraAg/audits` (per-program, version-by-version). DBC: Offside Labs ×7 (incl v0.2.0), OtterSec v0.1.3, Zenith ×8 (incl v0.2.0). DAMM v2: OtterSec+Offside(→v0.2.2)+Zenith. DLMM: most audited (Offside/OtterSec/Sec3/Sherlock/Zenith, 2yr).
- **DBC Code4rena Aug22–Sep12 2025 @ commit 30dd2a1** (award $104.5k): 0 High, 2 Med — F-50 (rate-limiter bypass via Swap2 discriminator not checked) + F-118 (zero-fee when FeeRateLimiter cliff=0). BOTH FIXED in my HEAD (process_swap.rs:487 now checks Swap2 + Swap2WithTransferHook). These + all audit findings = KNOWN → dup-excluded. MUST read C4 report + v0.2.0 audit PDFs before submitting anything.
- **The edge:** v0.2.0 added transfer-hook pools + `claim_protocol_fee2` (protocol_fee wrapper) + compounding collect_fee_mode AFTER the C4 comp → only 2 audit firms, no crowd. Least-swept surface.

**IMPACT SIZING (critical nuance):** DBC standing TVL ≈ **$28k** (tokens graduate out) — impact must be argued from FLOW ($296M/30d) + in-flight per-pool reserves (migration_quote_threshold, tens of SOL each × thousands of pools) + migration LP transfers. DLMM = $178M TVL (63%, biggest $, most swept). DAMM v2 = $24M TVL (newest core, cleaner impact story).

**GO decision:** hunt DBC + DAMM v2 **drift + seam**, skip DLMM (swept + no source). Targets: (1) v0.2.0 transfer-hook/claim_protocol_fee2/compounding vs existing invariants; (2) DBC→DAMM migration value-conservation; (3) multi-bucket fee/surplus SOLVENCY — vaults hold protocol/partner/creator trading fees (base+quote) + surplus (partner+creator vs protocol) + migration_fee(bitmask) + protocol_liquidity_migration_fee + pool_creation_fee; rounding/ordering across buckets. Partner who creates a config = NON-privileged in-scope actor (malicious-config → steal-from-trader/creator is payable). Gate every candidate on payable-impact + not-in-known-set. ~/Desktop/ManuPoke/meteora-2026.

**== FINAL VERDICT 2026-07-21: EXECUTED-NULL, fortress. RE-SOURCE. ==**
Manual + 2 adversarial workflows exhausted. ALL surfaces SOUND (match heavy audits): DBC swap/curve/migration(v1+v2)/surplus-ledger(solvent every ordering)/create_config(sum%==100)/rate-limiter(sysvar-omission hard-reverts, not F-50)/fee-claim-authz; DAMM v2 concentrated-math(canonical UniV3, pool-favoring rounding, numeric-checked at 2^96/2^64)/add-remove(add=ceil remove=floor→strict user loss)/split_position2(refresh-before-split conserves)/delegate-authz(ToOwner destinations = owner-ATA validated)/rewards+dead-liquidity/layout-V0→V1(touches only decorative fields). 
Top lead DBC "any signer drains partner/creator fee+surplus" = **FALSE POSITIVE**: authz is in lib.rs `#[access_control(is_partner_fee_claimer/is_pool_creator)]` attrs (lib.rs:118/128/153/193/203/220), NOT handler body → [[feedback-anchor-access-control-attribute]]. Repo's own expectThrowsAsync("Unauthorized") test asserts it. Nearly built a PoC for a non-bug; test harness was the tell.
Only surviving candidate = DBC migrate_meteora_damm passes unvalidated `virtual_pool_lp` (dev `// TODO check this address and validate`) as DAMM-v1 LP recipient → REFUTED+OOS: DAMM-v1 (`Eo7WjKq67rjJQSZxS6z3YkapzY3eMj6Xy8X5EQVn5UaB`) NOT in-scope; mature 2022-audited program constrains it; legacy path. 
Deployed DBC .so dumped @ meteora-2026/dbc-deployed.so; litesvm harness needs anchor-built IDL (not shipped) to run. Consistent w/ landscape: heavily-audited = executed-null. Don't re-audit.

**== NON-THEFT impact ledger ALSO NULL (2026-07-21, per operator "on ne cherche pas que le theft") ==**
KEY: DBC has NO admin pause/recovery ix (lib.rs) → a DBC permanent-lock/insolvency WOULD be payable (not the temp/recoverable exclusion). Hunted freeze/lock/insolvency/DoS: (1) DBC migration permanent-lock = SOUND — overshoot reverts the WHOLE completing swap (required_base_balance recomputed post-apply incl locked_vesting), compounding pre-validated at create_config w/ IDENTICAL amounts reused at migrate, surplus solvent (Σ=threshold), 2nd-position post-fix Rounding::Down; create_locker CANNOT be griefed (locker create_vesting_escrow_v2 needs `base` SIGNER = DBC PDA, no front-run) and DBC validate() pre-satisfies every locker reject-code (FrequencyIsZero/AmountIsZero/MathOverflow). (2) DBC claim-lock = SOUND (migration never writes reserves→claims stay reachable; flags role-partitioned). (3) DAMM v2 position-freeze = SOUND (all vested_liquidity writers guarded, split conservation proptest'd, close.is_empty reachable, permanent_lock owner-gated; **SetPoolStatus(disable) can't freeze funds — can_remove_liquidity is INDEPENDENT of is_enabled, only needs current_point>=activation_point**). (4) DAMM v2 reward-lock = SOUND (all 3 reward legs FLOOR → Σclaimed≤funded, no last-claimer-loses; ineligible vs dead-liquidity mutually exclusive by pool type). (5) swap-brick = SOUND (volatility math can't overflow: MAX_SQRT·2^64/MIN_SQRT=3.40269e38 < u128::MAX). NUKE barrage mechanically clean (dep-advisories OOS, clippy style-only, negative-space 22 classes hand-closed). COMPREHENSIVE fortress across theft AND non-theft. RE-SOURCE.

**UPDATE 2026-07-21 — DLMM opened via /solfork → confirmed MAXIMAL FORTRESS (refutes "money-left-on-table").** Earlier I hypothesized DLMM was $ left on the table (skipped "no source"). EXECUTED check kills that: dumped the deployed program (LBUZKhR…, ~/Desktop/ManuPoke/meteora-2026/dlmm/dlmm.so), recovered the on-chain Anchor IDL (**v0.12.0, 76 instructions**), and pulled the vendor audit set (github.com/MeteoraAg/audits). Deployed v0.12.0 = **4-firm audited on the EXACT deployed version** (OtterSec/Offside/Sherlock/Zenith) + Sherlock comp + audits extend one version BEYOND deployed (Offside 0.13.0) + 2yr history (0.8.x–0.13.0). The only plausible seams (limit_order, rebalance, Token2022/transfer-hook) are the FRESH v0.12.0 modules the auditors explicitly dissected — Offside "0.12.0 adds limit orders", OtterSec "Stale Bitmap After Limit-Order Swaps", Zenith M-1 "rounding in partial-filled limit order cancel", + 44–56 token2022/hook mentions each. By G3 AXIOM any hit there = dup/FP. NO positive drift (deployed 0.12.0 ≤ newest audit 0.13.0). lb_clmm program source NOT public (dlmm-sdk = client SDK only) → genuine solfork target, but deployed=audited. **Verdict: correctly deprioritized — "no source" was a red herring, "most swept" was real and now CONFIRMED via deployed-version audit-set. RE-SOURCE, do not re-audit.** Meta: my own "money-left-on-table" was an un-executed hypothesis; the audit-set retrieval killed it (Maxim applied to my own claim). The audit-set-retrieval step is now the [[feedback-check-prior-audits-and-competitions-at-intake]] gate made executable against the DEPLOYED version.
