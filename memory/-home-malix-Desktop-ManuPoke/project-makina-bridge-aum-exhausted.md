---
name: project-makina-bridge-aum-exhausted
description: Makina Core (Cantina $500k) bridge/AUM + role-acquisition surface audited executed-NULL for untrusted; live surface = Caliber weiroll accounting
metadata: 
  node_type: memory
  type: project
  originSessionId: 76238c5f-a7fa-4a5f-8c3b-19dd78e66c8f
---

Makina Core smart contracts (Cantina bounty $500k, MakinaHQ/makina-core @29e0731, cloned /home/malix/Desktop/BUGS/makina-core). Cross-chain hub-and-spoke strategy vaults: Machine (hub, deposit/redeem/share-price) ↔ Caliber (weiroll exec engine) ↔ CaliberMailbox ↔ BridgeAdapter (Across/LZ/CCTP). 4 audits (ChainSecurity, OtterSec, Sigma Prime, Enigma Dark).

Ran the arsenal 2026-07-13/14: nuke → extract → darkside → power → power. The **bridge-accounting + AUM + role-acquisition surface is executed-NULL for an untrusted actor** — do NOT re-audit it from scratch:
- **extract**: share-price/convert rounding math dead at Gate-1 — `convertToShares`/`convertToAssets` (Machine 284/292) both floor toward the protocol; round-trip loses dust to the vault. No caller-chosen direction.
- **darkside candidate C** (in-flight bridge double-count in `MachineUtils._getTotalAum` 277-295): dead by ATOMICITY — `CaliberMailbox.manageTransfer` updates `_bridgesIn` (119) then `notifyIncomingTransfer`→netAum (124) same tx; `getSpokeCaliberAccountingData` reads both in one view (82-83). No snapshot can desync. The devs' `invariant_totalAum` tests exactly this conservation (on Across).
- **Door-A gap (executed-confirmed)**: the conservation invariant drives ONLY `ACROSS_V3_BRIDGE_ID`; LayerZero (`lzCompose`) and CCTP (`receiveCctpV2Message`) have NO conservation test. BUT both read guarded: CCTP `amount = balanceOf delta` (real, like Across); LZ `amount = amountLD` (message) gated by `msg.sender==receiveSource` + trusted canonical OFT (`getOft==_from`) + balance-floor `balanceOf ≥ reserved+amount`.
- **power (bridge counters)**: 32 executed auth-rejections. Every `_reservedBalances`/`_bridgesIn/Out` writer is `onlyController`/`msg.sender==caliber`/`_isBridgeAdapter`/`onlySecurityCouncil`, and all 3 inbound receive paths revert `CallerNotAuthorizedSource` + `UnexpectedMessage` (the `expectedInMessages` mechanic pre-auth is the real keeper) + `InvalidInputAmount` + LZ `InsufficientBalance` (the balance-floor). Untrusted reaches no writer.
- **power (role acquisition)**: 17 executed rejections — setMechanic/setSecurityCouncil/setRecoveryMode/setRestrictedAccountingMode/add+removeAccountingAgent all revert for callers without role; createMachine gated. Plus `_disableInitializers` 100% on impls, stock OZ `AccessManagedUpgradeable`, `_create3` factory-gated. Residual = compromised trusted role = OOS rogue-admin.

Build note: integration/invariant tests need `FOUNDRY_PROFILE=ir forge build` FIRST (mocks loaded via `vm.getCode("out-ir-based/...")`); without it setUp fails `vm.getCode: no matching artifact`.

**Freeze/insolvency/DoS row — hunted 2026-07-19 (impact-ledger lens validation), also executed-NULL in-repo:** the non-theft rows a theft-only pass skipped were dug and all closed with dispositive static disconfirmers. (a) **Redemption is decoupled from live-AUM** — `deposit`/`redeem` (Machine ~434/465) price off cached `_lastTotalAum` and mutate it inline; `maxWithdraw = accountingToken.balanceOf` (no AUM read); a revert anywhere in live-AUM computation freezes only the AUM *mutator*, never a share exit → no permanent-freeze. (b) **A1 permissionless-`updateTotalAum` insolvency is impossible** — `MachineUtils.updateTotalAum` writes the cache from live truth (L43) BEFORE the revertible rate-check (L47-54) and advances the timer (L56) only on non-revert; the two states the attack needs (timer pinned to now → budget≈0 AND stale-high cache) are mutually exclusive (pinning requires a successful call, which L43 already refreshed to truth; failing reverts atomically). `updateTotalAum.t.sol:235-238` calls it twice in one block with NO expectRevert = budget=0 alone does not brick. Honest tier = Informational gas-griefing of an intended-permissionless keeper endpoint (self-healing at maxRate/sec, SecurityCouncil-clearable `Machine.sol:421`). **Genuine residue = A2, and it is OFF-REPO:** accounting-instruction SEMANTIC poisoning (an allowlisted weiroll instruction reading manipulable spot state) is real per the authz≠semantics gap, but needs a specific DEPLOYED merkle-authorized Caliber strategy that does NOT exist at this HEAD — not a makina-core repo finding. The lens correctly POINTED at it; confirming it needs the deployed instruction set.

**2026-07-21 REVALIDATION (operator asked "quoi de Makina").** My A1+A2 lens was VINDICATED by a REAL exploit: **Jan-20-2026 Makina lost $4.13M / 1,299 ETH** (Decrypt/TheBlock/CertiK/Verichains confirmed) — flash-loan manipulated a DUSD/USDC Curve pool, permissionless `updateTotalAum()` read the spot via a weiroll `calc_withdraw_one_coin` instruction (no TWAP/staleness) → AUM 5.1B→847B → deposit-cheap/exit-rich. EXACTLY A1(permissionless updateTotalAum)+A2(spot-reading weiroll instr). BUT the bounty repo @29e0731 (Jul-2, 6mo post-hack) is DOUBLE-PATCHED + the vector is DISCLOSED → **OOS/dead**: `Machine.sol:417 updateTotalAum() now onlyAccountingAuthorized` (was permissionless) + `OracleRegistry` w/ per-feed staleness thresholds (replaced raw Curve spot). Today-impact HIGH ($100M+ TVL, live, 6 chains) but the exploited class is now the MOST-hardened+watched surface. **VERDICT = NO-GO (high-impact TRAP)**: disclosed+double-patched vector OOS, in-repo surface already executed-NULL, only live seam = a deployed Caliber's merkle-authorized weiroll accounting instruction that STILL reads manipulable spot bypassing OracleRegistry — off-repo, low-P (team maximally alert post-$4M; operator-authorized instructions likely trusted/OOS). Textbook [[feedback-tool-complete-stop-polishing]] (hunting the just-patched class on a hardened alert protocol). SKIP → Perena (drafted finding) or fresh target. See [[feedback-payable-impact-not-just-theft]] (Makina was its validation target: lens sound = generated 4 named executed-NULL non-theft rows, but no in-repo finding), [[project-cantina-nuke-triage-2026-07]], [[project-nuke-static-barrage-skill]].
