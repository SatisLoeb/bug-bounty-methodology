---
name: project-stackingdao-stbtc-double-count
description: "StackingDAO ststxbtc reward double-count (stale external-position) = PROVEN High, Clarinet PoC PASSES, READY_TO_SUBMIT (Immunefi, operator-gated)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 3c399e15-ef33-40ad-abe1-f8688fee64f0
  modified: 2026-07-26T20:02:14.356Z
---

StackingDAO (Immunefi $100k, Stacks/Clarity, stSTX liquid staking + **ststxbtc** = sBTC-yield distribution). Surfaced by the 2026-07-23 landscape scan as "StackingDAO stBTC, freshest low-dup Perena-share-price class". **First confirmed finding of that re-source** after a null streak (OnRe + stBTC-btc-v3-delta both cleared). Workspace ~/Desktop/BUGS/stackingdao-2026 (clone `contracts/` @ HEAD a8d7d2a; SCOPE/findings/submissions/poc/evidence).

**Gate corrections (both scan claims were WRONG, caught at intake):** NOT "2 audits / fresh 2026" — it's **4 audits** (CoinFabrik 2023-11 + Clarity Alliance 2024-11/2025-01/2025-06), code is **2025** not 2026. The in-scope `stacking-dao-core-btc-v3` (added 2025-07-21, post the June-2025 audit) has a **TRIVIAL** delta vs audited btc-v2 (`withdraw-idle-fee 0→100 bps` + cosmetic `tx-sender`→`sender` where `sender ≡ tx-sender` bound at L197). So btc-v3 ≈ audited. The real bug is in the ststxbtc reward subsystem, NOT the btc-v3 delta. **Scope LOCKED (Immunefi "Show all" 2026-07-23): BOTH `ststxbtc-tracking-v2` (logic) AND `ststxbtc-tracking-data-v2` (state) are explicitly in-scope (added 2025-07-10); `ststxbtc-token-v2`/`rewards-v5` too. In-scope impacts confirmed: High "Theft of unclaimed yield" + Critical "Protocol insolvency" — both fit.**

**F-01 (PROVEN High, theft-of-yield / reward-pool insolvency):** the ststxbtc reward-per-token system counts a holder's stake in two independent buckets — **wallet** (refreshed automatically by ststxbtc-token-v2 transfer/mint/burn hooks, always fresh) and **external positions** like Zest (refreshed ONLY by manual/permissionless `refresh-position`, no auto-trigger — grep-confirmed, and `position-zest-v2` is a read-only 8-line adapter that never calls back). `claim-pending-rewards` pays the STORED position `amount`, never re-reading live `get-holder-balance`. So after a holder **deposits ststxbtc into Zest → refresh-position → withdraws back to wallet**, the wallet is fresh (=X) while the Zest position stays STALE (=X): the same X real tokens accrue+claim rewards TWICE → drains the shared sBTC pool. No global `Σamount ≤ supply` invariant; the reserve check (ERR_OVER_RESERVE) only bounds refresh-position increases, never the wallet side.

**Clarinet PoC PASSES on the CURRENT (C-01-fixed) code** (`poc/stbtc-double-count-poc_test.ts`, mirrors position-mock ≡ position-zest trait): attacker with X real ststxbtc → pending = 2× an honest holder with the same X; attacker claims the WHOLE pool (1e13 sBTC), honest holder gets 0. Executed myself, output in evidence/. Supply stays consistent (2X = attacker X + honest X) → double-count is from the stale stored amount, not minted tokens.

**Dup-defense (solid, PoC-sealed):** audit **[C-01] Critical "Draining via Double-Counted Rewards" = RESOLVED but DIFFERENT mechanism** (save/get-pending self-reference double-add, fixed by overwrite) — **the PoC runs on that fix and STILL double-counts** → orthogonal. [QA-07] (Acknowledged) = under-claim/deactivation only (holders who don't refresh LOSE), never over-claim-via-active-withdrawal. [L-02] (Acknowledged) = reserve cap, PoC passes it.

**Reachability CONFIRMED on-chain** (read-only Hiro API, key-free): ststxbtc total-supply 27.7M, **2680 holders**, cumm-reward active; `get-supported-positions(position-zest-v2)` = **active:true, total≈295,741 ststxbtc** (c32 checksum-validated read); v6 actively refreshed (10 refresh/50tx). Attack fully permissionless; only privileged precondition (Zest activation) already satisfied on mainnet.

**Honest caveat (in the report's limitations):** theft magnitude = X × Δcumm over the stale window; a keeper calling refresh-position bounds it to ~1 cycle/position but does NOT eliminate it (attacker claims the stale position directly + repeats deposit/withdraw; unbounded if keeper misses a position). Didn't reverse keeper cadence/coverage. Severity mapped **High** (theft of unclaimed yield); Critical-insolvency angle noted but left to the program (insolvency is of the sBTC reward subsystem, not core stSTX backing) [[feedback-map-severity-to-program-rubric]].

**RABAT-JOIE DEPLOYED-SOURCE PASS (2026-07-24) — finding SURVIVES, no kill; the Ammalgam/Perena trap checked and it CONFIRMS here.** The one load-bearing fact the report never quoted = **the `add-rewards` denominator**: `reward-per-token = amount×1e10 / get-total-supply` (`tracking-v2:101`), and `get-total-supply` (`tracking-data-v2:10,46`) is a standalone data-var set ONLY by the token on mint/burn to `(ft-get-supply ststxbtc)` (`token-v2:82,94`) — **NOT the Σ of tracked positions**. So the stale phantom X is EXCLUDED from the denominator → Σclaims (1.5R in the 2-holder PoC) > pool (R) → **insolvency is real, not self-diluting**. Had the denominator been Σtracked, the double-count would self-dilute to harmless redistribution and F-01 would be DEAD. That single line is the seam. Verified `save-pending-rewards` BANKS the over-accrual into `saved-rewards` before zeroing → keeper crystallizes, never claws back. PoC faithful (drives real mint/burn → its total-supply=2X is what mainnet produces; position-mock trait-identical, only get-holder-balance exercised; position-zest-v2 = 1-directional read-only adapter, no callback). **Deployed==clone diff (Hiro, SP4SZE…):** `ststxbtc-tracking-data-v2` byte-identical; `tracking-v2`/`token-v2` differ ONLY in real sBTC-token addr (`SM3VDXK…sbtc-token` → claim moves REAL sBTC) + token-uri string — nothing touches the mechanism. **Submission hardened** (submissions/F-01…md) against the 2 downgrade angles: (1) reframed Impact off the "whole-pool" 2-holder artifact → pro-rata ~2×-share + subsystem-insolvency, magnitude = phantom×Δcumm/supply repeatable; (2) rewrote keeper limitation (banks, doesn't claw); added fact-4 (denominator) to root cause + deployed-diff citation. Lesson applied: read the DENOMINATOR / primary state, not the report prose [[feedback-model-accounting-invariant-not-economic-ideal]] [[feedback-diff-forward-to-head-not-just-scope-changelog]].

**§2 DoS (operator-added, rabat-joie LIVE-verified 2026-07-24) — HOLDS to the digit.** The SAME phantom that double-counts also pushes zest `supported.total` above its reserve; the `ERR_OVER_RESERVE` guard (tracking-v2:68) fires ONLY on the increase side, so after the attacker's un-refreshed withdrawal (total stays R+X, reserve `pool-vault` drops to R) every honest depositor's `refresh-position` computes new-total (R+X+D) > reserve (R+D) ⟺ X>0 → reverts `u10002003`. = temporary freeze of NEW/increasing Zest registrations (existing synced positions keep accruing). Independent live reads (my own @stacks/transactions callReadOnly, NOT the report's prose): `get-supported-positions(position-zest-v2).total = 295654867521` **==** `get-holder-position(pool-vault,pool-vault).amount = 295654867521` → **EXACT saturation, slack 0 → fires at X=1**; reserve principal = `SP2VCQJGH7PHP2DJK7Z0V48AGBHQAW3R3ZW1QF4N.pool-vault`, itself an active supported position (custody earns 0 via is-holder-position, total=0); supply 27746064338767, next-holder-index 2681. Saturation is the SYNCED equilibrium (total=Σtracked=Σreal-in-Zest=reserve), not a fluke → robust. PoC test 2 (L109-138) asserts `(err u10002003)` and PASSES (2 green). Maps to SCOPE in-scope High "temporary freezing of unclaimed yield". §1↔§2 interlock: clearing the DoS (refresh the attacker's phantom) BANKS the theft (save-pending) → protocol forced to choose unpaid-honest OR fund-the-theft. **Arithmetic CHECKED both framings, NO blemish:** report's `reserve = R−X+D` is CORRECT under the existing-depositor model (attacker's X already in the saturated total R → withdraw-no-refresh leaves total=R, reserve=R−X; honest deposit D → reserve R−X+D, new-total R+D, R+D>R−X+D ⟺ X>0). (My first pass wrongly flagged it — I'd mixed the fresh-deposit model's total=R+X with this model's reserve; both models give divergence exactly X, both revert. Verify in BOTH directions before conceding a defect in the operator's work.) Operator did the primary-state homework this time; opposite of Ammalgam/Perena — every falsifiable live claim verified to the digit.

**V3 (operator-added: 2 positions + PRODUCTION EVENTS) — rabat-joie on-chain-verified 2026-07-24, HOLDS.** Two active Zest positions confirmed live: `position-zest-v6` (MAIN, active, total 7322352766815, reserve `SP1A27KFY4XERQCCRCARCYD1CC5N7M6688BSYADJ7.v0-vault-ststxbtc` amount 7322352767815 → **slack +1000**, ~27.5% of supply) + `position-zest-v2` (total==reserve 295654867521, slack 0). `position-mock` inactive. **Reserve is PER-POSITION → N positions give (N+1)X theft on X capital (v6-phantom doesn't block v2-phantom); verified 3X reachable.** **The 3 claimed `ERR_OVER_RESERVE` production events are REAL on-chain** (hunted tracking-v2 tx history, `abort_by_response`, `(err u10002003)`, all `refresh-position` on `position-zest-v6`): tx `0xa29fd9…`@2026-06-27T05:58:38 & `0x3b5fae…`@07:00:14 (holder arg `SP33HRNWBTQK9GEPQN7G4SD9D7RCNGAQZDM53EHX4`, ~1h retry both fail), tx `0x2bd0ca…`@2026-07-07T19:49:27 (holder `SP2410YCCWEFF99XHCNGSBDC60WBWXFC8B5433C56`). **Caller of all 3 = single keeper `SP2XYTH81YP55YAZ5HMVTG5ABEPCH4FPRA07M4G2R`** → proves the protocol's OWN keeper fails to sync increasing holders while a phantom sits (guard fires only on new>prev increasing refresh ⟹ total>reserve pre-existed ⟹ phantom present — inference SOUND; phantom arises organically from un-refreshed Zest withdrawals). 2 distinct VICTIM holders (the blocked args), NOT the phantom-creator. **[CORRECTED V5-round] The timestamp "blemish" I flagged was WRONG — over-flag, retracted.** Report's times (06:00:34 / 07:01:00 / 20:00:39) are the Stacks `block_time_iso` (canonical, precise); I'd compared against the coarser `burn_block_time_iso` (Bitcoin anchor 05:58:38 / 07:00:14 / 19:49:27). Confirmed via `/extended/v1/tx/{id}`: block_time == the report's values exactly. Report is CORRECT; keep the times. Adding the tx hashes is an optional convenience, NOT a fix. (Lesson: verify a suspected defect in BOTH directions before flagging — symmetric to the "reserve=R−X+D" over-flag earlier this session.) Everything else in V3 exact. Verify tooling: `@stacks/transactions` 6.17 callReadOnly + Hiro `/extended/v1/address/{cid}/transactions` paginate+filter `tx_result.repr~u10002003`.

**V4 (operator co-triaged; rabat-joie on-chain-verified 2026-07-24) — all 4 operator corrections CORRECT, NO hidden leak.** `position-zest-v6` DEPLOYED source fetched = one-directional read-only adapter reading `SP1A27…v0-1-data get-user-ststxbtc-balances` (ststxbtc-DENOMINATED) — Fact-2 holds on the MAIN position; report wrongly cited v2 as "the adapter" (v6 = 96% of Zest-tracked encours / 26.4% of supply) → cite v6. The ststxbtc-denomination is load-bearing: it makes deposit-δ==reserve-δ 1:1, which is the ONLY reason "guard fires on increasing refresh ⟹ phantom pre-existed" is sound (a share/interest-bearing balance would trip the guard with no phantom) — rests on Zest crediting balance 1:1 w/ real vault ststxbtc (Zest-side detail). **Full inventory (clean CV decode, live get-cumm-reward=279593):** v2 ACTIVE total 295654867521 (slack 0), v6 ACTIVE total 7322352766815 reserve v0-vault-ststxbtc 7322352767815 (**slack +1000**); DEACTIVATED w/ residual totals: v3 total 2210000 **deact-cumm 188092**, v4 total 1705000 **deact 188092**, v5 total 500000 **deact 194129**; v1/v7/mock total 0. **Leak check RESOLVED (operator's offset-decode had drifted; clean values above): all residual positions have deact-cumm NON-ZERO → get-pending-rewards takes the FROZEN branch (`holder≠position ∧ deact≠0`) → capped at deactivation, NO live accrual.** (If any had deact==0 w/ non-zero total → live-cumm branch → real leak; none do. Residual one-time frozen claim = dust, e.g. v3 ≤ 2210000×188092/1e10 ≈ 41 atomic sBTC, legit pre-deactivation rewards.) Residual totals also permanently lock reactivation (`set-supported-positions` active-branch asserts total==0 else ERR_POSITION_USED, tracking-v2:213) = literally QA-07 point-3 territory, NOT a new finding. Other 2 corrections valid: (3) restore the deployed==HEAD byte-identical diff limitation (dropped in V4) + live-reads recap; (4) "stays above reserve as long as ANY phantom exists" is imprecise — true v2 (slack 0), FALSE v6 (needs phantom > ~1000 slack) → "phantom exceeds the position's residual slack (0 on v2, ~1000 on v6)". After these 4 fixes: no open axis. Independent-adversary value = checked operator's own findings vs primary state + resolved the decode, didn't rubber-stamp.

**OPERATOR LIVE-STATE PASS (2026-07-24) — finding UPGRADED to two impacts, report+PoC hardened.** Operator ran Hiro `call-read` on deployed contracts and found the reserve at **EXACT saturation**: `supported-positions(position-zest-v2).total == holder-position(pool-vault,pool-vault).amount == 295654867521` (equal to the unit; reserve=pool-vault, auto-refreshed by token hook = real ststxbtc Zest custodies, zero slack BY DESIGN). This turns the same root cause into a **SECOND impact — a permissionless DoS on honest Zest depositors**: attacker's un-refreshed withdrawal drops pool-vault by X but leaves `total` unchanged → `total>reserve`; any honest depositor's `refresh-position` then computes new-total(R+D) > reserve(R+D−X) → **ERR_OVER_RESERVE (u10002003)**, position never registered, zero rewards until someone refreshes the attacker's stale position. Fires from X=1, immediate, live-proven. Corollaries (all on the same guard): **2X cap is STRUCTURAL** (a 2nd attacker account also hits ERR_OVER_RESERVE → own phantom blocks expansion → buries multi-position idea, write "capped 2X" as fact); keeper CAN lift (on attacker's own pos `new(0)>prev(X)` false → guard short-circuits → refresh passes) so DoS window = delay till keeper touches THAT position (2681 holders = not every cycle). Also confirmed CLEAN: `pool-vault` is itself an active supported position with `is-holder-position` true → `get-pending-rewards`=u0 for it → Zest-custodied ststxbtc accrues nothing at vault level → no permanent structural double-count (closes reader's 1st objection). **REFRAME: the reserve IS the solvency invariant the bug violates (`Σ tracked ≤ ststxbtc in Zest`) — not "no global invariant" but "a deliberately-written guard pushed out of its bound."** FIX CORRECTED: my trait-based fixes were INFEASIBLE (`claim-pending-rewards(holder principal, position principal)` takes a bare principal, Clarity can't dyn-call get-holder-balance on it; hook has no trait ref) → the implementable fix = refuse to pay when `supported-positions.total > reserve.amount` (both already in storage, no interface change). Numbers corrected: Zest=295,654.867521 (was 295,741, off by 86), holders=2681 (next-holder-index). **Second Clarinet PoC test ADDED + PASSES: honest refresh-position → `(err u10002003)` executed.** Report retitled (two impacts) + Impact split §1 theft §2 DoS + economic-viability line (theft pays if sBTC-rate>Zest-rate; DoS ~free). Open: Zest same-block deposit+withdraw? (atomic cycle would defeat keeper). Header comment ("positions need refresh-position") kept OUT of opening (double-edged = "documented, user's job to refresh").

**OPERATOR LIVE-STATE PASS #2 (2026-07-24) — 2 HARD corrections + finding UPGRADED (DoS already in production). Verified all myself before editing.** (1) **Report was anchored on the WRONG position.** `get-supported-positions` registry (on-chain, NOT repo — repo ships only position-zest-v2.clar) has TWO active Zest positions: **`position-zest-v6` (MAIN: total 7,322,352,766,815 = 7.32M ststxbtc ≈25× v2 ≈27.5% of supply, reserve `SP1A27…v0-vault-ststxbtc`, margin +1000)** and `position-zest-v2` (legacy: 295,654,867,521, reserve `SP2VCQ…pool-vault`, margin **0/exact saturation**). My "the Zest position"/exact-saturation/"fires at X=1" was v2-only — FALSE for the main v6 (needs X>1000). Fixed: enumerate both, cite v6 as main. (2) **"structurally capped at 2×" = FALSE, REMOVED.** Reserves are PER-position → same X sequential deposit/refresh/withdraw on v6 then v2 = 3X tracked on X capital; N positions → (N+1)X. Operator's own error too (retracted the multi-position idea last turn on StackingDAO's marketing page instead of the on-chain registry — the exact prose-over-source mistake). (3) **UPGRADE: `ERR_OVER_RESERVE` (u10002003) has ALREADY FIRED in production** — 3× on v6 (2026-06-27 06:00 + 07:01 keeper-RETRY-also-fails, 2026-07-07 20:00), 2 distinct honest holders blocked, no auto-heal. Proves a phantom existed organically (un-refreshed Zest withdrawals) → the vuln's precondition is a recurring prod state; attacker makes it deliberate/free/persistent. DoS reframed hypothetical→already-happening + mapped to in-scope "temporary freezing of unclaimed yield" (not unmapped prose). (4) Keeper cadence MEASURED: 155 refresh/41.1d/78 holders/152 ok/3 err → ~once per position per SEVERAL WEEKS (window=weeks not hours). (5) **Phantom hunt = NULL (honest, not claimed):** the 10 recently-refreshed v6 holders all tracked==real (keeper just synced them) + v6 currently +1000 margin = no live phantom right now; the 3 ERRs prove they recur. Cross-ref method = get-holder-position(h,v6) vs `SP1A27…v0-1-data get-user-ststxbtc-balances(h)`. (6) PoC test 2 given a CONTROL: same honest refresh PASSES at saturation `(ok u500000000)`, REVERTS with phantom `(err u10002003)` → revert attributable to phantom not mis-calibrated mock. Both tests PASS. Reusable scripts: `fork/`(none)—Hiro call-read + c32 encoder in scratchpad stx_positions.py/stx_verify.py/stx_phantom.py. Report retitled ("already firing on mainnet"), em-dashes 0. LESSON re-confirmed hard: **enumerate the on-chain registry, never assume N=1 from prose** [[feedback-diff-forward-to-head-not-just-scope-changelog]] [[feedback-model-accounting-invariant-not-economic-ideal]].

**OPERATOR LIVE-STATE PASS #3 (2026-07-24) — 4 polish corrections, no kill; "no open axis left" per operator.** (1) **Report cited `position-zest-v2.clar` as "the adapter" but v2 is the SMALL/legacy one** — fixed to cite `position-zest-v6.clar` (main, ~96% of tracked Zest total; `get-holder-balance` = `SP1A27…v0-1-data get-user-ststxbtc-balances`), v2 noted same-shape. (2) **JOINT of the ERR inference:** v6 reads an **ststxbtc-denominated** balance → deposit-delta & reserve-delta move 1:1 → the 3 production `ERR_OVER_RESERVE` fires prove a phantom (tracked > real), NOT a rebase/interest artifact. Without this a share/interest token could trip the guard without a phantom → the whole prod-evidence inference would collapse. (3) **Full inventory (clean re-decode, operator's slipped):** 7 Zest adapters v1-v7 + mock; **2 ACTIVE (v2 295,654,867,521 / v6 7,322,352,766,815)**; 5 deactivated, 3 with residual nonzero totals (v3=2210000/v4=1705000/v5=500000) whose `deactivated-cumm-reward`=188092/188092/194129 (SANE, my clean decode) → accrual FROZEN via get-pending-rewards branch + reactivation PERMANENTLY LOCKED (set-supported-positions `is-eq total u0`→ERR_POSITION_USED, tracking-v2 L213) → no leak, answers "do dead positions leak?". (4) margin phrase fixed (phantom must exceed residual margin: 0 on v2, ~1000 on v6, not "any phantom"). Deployed-diff/byte-identical limitation confirmed still intact (grep=1). Verify scripts: scratchpad stx_inv.py (full registry + clean field decode). em-dashes 0.

**OPERATOR RABAT-JOIE PASS #4 (2026-07-24) — 5 finishing corrections, ALL applied + self-verified, "je n'ai plus rien / la grille est passée en entier".** Pure finition, no kill. (1) **v6 correction was half-applied:** Fact-2 led on v6 but the mock-fidelity limitation still credited only `position-zest-v2` → reworded to anchor faithfulness on BOTH live adapters, naming v6 as the main (~96%). (2) **Arithmetic fix §2:** "v6 ≈27.5% of supply" was WRONG for v6 alone — v6=7322352766815/27746064338767=**26.4%**; the 27.5% is v6+v2 (7618007634336=27.46%). Verified myself. Fixed to "26.4%, ~27.5% counting v2". (3) **Margin qualification propagated:** Steps-to-reproduce ("every honest depositor reverts") and Economic-viability ("X=1 unit") both silently assumed v2's zero slack → now qualified (any amount on v2, >~1000 on v6). (4) **Enumeration honesty:** a Clarity map has NO read-only enumeration → changed "the registry LISTS seven adapters" to "probing by name SURFACED", + added the exclusion evidence (41-day keeper history only ever refreshes v2/v6 → bounds the live set). Same prose-over-primary-state class as the inventory fix, don't reintroduce it in the sentence that corrects it. (5) **Header↔mapping consistency:** severity banner said "permissionless denial of unclaimed yield", Impact maps to in-scope "temporary freezing of unclaimed yield" → banner now uses the mapped chain (read first). Also swept the last en-dash (`v1–v7`→"v1 through v7"): em-dash AND en-dash now both 0. Operator's method note carried: grep-on-latest proves present-now, not "never dropped in an intermediate version" — diff prior-version→current before sending. Residual risk is NON-technical: whether the team stretches the QA-07 ack despite the opposite-sign demonstration; best available counter built on their own text, plays out on their side.

**OPERATOR PASS #5 (2026-07-24) — 2 nits, applied + verified, then "Envoie" (cleared for submission).** (1) **`3X`/`(N+1)X` rested on an unverified precondition** — "run the same on v2" assumes the legacy Zest market behind v2 still ACCEPTS deposits; v2 holds 295,654 ststxbtc but "not empty" ≠ "open to entry". Only v6 verified open → reframed `2X` on v6 as the guaranteed FLOOR, `3X`/`(N+1)X` as conditional, and folded the v2-open-to-deposits question into the existing Zest-side limitation (same contracts to consult as the same-block-cycle question). Claim-posed-as-fact → now hedged. (2) **`F-01` was used 4× in dedup as an identifier never defined in the doc** — a reader hunts a missing reference. Removed the internal label entirely (external single-finding report), replaced with natural phrasing ("this finding" / "what I'm reporting"); the AUDITOR's own IDs (C-01/QA-07/L-02) stay, those are lookup-able. em/en-dash still 0/0. Operator's close: every axis shut by an ARTIFACT not an argument (PoC executed w/ control, deployed==diffed code, both impacts on independently-readable on-chain state, dedup on their own auditor's text, inventory stated with method+limits). Only residual is non-verifiable-my-side: whether the team stretches the QA-07 ack past the opposite-sign demo. "Envoie."

**RESPONSE-PHASE PLAYBOOK (operator directive, use if/when the team replies):** if the response pushes on the THEFT leg §1 — "dilution / magnitude / just redistribution between users" — do NOT relitigate the `add-rewards` denominator (correct but interpretation-space, where "it's arguable" is their win). **Re-anchor on the three production `ERR_OVER_RESERVE` events (§2):** their own keeper, their own MAIN position v6, their own retry an hour later failing (tx `0xa29fd9…`/`0x3b5fae…` 2026-06-27, `0x2bd0ca…` 2026-07-07). They can't requalify their own executed txs. Fight on the leg whose evidence is THEIRS, not the leg whose mechanism they can reframe. See [[feedback-under-downgrade-pivot-to-defenders-own-artifacts]]. (§1 still stands — this is which leg to STAND ON under push, not which to drop.)

**⛔ SUPERSEDED 2026-07-26: PROGRAM REMOVED FROM IMMUNEFI (operator). F-01 is NO LONGER FIREABLE — no payable venue unless StackingDAO relists or a different platform hosts it. The "fire at discretion" / RESPONSE-PHASE PLAYBOOK below are DEAD unless the program returns. Finding stays real + PoC-proven (disclosure-only value). Do NOT propose submitting it.**

**STATUS (pre-removal, historical): CLOSED — ON ICE (parked 2026-07-24, "on garde le finding au frais"). Finding COMPLETE + preserved.** Operator cleared it ("Envoie" after PASS #5) then chose to hold rather than fire now — the report is done, the only remaining step is the operator's own post on Immunefi (account/pseudonym, outward-facing/irreversible; I do not submit). Dossier closed clean: OUTCOMES.jsonl final entry `cleared_on_ice`, .lifecycle-status `closed:` line, internal findings/F-01 bannered as superseded by submissions/F-01, OPSEC-swept (deliverables key-free; only hits are StackingDAO's own upstream simnet test vectors under contracts/). **To re-activate: read submissions/F-01 + the RESPONSE-PHASE PLAYBOOK above; nothing to re-derive.** 5 rabat-joie passes absorbed (denominator/DoS · v2↔v6+(N+1)X+prod-events · adapter+inventory+margin · these 5 finishing). Operator: "je n'ai plus rien" after PASS #4.** Report = submissions/F-01 (chill-styled, human-triage, dup pre-empted, limitations honest, em/en-dash 0). Meta win: the landscape-scan's freshness/audit claims were wrong (like OnRe), the gate caught it, but the CROWN-JEWEL seam (reward-tracking staleness = the reason to be here) still held [[feedback-check-prior-audits-and-competitions-at-intake]] [[feedback-diff-forward-to-head-not-just-scope-changelog]] [[feedback-today-impact-before-poc]]. Clarinet PoC harness reusable for any Stacks/Clarity target.
