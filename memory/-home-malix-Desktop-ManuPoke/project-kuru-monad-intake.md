---
name: project-kuru-monad-intake
description: "Kuru (CLOB+AMM vault DEX on Monad, Cantina) — GO/live lead: KuruAMMVault flat-owed partial-fill PnL grab by dust-share withdrawer; needs Monad-fork PoC"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1cddd5e1-a775-4c40-bc31-1244e19cbfd8
---

**Kuru** — CLOB + AMM "Active Vault" hybrid DEX on **Monad mainnet** (EVM). Cantina bug bounty. FRESH (no prior corpus). Fresh clone: `~/Desktop/ManuPoke/kuru-fresh/{Kuru-contracts-dex-public, -active-vault-public, -flow-public}`. Scope: DEPLOYED on-chain = source of truth (GitHub reference-only) EXCEPT **Kuru Active Vaults where GitHub IS authoritative** (Vault.sol + LiquidityShares.sol). In-scope deployed: KuruFlowEntrypoint/Router, KuruAMMVaultImpl(0xDC…70F4), KuruForwarder(+Impl), KuruUtils, MarginAccount(+Impl), MonadDeployer, OrderBookImpl, Router(+Impl). OOS: vault-DoS-low-liq, badly-set-market-params, flip-order-minSize-DoS, **KuruForwarder direct-market-price-check (scoped to Forwarder ONLY)**, non-standard ERC20.

**TWO DIFFERENT VAULTS — don't conflate:**
1. **Active Vault** (`-active-vault-public/src/Vault.sol` + LiquidityShares.sol) — standalone MM vault posting real orders; GitHub-in-scope. READ IT: notional = getRestingBalances(free margin) + getPostedBalances(open-order value); double-count REFUTED (placing order `_consumeFunds→marginAccount.debitUser` so resting excludes posted). Core accounting careful. BentoBox share math (+1/+1e8 offset).
2. **KuruAMMVault** (`-dex-public/contracts/KuruAMMVault.sol` + AbstractAMM.sol) = deployed `KuruAMMVaultImpl` 0xDC…70F4, **integrated INTO the orderbook** (AbstractAMM provides vault liquidity as virtual orders vaultBestAsk/Bid, sizes vaultAskOrderSize/BidOrderSize, partiallyFilledAsk/BidSize). totalAssets()=margin free balances only. Share price via `_convertToShares`/`_convertToAssetsWithNewSize` + `_returnVirtuallyRebalancedTotalAssets` (50/50 rebalance at manipulable vaultBestAsk). **THIS is the crown jewel; I first read the wrong vault, swarm corrected it.**

**LIVE LEAD (REAL_NEEDS_POC / SUSPEND_HARNESS) — flat-owed partial-fill PnL grab:**
`KuruAMMVault._convertToAssetsWithNewSize` (KuruAMMVault.sol:397-450), withdraw path. When `partiallyFilledAskSize >= _newAskSize || partiallyFilledBidSize >= _newBidSize` (vault ask/bid ~fully partial-filled), the withdrawer's payout = `mulDiv(shares, reserveBase±owed, supply) ± uint256(|baseOwedToVault|)` (lines 429/433/438/442) — the `± owed` is a **FLAT term OUTSIDE the proportional mulDiv**, independent of `shares` burned. So a **dust-share FIRST-withdrawer absorbs the ENTIRE pending partial-fill PnL** (`|owed|` = net base/quote the vault gained from partial fills), which belongs to all LPs proportionally. `_nullifyPartialFills=true` resets it after → one-shot per partial → first dust withdrawer steals it from remaining LPs. Survives OOS (price-clause scoped to Forwarder; positive-liq extraction not low-liq DoS; untrusted-reachable: anyone holds dust shares + causes/awaits a partial fill + withdraws).
- **OPEN QUESTIONS for the fork PoC** (why SUSPEND not REAL): (a) branch reachability — for a dust withdrawer `_newAskSize≈full`, so needs `partiallyFilledAskSize≥full ask`, but AbstractAMM resets askPartiallyFilledSize=0 on price-move (lines 83/150) → is the "partial ≥ newSize, not price-moved" state reachable? (b) NET extraction — the attacker who CREATES the partial (trades through vault) PAYS the spread; does the flat-owed grab exceed that cost? (c) does the vault's margin actually HOLD `|owed|` (else withdraw reverts on marginAccount.withdraw). NEEDS Monad state-fork PoC: clone live KuruAMMVault+OrderBook+MarginAccount, construct partial-fill state, withdraw dust shares, measure delta vs cost.

**Swarm (5-subsystem, 15 verdicts): 0 clean REAL, all else refuted/OOS but residuals converge on KuruAMMVault reconciliation.** Other refuted: OrderBook fill-conservation (maker credit roundDown / debit roundUp = protocol-favorable; per-currency conserved via price discretization pricePrecision<vpp=1e18 forcing P_taker=P_maker); creditUsersEncoded decoder safe (128B static tuple, verifiedMarket-gated); MarginAccount uninit-proxy (live state proves initialize consumed); flip-order re-post conserved. Residual worth a look: Router `anyToAnySwap` no msg.value==Σinputs assert + no residual sweep (latent, not payable today) — verify DEPLOYED Router bytecode.

**2026-07-24 UPDATE — flat-owed lead REFUTED by EXECUTED local-deploy PoC (conservation holds). Vault crown jewel = executed-null.** Built full Kuru stack locally (Router+MarginAccount+OrderBook+KuruAMMVault, OZ pinned **v5.5.0** — v5.6.1 breaks empty-data proxy deploy w/ ERC1967ProxyUninitialized; SIZE_PRECISION=1e10/PRICE=1e2/SPREAD=30; `test/VaultReconPoC.t.sol` + `VaultSandwichPoC.t.sol` in kuru-fresh dex repo). Results:
- **Flat-owed partial-fill grab (KuruAMMVault:426-443): REFUTED.** Forced the branch to FIRE (attacker=50% vault, fill bid to 99% → bidPartiallyFilled 2.97e9 > newBidSize ~1.5e9 → branch fires). Conservation HELD EXACTLY: attacker net = **0 ETH / −1 wei USDC**, LP1 recovered exactly seed (100 ETH/200000 USDC), Σwithdrawals ≤ vault assets (drained to MIN_LIQ dust). The `±owed` flat term correctly settles the withdrawer's pending partial-fill PnL — NOT a bug. (Dust-withdrawer variant already dead: bidPartiallyFilledSize resets to 0 at full-consume, AbstractAMM:208-209, so `partial≥newSize` unreachable for small f.)
- **Deposit price-manip sandwich: REFUTED.** Self-sandwich (buy base→ask 2000→18026 9×→deposit→sell back→withdraw) = **−89,939 USDC net loss.** Deposit gate `require(_expectedQuoteAmount = baseDeposit*askPrice <= quoteDeposit)` (KuruAMMVault:203) forces proportional quote at CURRENT price → can't deposit cheap at manipulated price; shares fair-at-price; manip cost dwarfs gain.
Both primary vault leads executed-refuted. Combined w/ swarm (OrderBook fill protocol-favorable+per-currency-conserved, MarginAccount gated, Active Vault double-count refuted) → **Kuru vault + core = EXECUTED-NULL on the crown jewel.**
RESIDUALS NOT PoC'd (if re-engaging): victim-sandwich (analytically fair-at-price, low EV given self-sandwich loss), Active-Vault single-side swap paths (minOut-gated), Router anyToAnySwap native-stranding (verify DEPLOYED bytecode), deeper OrderBook flip-cancel. RE-SOURCE likely. [[feedback-model-manipulation-cost-before-crediting-twap-finding]] [[feedback-invariant-that-passes-is-not-a-finding]]
