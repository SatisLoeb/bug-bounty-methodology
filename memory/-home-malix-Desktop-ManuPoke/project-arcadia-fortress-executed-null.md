---
name: project-arcadia-fortress-executed-null
description: "Arcadia Finance (HackenProof, Base/OP/Unichain margin protocol) — 10-audit fortress, all fresh drift + cross-file seams EXECUTED-NULL → RE-SOURCE"
metadata: 
  node_type: memory
  type: project
  originSessionId: d1f7d135-d6b0-4927-99e3-6e7cb746528b
---

Arcadia Finance smart-contract bounty (HackenProof, LIVE, Critical tier on all 3 in-scope repos; KYC+POC required, $5 submission cost). Non-custodial composable margin protocol for leveraged yield on AMM LP positions. Repos cloned ~/Desktop/BUGS/arcadia-2026: accounts-v2 @ad1c797, lending-v2 @c261329, asset-managers @ca02535 (all built green, PoC-ready). Deployed accounts: Margin V2 0x78Db6a136EdD0F70bEd7a6eb5ca2fDF6eE16E8D6 + Spot V2 0xe976BFb4… (all chains); Margin V1 0xbea2B6… + Spot V1 (Base only, legacy).

**FORTRESS — 10 audits 2023-12→2026-01** (Trust, Pashov, Renascence x3, Sherlock x4) in-repo at arcadia-finance-audits/audits-v2. Scope is surgically fenced to kill known findings: Dutch-auction frozen-share valuation OOS, exposure-caps NOT a solvency invariant (maxExposure miscount = OOS Low), trusted roles (owner/initiator/keeper/guardian) + trusted third-parties (Chainlink, CoW solvers) OOS, recoverable/reversible never Critical, single-user capped at High, live-on-chain-state is sole reference. See [[feedback-read-both-or-clauses-in-exclusions]].

**HUNT DONE (ultracode): 10-surface map→hunt→3-lens-verify workflow (21 agents, ~3M tok) → 0 candidates.** Maps were substantive with EXECUTED self-refutation; every fund-moving asset-manager path is initiator-gated (trusted→OOS). Then 3 focused cross-file-seam agents (the seams no file-scoped agent could close), each with a Foundry PoC:
- **(A) cross-account strand** in CL-manager UniV4 absolute-balanceOf + native-ETH/WETH dance + RouterTrampoline → REFUTED (200-run fuzz PoCs, residual=0; _unstake unwraps full WETH exactly compensating _claim reset; _approveAndTransfer drains deposit+fee==balances[i]; residual only via donation=OOS+skim-recoverable). Latent smell only: Slipstream._unstake `balances[2]=rewards` overwrite vs _claim `+=` (saved by gauge zeroing earned).
- **(B) junior-tranche permanent freeze** via settleLiquidationUnhappyFlow checked `TRL-badDebt` (LendingPool.sol:1044) w/ concurrent auctions → REFUTED (2 PoCs). Invariant TRL≥realisedDebt≥openDebt>badDebt always holds (assets≥liabilities; unhappy-settle shrinks both sides in lockstep). Only trigger = full-insolvency (OOS market risk), dev-documented, AND cleared by permissionless donateToTranche → Low. Sub-lead _calculateTotalShare div-by-0 also dead (generateAssetData can't emit amount-0; cutoff branch skips divisor).
- **(C) cross-version reentrancy**: V1/V2 have 0 AccountsGuard refs, V3/V4 use the global single-slot lock → topology hole REAL but REFUTED (3 PoCs). LendingPool books debt atomically before untrusted actionTarget runs; each account health-checks its OWN collateral (no double-use); only cross-account shared writer reachable w/o guard = Registry exposure = OOS AND never feeds valuation/health (getValue is STATELESS). Bonus: factory maps v1/v2→AccountPlaceholder (which DOES lock, no fund ops) so a functional non-guard account can't be created.
- **AerodromePoolAM stable-pool k-overflow** revert-DoS → CLOSED by arithmetic: k=a·b overflows only at ~87B tokens/side (>>Base stablecoin supply), audited-core, unrealistic-precondition → OOS.

Also checked & null: CowSwapper (orderHash binds buyToken=tokenOut + appData binds account/tokenIn/amountIn; no swap w/o trusted-initiator sig; zero-rest-balance invariant holds), staking-AM uint128 cap PR#349 (only fires at totalStaked≈1wei, self-harm, Medium-max), UniV4 LP valuation (oracle-anchored min-of-convex = conservative, fees capped, remove-hooks disallowed), Registry/oracle aggregation (stateless valuation, no untrusted writer).

**(C) EXECUTED-CLOSED (upgraded from structural):** built a real-AccountV1 PoC — lending-v2/test/probe/CrossAccountReentryV1.probe.t.sol. deployCodeTo real AccountV1 at the version-1 impl addr 0xbea2B6 (fixture stubs it as guard-using placeholder) + pool.setAccountVersion(1,true). Result (PASS): the re-entrant pool.borrow that REVERTS AccountsGuard.Reentered for a V3 second account SUCCEEDS for a legacy V1 second account (hole is REAL) BUT no corruption — realisedDebt==sum of both debts (no double-count), pool paid exactly aLoan+bReenterBorrow (no liquidity double-spend), both accounts healthy on their own collateral. Guard hole real + harmless, confirmed end-to-end.

**VERDICT: FORTRESS, fully exhausted (not conceded — every gate pierced with an executed artifact). NO payable finding for solo. RE-SOURCE.** Don't re-audit. Residuals all low-EV: RedStonePushOM staleness-vs-push semantics (owner cutOffTime config=OOS). Pattern-matches [[project-tare-sherlock-fortress]], [[project-valantis-stex-fortress]], [[project-rheo-veryliquid-intake]], [[project-reserve-dtf-filler-seam-null]] — heavily-audited EVM = executed-NULL for solo. PoC evidence files left in asset-managers/test + lending-v2/test.
