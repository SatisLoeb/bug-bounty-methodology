---
name: project-okx-smartwallet-executed-null
description: "OKX Labs smart-wallet-evm (the one un-swept repo of the earned-null OKX scope) = fresh but fortress, 8+8-agent executed-NULL"
metadata: 
  node_type: memory
  type: project
  originSessionId: 22b58339-599f-4569-b2f0-7ae17db7e4a7
---

OKX Labs bug bounty (private; $1M Crit / $100k High / $10k Med per OUTCOMES). The whole 9-repo scope was already **earned-null 2026-07-05** ([[feedback-consult-operator-bugs-corpus-first]] — see `okx-labs-onchain-breadth-earned-null-2026-07-05` in OUTCOMES.jsonl: routers/PMM/Boost = residue-scoop DUST, refuted on-chain). On 2026-07-22 re-ran `/nuke` on the scope.

**Drift check (the only fresh mechanical angle):** 7 of 9 in-scope repos byte-identical HEAD to the exhausted clones (zero drift → re-barrage = re-derive dust). Two repos had NO local clone / were never mechanically barraged: **`okx-smart-wallet-evm`** and **`Boost-TokenDistributor-Solana`**. Cloned both to `~/Desktop/BUGS/okx-2026/`.

**okx-smart-wallet-evm** (HEAD 95aa59bbc22a; deployed 7 mainnets ETH/XLayer/Base/OP/ARB/BSC/Polygon; factory 0xDd3FEa01cD550C9EFfC893f346690b9A649f35EF): modular ERC-4337 + EIP-7702 + **chainless** (cross-chain) wallet, 32 files / ~2067 LOC, highest ceiling in scope (auth-bypass/priv-esc = Crit). Build fix: shallow submodules needed full fetch; `lib/smart-wallet-recovery` = PRIVATE/missing (github.com/okx/smart-wallet-recovery 404, tests-only import → moved test/Recovery.t.sol aside); added `FreshCryptoLib/=lib/webauthn-sol/lib/FreshCryptoLib/solidity/src/` remapping; `DEPLOY_FACTORY_SALT` env → **366 dev tests pass**.

**EXECUTED-NULL** = manual full read + NUKE barrage + 8-finder×8-skeptic Door-A workflow (wf_48a1ecd3-f90, 16 agents, 0 err, 1.35M tok) + 3 in-repo audits (CertiK Sept-2025 **0-Crit/0-Major**, BlockSec, OKX self-audit → all OOS "prior audits"). **8/8 seams KILL, 0 survivors, 0-worth-PoC.** Every crown-jewel gate holds STRUCTURALLY (cheap-gate → ZERO PoCs burned, across-pattern [[project-across-svmspoke-intake]]):
- **Chainless** (Crit): selector-only allowlist (`ChainlessLib` addOwner/upgrade self-calls, chainId omitted) BUT execution admin-gated — `_batchCall` `NonAdminSelfCall` on every self-call, and the keyHash reaching it is the AUTHENTICATED signer on all 3 entrypoints (not attacker-chosen) → non-admin passes `validateUserOp` (no admin check) but reverts at exec, zero state. Admin-signed cross-chain scope = the intended tested feature = OOS centralization. Two nonce spaces (EntryPoint vs wallet `_nonces[196]`) + two hash domains (EIP-191 userOp vs EIP-712 sansChainId relayer) can't cross-collide.
- **Merkle** (Crit): 2nd-preimage/internal-node forgery needs attacker-chosen leaf; leaf = `hashTypedData(structured EIP-712)` on all 3 sinks → never attacker-controlled ([[feedback-invariant-that-passes-is-not-a-finding]]).
- **delegateAndRevert** (Crit, the ONLY semgrep HIGH): `target.delegatecall(data)` then UNCONDITIONAL `revert` next line (EntryPoint sim pattern) → all state (incl. selfdestruct, EIP-6780) rolls back. Not exploitable.
- Cross-path sig reuse: 3 distinct typehashes (MESSAGE vs BATCHED_CALL vs EIP-191) — one sig can't satisfy two paths. Hook: bound per-key via `getHook(_ownerSettings[keyHash])` for the authed key. Decode divergence: callData is sig-committed on both decode paths (DecodeLib fuzzed + overflow-hardened). Init front-run: salt binds owner-set + onlyFactory. Storage: `SmartWalletEntry ... layout at 0x653ff6…` relocates all state to ERC-7201 root (no 7702 slot collision).

**Boost-TokenDistributor-Solana** (9ea1ae6c8730; deployed mainnet `2Ab8No85xrnnd2rFamKjQqGAK6Qu9EQ4eEh59oaZEPBL` BPFLoaderUpgradeable = eligible; Anchor 0.31.1, 7 ix ~600 LOC): /solfork'd 2026-07-23 (OPEN-source → RE half skipped; live-fork execution = the payable part). Money-exit = `claim` (merkle-gated) + `withdraw` (owner-gated). Saturation = OKX **self**-audit 2025.08.07 (1 Low centralization + 1 Info, 0 Crit/High/Med) BUT self-audit + post-audit drift (audited `0b004641…` ≠ HEAD `9ea1ae6c…`) → executed the crown jewel, didn't trust it. **Crown jewel = double-claim via `close_claim_status` reset** (canonical merkle-distributor bug + Door-C deviation: they added `close_claim_status` + `init_if_needed` claim_status = the exact recipe). **EXECUTED REFUTATION** (anchor build → litesvm real bytecode, `tests/attack_double_claim.js`, 5/5 pass): reset window (`now>end_time`) and claim window (`now≤end_time`) are DISJOINT — close in-window REJECTED 6009 DistributionNotEnded; re-claim w/o reset REJECTED 6013 InvalidAmount (cumulative `claimed_amount` guard); past-end close OK but fresh claim REJECTED 6008 DistributionEnded → attacker keeps only legit 500, vault untouched, NO double-drain. Merkle 2nd-preimage = preimage-hard leaf (`hashv(claimant‖amount)`, no domain-prefix but attacker can't control leaf). operator/owner auth = Signer+key-match = centralization OOS. `distributor` not-PDA-validated in claim = harmless (vault+claim_status+root all bind `distributor.key()`). Build gotchas: `layout at` N/A here; node22 strips TS → write PoC as plain `.js` for mocha; error codes count from 6000 (repo's own test error-map is stale off-by-one — DistributionEnded=6008 not 6007). 1 PoC burned, refuted the theft.

**Verdict: RE-SOURCE off the WHOLE OKX SC program** — now executed-NULL across all 9 repos + the two un-swept ones (wallet + distributor). Real EV is off-chain/API (OOS here, per the 2026-07-05 outcome). Only re-open if OKX ships a NEW contract holding standing balance or an in-scope off-chain/API surface. Don't re-audit.
