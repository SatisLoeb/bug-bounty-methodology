---
name: project-phantom-onchain-noncustom
description: "Phantom Cantina bounty — on-chain assets (PSOL/CASH) are non-custom; payable surface is off-chain/client, not the programs."
metadata: 
  node_type: memory
  type: project
  originSessionId: e128024b-a2c9-4cc6-b0e0-d5a42651f787
---

Phantom bug bounty (Cantina, live 1 Jul 2026, max $100k, Onchain Critical=$100k headline). Workspace: `~/Desktop/BUGS/phantom-recon/TARGET-DOSSIER.md`.

**P0 gate resolved with executed on-chain reads (orca Rule 43): the two in-scope on-chain assets have NO Phantom-authored custom program.** Don't re-audit them from scratch.
- **PSOL** `pSo1f9nQ…` = instance of the canonical **spl-stake-pool** program (Phantom docs: "nine audits, 0.1% withdrawal fee"), stake pool `pSPcvR8Gm…`, SPL Token, ~1.45M pSOL supply. Bug there hits all SPL-stake-pool LSTs, 9× audited → near-zero custom surface. SKIP for solo SC.
- **CASH** `CASHx9K…` = **Stripe "Open Issuance" (Bridge)** USD stablecoin on **Token-2022**, ~127M supply. mintAuth `HJT2…`, permanentDelegate + confidentialTransfer authority = `E7Jd…` (issuer), **transferHook.programId = null** (no Phantom program in transfer path). Issuance = third-party vendor = OOS. Stock Token-2022 + issuer controls, not a custom-program bug.

**RE-SOURCE (where Phantom custom code + money movement live):** (1) the **CASH off-chain↔on-chain orchestration seam** — Stripe issuance ↔ api.phantom.app backend ↔ Token-2022 ↔ Cash accounts/bank-funding/P2P-by-username/Visa-card/idle-yield (freshest, funds move, seam nobody owns → /upshift); (2) client-side signing / tx-construction / tx-analysis (extension `bfnaelmomeimhlpmgjnjophhpkkoljpa` + mobile); (3) web/API (auth./connect./api.phantom.app, trade.phantom.com). Per-group Critical caps: Onchain $100k / Infra $50k / Client $50k / Web $20k.

Recommended entry: **/wide** (allocate depth first — on-chain jackpot is a mirage) → **/upshift** on the CASH seam. NOT mrrobbot/darkside/extract on the programs. Payments/fintech shape → FINANCIAL-SYSTEMS-HUNT (3-10x payout, lower competition). See [[feedback-consult-operator-bugs-corpus-first]].

**4 passes run 2026-07-14 (all account-free static/passive; each hit a HARNESS+SUSPEND — every payable vein needs live authorized testing the program requires as "proof of exploitability"):**
- **/wide**: ranked client-side #1 (account-free), web/API #2, CASH-seam #3. Files: STAGE0-ALLOCATION.md.
- **/darkside (client extension)**: tx-preview invariant backstopped by Blowfish scan + **Lighthouse on-chain assertions** = fortress; P3 signMessage/SIWS displayed≠signed is the only backstop-free vein but `isValidUTF8`+hex-fallback+SIWS-domain-whitelist defend it. SUSPENDED. HIGH-saturation. DARKSIDE-client-txdecoder.md.
- **/upshift (CASH seam)**: freeze authority `AyCP3o…` DORMANT (3 sigs) + permanentDelegate `E7Jd…` active → card holds ≠ on-chain freeze; CASH sends are client-signed relays (`/cash/v1/users/{id}/cash-account/send` w/ signedTransaction). VEIN #1 card auth↔settle double-spend on self-custody token = strong thesis but deciding fact (self-custody-ATA vs custodial-float) is server-side + PoC = live authorized card test = fraud. UPSHIFT-cash-seam.md.
- **/gravedigger (web/API, BEST LEAD)**: KMS is Phantom-custom (`@phantom/openapi-wallet-service`, NOT Turnkey → in scope). **G-01: `IdTokenVerificationClaims.aud` is [OPTIONAL]** + client OIDC type omits aud → if prod registers OIDC authenticators without `aud`, a Google id_token for the same `sub` from an attacker OAuth app stamps the victim's embedded wallet = mass ATO. Google sub is cross-client-stable (Apple is per-app). SUSPENDED — **one live read (is aud set on a test embedded wallet?) resolves it**; the sharpest lead, worth an operator live test. GRAVEDIGGER-oidc-kms-seam.md.

META: Phantom = mature/well-defended, mostly web2/off-chain; NO submittable finding from pure account-free/passive solo work (program requires proof-of-exploitability → all veins need live authorized tests). Two theses worth live resources if engaging: gravedigger G-01 (OIDC aud) > upshift vein#1 (card double-spend).

**COLD POKE (apparatus-OFF, 2026-07-14) — found surfaces the 4 apparatus passes never listed (POKE-juicebox-recovery.md):**
- **P-04 (STRONGEST, fresh) Auto-Confirm ("Automation") usdLimit bypass:** Auto-Confirm = CEL policy `DAPP_CONNECTION_USER` `{usdLimit, usdCentsLimitPerDay, allowedMethods, solanaProgramAllowlist: Bs[appId]??null}` enforced by Automation API (`/automations/policies`). Program allowlist defaults null (unrestricted) off the hardcoded `Bs` map; even restricted appIds allowlist SPL Token. Door-C: an SPL Token `Approve(u64::MAX)`/`SetAuthority` = $0 transfer value → passes usdLimit → allowed program → auto-signed NO prompt → delegate drains. Load-bearing unknown = does the Automation backend flag Approve regardless of value (server-side). HARNESS+SUSPEND (2-owned-wallets, devnet). Less-tested than OAuth.
- **P-01 Juicebox recovery** = 2-of-2 realms (production-juicebox.phantom.app + juicebox.rpcpool.com), recover_threshold 2, Standard2019. Concern = is rpcpool a genuinely independent realm operator (else Phantom reaches threshold alone → offline PIN brute-force). Unconfirmable from bundle.
- **P-02 signer.phantom.app** = takeover REFUTED (NODATA, no dangling CNAME, Phantom's zone); dead host in RN SDK 0.0.39.
- **P-03 connect.phantom.app redirect_uri** = server-validated vs appId, no read-only bypass, dup-heavy → low EV.
- New hosts poke found (apparatus missed all): signer/blowfish-blocklist-proxy/node-proxy/data/rt/gas-price-oracle/sanity-proxy-v2/eppo-proxy/production-juicebox/staging-*.phantom.app.
LESSON APPLIED: [[feedback-manual-poke-mandatory-bracket]] — poke-first found P-04 (real Door-C vein) that no skill/corpus class names; ran it BY HAND, no workflow.

**NUKE barrage run on spl-stake-pool source (2026-07-18, demonstration only — programs are OOS-adjacent):** cloned `solana-program/stake-pool` HEAD = **v2.0.3 @ 55927ab** (deployed PSOL bytecode ≠ HEAD; reproduce-build needed for real attribution). Barrage = solana/cargo, 691 sig → 1 high cluster (cargo-deny license/dep hygiene, NOT theft), 22 silent theft-classes = the deliverable. Mechanical surface DEFENDED: (a) `overflow-checks` absent from release BUT all value-math (`calc_pool_tokens_*`, `calc_lamports_withdraw`) returns `Option<u64>` via `checked_*` (34 calls, raw ops only in `#[cfg(test)]`); (b) only **3 `unsafe` blocks** total (`big_vec.rs:51,74` sol_memmove in `retain()`, `state.rs:731` sol_memcmp) — all operate on ptrs from Rust-bounds-checked slices (`self.data[a..b]` panics before `.as_mut_ptr()`) → worst case = panic/DoS on validator-list path, not memory-corruption/theft. Tooling gaps noted: cargo-audit DB parse-fail (CVSS4.0), solana-lints dylint no-libraries, geiger needs abs-path on `program/`. Freshest on-chain class if ever forced: **Token-2022 mint-extension handling** (v2.0.3 just added `spl-token-2022-interface`).

**Token-2022 vein PUSHED to executed-NULL (2026-07-18) — DEFENDED, manager-trust-bounded:** the whole extension surface is carefully hardened. (1) init (`processor.rs:767-797`): pool mint supply==0, decimals==native, mint_authority==withdraw-PDA, **freeze_authority MUST be None** (`:787`, the "// but a freeze authority is not" comment IS enforced, not just noted), extensions ⊆ `is_extension_supported_for_mint` (8-item allowlist, `state.rs:500`). (2) fee account validated via `check_manager_fee_info` (`state.rs:295`: owner/init/mint-match + fee-account allowlist `state.rs:523`: only Uninitialized/TransferFeeAmount/ImmutableOwner/CpiGuard; ConfidentialTransferAccount deliberately EXCLUDED w/ documented reason) at init (`:799`) AND on set_manager (`:3467`). (3) deposits = `mint_to` only (no transfer-fee); withdraw fee = `transfer_checked` (`:3089/:3255`) but the fee tokens go to the MANAGER's own account → transfer-fee shortchanges only the manager, never depositor/reserve. (4) withdraw waives fee (=0) if fee-acct invalid → no DoS. (5) exchange-rate uses INTERNAL `pool_token_supply` (mint++/burn--), robust to transfer-fee harvest/withdraw (those don't change supply). (6) DefaultAccountState neutered by freeze_authority==None; Interest/Confidential/Metadata = display-only/raw-amount-inert. **No untrusted-attacker path; every residual reduces to a manager configuring a hostile-but-disclosed LST (by-design, extension explicitly supported).** On-chain surface now FULLY closed for solo. Verdict unchanged: EV off-chain. Artifacts: `spl-stake-pool-src/.nuke/20260718-201315/`.

**G-01 (OIDC aud) PUSHED via static SDK read (2026-07-18) — DOWNGRADED to LIKELY-REFUTED for the consumer flow (correcting the earlier "sharpest lead" ranking):** confirmed the FOOTGUN at contract level — `sdk/ows` `IdTokenVerificationClaims{ sub:required, iss?:null, aud?:null }` (only sub required) + extension Zod `idTokenClaims{sub,iss,aud:.nullish().default(null)}`. BUT the architecture refutes the exploit path: `sdk/epc`+`sdk/client` show the KMS is authenticated by a **`authenticatorKind:"keypair"` stamper** (StamperWithKeyManagement/AuthHeaderStamper), NOT a raw Google id_token per request; Google login is **mediated server-side by Phantom's own auth** (`auth.phantom.app`, `authProvider:"phantom"`, an "a2t" token) → the only client-side iss/aud check binds Phantom's OWN issuer (`iss="https://phantom.app"`, `cO="https://api.phantom.app/oauth2/auth"`), no `accounts.google.com` issuer verified client-side, no Google client_id in the extension. The optional-`aud` `ExternalOidcAuthenticator` is a KMS feature for **third-party integrator** OIDC orgs (their config, OOS-flavored), not the Phantom Google consumer path. No reachable point accepts an attacker-`aud` Google token as a KMS credential (attacker would need to forge Phantom's a2t/issuer keys). Formal settle still = Tier-1 HAR read (does the token stamped to `api.phantom.app/v1/wallets` carry `iss=accounts.google.com` → lives, or keypair-stamp/`iss=phantom.app` → dead) but source now PREDICTS dead. **Leading off-chain thesis reverts to P-04** (Auto-Confirm $0-Approve usdLimit bypass); its load-bearing fact is server-side (does Automation backend flag Approve) → needs the 2-wallet devnet test, client-side half firmable statically from `crx/`.

---
**UPDATE 2026-07-20 — FULL-TAXONOMY OFF-CHAIN PIVOT (operator: "avant on cherchait exclusivement le vol").**
Re-verified on-chain FRESH: PSOL (pSo1…RQfL) = standard SPL Token, mintAuthority=2zeXA69d… (spl-stake-pool PDA), freeze=none
→ spl-stake-pool instance, OOS third-party program. CASH (CASHx…CASH) = Token-2022, authorities = Stripe/Bridge (E7Jd…
permanentDelegate+mintClose+confidential, 3etm… transferHook authority but programId=NULL, freeze=AyCP…) → Stripe asset,
not Phantom. ON-CHAIN CONFIRMED DEAD (non-custom, verified). BUT the payable surface was mis-framed as theft/on-chain.
Phantom's OWN focus areas = 3 classes, only 1 is theft: (1) seed/key/auth-token/sensitive-data/privileged-internal-data
EXPOSURE [confidentiality+deanon crown jewels], (2) build/deploy/cloud/prod ACCESS [supply-chain], (3) theft.
→ Phantom = a WALLET-CLIENT + API + INFRA engagement (orca/upshift2/expand-surface), NOT smart-contract.
Fetched the deployed artifact: extension v26.22.0 unpacked at scratchpad/phantom-ext/ext (227 JS, manifest perms: identity/
webRequest/scripting/host *://*/*). Recon: OAuth account system (auth.phantom.app, launchWebAuthFlow, JWT/Bearer/refresh
→ ATO/token/authz crown jewel), seed/key handling (mnemonic/privateKey/secretKey/exportSeed/chrome.storage), backend
(api/node-proxy/rt/juicebox/trade.phantom.app), telemetry (datadog/sentry → deanon), 218 source-maps. Impact-ledger workflow
wf_9034b2f7-43b running. FP-wall is aggressive (mature wallet). Apply [[feedback-payable-impact-not-just-theft]] (2nd recurrence
this session after Symbiotic), [[feedback-read-both-or-clauses-in-exclusions]]. Bundle re-fetchable: Chrome Web Store id
bfnaelmomeimhlpmgjnjophhpkkoljpa.
