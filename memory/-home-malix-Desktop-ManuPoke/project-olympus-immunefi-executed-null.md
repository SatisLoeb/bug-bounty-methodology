---
name: project-olympus-immunefi-executed-null
description: "Olympus Immunefi ($3.33M, theft-only) = fortress; the one live lead (CD LimitOrders orphan→yield) killed on materiality (dust); executed-NULL, RE-SOURCE"
metadata: 
  node_type: memory
  type: project
  originSessionId: 202cb2ec-785f-4c7c-9bf5-7acc60927d5b
---

**Olympus DAO — Immunefi ($3,333,333 max) = theft-only fortress, hunted to executed-NULL. RE-SOURCE.**

Intake 2026-07-23 via 8-agent recon workflow + personal source-read of the single surviving lead. Scope = 72 assets, but payable impacts are ONLY {loss of treasury / user / bond funds} (Critical). Freeze / griefing / governance / 51% / centralization / privileged-access / oracle-inaccuracy-except-flashloan are ALL explicitly OOS → the usual non-theft levers ([[feedback-payable-impact-not-just-theft]]) are unavailable here. Program is "Primacy of Rules" (only listed assets pay). ~$271.9k paid in 4+ yrs vs $3.33M ceiling = classic high-ceiling/low-payout fortress tell.

**Saturation (grounded):** core Default framework = C4 2022-08-olympus (18 files) + Sherlock 2023-11 RBS2.0. Cooler V1 Clearinghouse = Sherlock 2023-01 + 2023-08 @ c6f2bbe. Governance = Sherlock 2024-01 (OOS). **Freshest high-value code (2025-2026, in scope, ZERO public contest, private-audit-only):** Cooler V2 (MonoCooler/DLGTE[$75M tag]/CoolerLtvOracle/CoolerTreasuryBorrower, panprog+Electisec+Nethermind), Convertible Deposits (Guardian+Trust), CCIP/LayerZero bridges, v1 Migrator (0x5131654e, deployed 2 Mar 2026, only in-repo audit/2026-01). NOTE: static WebFetch only renders page-1 (12 assets) of the JS-paginated Immunefi scope — the 2026-added rows (MonoCooler 0xdb59…, CD Auctioneer Limit Orders 0x7d8f82A0…, etc.) are real but need the live logged-in scope tab or SSR to confirm listing.

**The one live thread (synthesizer's top lead) — CD LimitOrders `0x7d8f82A0…` (`src/policies/deposits/LimitOrders.sol`, author "Zeus", NOT in either CD audit scopefile = true un-audited sibling of the audited ConvertibleDepositAuctioneer it wraps):** `fillOrder()` debits the order owner the full `fillAmount_` (`depositSpent += fillAmount_`, `totalUsdsOwed -= fillAmount_+incentive`) but the auctioneer `bid` only converts `depositIn ≤ fillAmount_`; the remainder `fillAmount_-depositIn` is re-deposited to sUSDS held by the contract and, since totalUsdsOwed already dropped by the full amount, surfaces as `getAccruedYieldShares()` and is swept to admin `yieldRecipient` by `sweepYield()`. Loss-of-user-funds BY CONSTRUCTION — the only question was materiality.

**KILLED on materiality (personally executed the extract gate against real `_previewBid`/`_bid`):** the auction is INFINITE-CAPACITY — on tick depletion/threshold-cross the price steps UP (`_getNewTickPrice`) and a fresh full tick opens, so the `_previewBid` loop always consumes the full deposit; its only non-full exit is the `convertibleAmount==0 break` which fires when leftover buys <1 OHM-atom (`remainingDeposit·1e9 < tickPrice`). So orphan `= deposit_-depositIn < ~1e-8 USDS` per fill = dust. Confirmed by: (1) `bid` and `previewBid` share `_previewBid` on identical block/tick state → `ohmOut==minOhmOut` exactly, minOhmOut gate is satisfied-by-construction NOT a barrier, and depositIn identical (no preview/execute divergence); (2) dev test `test_fillOrder_givenDifferentActualAmount` bounds the injected diff to 0-100 wei (their model = dust→yield is intended); (3) `getAccruedYieldShares` uses `previewWithdraw` (rounds UP shares reserved) → over-reserves principal, sweep can only take true surplus. My earlier hope that maxPrice-slippage bounds f=1-p/maxPrice to a MATERIAL fraction was WRONG: infinite capacity means depositIn≈full always, so f≈0 regardless of maxPrice.

**Everything else executed-dead by the workflow (3 grounded deep-reads, ~440k tok):** Cooler V2 core theft paths refuted by accounting-conservation (2 independent agents); Bond Manager 0xf577 every state-changer onlyRole/whitelist → unprivileged path OOS; OHM/gOHM/sOHM/Staking + MINTR/TRSRY = single-minter role-gated conservation fortress; Price/PriceConfig 2026 drift = oracle-inaccuracy OOS (only flashloan-manip-to-fund-loss pays, no such chain).

**Verdict: NO-GO / RE-SOURCE.** Personally killed the best lead the whole swarm surfaced; base rate is a theft-only 4-firm-audited fortress. Only residual with non-trivial EV if forced to stay: **v1 Migrator 0x5131654e** (freshest asset, least externally reviewed, cross-version V1→V2 accounting seam, deployed==audited unverified) — but negative base-rate EV. Don't re-audit the rest. Clone was ephemeral (scratchpad). Pairs w/ [[feedback-check-prior-audits-and-competitions-at-intake]] [[feedback-hunt-dont-narrate-ev]] [[feedback-tool-complete-stop-polishing]].
