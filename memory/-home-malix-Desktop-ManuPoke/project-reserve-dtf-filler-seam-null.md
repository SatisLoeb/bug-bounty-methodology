---
name: project-reserve-dtf-filler-seam-null
description: Reserve DTF r5.0.0 trusted-filler seam (Folio↔CowSwap async fill) audited NULL-COÛTEUX; RE-SOURCE to RebalancingLib auction-math / StakingVault / core
metadata: 
  node_type: memory
  type: project
  originSessionId: 399efac4-1073-4669-9c21-60111c4ae1a6
---

Reserve Protocol Bug Bounty (Cantina, $10M max / High $100k / Med $5k / Low $1k; deposit $100; 166 findings already submitted). Heavily saturated: ~20 prior audits listed as OOS known-issues (ToB, Trust Security, Pashov, Code4rena, Solidified, Halborn, Ackee). In-scope repos cloned to `/home/malix/Desktop/BUGS/reserve-2026/`: `protocol @ 4.2.0-rc1`(3ad40fa, 227 contracts, Hardhat, deps installed --legacy-peer-deps) + `@ 3.4.0-rc1`(78152ed); `reserve-index-dtf @ r5.0.0`(241f0d2, Foundry, 27 contracts, deps in node_modules, builds `forge build`) + `@ r4.0.0`(0a60baf); `trusted-fillers @ main`(5ca2633, Foundry). Tags: `r5.0.0`/`r4.0.0` (not rc-5.0.0), `4.2.0-rc1`/`3.4.0-rc1`.

**Fresh surface = r5.0.0 DTF + trusted-fillers (post-date every audit — Folio ≤4.0.0/DTF ≤v1 is the OOS ceiling).** `/nuke` barraged both (DTF: 524 sig→304 clusters, 3 corrob; TF: 83→62); nearly all HIGHs were OZ deps (Governor/Timelock/Proxy/Math) = OOS noise.

**Trusted-filler seam (Folio↔CowSwapFiller↔CowSwap) = NULL-COÛTEUX, hand-verified 2026-07-20. Don't re-hunt it.** darkside Door C, 4 primitives all refuted by dispositive STRUCTURAL facts (not just PoC):
- **createTrustedFill (Folio.sol:742) permissionless + skips the `bidsEnabled` check that bid() enforces (729) = INTENDED, not a bypass.** CHANGELOG.md:5 (Release 5.0.0) verbatim: "Add ability to disable permissionless bids and restrict all trading to trusted fillers." Disabling bids ROUTES trading through createTrustedFill by design. CHANGELOG.md:17 "all auction limitations still apply" — createTrustedFill enforces price/size/availability via _getBid→getBid→_price; fillCreator=address(Folio) (caller is only clone-salt entropy); price is Dutch-auction-derived not caller-set. No value leaves.
- **async-fill accounting insolvency — REFUTED.** `_balanceOfToken` (Folio.sol:846-855) adds `token.balanceOf(activeTrustedFill)` but every money-path (mint:390/redeem:444/distributeFees:478) is `sync`→_poke(1061)→_closeTrustedFill(1123) runs BEFORE the body; redeem transfers from the FOLIO's own balance so mid-swap the sellToken leg reverts (self-resolving DoS) not over-pays; addToBasket is onlyRole. No untrusted state-changing path reads a live filler.
- **sync-revert freeze — REFUTED (not permanent).** swapActive() short-circuits false when block.number≠blockInitialized (CowSwapFiller.sol:104 + isValidSignature block-pin :88); reverts block N, succeeds N+1. Dev-acknowledged 1-block DoS (Folio.sol:253). Attacker can't even make swapActive() true alone (needs a partial CowSwap settle).
- **bid-callback donation grief — REFUTED, dev-acknowledged** (Folio.sol:277 "known: can be griefed by token donation").
- **Residual = TRUST SEAM (not untrusted-reachable, → informational at most):** the "filler settles same-block-atomic" invariant is delegated to filler-impl correctness with NO Folio-side guard; a FUTURE non-CowSwap whitelisted filler spanning 2 txs could break basket accounting. Governance/whitelist risk (owner adds bad filler), not a bug on current scope (only CowSwap whitelisted, block-pinned).

**OFFICIAL Reserve SC impact table (operator-supplied 2026-07-20 — the payable ROWS, use verbatim):** CRITICAL = (a) ANY governance voting-result manipulation · (b) direct theft of user funds at-rest/in-motion (NOT unclaimed yield) · (c) permanent freezing of assets. HIGH = theft of unclaimed yield · permanent freezing of unclaimed yield · protocol insolvency. MEDIUM = temporary freezing of RToken ERC20 functionality · temporary freezing of funds · griefing (no profit motive, damage to users/protocol). LOW = fails-to-deliver-returns-without-loss · theft of gas · unbounded gas. → **Top prize = governance-manipulation (Crit), NOT theft.** (This is why the filler-seam temp-freezes were correctly Medium, not the reflex Critical.)

**`/extract` on RebalancingLib auction math = NULL (Gate-1, by lecture 2026-07-20).** Theft dies at Gate-1: all bidder-facing rounding is protocol-favoring — sellLimit/sellLimitBal Ceil (sells less), buyLimit/buyLimitBal/sellAvailableFromBuy Floor (buys less), bidAmount Ceil (bidder pays more), _price decay truncates k down (slower decay=higher price). Fresh r5.0.0 delta (maxAuctionSize + traded caps) = conservative subtraction, underflow-guarded, per-auction. `openAuctionUnrestricted` (Folio.sol:646, permissionless) supplies ONLY rebalanceNonce and collapses to the rebalance's stored spot weights/initialPrices/spot limits (REBALANCE_MANAGER-set, 673-688) — untrusted injects nothing. MathLib.ln/exp = thin PRB-Math wrappers (audited). Non-theft rows (perma-freeze/DoS) clean: basket tokens are role-gated (addToBasket onlyRole, rebalance tokens REBALANCE_MANAGER), _price div-by-auctionLength guarded by the startTime/endTime early-returns. Residual = launcher mispricing (trusted-config concern, not untrusted-math). Don't re-extract the auction math.

**NEXT = the CROWN-JEWEL row: governance voting-result manipulation (Critical) on StakingVault + FolioGovernor.** StakingVault(377 LOC) `is ERC4626, ERC20Permit, ERC20Votes, Ownable` — a TRANSFERRABLE ERC4626 vault whose SHARES are the voting token, unstake gated by a delay (UnstakingManager), PLUS native asset()+other reward-token accounting (`rewardsBalance = balanceOf(asset()) - totalDeposited`, :116; custom _deposit :121 / _withdraw :134 "can't use builtin"). FolioGovernor = OZ Governor(Votes/QuorumFraction/TimelockControl) with dynamic proposalThreshold = threshold%×getPastTotalSupply (Ceil, :90). Door-C singularity = the ERC4626-shares-as-votes composition: hunt vote-inflation via the deposit/reward/share-ratio seam, checkpoint/_update timing on transfer, and the unstake-delay vs vote-removal interaction. Then core protocol-4.2.0 barrage (deps ready). See [[feedback-payable-impact-not-just-theft]] · [[project-nuke-static-barrage-skill]].

**GOVERNANCE hunt done 2026-07-20 (hand-verified): Critical gov-manipulation row = NULL.** All refuted with green PoCs: vote-inflation withdraw→cancelLock re-mint (re-mint ≤ burned, 0 votes during lock window, no double-vote), pass-malicious/duck-threshold (OZ snapshot semantics hold), balanceOf(asset())-totalDeposited underflow-freeze (totalDeposited<balanceOf strict by construction — handout is a FRACTION of the unaccounted, no untrusted path moves asset() without decrementing). **Best candidate (the ONE report-worthy item of the whole engagement): `StakingVault.removeRewardToken` permanently strands passive holders' unclaimed reward yield.** removeRewardToken (:192) removes from the live set with NO pre-accrual → claimRewards' accrueRewards modifier iterates only the LIVE set so _accrueUser is skipped for the removed token (:206/262) → a passive holder whose lastRewardIndex predates the stream gets accruedRewards=0, their earned share stuck in the vault; irreversible (disallowedRewardTokens :193 blocks re-add :180); contradicts the in-code guarantee ":200-201 supports claiming for removed tokens". Maps to official row **"permanent freezing of unclaimed yield = HIGH"**, BUT trigger is onlyOwner/governance (normal action = ending an incentive program, NOT admin-rug) → likely triaged to Med/Disputed. Needs DEDUP vs Trust v1 (Dec 2024) + Cantina Jan-2025 audits. PoC: test/RewardStrand.t.sol (green). Secondary (Low): quorum-inflation censorship via future-snapshot denominator (untrusted-reachable but DOMINATED by cheaper "vote against" 5e18<26e18, only bites thin-margin proposals, dup-risk Trust TRST-SR-2); reward-loop balanceOf-revert temp-freeze (owner-recoverable via removeRewardToken which makes no token call → Med→Low). This is the impact-ledger working: the yield-FREEZE finding is exactly what a theft-only pass would have missed.

**DEDUP KILLED the best candidate + CORE = fortress → whole target NO-GO (2026-07-20, dedup-proven).** The removeRewardToken finding is a WORD-FOR-WORD DUP of **Trust Security v1 TRST-M-4 "Removing reward tokens may cause users to lose rewards"** (Status: Open, team-accepted "won't fix, just warn users") — a Known Issue OOS on the Cantina scope. The Trust v1 ToC (audits/trust-security/v1-audit-dec-2024.pdf in the repo) is a graveyard: TRST-M-6 read-only-reentrancy toAssets, TRST-M-8 donation→decayed-price, TRST-L-5 totalSupply→0 unusable, TRST-L-6 governance share-deflate, TRST-L-8 reward-accrual grief, TRST-R-5 timelock-executor — EVERY angle our 3 DTF hunts surfaced is already named. **Core barrage (protocol-4.2.0, Hardhat, 1805 sig→891 clusters/22 corrob): in-scope Reserve signals are FPs/audited** — DutchTrade.bidWithCallback guarded by TradeStatus state-machine (status=104, stateTransition modifier), Asset.savedLowPrice lazily-set in refresh():101 (slither FP); top raw signals are P0 (OOS, deployed=P1), mocks (OOS), OZ (OOS). 7-firm fortress (ToB 2022, Trust 4.2/3.2/3.1, C4 2.1/3.0/4.0, Solidified, Halborn, Ackee — all OOS). **FINAL: Reserve $10M = dedup-proven NO-GO for solo fresh findings. Don't re-audit. RE-SOURCE to a different target.** Audit PDFs live in dtf-r5.0.0/audits/ — grep them at INTAKE next time to dedup before hunting (would have saved the governance pass).
