---
name: project-valantis-stex-fortress
description: "Valantis STEX (Remedy bounty, <=200k) in-scope STEXAMM+stHYPEWithdrawalModule = 4-audit fortress, artifact-backed NO-GO for solo untrusted attacker"
metadata: 
  node_type: memory
  type: project
  originSessionId: c54b4864-e424-45cb-9782-e6ca32373ca0
---

Valantis Stake Exchange (STEX) AMM — Remedy bug bounty, Critical up to $200k. Repo ValantisLabs/valantis-stex @27b5c74. IN SCOPE only: `src/STEXAMM.sol` (759L) + `src/withdrawal-modules/stHYPEWithdrawalModule.sol` (684L). kHYPE module, lending modules, StepwiseFeeModule, DepositWrapper, STEXLens = OUT of named scope.

**Design:** token0=stHYPE (rebase LST, balanceOf=HYPE-equiv), token1=WHYPE. Built on Valantis SovereignPool with isToken0Rebase=isToken1Rebase=true → reserves = live balanceOf. convertToToken0/1 are IDENTITY (1:1). Swaps hard 1:1-minus-fee; fee (StepwiseFeeModule) ONLY on token0→token1; token1→token0 is FEE-FREE 1:1. Async LP withdrawal: non-instant withdraw burns shares + records token1 debt (`_amountToken1PendingLPWithdrawal`), token0 stays in pool; debt settles ONLY via native from owner-driven unstakeToken0Reserves→overseer→native→update()→claimable (FIFO by cumulative checkpoint)→claim().

**VERDICT = NO-GO (fortress). Don't re-audit the in-scope core from scratch.** 4 audits (Hexens Mar/May25, Zenith Mar/Aug25) ALL resolved; current code matches fixes. Zenith-Mar findings map exactly onto the hot zones: H-1/M-3 instant-withdrawal fee base, M-2 amount1SwapEquivalent double-count (deprecated STEXRatioSwapFeeModule removed), H-2 excess-native (fixed by calling `update()` FIRST in deposit/withdraw). L-1 (claim should trigger update) = acknowledged, keeper-mitigated.

**Why fortress (artifact-backed):** value is CONSERVED across swaps+deposits+withdrawals (1:1 + LP fee stays in pool), so the only natural findings map to OOS carve-outs: (a) fee/price manip NOT beating the convert quote = explicitly OOS; (b) async-settlement liquidity blocks = owner-recoverable (stakeToken1→unstake) = temporary/OOS; (c) reverting claim() recipient = own-fund freeze by LP's own recipient choice, does NOT brick others (FIFO uses monotonic cumulative counters) = OOS self-DoS; (d) external lending insolvency = OOS. 6-angle adversarial workflow (doorC / instant-flow / rebase-yield / queue-griefing / donation-timing / manager-fee-solvency) → 0 survivors. My PoC `test/z_poke.t.sol` (5 tests, all PASS): round-trip-no-profit, donation+permissionless-update sandwich no-profit, swap-drain solvency+recoverability, 50-cycle rounding grind no-leak, reverting-recipient does-not-brick-queue.

Method that worked: manual poke FIRST (read both files + SovereignPool swap/fee/reserve internals + fee module + mocks + test suite + all 4 audit PDFs), THEN machinery to package/verify. Consistent with [[feedback-manual-poke-mandatory-bracket]] and [[feedback-today-impact-before-poc]]. Repo clone needs `git submodule update --init --recursive` (valantis-core etc). Builds clean (solc 0.8.35). Considered profit-oriented-block-stuffing (bounty HIGH cat) → no clear in-scope vector (no liquidation, fee-congestion manip is OOS).

**kHYPEWithdrawalModule (1203L, NOT in the named scope but checked "if added"): also a fortress, NO-GO.** It's the freshest, least-audited surface (novel identifiers coveredByQueuedUnstake/PendingUnstakingOffset/burnFeeBips/rebalanceToken0/settlePendingWithdrawals appear in ZERO of the 4 audit PDFs). Deviations from stHYPE: dynamic kHYPE↔HYPE rate (convertToToken0/1 = StakingAccountant oracle, not identity), token0 unstake FEE + burnFeeBips floor, owner offset virtual-reserve, bounded 5-slot pending-unstake tracker, coveredByQueuedUnstake fee gross-up. Two REAL quirks, both OOS: (1) owner offset `_amountToken0PendingUnstakingOffset` DOES inflate real withdraw payout/deposit denominator despite "display-only" natspec (natspec-vs-behavior footgun worth an INFORMATIONAL/hardening note) — but single writer, onlyOwner, zero coupling to real tracker, untrusted actor can't set/desync it → OOS-owner; (2) cancellation coverage desync — `_syncPendingUnstakeRequests` drops a cancelled request without decrementing coverage → under-reserves by exactly one unstake-fee gross-up (~10bip), but EXCLUSIVE trigger is Kinetiq-gov cancellation (OOS), solvency-preserving (kHYPE returns in full), owner-reconcilable, README-acknowledged. Confirmed by BOTH my harness (test/z_khype_poke.t.sol, 5 tests) AND an independent 5-angle adversarial workflow with its own PoCs — convergent 0-survivor verdict. confirm-drift killed (increment==decrement bit-identical same-tx same-fee), claim/FIFO killed (SafeCast.toUint96 reverts no-truncation, CEI+shared guard), roundtrip-rate-fee killed (deposit-during-pending nets negative, gross-up grants zero free value). If team contact: report the offset natspec/footgun as informational.
