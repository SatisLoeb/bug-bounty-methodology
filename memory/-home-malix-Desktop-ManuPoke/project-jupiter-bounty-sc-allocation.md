---
name: project-jupiter-bounty-sc-allocation
description: "Jupiter OtterSec/OOOSec bounty Stage-0 SC allocation 2026-07-21 — Lend=fortress, RFQ/JupUSD-edges fresh, web-recon OOS"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7a1cec50-aec0-40f4-9311-a86e2cb19f35
---

Jupiter bug bounty (OtterSec-hosted self-bounty, ooosec.com/programs/jupiter — NOT Immunefi). Stage-0 allocation 2026-07-21, 8-surface research + adversarial-verify workflow. Dossier: ~/Desktop/BUGS/jupiter-recon/INTAKE-2026-07-21-SC-ALLOCATION.md

**Program economics (dup calibrator):** live ~4mo, 25 reports resolved, **$47.8k paid, top single $15k**, 13% acceptance. No public Critical landed yet; $250k–$500k tiers unclaimed. Named repeat hunters compete continuously.

**Severity caps:** Lend & Perps = Critical ($500k); Aggregator/RFQ/JupUSD/Limit-DCA = High ($250k); Prediction/Offerbook/RewardHub/Lock/DAO = Medium ($20k).

**Prior ~/Desktop/BUGS/jupiter-recon (2026-03-04) is DEAD under formal scope:** all Fluid.io findings = third-party infra OOS; single-EOA upgrade-authority = OOS ("known and accepted"); web = info-disclosure OOS. Confirms [[feedback-check-prior-audits-and-competitions-at-intake]].

**Verdicts (all adversarially verified):**
- **Jupiter Lend = FORTRESS, SKIP.** 7 firm audits + 2× Certora FV + 2 C4 contests; C4 2026-02 report PUBLISHED 2026-07-11 = 0H/3M/86QA; operator already submitted (F-85). All contest findings now "known issues". Only post-C4 HEAD-drift.
- **JupUSD (jup-stable, PUBLIC github.com/jup-ag/jupusd-program) core = FORTRESS.** Pashov report is PUBLIC and OWNS the oracle-aggregation/mint-redeem seam (L-03 MIN-agg, L-04 weak Doves, L-09 depeg-arb, L-12 fee/round) → known-issue dup. DIG only thin edges: custodian↔off-chain-Anchorage-NAV withdraw; cross-program valuation. `psm` (GFU42W…) NOT in scope.
- **RFQ = the DIG (operator's off-chain-signature specialty).** v1 (61DFfe) PUBLIC + one thin 3-day Offside audit (Nov 2024); v2 (fd3n) CLOSED + UN-AUDITED (deployed Feb 2026); no crowd contest ever. Freshest: v1 validator post-audit drift PR#81 May-2026 integrator-fee state machine (open-source, never re-audited); highest-value: v2 fill_exact_in instructions_sysvar completeness (token-2022/ALT/CPI-wrapper co-signature reuse).
- **Cross-program (freshest, Crit $500k ceiling):** JupUSD added as JLP collateral **Jun 30 2026** (~3wk pre-intake), valued flat $1 via RedStone inside Perps while its redeem rate is oracle-derived — depeg→pool-arbitrage. Post every audit. Spans open JupUSD + closed Perps.
- Perps/Aggregator core = closed-source fortress, DIFF-ONLY. Limit/DCA = SKIP (low tier). Web/extension = $20k ceiling, closed CRX, active comp.

Gates hold [[feedback-trigger-reachability-is-payability-gate]] [[feedback-read-both-or-clauses-in-exclusions]].

**UPDATE 2026-07-21 — RFQ v1 = EXECUTED-NULL on theft.** Operator picked RFQ. Cloned jup-ag/rfq-webhook-toolkit; darkside Door-A + 14-agent hunt + 2 green PoC probes. On-chain order-engine (61DFfe, audited/stable) + off-chain validator order-engine-sdk/src/fill.rs (un-audited Apr–May-2026 drift). 9 candidates, 0 survivors. Maker blind-signs slot-0-as-fee_payer after validate_fill; every non-fill value-move pinned to TAKER authority/source + Anchor token::authority backstop → maker un-drainable. Lighthouse allowlist SAFE (disc [5,6,9,10]=Assert* read-only, 1-byte Borsh, MemoryWrite excluded). Integrator #81 state-machine mis-classification impossible (receiver-ATA=PDA can't grind to integrator.destination). 2 OOS-only observations: uncapped priority-fee (griefing, fee→leader, self-inflicted/gas-opt OOS) + ALT-panic self-DoS. Green PoCs prove ACCEPTANCE not HARM [[feedback-invariant-that-passes-is-not-a-finding]]. Dossier ~/Desktop/BUGS/jupiter-recon/rfq/RFQ-V1-EXECUTED-NULL.md.

**UPDATE 2026-07-21 b — cross-program JupUSD→JLP = GATE-KILLED; JupUSD edges = OOS-walled.** Operator picked the cross-program seam. Cloned jup-ag/jupusd-program (open-source jup-stable+psm). JupUSD value model: it's the LP token vs USDC/USDtb collateral; mint/redeem take min(oracle,one_to_one); band min_oracle_price_usd=$0.50/max=$1.00 → JupUSD CAN depeg to ~$0.50 while operating (open side of the seam is REAL). BUT the closed-Perps gate KILLS it: RedStone's JupUSD JLP feed is a LIVE push MARKET feed (aggregates secondary spot from 50+ exchanges, 0.2% dev/24h heartbeat) — NOT flat-$1 (verified RedStone blog verbatim). So on a depeg, benefactor-arb drags JupUSD market price down + RedStone tracks it + JLP revalues down → no $1-vs-depegged gap; oracle_price≈acquisition_cost. Today: USDtb $0.9999, JupUSD $1.00, ~$53.1M supply (mint=Juprjzn…55USD; JUPUSDec…=the program), JupUSD is PASSIVE reserve (not on JLP mint-whitelist SOL/ETH/BTC/USDC/USDT). Residuals dormant: RedStone staleness bounded by 0.2% band (arber-contested), single-oracle-custody oddity (needs OOS oracle-manip). extract-gate killed it BEFORE the Perps decompile [[feedback-today-impact-before-poc]]. Also: JupUSD authz = mint/redeem benefactor-gated (KYC), everything else operator-role-gated (withdraw/custodian = OOS privileged). 
**NET: Jupiter fresh seams exhausted for unprivileged solo** — RFQ v1 null, cross-program killed, JupUSD edges OOS. RE-SOURCE candidate per [[feedback-check-prior-audits-and-competitions-at-intake]] standing lesson (careful+competed+13%-accept). ~/Desktop/BUGS/jupiter-recon/jupusd.

**UPDATE 2026-07-21 c — RFQ v2 (fd3n) decompiled → STRONG caller-gate, math not reached.** Operator picked v2 decompile. Full RE toolkit works: dumped rfq-v2.so, llvm-objdump-19 disasm, decompressed the ON-CHAIN Anchor IDL from PDA 34UvK (fill_exact_in: orderbook levels in ix-data, VWAP fill, user+fill_authority both signers, errors 6000-6005). Stood up a LIVE local-fork harness (solana-test-validator + real bytecode + solders driver, ~/Desktop/BUGS/jupiter-recon/rfq-v2/artifacts/). Gate `InvalidCaller`(6004) is body-level (post-account-validation), reads instructions_sysvar + sol_try_find_program_address + sol_sha256; Jupiter Aggregator v6 JUP6Lkb embedded @.data.rel.ro+1957. EXECUTED-resisted 3 attacker models (all→6004): direct call, arbitrary-CPI (forwarder 9ghy), AND CPI from a forwarder deployed AT the JUP6Lkb address (spoofed aggregator id). => authenticates a forge-proof context (likely fill_authority PDA via real-aggregator invoke_signed). Math theft-surface unreached; needs (a) reverse the exact PDA/introspection predicate, (b) clone+drive real aggregator, or (c) binary-patch the guard. **Every deep Jupiter surface = fortress → RE-SOURCE.** Dossier ~/Desktop/BUGS/jupiter-recon/rfq-v2/RFQ-V2-RE-CHECKPOINT.md. (The reusable closed-source-Solana local-fork RE pipeline built during this engagement lives in the `/solfork` skill + BUGS/CLAUDE.md Rule 44, NOT in memory.)

**UPDATE 2026-07-21 d — RFQ v2 "forge-proof fortress" was WRONG: gate is weak introspection, but the fill is sound → payable theft is OFF-CHAIN.** Re-opened with /solfork (option a: reverse the predicate). Hand-verified disasm (text 2164-2298) + RE agent + EXECUTED fork bypass. The 6004 gate is NOT a PDA/sha256 fortress — it is pure introspection: `PASS iff program_id(load_current_index top-level ix)==JUP6Lkb (memcmp @rodata 0x3530d) && data_len>=8 && data[0..8] ∈ {route e517cb97…, shared_accounts_route, +2 route-family discs}`. NO find_program_address / sha256 / fill_authority gate 6004 (those run later, token-account handling). Why UPDATE-c's 3 tests all hit 6004: test-3 (forwarder AT JUP6Lkb) failed at the DISCRIMINATOR check, not a PDA. **EXECUTED PROOF**: built a fwd forwarder (strips 8-byte route prefix, CPIs target), deployed `--bpf-program JUP6Lkb fwd.so`; direct fill→6004, but fwd@JUP6Lkb with data `[route_disc ‖ fill_ix]` → **"Fill executed", guard bypassed, math reached**. Then reversed the math on the fork: Ask lots=floor(in/lot), gives lots*lot base / gets lots*px*tick quote; Bid symmetric; **value CONSERVED, mint-bound (ConstraintTokenMint 2014), overflow-checked (6000), no rounding leak**. Only edges = Ask lot=0 panic (ProgramFailedToComplete) + tick=0 zero-out, both maker-signed params = maker self-harm, not taker theft. **NET: fortress REFUTED (real authz weakness — gate binds neither immediate CPI caller nor fill params to the top-level Jupiter route), but the on-chain fill is sound, so a payable theft needs the OFF-CHAIN fill_authority signing model: fill_exact_in consumes NO on-chain nonce/request state → if makers pre-sign REUSABLE quotes, weak-gate + no-nonce = replay a stale quote under a decoy Jupiter route = maker drain; if fill_authority is a per-route PDA invoke_signed by the aggregator, closed.** THE remaining seam = the maker signing flow (rfq-webhook-toolkit / aggregator route→RFQ CPI), off-chain, operator's specialty. Checkpoint fully updated. [[feedback-trigger-reachability-is-payability-gate]] holds: fork drain = self-deal, not mainnet reachability.

**UPDATE 2026-07-21 e — RFQ v2 CLOSED = NULL payable (theft impossible), fortress-verdict corrected.** Operator said go; investigated the signing flow. TWO artifact-backed closes: (1) **maker funds pinned to fill_authority** — probe_drain.py: attacker calls the bypassed fill with a VICTIM maker's token accounts (owned by a key ≠ their fill_authority) → **ConstraintTokenOwner 2015** (both Ask+Bid). Program enforces `maker_{base,quote}.authority == fill_authority`; attacker's own fill_authority only self-deals; draining a real maker needs M's signature. (2) **off-chain flow (rfq-webhook-toolkit README + webhook-api/responses.rs): MM returns only a PRICE quote; JUPITER builds the tx; taker partial-signs; MM signs AND sends** — taker never holds the maker-signed tx to re-wrap, maker sig is blockhash-bound single-use → no replay (protocol field defaults v1, v2 a value = same contract). NET: the InvalidCaller guard IS weak introspection (fortress/PDA reading was WRONG, now corrected everywhere) but it's DEFENSE-IN-DEPTH — real security = token-owner constraint 2015 + maker per-tx signature + value conservation, all hold. Bypassing the guard yields only a fair mutually-signed conserving swap. **The whole Jupiter engagement is now exhausted: Lend fortress, JupUSD OOS, cross-program killed, RFQ v1 null, RFQ v2 null (guard weak but theft-closed).** Deliverable = the corrected fortress verdict + full executed map, NOT a submission. RE-SOURCE off Jupiter. Reinforces [[feedback-trigger-reachability-is-payability-gate]] (pierce a gate ≠ theft when the money-move is separately gated) + solfork's discipline (don't concede a gate un-executed, AND don't over-claim a bypass that yields nothing).

**CORRECTION 2026-07-21 (RFQ v2 — my "fortress" was WRONG).** The earlier "InvalidCaller = unforgeable PDA/sha256 gate" verdict was a First-Maxim concession error (inferred from find_program_address/sha256 STRINGS, never hand-verified the disasm — those syscalls are used in token handling, OUTSIDE the 6004 gate). Hand-verified disasm + fork bypass: 6004 is a PLAIN introspection check — PASS iff top-level instruction is a call to JUP6Lkb whose data[0..8] ∈ {4 Jupiter route discriminators}. My 3 fork-tests all hit 6004 only because I put the FILL disc as top-level data, not a route disc. BYPASS PROVEN on fork (forwarder at JUP6Lkb + [route_disc‖fill_data] → Fill executed). BUT resolved NOT-PAYABLE on-chain: (a) VWAP math is clean (floor-to-lot, remainder refunded = maker-favorable, overflow-checked, conserves — executed) ; (b) mainnet fill_authority = 2ASop4tRtRPYkw3b9XZQLka4RY8TLXtesEa3GfQJZZcn = Jupiter RFQ settlement HOT KEY (System EOA, owns maker_base/quote accounts, top-level signer of every fill). The guard is defense-in-depth; the load-bearing control is the fill_authority signature, which the bypass doesn't defeat. Real seam = the off-chain backend that 2ASop4tR signs after (v1 validate_fill analog, OOS Jupiter infra). LESSON: never concede an on-chain gate from string-inference — hand-verify the disasm or fork-execute the bypass (First Maxim). Full: ~/Desktop/BUGS/jupiter-recon/rfq-v2/RFQ-V2-RE-CHECKPOINT.md.
