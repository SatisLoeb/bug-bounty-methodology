---
name: project-dydx-pml-zeroprice-chainhalt
description: "dYdX v4-chain — permissionless zero-price market → all-validator chain-halt in clob EndBlocker. SUBMITTED Cantina #785 (High, v4-chain protocol) 2026-07-20; reachability+deployed-binary+anti-#407 all verified."
metadata: 
  node_type: memory
  type: project
  originSessionId: 399efac4-1073-4669-9c21-60111c4ae1a6
---

**SUBMITTED 2026-07-20 = Cantina finding #785 "Permissionless zero-price market halts the chain via the clob EndBlocker" — target=v4-chain (protocol), Impact=High, Likelihood=High.** Rubric-match: dYdX explicitly classes chain-halt/liveness=High ($50k-150k); Critical=fund-loss/insolvency/permanent-freeze-of-FUNDS/mint (halt is recoverable=liveness, so High not Crit — not modesty). Deliverables: REPORT-pml-zeroprice-halt.md=description · COMMENT-poc-and-custody.md=comment (3 PoC files+custody+reachability queries) · screenz/1-4.png (1=full-app-3legs+all-tests-PASS, 2=cap296<500/v9.6.4/zero-diff/180-ISOLATED, 3=53-listable-tickers anti-#407, 4=code-elements). Status: PENDING TRIAGE. If triager raises #407-style reachability: the state is attacker-WRITTEN (listing.go:72 Price:0) not emergent-gated; 4 gates pass on live mainnet; 53 concrete tickers. Triager-response discipline (report-nerve): executed artifacts only · no self-flagellation · no unprompted severity concession · no unverified-path mention.

dYdX v4-chain, Cantina bounty. Repo /home/malix/Desktop/BUGS/dydx-v4chain-2026 (Go/Cosmos-SDK L1, HEAD 377842177d4a, protocol/ = crown). 6 Informal Systems audits in-repo audits/ (2023-Q4→2024-Q2+, hunted halts on the CORE). Severity: chain-halt/liveness = **High $50k-150k**; insolvency/perma-freeze = Crit $150k-1M+. Mandate = non-theft.

**LIVE CANDIDATE (first real find of the session, hand-verified 2026-07-20): permissionless zero-price market → deterministic all-validator chain-halt.** Post-2024 x/listing module broke the pre-2024 implicit invariant "ACTIVE clob pair ⟹ nonzero oracle price" that every Informal audit relied on. 0/6 audits (novel — grep confirmed).

CHAIN (every leg verified by MY read or MY PoC re-run):
1. **Untrusted create → poison state.** `MsgCreateMarketPermissionless` (x/listing/keeper/msg_create_market_permissionless.go) — NO HasAuthority, only gate = numPerpetuals>MarketsHardCap(500, mainnet under). In NormalMsgs (app/msgs/normal_msgs.go:239, externally submittable). Cost ~10k USDC via DepositToMegavaultforPML = LOCKED not burned (recoverable). CreateMarket writes MarketPrice{Price:0} (listing.go:72); CreateClobPair sets ClobPair_STATUS_ACTIVE (listing.go:97). Requires ticker ALREADY in marketmap (GetMarket→ErrMarketNotFound else).
2. **Order placeable at price 0.** validateOrderAgainstClobPairStatus reads oracle ONLY in STATUS_INITIALIZING branch (clob_pair.go:494); STATUS_ACTIVE falls through, no oracle read. Non-TWAP conditional collat uses order.Subticks not oracle (orders.go:1079); margin IMR=0 → passes. [leg-3b read-confirmed, HIGH conf, NOT yet executed end-to-end]
3. **Halt.** clob EndBlocker abci.go:118 calls MaybeTriggerConditionalOrders UNWRAPPED → untriggered_conditional_orders.go:240 GetOraclePriceSubticksRat ONCE per clob-pair-with-untriggered-order (1 resting order suffices) → orders.go:1186 panic(ErrZeroPriceForOracle "Oracle price must be > 0"). EndBlock panics NOT recovered (dydx's own comment app.go:1843 "will lead to consensus failures") → ALL validators die same block.
4. **Timing robust:** PreBlocker (Slinky price-set) runs BEFORE DeliverTx, so create-block always has price 0 at its EndBlocker → SAME-BLOCK halt (create tx + conditional-order tx in block N → block N EndBlocker halts), regardless of ticker priceability. Poison order in committed state → restart re-panics → permanent freeze until emergency binary patch (→ Critical characteristic).

PoCs (BOTH re-run by me, PASS): x/clob/keeper/zz_pml_zeroprice_halt_test.go = propagation; **zz_pml_e2e_halt_test.go = END-TO-END, leg-3b EXECUTED** — real `msgServer.PlaceOrder(conditional)` SUCCEEDS on the price-0 STATUS_ACTIVE clob pair (funded Alice_Num0_100_000USD), THEN MaybeTriggerConditionalOrders panics "Oracle price must be > 0" (orders.go:1186). Placement collat uses order.Subticks not oracle (orders.go:379-381, non-TWAP). Create-leg set up via test helpers mirroring listing.go:62-97 exactly (read-verified: handler is code-simple, no authority).

**FULL 3-LEG e2e GREEN (x/listing/keeper/zz_pml_fullapp_halt_test.go, I ran it): ALL legs via REAL msg servers on the full test-app** — LEG-1 real MsgCreateMarketPermissionless→ACTIVE clob pair id=2 price=0; LEG-2 real MsgPlaceOrder(conditional) rests; LEG-3 clob EndBlocker panic orders.go:1186 → halt (market id 33). Genesis needs LiquidityTier id 7 + funded Alice+Megavault subaccounts + a marketmap ticker (TEST2-USD) + ctx.WithIsCheckTx(false) + order subticks/quantums as multiples of the long-tail SubticksPerTick(1e6)/StepBaseQuantums. 3 PoCs total (all re-run by me, PASS): zz_pml_zeroprice_halt (propagation), zz_pml_e2e_halt (keeper-level real-placement), zz_pml_fullapp_halt (full 3-leg).

**REACHABILITY CONFIRMED on mainnet 2026-07-20 (read-only public queries, no tx) — all 3 conditions FAVORABLE → LIVE Critical not latent:** (1) permissionless listing ENABLED+used — 180 ISOLATED markets on mainnet (MOTHER/MOG/GOAT-USD; x/listing answers `/dydxprotocol/listing/markets_hard_cap`=500); (2) hard cap NOT reached — 296 perpetuals on-chain (`/dydxprotocol/perpetuals/perpetual` total) < 500, ~200 headroom; (3) LISTABLE ticker exists — 54 marketmap tickers not-yet-listed (A/ACH/ALCX/BAT…), and PoC proved even a DISABLED marketmap entry lists → stays unpriced → oracle price 0 PERMANENTLY (widens window same-block→any-time). **VERSION MAILLON — precise framing 2026-07-20: mainnet app v9.6.4 @ commit 503b9157 (node_info). That commit is NOT in the public repo (build ahead of last public tag protocol/v9.6.3; not fetchable by SHA/tag/release-branch) — so CANNOT diff deployed bytes directly, DO NOT claim "byte-identical to deployed". What IS true: the 4 vulnerable files are byte-identical from the v9.6.3 tag through HEAD (0 diff each) → vulnerable path stable across the whole public v9.6 line; mainnet 9.6.4 = a patch on that line (a fix to the EndBlocker panic would be security-relevant + a known-issue, dedup found none). Finding is proven against the IN-SCOPE main branch (HEAD), which is what scope names. **CHECKPOINT 9 PASS (strict)**: verified from a FRESH git-worktree at the exact scope commit 377842177 (clean tree, no PML files), dropped only the 3 posted test files, `go test ./x/clob/keeper/ ./x/listing/keeper/ -run TestPML_...` → all 3 PASS, both packages ok. Reproduces from scope tree + 3 files, zero working-file deps. Report version paragraph corrected to this precise framing (names 9.6.4/503b915, honest about non-public commit). 180 ISOLATED markets haven't halted organically because each is Slinky-priced within ~1 block and nobody places a conditional order on an unpriced market (the attack is the deliberate act). Disabled-marketmap-entry vector = permanent price-0 (listing only READS marketmap, never enables the entry — listing.go:56/121, verified — so Slinky never prices it) → removes the same-block constraint. Bug confirmed on ALL 3 layers: HEAD PoC + deployed binary (zero-diff) + feature/config live. No residual.

**ANTI-#407 REACHABILITY AUDIT (the maillon that killed my prior dYdX finding #407 F-T233):** #407 was a deterministic PrepareCheckState panic (mechanism proven via genesis-seeded state) but REJECTED as "latent hardening, not exploitable" because the trigger state (zero-positions+NC<0+in-daemon-list) is EMERGENT and the state-transition gate `IsValidStateTransitionForUndercollateralizedSubaccount` (subaccounts/lib/updates.go:118-128) REJECTS every organic arrival path → attacker can't force it, only genesis reaches it. Lesson = [[feedback-trigger-reachability-is-payability-gate]]. **The PML finding is the INVERSE — reachability verified gate-by-gate on mainnet, FAVORABLE:** the poison state is NOT emergent (no state-transition gate to reject it) — the attacker WRITES it directly via CreateMarketPermissionless (listing.go:72 Price:0 unconditional). Every gate on that tx passes on live mainnet: (1) hard cap 296<500; (2) ticker in marketmap (54 unlisted); (3) referencePrice!=0 check listing.go:129 — 53 of 54 pass (A/USD refPrice=8156539224, ACH, ALCX, BAT, BIGTIME…); (4) NO enabled-gate (grep-confirmed + PoC lists a disabled entry). All 53 are disabled → Slinky never prices → permanent price-0. Full-app PoC drives the REAL msg (not seeded). So #407's exact death-maillon (attacker-reachability of the trigger state) is CLOSED here with 53 concrete listable mainnet tickers. Report strengthened with this gate-by-gate proof to pre-empt the #407-style "seeded-only/not-organic" rejection. **REPORT DONE: ~/Desktop/BUGS/dydx-v4chain-2026/REPORT-pml-zeroprice-halt.md (report-nerve+chill, closing-gate clean). Tier=Critical (permanent system disruption, reachability established). Operator-gated: SUBMIT + post chain-of-custody comment. This is the strongest find of the session — the arc [[feedback-payable-impact-not-just-theft]]+[[feedback-trigger-reachability-is-payability-gate]] paid off: de-bias→hunt non-theft→re-source off fortresses→L1-liveness=High→executed halt→confirmed reachability.** Tie: the 3 other leads (accountplus gas-mispricing, revshare/affiliates unbounded-iter, float log10 nondeterminism) all REFUTED by the workflow. See [[feedback-payable-impact-not-just-theft]] (the impact-ledger WORKED — non-theft chain-halt found where theft-only would've missed) · [[feedback-execute-the-central-link]] (build the e2e PoC).

---
**OUTCOME 2026-07-20: SUBMITTED → returned DUPLICATE. Dead.** Real+reachable+in-scope finding but already reported (dYdX = mega-name, high-dup, exactly the saturation risk the landscape scan flagged when it DROPPED dYdX). Confirms saturation/dup is the killer even for a valid finding. Do not re-pursue.
