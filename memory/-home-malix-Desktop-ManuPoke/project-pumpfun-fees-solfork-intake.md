---
name: project-pumpfun-fees-solfork-intake
description: "pump.fun Pump Fees (/solfork) — fresh un-audited fee-sharing subsystem, theft seam pinned to 2 executable authz questions"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7a1cec50-aec0-40f4-9311-a86e2cb19f35
---

pump.fun Cantina bounty $500k Crit / $100k High. 3 closed-source-deployed Anchor programs, provenance-hashed to ~/Desktop/BUGS/pumpfun-2026: pump `6EF8…` (5d65238f, slot 433095571, 10MB), **pumpfees `pfeeUxB6…` (1337a822, slot 420801948, 948KB = small/fresh = the DIG)**, pumpamm `pAMMBay6…` (e97dab8f, slot 433112355, 10MB). All authority `7gZufwwAo17y…`. Helius RPC in memory.

SATURATION GATE = PASS-FRESH: the 9 audits were on PumpSwap AMM (Mar-2025). The **Creator Fee Sharing system launched Jan-9-2026** (split to ≤10 wallets, transfer ownership, revoke authority) — no public audit named for pump-fees. Separate from the AMM 9-audit fortress. GO.

IDL recovered on-chain (anchor:idl PDA 6hgWp61Y…, 29 ix / 9 accounts / v0.1.0 → idl.json). Money-out surface: claim_social_fee_pda(_v2) [global social_claim_authority signer = trusted backend, NOT attacker path], sweep_buyback [buyback_vault.authority has_one], and the FRESH seam = **fee_sharing**.

THEFT SEAM (fee-sharing): a `SharingConfig` PDA(sharing-config|mint), one per coin, {mint, admin, admin_revoked, shareholders:Vec<{address,share_bps}>}. `update_fee_shares` DRAINS `coin_creator_vault_ata` and distributes to attacker-supplied shareholders[] (remaining_accounts), via vault PDAs keyed on SHARING_CONFIG (creator-vault|sharing_config). So whoever controls the sharing_config controls that coin's creator-fee stream. Theft = 2 executable authz questions, NEITHER closed at IDL level:
  Q1 create_fee_sharing_config (args EMPTY, payer signs, NO has_one binding payer↔bonding_curve.creator/pool.coin_creator): is SharingConfig.admin set to payer (→ anyone binds a config to a VICTIM mint + becomes admin) or to the real creator? BondingCurve.creator + Pool.coin_creator are both in the account set so it CAN bind-safe — must verify it DOES.
  Q2 update_fee_shares (authority signer, NO IDL has_one=admin on sharing_config): is authority checked ==sharing_config.admin in the body (require_keys_eq / "Invalid admin" string present) or missing?
If EITHER loose → redirect victim coin's creator fees to attacker wallets = Crit theft.

FORK GROUNDED: pump global `4wTV1Ymi…` EXISTS (owner 6EF8, 1045B), fee-program-global `CHqnuTkj…` EXISTS (owner pfeeUxB6, 338B) — both cloneable. create's `global` = pump global. Program CPIs (sol_invoke_signed_rust) + carries pump_program+pump_amm_program+event auths → create likely CPIs pump to register the fee-PDA as coin creator → fresh-fork dummy accounts fail CPI validation → MUST state-fork (clone globals + a real bonding_curve + deploy pump.so). Disasm = opaque sBPF (llvm-objdump flat 95k lines, 3 func labels, no CFG — skill's hard case; Ghidra/rbpf needed to read the admin-store, else fork). solders 0.27.1 OK.

EXECUTED FORK VERDICT (state-fork, real graduated coin HHV2…pump / pool D5JW3L… / bonding_curve EyGB2…, attacker≠coin_creator):
- **Q1 = executed-CLOSED (NULL).** create_fee_sharing_config as attacker payer → **NotAuthorized 6016 @ create_fee_sharing_config.rs:272**. Account init even ran (system CPI depth-2) then the creator-authz gate reverted. Graduated coins authz off **pool.coin_creator** (@ raw offset ~243, not bonding_curve). So NOBODY can bind a SharingConfig to a coin they don't own — the PRIMARY/broadest theft vector (redirect a victim coin's fees) is gated. (Non-graduated coin first gave AccountNotInitialized 3012 on pool → create requires a graduated coin.)
- **Q2 = executed-CLOSED (NULL).** Overwrote pool.coin_creator @211 (the REAL Anchor field; @243 held a duplicate/reserved copy that broke deser) → create_fee_sharing_config as controlled creator SUCCEEDED (admin=creator_kp). Then update_fee_shares as attacker(≠admin) with shareholders=[attacker:10000bps] → **NotAuthorized 6016 @ fee_sharing/mod.rs:41** (authz fires early, 32.8k CU, before vault access). update requires authority==sharing_config.admin. REJECTED.
VERDICT: **fee-sharing seam = EXECUTED-NULL / fortress on both theft vectors** — Midas-pattern (fresh Jan-2026 feature, carefully authz'd: create gates payer==pool.coin_creator@line272, update gates authority==admin@mod.rs:41, both NotAuthorized 6016 on the real bytecode). Gotcha: solana-test-validator lost state between separate bash calls once (bare genesis → ProgramAccountNotFound) → run create+attack in ONE invocation. Fork plumbing fully proven. crank_donation_fee_pda PROBED (today-impact HIGH: relay RLAYHr9T active, 48,193 DonationFeePda + 514,521 SharingConfig on mainnet — donations very live). 6 accounts "FREE at IDL level" (quote_mint, donation_relay_event_authority, mint_whitelist, epoch_tracker, debouncer, debouncer_ata) BUT all are relay-owned singletons: mint_whitelist/epoch_tracker/debouncer owned by RLAYHr9T (relay), debouncer_ata=ATA(debouncer), donation_fee_pda_ata=ATA(donation_fee_pda) PDA-bound source. Fund flow donation_fee_pda_ata→debouncer_ata is relay-validated in the CPI (owner+seed) → permissionless cranker can't swap a controlled destination (fake debouncer fails owner check, fake ata fails derivation, atomic rollback). = STATIC-NULL (strong, corroborated by 48k safe cranks; not executed-swap-confirmed but relay-ownership is the executed artifact via getMultipleAccounts). Epoch/whitelist swap = at most rate-limit/whitelist bypass = griefing not theft.
PUMP-FEES VERDICT: fortress across all 3 examined money-out vectors (create-gated, update-gated, donation relay-validated). Aligns [[feedback-tool-complete-stop-polishing]] (/solfork niche swept). RE-SOURCE — remaining un-driven ix (sweep_buyback[buyback_vault.authority hasOne], claim_social[global backend authority]) are also authority-gated by IDL; low P. Go Perena (finding drafted) or a payable non-solfork vein. All artifacts ~/Desktop/BUGS/pumpfun-2026.

OLD-NEXT (superseded): state-fork a real pump coin (clone pump `global`, pumpfees fee_program_global+global, a mint+its bonding_curve+pool), deploy the 3 .so, drive create_fee_sharing_config as a fresh attacker payer → read resulting sharing_config.admin (byte-compare vs attacker vs bonding_curve.creator). Then drive update_fee_shares from a non-admin. Guard per solfork Correction 2/3 (gate verdict ≠ theft; the "Only Pump/PumpSwap may call" CPI gate is on get_fees not the sharing ix). Scope note: bounty tags contracts "Devnet Only" (demo there) but IDs are mainnet-live — [[feedback-commit-anchored-scope-pays-deployment-impact]].
