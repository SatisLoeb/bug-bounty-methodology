---
name: project-doppler-rehype-intake
description: "Doppler (Whetstone, Cantina $50k) — DIFF-mode on fresh Rehype hooks; 1 REAL modest (migrator MAX_SWAP_FEE cap omission) + 2 SUSPEND (no-slippage internal swap, needs fork PoC)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1cddd5e1-a775-4c40-bc31-1244e19cbfd8
  modified: 2026-07-25T12:20:46.518Z
---

**Doppler** — Uniswap-V4-hook liquidity-bootstrapping (Whetstone). Cantina bounty, Crit up to $50k / High $25k / Med $5k. Scope = mainnet-deployed contracts only; **previously-found Cantina issues OOS** (portfolio ba92f3ff); MEV/front-running OOS. Fresh clone ~/Desktop/ManuPoke/doppler-fresh/repo (HEAD d0be38d). Prior: my Feb-2026 `doppler-target` pass = "trust-boundary/grief only, no no-priv theft" (old version); triage note flags Doppler mega-saturated (758 findings) → DIFF-mode only. V2 inactive (repo CLAUDE.md).

**In-scope FRESH surface = the Rehype fee/rebalance/buyback hooks** (deployed 6 chains, post-audit): RehypeDopplerHookInitializer(862) + RehypeDopplerHookMigrator(765) + FeesManager + DopplerHook{Initializer,Migrator}. Core (Doppler.sol auction, Airlock, V3/V4 init/migrators) = OOS-audited. The Rehype hook runs on EVERY user swap (_onSwap): collects LP fees (max 80%), splits via WAD matrix (buyback/beneficiary/LP), 5% to airlock owner, does INTERNAL swaps to reinvest as full-range LP + buyback.

**Swarm (5-subsystem, 14 verdicts): 1 REAL, 2 SUSPEND, 9 refuted, 2 trusted.**

**F-01 REAL (modest, Medium-at-best) — migrator omits MAX_SWAP_FEE cap (unguarded sibling):** `RehypeDopplerHookMigrator._onInitialization` stores `getHookFees[poolId].customFee = initData.customFee` (RehypeDopplerHookMigrator.sol:93) with NO cap → applied at `_collectSwapFees` line 742 `feeAmount = mulDiv(feeBase, customFee, 1e6)`. The sibling `RehypeDopplerHookInitializer` enforces `require(startFee/endFee <= MAX_SWAP_FEE=0.8e6)` (lines 121-122) from the SAME shared RehypeTypes constant; migrator's import block omits MAX_SWAP_FEE. So a migrated (graduated) pool's fee-setter can set customFee up to 100% (uint24, SWAP_FEE_DENOMINATOR=1e6), exceeding the protocol's deliberate 80% swapper-protection guardrail. Fee taken from unspecified/output token, reduces swapper output.
- **SEVERITY-BOUNDING GATES (verified, why modest):** (a) **setter = asset TIMELOCK** or its delegated authority (`setDopplerHook` DopplerHookMigrator.sol:397-399; migrator initialize onlyAirlock fed by creator calldata) → the pool's OWN governance → risks "centralization by design" OOS. (b) **Quoter INCLUDES the fee**: DopplerLens quotes via `poolManager.unlock` through the real hook → afterSwap fee reflected → swappers w/ minAmountOut auto-protected → NOT silent theft; only naive/no-minOut swappers lose, and pool visibly un-tradeable at 100%. Mild aggravator: airlock owner takes 5% of the excess fee (protocol profits).
- **Submittable framing = guardrail-regression Medium**: the ≤80% cap is a deliberate swapper-protection the migrator regresses on the now-public graduated pool; NOT intended centralization (sibling proves intent to bound). BUT contestable as trusted-timelock/centralization + minOut-protected. Honest Med/Low.

**F-02/F-03 SUSPEND — no-slippage internal swaps:** `_executeSwap` sets `sqrtPriceLimitX96 = MIN/MAX extreme` (RehypeDopplerHookInitializer.sol:435, migrator:377) = ZERO slippage bound; buyback+rebalance swaps run in afterSwap on the SAME pool at the triggering user's price; only liveness/reserve guards. Untrusted-reachable sandwich of hook fees. **Killer objection**: attacker's front+back legs each pay up to 80% hook fee (only hook self-swaps exempt, line 160), and the dislocated internal swap is a WAD-fraction of an already-fractional fee = small victim → generically net-negative. Needs a **foundry fork PoC** to find the profitable envelope (thin bootstrap liquidity + LOW fee so legs cheap + large pre-accumulated fees). Likely MEV-OOS or sub-threshold. NOT PoC'd (session length).

**2026-07-24 — F-02 REFUTED by EXECUTED local-deploy PoC (net-negative every config).** Built full V4+Airlock+Rehype harness (deps: OZ v5.5.0-era via submodule init, pinned view-quoter-v4 586e467, symlink/remap fixes for nested v4-core — see repo remappings.txt edits). `test/integration/F02SandwichPoC.t.sol` inherits `RehypeDopplerHookIntegrationTest`, config `numeraireFeesToAssetBuyback=WAD` + RouteToBeneficiaryFees, SEPARATE victim, attacker front-run+back-run sandwich of the hook's unprotected internal buyback. **Attacker NET across fee sweep: 0.3%=−0.24, 3%=−534, 10%=−1851, 30%=−5069 numeraire — ALL NEGATIVE, loss GROWS with fee.** Root: internal swap is processed immediately each swap (fees0/1 zeroed) so it's only a one-swap-fee-fraction (small), while the attacker's own legs pay the pool fee — bigger fee = bigger internal swap BUT proportionally costlier legs → net loss grows. The no-slippage `sqrtPriceLimit` (extreme) is a real code smell / defense-in-depth gap but NOT net-extractable = **not payable, MEV-OOS/informational.** Only F-01 (migrator fee-cap omission) remains as a modest submittable.
NEXT if re-engaging: (1) submit F-01 as Med guardrail-regression (cheap, concrete, but modest + contestable); (2) F-02 dead (executed-refuted), do NOT submit as theft. [[feedback-model-manipulation-cost-before-crediting-twap-finding]] [[feedback-map-severity-to-program-rubric]]

**2026-07-24 — F-01 EXECUTED-PoC'd + report WRITTEN (chill voice, grep-gate clean).** Built `test/integration/F01FeeCapPoC.t.sol` (inherits `RehypeDopplerHookMigratorIntegrationTest`, reuses real Airlock+migrate+swap plumbing). Migrates 2 identical pools @0.3% vs @100% customFee, same 0.5 ETH buy. **Result: swapper receives 55_930_487_498 asset @0.3% vs EXACTLY 0 @100%; migrator stored customFee=1_000_000 (>MAX_SWAP_FEE=0.8e6) with no revert/clamp.** All 24 migrator-suite tests still pass. Asymmetry grep-confirmed: initializer imports+enforces MAX_SWAP_FEE (RehypeDopplerHookInitializer.sol:41 import, :121-122 require); migrator has ZERO MAX_SWAP_FEE refs, stores raw @:93, applies @:742. HONESTY BOUNDS baked into report: customFee written ONCE at _onInitialization (no post-graduation setter → NOT a fee-hike rug), set by creator/authority config at airlock.create, minOut-swappers protected (DopplerLens quote includes afterSwap fee). Severity presented as hedged Low/Med (contestable as centralization/user-config), triager to place. Report = `~/Desktop/ManuPoke/doppler-fresh/repo/F-01-migrator-fee-cap-report.md`. Fix = 1-line `require(customFee <= MAX_SWAP_FEE, FeeTooHigh(...))` in migrator.

**2026-07-24 — 4 HARD GATES run (operator rabat-joie critique). F-01 UPGRADED + report rewritten.**
- **GATE 1 (mainnet deploy) PASS + byte-identical.** `RehypeDopplerHookMigrator` LIVE on Ethereum mainnet (chain 1) + Base (8453) @ `0x975f9d1939cf6e4a3c9d99f9d41e6411cf4da23b` (per repo Deployments.json). Fetched deployed runtime via publicnode RPC (14206 bytes) vs local HEAD d0be38d artifact (14206): masked 19 immutable slots → 0 byte diffs, metadata incl → deployed == tested code EXACTLY. NOT OOS-by-non-deployment.
- **GATE 2 (in-scope repo) PASS.** origin = whetstoneresearch/doppler; file lives there.
- **GATE 4 (escalation customFee>1e6) DONE — UPGRADES finding.** `test/integration/F01EscalationPoC.t.sol`. Migrator stores ANY uint24 up to 16_777_215 (~1677%) w/ NO cap. TWO regimes: (a) 80-100% strips swapper (0 output @100%); (b) **>100% → EVERY swap REVERTS (0x08c379a0) = pool PERMANENTLY BRICKED** (customFee written once @_onInitialization:93, NO setter → unrecoverable; migrated liq + all token-holders locked out). Brick regime has ZERO minOut-dependency (sidesteps the reducer). Cross-pool singleton drain REFUTED (v4 flash-accounting forces swapper to settle → sell reverts, "deltaAfter1 not>=0"). 
- **GATE 7 (intent doc) — backs the regression argument.** MAX_SWAP_FEE cap DOCUMENTED (docs/RehypeDopplerHookInitializer.md:49 "startFee/endFee must be <= MAX_SWAP_FEE"); migrator LP-fee cap 15% also doc'd (DopplerHookMigrator.md:64). Migrator Rehype customFee = ONLY fee input w/ neither doc'd nor enforced bound (RehypeDopplerHookMigrator.md:35 "Static hook fee, in millionths"). = gap not design.
- **SEVERITY re-mapped** (was self-downgraded to Low): now Impact defensible-HIGH (notable financial loss + permanent liquidity lock no-recovery) × Likelihood MEDIUM ("possible under certain conditions" = creator sets customFee>cap, contract accepts w/ no guard). Their matrix Med×High = HIGH (up to $25k). Presented as mapping-to-their-rubric, let-them-place (not asserted).
- **GATE 3 (dedup) — CLEAN vs linked portfolio, but REAL GATE is elsewhere + operator-only.** Portfolio ba92f3ff = Doppler BASE-contracts Competition Jan 15-22 2025 (21 valid findings, pulled public PDF cdn.cantina.xyz/reports/cantina_competition_doppler.pdf). NONE touch fee-cap/customFee/Rehype; **Rehype hooks DID NOT EXIST in that Jan-2025 scope** → zero overlap, verifiable from outside. **BUT: a SEPARATE recent Rehype-hooks Cantina competition exists (57b00aab...) + its bounty (2c7af549...) — THAT is the true prior-art surface, findings PRIVATE, needs operator login.** ⚠️ Flips "fresh vein" assumption: Rehype was competition-audited → fee-cap omission may already be reported there. [[feedback-check-prior-audits-and-competitions-at-intake]]

**DECISION GATE before submit: operator must check the Rehype competition/bounty 57b00aab/2c7af549 findings (login-only) for a customFee/MAX_SWAP_FEE dup.** If clean there → submit (strong: mainnet byte-identical, 2 regimes incl permanent-DoS, doc-backed regression, PoCs). If dup'd there → withdraw.

**2026-07-25 — 2nd rabat-joie pass (6 pts). Report REWRITTEN brick-led; 2 overclaims KILLED by execution.**
- **P1 — 80% is ALREADY PERMITTED (measured).** `test/integration/F01ImpactBoundsPoC.t.sol`: same 0.5 ETH buy → 0.3% = 55_930_487_498 | **80% (allowed max) = 11_219_756_770** | 100% = 0. So skim-leg damage attributable to the MISSING CHECK = only the last step (11.2e9 = the 20%-of-output floor the cap preserves), NOT "up to 100% of a trade". Old title/impact overclaimed. **Brick is the only leg with NO analogue in the permitted range** → LEAD WITH BRICK.
- **P2 — brick = DoS, NOT fund-freeze (tested, overclaim retracted).** Migrator hook declares `beforeRemoveLiquidity:false`/`afterRemoveLiquidity:false` → liq ops never touch fee path; `StreamableFeesLockerV2.collectFees(poolId)` **SUCCEEDS on a bricked pool** (no revert). No-op-governance config → locked-position recipient = `DEAD_ADDRESS` → stays locked **BY DESIGN** (documented), NOT bug-caused; real-governance recipient releases normally post-lockDuration (no swap involved). ⇒ never write "funds frozen".
- **P4 — `Airlock.create` is PERMISSIONLESS (src/Airlock.sol:140, `external`, no caller gate; `_validateModuleState` validates MODULES not caller).** ⇒ customFee setter = arbitrary unprivileged user, NOT a trusted role ⇒ **kills the centralization/admin-trust objection**. Big strengthener.
- **P3 — real victim = BOOTSTRAP-AUCTION BUYERS** (paid during the Doppler auction, then creator's migration config bricks the graduation pool → can never sell into migrated liq). Beneficiary fee-stream also permanently 0 (fees only accrue on swaps). Precision: tokens NOT seized, another pool could exist → say "migrated liquidity", never "the only liquidity venue".
- **P5 — title/intent de-escalated.** MAX_SWAP_FEE documented for the INITIALIZER only (docs/RehypeDopplerHookInitializer.md:49), NOT a protocol-wide invariant. Honest framing = every OTHER creator fee is bounded (sibling 80%; `DopplerHookMigrator.initialize` MAX_LP_FEE=150_000=15% @:217, docs/DopplerHookMigrator.md:64) and Rehype migrator `customFee` is the ONE with neither doc'd nor enforced bound.
- **P6 — line refs verified @d0be38d**: migrator :84 decode, :93 store, :742 mulDiv, **:745-746** guard (revert is 746, was mis-cited 745); initializer :121-122.
Report now 1613 words, grep-gate clean (0 em-dash, 0 self-flagellation). Still Impact-High(brick)×Likelihood-Med, let-them-place, with the "creator configures own pool = self-inflicted" counter pre-stated + rebutted.

**2026-07-25 — 3rd rabat-joie pass. "BRICK" CLAIM REFUTED BY MY OWN TEST. Finding re-characterized as EXTORTIONATE, + PROFIT MOTIVE found.**
- **⚠️ "every swap reverts / permanently un-swappable" was FALSE — generalized from ONE shape (exact-input buy).** Ran control(0.3%)-vs-over-cap per shape: exact-input BUY+SELL revert ✓, but **exact-OUTPUT swaps EXECUTE** (fee lands on the unspecified=INPUT side → trader just overpays), and dust exact-input "succeeds" delivering 0 (early-return `outputAmount<=0` @:722). **A whale CAN exit** (1000× bag → exact-output sells all succeed). So: NOT bricked, NOT "holders can never sell".
- **KEY MECHANISM: the :745 guard tests `feeCurrency.balanceOf(address(poolManager))` = the SHARED V4 SINGLETON, not pool reserves.** Thin local singleton → exact-output reverts; `vm.deal(manager, 10_000 ether)` (mainnet-realistic) → **exact-output SUCCEEDS at ruinous price**. Must model fat singleton or you get the wrong verdict.
- **HEADLINE NUMBER (measured, replaces the brick claim):** to receive same 1e12 wei ETH, holder spends **113_396 asset on control vs 2_009_838 on over-cap = 17× overcharge → holder recovers 564 bps (5.64%) of fair value** (~94% loss on exit). Exact-output BUY 1e9: 0.00894 ETH control vs 0.15845 ETH over-cap.
- **PROFIT MOTIVE FOUND (kills "self-inflicted footgun").** `ProceedsSplitter._distributeSplit` pays `proceedsRecipient` a share of the **NUMERAIRE ONLY** by **DIRECT TRANSFER** (`safeTransferETH` :110), capped `MAX_SPLIT_SHARE=0.5e18` (50%), and in `DopplerHookMigrator.migrate` the order is **onInitialization(stores customFee) :298 → _distributeSplit :305 → locker.lock :367** ⇒ creator is paid INDEPENDENTLY of the graduation pool, attack does NOT self-punish. PoC'd: attacker `proceedsRecipient` received **49_999_999_999_999_713 wei = exactly 50% of the 0.1 ETH raise** while buyer left with 11_262_523_287 asset sellable only at 17× penalty. = rug w/ identified beneficiary.
- **ON-CHAIN LIVE CHECK (P3): ZERO `Migrate` events** from DopplerHookMigrator 0x1e40b087 (topic 0x3505876f) in last **~650k blocks ETH / ~1.05M Base** ⇒ **no over-cap pool live today**; latent defect on deployed prod code. Stated verbatim in report (Berachain-style honesty).
- Nit added: `MigratorInitData` mixes denominators (WAD routing rows vs millionths customFee) = accidental-trigger mode, one line, below the deliberate path.
Report rewritten, headline now "holders exit at ~5.6% of fair value". LESSON: [[feedback-invariant-that-passes-is-not-a-finding]] mirror — **never generalize a revert-claim from one call shape; sweep shapes against a control, and model the SINGLETON balance the guard actually reads.**

**2026-07-25 — 4th rabat-joie pass. BASELINE MEASURED on the new lead leg + ZERO-ADOPTION found. Report final.**
- **CLOSED FORM (the real finding): fee lands on the UNSPECIFIED side ⇒ exact-output trader pays `1 + customFee/1e6`× fair ⇒ `recovery = 1/(1 + customFee/1e6)`.** Covers the whole uint24 domain in one line, no per-value measuring.
- **80% BASELINE MEASURED on the exact-OUTPUT leg** (v3 had measured it only on exact-INPUT, the leg since downgraded). Asset spent for 1e12 wei ETH: **0.3% = 113_396 | 80% PERMITTED = 203_502 (5572 bps = 55.7% recovery) | over-cap uint24max = 2_009_838 (564 bps = 5.6%)**. ⇒ **damage attributable to the missing bound = 55.7% → 5.6%, ~9.9× worse than the worst PERMITTED config.** (`test/integration/F01BaselinePoC.t.sol`.)
- **TITLE OVERCLAIM #2 KILLED:** old title said "creator keeps up to 50% of the raise" — but `MAX_SPLIT_SHARE=0.5e18` is BOUNDED + intentional + documented and the bug does NOT unlock it. Split = the MOTIVE (payout independent of the pool ⇒ abuse is costless to the creator), NOT the damage. Moved out of title into "Why a creator would do it". Same error class as the earlier "documented 80% cap" overclaim.
- **INTERNAL CONTRADICTION FIXED:** PoC printed `bootstrap buyer can SELL: false` while the analysis (and the 5.6% figure, which IS a completed sale) says exact-output executes. Relabeled "can sell via exact-input: false" + pointer to the exact-output result.
- **⚠️ ZERO ADOPTION (full-history, decisive likelihood reducer, self-disclosed).** `DopplerHookMigrator` 0x1e40b087 deployed **26 Feb 2026** both chains; scanned its ENTIRE life for `Migrate` (topic 0x3505876f): ETH 24_450_000→25_609_380, Base 42_500_000→49_094_149 ⇒ **0 events on BOTH chains — no pool has EVER graduated through this migrator.** Plus RehypeDopplerHookMigrator 0x975f deployed **23 Jul 2026 (2 days pre-report, ETH block 25_590_350)**. ⇒ nothing exposed today; framed as "fix before the path carries value". Stopped arguing a likelihood tier: gave Impact-High(mechanism) + both halves of likelihood, let them place.
- Dust path resolved NOT attributable: input IS consumed & 0 asset delivered, but output rounds to 0 ⇒ `_collectSwapFees` early-returns (:722) ⇒ **fee never applied** ⇒ ordinary AMM dust rounding, not fee-caused.
- Multiples aligned (17.78× formula / 17.7× measured / ~1678%). Build gotcha: stack-too-deep in these PoCs → keep ≤3 locals per fn, split 4-arg console.log, one pool per test fn.

**2026-07-25 — 5th rabat-joie pass. SDK CHECK FOUND THE REAL LEAD LEG. Scope-conflict cleared. Report final (2078w).**
- **⭐ SDK ASYMMETRY = the finding's best evidence + its likelihood.** `doppler-sdk` (public, `whetstoneresearch/doppler-sdk`): `src/evm/utils/dopplerHookMigrator.ts` bounds `customFee <= 1_000_000` (**hardcoded literal, 100%**) while `src/evm/utils/rehypeDopplerHookInitializer.ts` bounds startFee/endFee by `DECAY_MAX_START_FEE` = **800_000 (80%)** from `src/evm/constants.ts` (comment: "intentionally higher than V4_MAX_FEE… 800_000 = 80%"). ⇒ **the SAME migrator-only omission is replicated in a SECOND codebase**, and the 80→100% band is reachable via the SHIPPED SDK with **ZERO bypass**. Proves 80% is the intended ceiling (named+commented constant, applied to the sibling).
- **NEW HEADLINE (measured): at `customFee = 1_000_000` (the SDK's OWN max) an exact-input swap SUCCEEDS, consumes the FULL input, delivers ZERO.** 0.5 ETH in → **0.5 ETH consumed, 0 asset out** vs 11_219_756_770 at the 80% ceiling. No revert ⇒ silent total loss for callers w/o real minOut (DopplerLens quote does include the fee, so minOut>0 reverts = protected).
- **TWO REACHABILITY TIERS now explicit:** (A) via SDK, ≤1e6 → zero-output silent loss [the likely case]; (B) direct `Airlock.create`, ≤16_777_215 → exact-input reverts, exact-output at 17.78× → 5.6% recovery [deliberate bypass, the escalation]. Earlier drafts led with (B) — wrong emphasis.
- **SCOPE-CONFLICT CLEARED (was a real risk).** Competition `57b00aab` = **the SAME Jan 15-22 2025 comp** (COMPLETED, commit 338d39d6, scope = Airlock/Doppler/V2-V3-V4/Governance, **NO src/dopplerHooks**) → the earlier agent's "newer Rehype competition" claim was WRONG. Bounty `2c7af549` = **LIVE since 7 Apr 2025, repo-wide scope, NO commit pin** ⇒ src/dopplerHooks@HEAD in scope, no code-in-competition conflict. Only unknowable = other researchers' private submissions to the live bounty queue (normal, unknowable by anyone outside).
- **Route-completeness PROVEN on-chain:** `BaseDopplerHookMigrator.onInitialization` is `onlyMigrator` vs an **immutable** MIGRATOR; `cast call 0x975f… "MIGRATOR()"` → **0x1E40b0875DDa35f41E15cFB475403859B8c860c4** = exactly the address whose full Migrate history I scanned ⇒ "zero events" covers EVERY route into the hook.
- **"No pool ever graduated" ≠ "theoretical" — distinction now written explicitly** (Berachain contrast: there the TRIGGER couldn't fire (72h gate); here the trigger works, is reachable today, permissionless, on byte-diffed production code via an SDK-exposed field — what's missing is someone *under* the trapdoor, not the trapdoor). This is the line separating Low/Info from a real finding; a rushed triager will conflate it.
- Table nit fixed: "holder recovers" bps are vs the **0.3% control**, not a fee-free pool → 5572 measured vs 5556 formula, gap = the control's own 0.3%. Stated under the table.

**2026-07-25 — 6th rabat-joie pass. Closing-argument pre-empted, intent anchor MOVED, report final (2341w). SUBMIT-READY.**
- **⚠️ THE SDK's `1e6` IS DOUBLE-EDGED — best available closure argument against the report, now pre-stated + answered IN the report.** Their reading: "someone wrote `must be <= 1000000 (100%)` = a DELIBERATE ceiling for the migrator's STATIC fee; the 80% belongs to the initializer's DECAY fee, different param, different job ⇒ no omission." My answer (from my own measurement): at the TOP of that ceiling the payer receives NOTHING (0.5 ETH in → 0 asset out, no revert) ⇒ a ceiling whose max permitted value makes the trade wholly confiscatory isn't a behaviour-chosen target, it's a bound on the WRONG QUANTITY ("fee ≤ 100%" vs "fee can't take the whole trade"). Conceded the other reading is available → if the team holds it, finding reduces to tier-two (direct-call). Own the counter, don't let it be sprung.
- **⭐ INTENT ANCHOR MOVED off the SDK onto the CONTRACT (the operator's key correction).** Do NOT lean on `DECAY_MAX_START_FEE` — the name is decay-SPECIFIC and serves THEIR reading. The real anchor: **`MAX_SWAP_FEE` is a GENERIC name in `src/types/RehypeTypes.sol`, the shared types file BOTH hooks import from — and the migrator's import block pulls 16 symbols including `SWAP_FEE_DENOMINATOR` (:49) but NOT `MAX_SWAP_FEE` (:46). Two constants 3 lines apart, one import block, only the denominator crossed.** Verified by parsing the import statement. SDK now cited for REACHABILITY only, never intent.
- **SCOPE HYGIENE: title must lead with the IN-SCOPE contract, not the SDK** (SDK is NOT in scope = repo `whetstoneresearch/doppler` only; an SDK-flavoured title invites "SDK bug → OOS → closed unread"). New title, one proposition, damage first: *"RehypeDopplerHookMigrator applies customFee with no upper bound: at 100% an exact-input swap takes the swapper's entire input and returns zero"*.
- Tier-one table cell FILLED: exit cost @1e6 (SDK ceiling) = **226_114** asset for 1e12 wei ETH = **5015 bps recovery** (formula 50%; gap = control's own 0.3%). No blank left in the table that quantifies the title.
- **Competition set ENUMERATED (not just searched):** Cantina competitions index renders statically, ~60+ entries, **exactly ONE** Whetstone/doppler-contracts entry (15-22 Jan 2025 = the completed comp). Bounty 2c7af549 LIVE since 7 Apr 2025, repo-wide, no commit pin. ⇒ no code-in-competition conflict, and no hidden Rehype competition exists.
- Fix section: also lower the SDK's `1_000_000` literal to `800_000` so client ≤ chain.
LESSON (durable): when a discovery strengthens a report, immediately ask what the SAME discovery hands the defender — the vendor's own looser bound is simultaneously your reachability proof AND their "deliberate ceiling" defence. State both.

**2026-07-25 — 7th pass: FINAL. Operator cleared "Envoie". Report SUBMIT-READY (2461w), full PoC suite GREEN.**
- **Tier-one narrative widened (last operator add): the EXACT-OUTPUT path at the SDK-permitted 1e6 is the WIDER victim class.** It does NOT revert, costs exactly **2×** (226_114 vs 113_396 asset for 1e12 wei ETH, = 1/(1+1)), and is bounded by **`maxAmountIn`, not `minAmountOut`** ⇒ a caller targeting a precise output (repayment, fixed-size buy) routinely leaves maxIn generous and pays double with no signal. Much more ordinary victim than the exact-input `minOut=0` case. Number was already in the table but missing from the prose.
- **Suite hygiene: `test_F01esc_crossPoolSellDrainAttempt` now `vm.skip(true)` + comment.** Its "FAIL" was v4's own settlement assertion (`deltaAfter1 >= 0`) inside PoolSwapTest = the REFUTATION, but a forge assertion marks a test red even when the revert is caught (Foundry behaviour, not a finding). Final: **179 passed / 0 failed / 1 skipped** across 7 F01 suites. Never hand over a PoC set with an unexplained red.
- FINAL ARTIFACT SET: `F01BaselinePoC` (0.3/80/100/uint24max exit costs + SDK-max zero-output + dust), `F01SingletonPoC` (fat-singleton exact-output 17.7×, whale exit, 564bps), `F01ProfitAndShapesPoC` (proceeds-split profit motive + shape sweep), `F01PrecisePoC` (shapes/dust threshold), `F01ImpactBoundsPoC` (exact-input 0.3/80/100 + locker release), `F01EscalationPoC` (uint24 sweep), `F01FeeCapPoC` (original 0.3-vs-100).
- **STATUS: report complete, operator to SUBMIT (Cantina login = operator-only action; I hold no credentials and submission is outward-facing).** Only residual unknowable = other researchers' private submissions to the live bounty queue 2c7af549.

**2026-07-25 — 8th pass: INLINE POC PACKAGED (Cantina has NO file attachments). Both artifacts final.**
- **Cantina constraint: no attachments** ⇒ PoC must be pasted as a COMMENT, report stays clean. Consolidated all 7 scattered PoC files into ONE paste-and-run file: `test/integration/F01ConsolidatedPoC.t.sol` (7 tests A-F, inherits `RehypeDopplerHookMigratorIntegrationTest`, no mocks/fork/extra deps). **30 passed / 0 failed** (7 mine + 23 inherited harness).
- **Deliverables:** `~/Desktop/ManuPoke/doppler-fresh/repo/F-01-migrator-fee-cap-report.md` (2564w, the report) + `F-01-poc-inline.md` (1804w, self-contained: run instructions + full .sol inline + verbatim output + test→claim mapping table). Report's Reproduction section now points at the single consolidated file (old 7-file refs removed = triager can't be sent chasing files they don't have).
- **VERIFIED THE PASTE, not just my copy:** extracted the solidity back OUT of the .md, wrote it to the canonical path, ran clean-room → 30/30 with **byte-identical gas values** ⇒ the pasted code is the code I measured. Also asserted embedded-code == on-disk programmatically.
- Test→claim map: A stored-verbatim(800k/1e6/u24max) · B **headline** 1e6→0.5ETH consumed/0 asset, 80%→11_219_756_770 · C exit 113_396/203_502/226_114/2_009_838 · C2 recovery 5572/5014/564 bps · D exact-output 0.00894 vs 0.15845 ETH (~17.7×, NOT blocked) · E attacker 49_999_999_999_999_713 wei + buyer can't exact-input exit · F locker.collectFees OK (LP not frozen).
- **Number fix:** report said 5015 bps @100%, measured integer is **5014** (113396*1e4/226114). Report must match the artifact exactly — corrected.
- Harness caveat documented in BOTH files: `_pool()` deals the v4 singleton 10_000 ETH because the :745 guard reads `balanceOf(poolManager)` (SHARED singleton) not pool reserves; thin local singleton makes exact-output revert for a non-mainnet reason. Only test D depends on it.
LESSON: when a platform can't take attachments, verify the PoC by round-tripping it THROUGH the deliverable (extract from the md and run that), not by running the working copy.

**2026-07-25 — SUBMITTED as Cantina finding #784** (Whetstone/doppler-contracts bounty 2c7af549, live).
Title: "RehypeDopplerHookMigrator has no customFee upper bound: at 100% an exact-input swap consumes the full input and returns zero" (124 chars). Submitted Likelihood=Medium × Impact=High → **High** (program matrix, High = up to $25k). PoC posted as comment (F-01-poc-inline.md, F01ConsolidatedPoC.t.sol, 30/30). Screenshot pack guide at `submissions/SCREENSHOTS-GUIDE.md` (04+05 captured; 03/06/10 need working DNS — publicnode/eth.drpc.org/1rpc.io all verified good, avoid llamarpc 521 + cloudflare-eth -32603).

**RESPONSE PLAYBOOK (if they push back) — all pre-answered IN the report, do NOT invent new ground:**
1. *"100% is a deliberate ceiling, the 80% is the initializer's DECAY fee"* ← THE closure argument, already stated + answered in § "On intent, including the reading that would close this". Answer = the measurement: at the top of that ceiling the payer receives NOTHING. A ceiling whose max value makes the trade wholly confiscatory is a bound on the wrong quantity. Anchor = `MAX_SWAP_FEE` is GENERIC-named in the SHARED `RehypeTypes.sol`, and the migrator imports 16 symbols from it incl. `SWAP_FEE_DENOMINATOR`(:49) but NOT `MAX_SWAP_FEE`(:46). Do NOT lean on the SDK's `DECAY_MAX_START_FEE` name (serves their side).
2. *"no pool affected / theoretical"* ← self-disclosed in § "Things that cut against this". Distinction to re-state: the TRIGGER works and is reachable today (permissionless, byte-diffed prod code, SDK-exposed field); what's missing is someone under the trapdoor, not the trapdoor. Contrast w/ a finding whose trigger cannot fire.
3. *"creator configures own pool = self-inflicted"* ← answered: protocol bounds this class everywhere else; harm lands on auction buyers not the creator; creator is paid by a transfer the bad config doesn't touch (`_distributeSplit` :305 runs after `onInitialization` :298, direct `safeTransferETH` :110).
4. *"minOut protects swappers"* ← only the exact-INPUT leg. The exact-OUTPUT leg is bounded by `maxAmountIn`, executes, costs 2× at 1e6, and is the wider victim class.
**STRONGEST GROUND = tier one (SDK-reachable 1e6, no bypass). Do NOT re-litigate on the 1678% direct-call tier — it's labelled the escalation.** Retracted-by-own-tests (keep, it's the credibility): brick claim, LP-freeze claim, cross-pool drain.

**2026-07-25 — #784 REJECTED. Closed on POLICY ("deployer responsibility"), NOT on technique. CLOSED unless they answer the ceiling question.**
- **They conceded everything technical**: mechanism, reachability, and every measurement went uncontested. Their close: "the over-cap fee must be selected by the party launching that specific token pool → creator-selected pool configuration rather than an unauthorized attack… treated as deployer responsibility."
- **Their point 1 is a STRAWMAN**: "arbitrary traders cannot call it or change an existing pool's fee" — never claimed. *I* documented the `onlyMigrator`/immutable-MIGRATOR gate myself and used it to prove Migrate-scan completeness. They re-stated my own reachability proof as if it refuted me.
- **What they did NOT touch = the only appeal axis**: the SIBLING BOUNDS. Not one word about `require(startFee/endFee <= MAX_SWAP_FEE)` (:121-122) or `MAX_LP_FEE`=15% (:217). Both are ALSO creator-supplied, same call, same calldata blob, and bounded anyway ⇒ the codebase already decided "deployer responsibility" does NOT apply to fee params. customFee is the lone exception.
- **Reply sent as ONE factual question, not an argument** (`repo/F-01-reply-784.md`, 253w): "what is the intended ceiling for customFee, 100% (the SDK's) or 80% (the shared constant's)?" + a one-line correction of the strawman. If 100% → accept the close. If 80% → the bound is missing. **Precision note:** startFee/endFee ride in the initializer's `onInitializationCalldata` and customFee in `liquidityMigratorData` = DIFFERENT fields of the SAME `CreateParams`, one `Airlock.create` call, one creator. Never say "same calldata blob" (imprecise, hands them an out); the reply now says "different fields of the same CreateParams", which preempts it. If challenged anyway: concede in one line, substance unchanged.
**THREE OUTCOMES, all terminal, nothing to prepare:** (a) "100%" → drop it, condition was self-imposed, do not negotiate; (b) "80%" → they conceded the bound is missing; expect a SILENT FIX, not a tier reversal, do NOT relaunch; (c) policy restated without answering → that IS the answer, thank them and move on. **Baseline: do NOT expect a reversal** (Perena didn't move on a comparable internal-consistency argument). Realistic ceiling = Informational, unpaid. Accept whatever comes back.
- **ROOT CAUSE = the ACTOR, not the mechanism** → durable lesson extracted to [[feedback-findings-die-on-the-actor-not-the-mechanism]] and encoded UPSTREAM as `6a-0 TEST DE L'ACTEUR` in the xsurface-prioritize skill (+ wired into its per-path output template). Doppler(actor==configurator) and Perena(no actor creates the state) form a VICE; StackingDAO is the shape that escapes it.
