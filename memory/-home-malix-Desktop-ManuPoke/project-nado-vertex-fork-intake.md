---
name: project-nado-vertex-fork-intake
description: "Nado (HackenProof $500k Critical, github.com/nadohq/nado-contracts) = a VERTEX PROTOCOL FORK on Ink L2. Original oracle-freeread thesis DEAD (no Chainlink consumer; sequencer-pushed price). Real surface = fork-diff + Nado-new NLP LP product."
metadata: 
  node_type: memory
  type: project
  originSessionId: 91ada56b-1372-4a8e-9cae-4aa535de28a0
---

HackenProof $500k, severity Critical. Scope = `github.com/nadohq/nado-contracts` (SC only). In-scope: theft/loss, unauthorized tx, tx-manipulation, **logic attacks (behavior ≠ business description)**, reentrancy, reordering, over/underflow. OOS: theoretical-no-PoC, **imported contracts**, **front-run-only**, gas/style. Chosen as #2 of the re-pointed oracle-lens sourcing ([[feedback-oracle-replay-refute-first-reflex-fix]]).

**INTAKE VERDICT (2026-07-23, manual-poke-before-machinery [[feedback-manual-poke-mandatory-bracket]]): the oracle-freeread thesis is DEAD; Nado is a VERTEX fork; real surface = the fork-diff.**
- Nado = **Vertex Protocol fork**, GPL-2.0, `nado-core` v1.2.2, Ink L2 (Blockscout-verified). Byte-for-byte Vertex architecture: `Clearinghouse`/`ClearinghouseLiq`, `Endpoint`/`EndpointTx`/`EndpointGated`, `SpotEngine`/`PerpEngine`(+State), `OffchainExchange`, `MathSD21x18`, `RiskHelper`, `WithdrawPool`, `ProxyManager`, `Verifier`. 28 core .sol (non-interface/mock).
- **NO external oracle lib** (grep: zero Chainlink/Pyth/RedStone/AggregatorV3/latestRoundData). Price = Vertex model: `priceX18[productId]` mapping **sequencer-pushed** (`Endpoint.sol:279 require msg.sender==sequencer`; `EndpointTx.sol:491`). `getPriceX18` checks only `!= 0`, **no staleness** — but that's the trusted-sequencer shape that died on Injective (no attacker stale primitive, symmetric direction) AND it's Vertex-core = dup-walled by Vertex's ToB audits. **The `oracle-consumer-freeread` harness does NOT apply here** — sourcing agent inferred a Chainlink/Chaos consumer from docs; real on-chain code is sequencer-price. Poke dissolved the thesis on contact (why manual-poke-first matters).

**REVISED PLAN — the payable surface is Nado's DEVIATION from Vertex (Rule 43: fork ≠ custom deviation; depth on the diff, not the audited shared core):**
1. **CROWN JEWEL = the NLP (Nado LP) product** — Nado-invented, NOT in stock Vertex. An LP token with `NLP_PRODUCT_ID`, a **sequencer-signed price** `priceX18[NLP_PRODUCT_ID] = signedTx.oraclePriceX18` (EndpointTx 547/567), an `NLP_LOCK_PERIOD`, and special-cased handling in `SpotEngine`(155/164/193/216)/`SpotEngineState:270`/`Clearinghouse:455`. A custom LP wired into Vertex health/margin with a pushed price = where value-math (extract) + price-consumption + margin meet. Highest EV, least audited. START HERE.
2. **NUKE-diff** Vertex-base → Nado HEAD to isolate every deviation (need to pin the exact Vertex commit Nado forked — Vertex public GPL contracts). [[project-nuke-static-barrage-skill]] `nuke diff`.
3. **Door-A**: Vertex's public audit findings (Trail of Bits) → check whether Nado's deviations reintroduce/break them.
4. Nado-new: `Airdrop.sol`, `DirectDepositV1.sol`. Code smell: `Endpoint.sol:280 // TODO: sequencer error state` (Vertex handles? Nado left TODO).
5. Provenance gate: verify deployed Ink contracts == repo HEAD v1.2.2 (deployed==scope).

Repo: `~/Desktop/BUGS/nado-2026/repo` (stabilized). Front-run-OOS ⇒ frame any timing vein as observable-standing-state, not a mempool race.

## Vertex-diff result (background wf, 2026-07-23) — base = `vertex-protocol/vertex-contracts` (VLP progenitor), MEDIUM conf (exact fork commit unknown; diff magnitude = heat-map not pure Nado-change)
**KEY CORRECTION:** Vertex is NOT LP-less — it has **VLP** (VLP_PRODUCT_ID=153, mintVlp/burnVlp, V_ACCOUNT). Nado's NLP = **rename+extension** of Vertex VLP (→NLP_PRODUCT_ID=11, N_ACCOUNT). **The mint/burn price math (`nlpAmount=quote/oraclePrice`) is INHERITED/audited — do NOT burn depth there.** The Nado-NEW deltas (attack these):
1. **4-day LOCK QUEUE** (entirely Nado-new; Vertex VLP had no lock) — `nlpLockedBalanceQueues` (SpotEngineState) + `handleNlpLockedBalance`/`tryUnlockNlpBalance`/`getNlpUnlockedBalance` (SpotEngine). Mint enqueues FIFO `unlockedAt=getOracleTime()+4d`; burn spends `unlockedBalanceSum`. N_ACCOUNT exempt. Attack: oracle-time source, FIFO merge on equal unlockedAt, delete/queue-index accounting.
2. **MULTI-POOL + PROFIT-SHARE authz** — `nlpProfitShare` (quote pool→recipient, gated ONLY by INITIAL health), **`clearNlpPoolPosition` (sweeps a pool's ENTIRE spot+perp book into N_ACCOUNT)**, `forceRebalanceNlpPool`, and **`_validateNlpRebalance` = ONLY sum(rebalance)==deltaQuote + per-element sign — NO check a pool actually has funds** (post-hoc health only). Thinnest validation = strongest signal.
3. **burnNlp fee** `max(ONE,quote/1000)` + MAINTENANCE-health post-check (dev comment admits fee-can-decrease-health).
4. `getPriceX18` now NON-VIEW emitting `PriceQuery` event (was view/pure in Vertex) — state-mutating read on every price consumption (reentrancy/gas/order on Ink).
- Inherited-verbatim: the sequencer-error-state TODO (Endpoint:280) is Vertex's, NOT implemented on Ink (no-op) — inherited, lower Nado-novelty.
- **Other deviation surfaces for AFTER NLP:** `OffchainExchange` (~1086 lines reworked, fee engine), `ClearinghouseLiq` (~500 lines, new MIN_SPREAD/NON_SPREAD_LIQ_PENALTY). Dropped from Vertex: SpotEngineLP/PerpEngineLp (LP folded into SpotEngine/Clearinghouse), sanctions list.
- Diff priority (agent): (1) lock-queue accounting, (2) pool authz + thin _validateNlpRebalance + clearNlpPoolPosition, (3) burnNlp fee/health, (4) getPriceX18 state-mutating.

## NLP DIG VERDICT (2026-07-23) — CROWN JEWEL = FORTRESS, 6/6 sub-veins NULL, 0 PoC burned
Adversarial dig (wf wcxt8plh1 + rerun wlymu94im, source-cited):
1. **locked-collateral liq-evasion — NULL**: locked NLP IS fully seizable in liquidation (treated as normal spot product; updateBalance moves TOTAL balance, re-locks on liquidator side, value transfers at oracle price immediately). Health values total NLP; liq trigger uses same total → no differential. Lock gates only the holder's burn/redeem, not seizure. (BaseEngine:163, SpotEngineState:262, SpotEngine:193/216, ClearinghouseLiq:609/518)
2. **rebalance/pool-solvency — NULL on REACHABILITY (the sharp one)**: REAL weakness confirmed — burnNlp has NO per-pool solvency post-check (unlike forceRebalanceNlpPool which loops per-pool INITIAL-health), so a burn split CAN drive an nlpPool insolvent without reverting. BUT `nlpPoolRebalanceX18` is NOT in the signed payload (typehash = `MintNlp(sender,quoteAmount,nonce)` only, Verifier:27); sequencer appends it. Fast path = `msg.sender==sequencer` (Endpoint:279); slow/permissionless path = MintNlp/BurnNlp ABSENT from processSlowModeTransactionImpl → terminal `revert()`. User controls quoteAmount + mint/burn choice, NEVER which pool is debited. ⇒ only the trusted sequencer can trigger = harming its own system, OOS. **Hardening note, not payable** [[feedback-trigger-reachability-is-payability-gate]].
3. **NLP price staleness — NULL (premise FALSE)**: NLP (product 11) is a fully-registered product, so the per-block `UpdatePrice` tx DOES price it (productToEngine[11]=spotEngine, no NLP exclusion). The `updateStates` skip is a LENDING-INTEREST-accrual skip, NOT a price skip (`_updateState` never writes priceX18). NLP priced fresh every block, trusted-sequencer, symmetric → Injective-dead. (My staleness hypothesis conflated interest-skip with price-skip — corrected.)
4-6. lock-queue accounting / JIT profit-share / N_ACCOUNT — all NULL (sound invariants; share-price-neutral; conserved keyless mirror).

**DELTA-PASS VERDICT (wf w0h4ndruv) — 4/4 NULL, executed refutations:**
- **Liq-penalty math NULL**: Nado's penalty FLOORS (MIN_NON_SPREAD=0.5%, MIN_SPREAD=0.25%) preserve penalty sign (RiskHelper _getWeightX18), conservation holds spot/spread/perp (`liquidatee_loss = liquidator_gain + insurance_fee`), insurance always NET-POSITIVE, self-liquidation NET-NEGATIVE to attacker (loses 0.5|p| to insurance). ClearinghouseStorage getLiqPriceX18:52 / _handleLiquidationPayment:579.
- **Negative taker fee NULL (premise misread)**: `TAKER_FEE_ACCRUAL_RATE_X18=-3bps` is a comparison THRESHOLD (OffchainExchange:598 makerAccruesTakerFee), NOT an applied rate. Default taker rate = +2bps (tier 0). Wash-trade = −2bps to protocol (collectedFees→X_ACCOUNT). Negative-tier needs updateFeeTier (msg.sender==clearinghouse) / updateTierFeeRates (onlyEndpoint) = owner-gated; the rebate-receiving special maker is an address(0)-prefix un-signable internal account. OOS.
- **burnNlp fee/health NULL**: MAINTENANCE post-check + own-signature (typehash `BurnNlp(sender,nlpAmount,nonce)` binds sender, Verifier:29) + sequencer-set oraclePriceX18 (not in typehash). Sender always nets −fee TO protocol; every grief is self-harm; dust case sound.
- **getPriceX18 non-view NULL**: body = read + require + `emit PriceQuery` (LOG1, no call-out → no reentrancy); no `view` caller exists (would be compile error); price slot read-once identical output. Informational only.

## FINAL VERDICT: Nado = EXECUTED-NULL FORTRESS-FORK. RE-SOURCE.
Oracle-freeread thesis evaporated (Vertex sequencer-price, no Chainlink consumer). Fork-diff pivot (Rule 43) was correct. Every attributable Nado-NEW deviation examined = NULL, **ZERO PoC burned** (all killed by source-read + adversarial verify): NLP subsystem 6/6 (lock-queue, pools, profit-share, staleness, N_ACCOUNT, liq-evasion) + 4 deltas (liq-penalty, neg-taker-fee, burnNlp-fee-health, getPriceX18). Only un-examined surface = `OffchainExchange` (~1086-line diff) — needs a Vertex fork-commit BISECT to attribute, and it's the MOST Vertex-ToB-audited engine = lowest-novelty/highest-audit = low EV. [[feedback-tool-complete-stop-polishing]] applies: freshest surface dead → RE-SOURCE (profile: the re-pointed lens wants a REAL Chainlink/Pyth consumer or a fresh off-chain-signer, not a Vertex clone). Residual only if operator wants exhaustion: OffchainExchange bisect+dig.
