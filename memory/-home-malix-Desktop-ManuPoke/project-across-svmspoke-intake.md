---
name: project-across-svmspoke-intake
description: "Across SVM spoke pool (Solana) /solfork hunt — GO, scope-verified, provenance-anchored, interface recovered, probe reframed"
metadata: 
  node_type: memory
  type: project
  originSessionId: 95853d7a-e236-418b-a353-da744a061ae8
---

Across Protocol **svm_spoke** on Solana — first real /solfork field hunt (2026-07-21). Chosen over Aori (no bounty found → unpayable). Picked because the July-2026 incident CONFIRMS the caller-reachability/event-spoof class is Across's soft belly (SVM leg less-hunted than EVM; sBPF RE is the swarm filter).

**Scope = GO (authorized).** Self-hosted bounty (docs.across.to/resources/bug-bounty, disclose to `bugs@across.to`). "All Across smart contracts and interactions… in scope" → svm_spoke in-scope. Rewards discretionary (OWASP Impact×Likelihood), NOT a fixed Immunefi tier. **Eligibility clause = the dup-gate: "must not have been previously known… or publicly disclosed."** → the April-2026 Asymmetric Research event-spoof→unbacked-fill disclosure + the July-17 incident instance are **OOS as disclosed**; only a NON-disclosed post-patch sibling is payable. Value-at-risk = relayer capital (Risk Labs), not user pools.

**Provenance (df48f0bc… anchor).** program_id `DLv3NggMiSaef97YCkew5xKUHDh13tVGZ7tydt3ZeAru` (authoritative from Anchor.toml `[programs.mainnet].svm_spoke`; the search-surfaced `5eykt4Us…` was WRONG — verify IDs, never trust a snippet). On-chain: BPFLoaderUpgradeable, Authority `DpfNVXiFJ8qKMXYqBaNVxUyS6gvwe8LYs8td7XJchKki`, last-deployed slot 422799440, 1470592 bytes, sha256 `df48f0bcb0a416ba0b401d18db115230d94834a5ca147ff8d08e7ff6d697a201`. IDL recovered on-chain (anchor:idl PDA `5LuZyL1ds6ThpYDx71WvraPyiFJeUihVSCsDn62v8Heh`): 27 ix, 9 accounts, **v0.1.0**. Workspace `~/Desktop/BUGS/across-2026` (svm_spoke.so + idl.json).

**Reframe (2nd Maxim — don't plaster fd3n's shape on Across).** No caller/target program split: the relayer is an EOA calling `fill_relay` top-level, so [[project-loopscale-solfork-intake]]-style caller-clone is MOOT. caller_reachability becomes an on-chain-verification question.

**Worklist (un-reversed hypotheses, NOT findings — need bytecode reversal + fork):**
1. **fill_relay** (disc `[100,84,222,90,106,209,58,222]`): args `relay_hash:[u8;32]` (caller-supplied!) + `relay_data:Option<RelayData>` + repayment_chain_id/address. fill_status PDA keyed on relay_hash → books relayer refund entitlement. THE gate to reverse: does the handler recompute relay_hash from relay_data and revert on mismatch, and does the hash bind recipient/output_token/output_amount/deposit_id? If it trusts the caller hash or omits a field → relayer books full repayment, delivers less = theft. Probe (statefork mode-b + sim.py): hold relay_hash, mutate one relay_data field, observe refund-entitlement vs tokens-transferred (CALLER_REACHABLE) vs revert (PARAMS_CONSTRAINED). **→ REFUTED at source (cheap gate, NO fork, 2026-07-21):** `is_relay_hash_valid` = `relay_hash == get_relay_hash(relay_data, state.chain_id)` enforced as an Anchor account CONSTRAINT (pre-body, [[feedback-anchor-access-control-attribute]] pattern but here it HOLDS). `get_relay_hash` (in utils/merkle_proof_utils.rs) = `keccak256( borsh(RelayData, message→keccak(message)) ‖ dest_chain_id_LE )` — binds ALL 12 fields + dest chain_id from state. No omission, no caller-trust. Residual = deployed-vs-HEAD differential (low-risk: declare_id matches, keccak256 syscall present, core mechanic). Vein #1 dead for theft.
2. **execute_relayer_refund_leaf** — **REFUTED at source (cheap gate, NO fork, 2026-07-21).** Merkle = faithful OZ port (commutative_keccak256 sorted pairs) + leaf always `to_keccak_hash(struct)` with a `[0u8;64]` EVM/SVM domain-sep prefix → no node-as-leaf second-preimage. Replay: bitmap account realloc = `max(base+leaf_id/8, current_len)` (monotonic, never shrinks) + `#[max_len(1)]` on claimed_bitmap gives EXACTLY the +1 byte set_claimed's `leaf_id/8` index needs (off-by-one absorbed, no DoS) + `set_claimed` resize(…,0) zero-fills → no replay/stale-bit. Refund bound: `remaining_accounts[i].key()==ATA(refund_addresses[i],mint)` (no redirect) + amount=leaf.refund_amounts[i] + vault balance check. Root authz (crown-jewel) CLOSED: `relay_root_bundle` needs `is_local_or_remote_owner = signer==state.owner || signer==self_authority_pda`; self_authority only invoke_signed by `handle_receive_message` gated by CCTP MessageTransmitter (seeds::program pinned) + `remote_domain==state.remote_domain` + `sender==state.cross_domain_admin` (HubPool); root_bundle `init` (monotonic id, no overwrite). On-chain fill+refund CORE = tight.
3. **deposit / unsafe_deposit — REFUTED at source (cheap gate, NO fork, 2026-07-21).** `_deposit` does `transfer_from(depositor→vault, input_amount)` THEN emits `FundsDeposited{input_amount}` → escrow==event, no emit-without-escrow, no emit>escrow. The `delegate` PDA = `derive_seed_hash(DepositSeedData{all params})` → relayed signer can't substitute recipient/amount/dest (wrong seed_hash → wrong PDA → transfer_from fails). deposit_id partitioned: `deposit`=`[0u8;28]‖number_of_deposits` counter, `unsafe_deposit`=keccak(signer,depositor,nonce) → no collision (28-zero-prefix keccak infeasible). caller-nonce forges nothing (deposit stays backed). Encoding `[u8;32]` is on `output_amount` (destination, not escrowed) — Solana-origin escrows u64 input_amount == event. **KEY:** the disclosed April "spoof deposit events" class is NOT an on-chain deposit bug (on-chain ALWAYS escrows) → it lived OFF-CHAIN in the relayer/data-worker EVENT VERIFICATION (parsing a self-CPI FundsDeposited without a real backing deposit). That is a DIFFERENT repo (across-protocol/relayer / indexer / SDK), in-scope ("bots and off-chain code") but NOT sBPF → the swarm-filter edge (RE-tamizes) EVAPORATES + post-incident hardened.

**FINAL VERDICT: on-chain svm_spoke = complete executed-fortress at source (fill #1 + refund/merkle #2 + slow-fill + deposit #3 ALL refuted, ZERO forks burned — cheap-gate discipline win).** The payable sibling's only plausible home = OFF-CHAIN relayer event-verification, where the sBPF edge doesn't apply. → RE-SOURCE (profile [[project-midas-solana-executed-null]]); optional low-edge dig = across-protocol/relayer event verification if chasing the disclosed class directly. Residual (all veins): source-HEAD not deployed-executed differential (low-risk: declare_id==deployed, keccak256 present, core mechanic).

**OOS boundary to pin before claiming (2b):** OZ audit commit `401e24ccca1b3af919dd521e58acd445297b65b6` (Dec 2024) covered deposit/fill/bundle/slow-fill/CCTP but "not every fill authz / granular deposit-event"; 2 highs (forced-claim-account PR#847 partial; cost-asymmetry unresolved) = known/OOS. Deployed is 18mo post-audit. NEXT: read the Asymmetric April writeup + contracts git history around the patch to draw the disclosed-instance line exactly. Gate every candidate w/ [[feedback-check-prior-audits-and-competitions-at-intake]] + [[feedback-trigger-reachability-is-payability-gate]] + [[feedback-read-both-or-clauses-in-exclusions]].
