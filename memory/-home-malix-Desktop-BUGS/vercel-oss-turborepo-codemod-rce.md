---
name: vercel-oss-turborepo-codemod-rce
description: Vercel OSS bounty intake — the one hand-verified DIG is turborepo @turbo/codemod migrate yarnPath RCE (incomplete remediation of CVE-2026-45772)
metadata: 
  node_type: memory
  type: project
  originSessionId: 5f9bd21d-c8bc-4eec-a7f7-94bbaef41a31
---

**STATUS: SUBMITTED 2026-07-08 → HackerOne #3851071, New (Open), Medium, weakness Code Injection (CWE-94), asset github.com/vercel/turborepo, CVE-ref CVE-2026-45772, handle `malikb31s`.** PoC zip + 5-screenshot pack attached (auto-triage required an archive). Awaiting triage. Report `vercel-audit/reports/turborepo-codemod-rce.md`, PoC `vercel-audit/submissions/poc/turborepo-codemod-rce-poc.zip`, screenshots `vercel-audit/vercelscreens/`.

Vercel OSS bug bounty (HackerOne `vercel-open-source`, CVSS4.0, tier-1 min adj-CVSS ≥3.8, tier-2 ≥7.0; MUST be in distributable npm code, NOT dev-mode/experimental). Fresh intake 2026-07-08, 14 in-scope repos. Workspace `~/Desktop/BUGS/vercel-audit/`.

**Allocation outcome (breadth workflow: 14 profiles + 6 deep-seams, all executed disconfirmers):** exactly ONE `DIG`, five PARK/SKIP on the reachability kill-gate.
- **turborepo = DIG (the finding).** `@turbo/codemod migrate <dir>` re-execs project-local yarn at `cwd=project.paths.root` via the attacker's `.yarnrc`/`.yarnrc.yml` `yarn-path`/`yarnPath` → arbitrary code exec as the victim. **HAND-VERIFIED end-to-end by me** (not agent-reported): `npx @turbo/codemod@2.10.4 migrate ./victim --force` → dual-sink fire (`get-current-version` `yarn turbo --version` + `shutdown-daemon` `yarn turbo daemon stop`), marker `/tmp/PWNED_TURBO_CODEMOD` written uid=1000, on the LATEST published (post-fix) package. Harness at `~/Desktop/BUGS/vercel-audit/victim/`.
- **It is an INCOMPLETE REMEDIATION of GHSA-3qcw-2rhx-2726 / CVE-2026-45772** (patched ≥2.9.14): that fix only converted the package-manager DETECTION path (`turbo-utils/managers` → cwd=tmpdir + parse yarnPath instead of exec). The codemod migrate flow's OWN yarn calls were never touched — shipped 2.10.4 dist: `grep -oE 'cwd:[a-z]\.paths\.root' dist/cli.js|wc -l`=5 exec-at-project-root sites (11 total `paths.root` occ), `tmpdir`=0. (Careful: NOT `grep -c` — bundle is one minified line; use `grep -o ...|wc -l`. Earlier "6" was a mis-count, corrected to 5.) Dup-clean: no 2nd advisory, no post-fix issue for the migrate flow.
- **7-cell yarn-binary × config MATRIX (executed, blinds reachability — operator demanded it, the Injective 'value traverses intact' discipline):** the load-bearing gate is *a yarn invokable on PATH*, and *config format must match the served yarn*. Fires: [global classic yarn + `.yarnrc`], [global classic + `.yarnrc.yml` via 1→2 handoff], [corepack no-global-yarn + `.yarnrc`+classic-pin], [corepack + `.yarnrc.yml`+Berry-pin = typical modern victim]. Does NOT fire: [corepack Berry-pin + classic `.yarnrc` (cross — Berry ignores .yarnrc)], [no config file], [NO yarn on PATH at all]. So it's NOT an artifact of a local global yarn (corepack rows fire clean); attacker controls both config files AND `packageManager` field → ships the matching pair. C2 (the anti-artifact cell) RE-CONFIRMED: global yarn dir removed from PATH, `/proc/<ppid>/cmdline` proved the executing parent was the corepack yarn shim (not the global binary), corepack env markers (COREPACK_ROOT/YARN_IGNORE_PATH) present. Proven by execution, not inferred. Honest precondition = victim can run yarn (global install OR corepack enabled), norm for yarn projects, not a bare Node box. Severity lever = it's THE documented upgrade command (`npx @turbo/codemod migrate`), not an exotic action → argues Medium above the sibling's Low.
- **Severity calibration (honest):** vendor rated the identical class **`low`** on GHSA-3qcw. Commit Low/Medium FLOOR, argue High-CVSS ceiling; the "incomplete fix of a CVE they already accepted+shipped" framing is undismissable. Modest but real; clears tier-1 floor; likely CVE credit + small bounty. Report → report-nerve + chill (HackerOne human triagers), submit under handle **SatisLoeb** on hackerone.com/vercel-open-source. H1 framing (the real risk = "duplicate/already-fixed" close, not "not-a-bug"): anti-dup paragraph preempts it (NOT re-reporting CVE-2026-45772 = detection path in turbo-utils, fixed/closed; THIS = migrate flow in turbo-codemod, different module the fix never touched, proven on deployed bundle 5-project-root-exec-sites-vs-0-tmpdir, no 2nd advisory found); scope line (bug in vercel/turborepo tier-1 source, verified live on published @turbo/codemod npm); severity = Medium on H1 scale, GHSA-3qcw Low cited as REFERENCE not binding, documented-upgrade-command as the lever, explicitly NOT Critical despite tier-1-max-Critical. Submission OPERATOR-GATED (his H1 session).
- **Runner-up:** vercel-labs/agent-skills `vercel-optimize` skill interpolates `.vercel/project.json` `projectId` UNENCODED into `vercel api /v9/projects/${projectId}` while every sibling id is encodeURIComponent'd (vercel.mjs:519 vs :521) → authenticated GET request-forgery as victim's `vercel login` token. Fresh, executed PoC done, but GET-only + response-to-victim → Low/Med, needs an exfil channel for Med+.
- **PARK/SKIP (reachability-capped):** workflow (isWebhook self-refuted; value only in closed managed backend), vercel (download.ts symlink/traversal real but CLI paths no-op via skipDownload/isDev, build-fleet closed), flags (mergeProviderData prototype-pollution real but zero untrusted callers in shipped adapters), ai (SSRF-via-DNS = hard dup of open issue #13510).

Related: [[feedback-reachability-is-kill-gate-not-severity-modifier]] [[feedback-default-posture-thief-not-fortress-prover]] — the 5 PARKs are all "mechanism real, impact behind a closed backend / refuted in shipped usage"; the ONE payable finding is the incomplete-fix with clean untrusted reachability. C4-pipeline pattern (verify a published fix is complete) paid again.
