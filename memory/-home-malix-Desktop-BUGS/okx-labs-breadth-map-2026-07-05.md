---
name: okx-labs-breadth-map-2026-07-05
description: OKX Labs $1M bounty — breadth-map corrects prior false fortress-null; audits are thin (post-audit + adapter layer unseen);
metadata: 
  node_type: memory
  type: project
  originSessionId: 2d95b927-7809-478e-8486-a55aa39f8412
---

**STATUS: CLOSED earned-null 2026-07-05** (OUTCOMES id `okx-labs-onchain-breadth-earned-null-2026-07-05`, reward 0). On-chain SC = real mechanism defects, NO material untrusted-reachable loss. Residue-scoop cluster = dust (on-chain evidenced). Solana `fee.rs` trim = OOS-centralization (7 swap entrypoints all bind source `token::authority=payer=Signer`, no delegate/relayer; min_return post-trim `:481`; user floored at `expect_amount_out`). Aptos S3 = dormant **Low** design flaw (shared-global escrow PDA `0xf61e…`, empty on-chain, is_FA strand self-reverts), HELD not filed. wallet↔router NULL. **Re-open only on:** an OKX contract holding a standing material balance, smartSwap/_exeAdapter enabled at volume with no-refund adapters, or in-scope off-chain/API. Real EV = off-chain (OOS here).

**OKX Labs bug bounty** (Critical ≤$1M/High ≤$100k/Med ≤$10k), 9 in-scope repos (main only): DEX-Router-{EVM,Solana,Sui,Aptos}-V1, Web3-DEX-EVM-PMM, Boost-TokenDistributor-{EVM,Solana}, Boost-Token-Locker, okx-smart-wallet-evm. Workspaces: `~/Desktop/BUGS/okx-labs-audit/` (canonical, all 9 repos + audit PDFs→txt), `okx-darkside-audit/` (Jun-11 WIP). Deliverable: `okx-labs-audit/BREADTH-MAP-2026-07-05.md`; raw agent JSON in `_bm_harvest/`. IntentSwap is OOS (not in the 9-asset scope despite having an audit).

**Prior Jun-20 OUTCOMES logged `no-finding-fortress` = closure-bias** (per [[feedback-closure-bias-expand-voies-hunt-seams]]): several darkside leads were routed to "workflow verifying" and NEVER closed. The 2026-07-05 thief-mode breadth-map workflow (`wf_a0e745a6-e90`) actually CLOSED them with artifacts: PMM settle-limit-vs-slippage → DEAD (~3% max shortfall, single-fill, taker self-prices); Sui min-return → DEAD (all 42 wrappers mapped enforce/delegate/NEITHER); Solana commission_account → DEAD (payer is Signer, cross-user impossible).

**MASTER THESIS (verified):** the audits covered each router's CORE skeleton but NOT the value-moving ADAPTER/WRAPPER layer nor the POST-AUDIT commission/trim/settlement rewrites. EVM router audit is 2025-05-14 but `DagRouter.sol`+`CommissionLib` rewrite(+1636 lines)+`trim` are all post-audit. Every surviving lead sits in this gap (Rule 4: can't be dismissed as audit-known). The recurring class = **adapter-residue-scoop** on every chain.

**#1 lead — CONFIRMED mechanism (not yet a finding):** `Web3-DEX-EVM-PMM/src/PmmAdaptor.sol` (298 LOC, ENTIRELY un-audited). `sellBase`/`sellQuote` are `external`/permissionless; `_executeV{1,2,3}Order` does `safeApprove(takerAsset, pool, balanceOf(this))` then `_call(pool)=pool.call` with attacker-supplied `pool` → a malicious pool pulls the approval = drains the adapter's entire resting balance of any token, no maker sig. This is the PMM-side SIBLING of the un-submitted EVM P-ADP1 (unguarded adapter callback, drafted Medium fork-PoC in okx-darkside-audit). **Severity gate = does a deployed adaptor hold a resting balance NOW** — on-chain `cast call balanceOf` check on ETH `0xce937da1ffd21673Aa1e063459873F30189A2193` (+ARB/Base/BNB in the repo CLAUDE.md). Standing balance → Medium/High; residue-only → Low. Kill-Gate Q6/Q10 dup-check needed (industry-known confused-deputy adapter class).

**WORKFLOW COMPLETE (25/25).** Map = `BREADTH-MAP-2026-07-05.md` (synthesis + a POST-SYNTHESIS CORRECTION section). Standing-Order-2 catch: synthesis agent FALSELY said "crown grids never ran" (resume data-flow quirk dropped them from its input) — they DID run; results harvested to `_bm_harvest/CROWNGRID__*.json`.

**GRAND CONVERGENCE:** every Medium+ lead is ONE class on 5 surfaces — **residue-scoop**: does an OKX contract/adapter/PDA hold a sweepable standing balance? EVM P1 (STILL-LIVE, CommissionLib.sol:499/506 raw balanceAfter vs :807 delta), EVM P-ADP1 (18 unguarded callbacks confirmed vs 8 guarded), EVM DagRouter (new 215-line contract, `_exeNode` full-balance, ZERO audit), PMM PmmAdaptor #1, Aptos S3 (shared-PDA sweep, the ONE SEAM-LEAD of 6). S5 wallet↔router = NULL (payer==msg.sender holds, artifact-cited). Smart-wallet grid added a HIGH (AllowanceManager×DexRouter interpose — wallet-internal allowance, distinct from S5, agent-rated so verify) + 2 Medium (isValidSignature merkle no-nonce replay; _batchCall session over-scope).

**#1 PMM PmmAdaptor — FORK POC PASSES (executed):** `findings/PmmAdaptorPermissionlessDrain.t.sol` (run from `okx-darkside-audit/poc-project/`), live ETH fork vs real deployed adaptor 0xce937…2193: unrelated EOA calls `sellBase(attacker, EvilPool, order{takerAsset:USDC})` → drains 100k USDC, no maker sig. Access-control defect PROVEN on deployed contract. `deal()` seeds the residue = explicit precondition (IS = drain-any-held-balance; IS-NOT = natural accumulation — standing bal 0 all 4 chains). Adaptor ACTIVELY USED (45 inbound token xfers MANA/USDT/LINK) so mid-tx window real, OKX sweeps to 0 between txs. **Severity = Medium floor** (permissionless + PoC + active + internal-inconsistency: OKX guards 8 sibling DEX V3 adapters + UniV4 but PMM adaptor unguarded), NOT standing-High. Also killed by direct read: smart-wallet isValidSignature merkle lead = DESIGN (validUntil checked SmartWallet.sol:338, merkle-batch intended, 2nd-preimage useless) → grid Medium was inflated. NEXT-for-High = enumerate 18 EVM DEX adapters (P-ADP1, no-refund/partial-fill-prone, likelier standing residue) via call-trace + balanceOf.

**LIVE ON-CHAIN RESIDUE CHECK (cast, 4 chains):** DexRouter proxy + PMM adaptor + PMM protocol = **0 residue on ETH/ARB/Base/BNB, all hot tokens + native.** ⟹ the whole cluster is **CONDITIONAL create-then-scoop, NOT a standing drain** → honest floor **Medium**, ceiling High only if residue-creation (partial-fill on no-refund adapter / FoT / decode-mismatch / CLMM dust) is shown attacker-triggerable vs an arbitrary victim (the fork-PoC that decides it; drafted P-ADP1 4.88M-USDC-strand is the template). **TODO not yet run:** enumerate the 18 EVM DEX-adapter contract ADDRESSES (separate from router, not in repo) + balanceOf them — P-ADP1 residue lives THERE, untested. NEXT solo step = convert one "conditional" into an "observed victim" (18-adapter balanceOf, or PMM create-residue fork, or wallet isValidSignature replay). Deployed ETH DexRouter proxy 0x5E1f62Dac767b0491e3CE72469C217365D5B48cC; PMM adaptors ETH 0xce937…2193/ARB 0x50FE…313c/Base 0x4997…7995/BNB 0x61e3…817F.
