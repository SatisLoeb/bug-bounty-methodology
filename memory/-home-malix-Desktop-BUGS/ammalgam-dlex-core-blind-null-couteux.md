---
name: ammalgam-dlex-core-blind-null-couteux
description: "Ammalgam DLEX core (Cantina bounty) blind SC re-hunt — saturation/solvency/callback composition executed-robust, NULL-COÛTEUX, re-source"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0e8b7796-917c-4113-a4d6-26f7932d10af
---

Ammalgam DLEX core (Ethereum mainnet, Cantina BOUNTY), blind day-0 re-hunt 2026-07-11. Novel oracle-free lending-AMM (Uniswap-V2 x*y=k fused with 6-token lending, internal geometric TWAP, tree-based Saturation caps). Maximally-audited fortress: 3×ChainSecurity + Cantina $60K contest + formal verification. Workspace `~/Desktop/BUGS/ammalgam-audit/`; blind PoC dir `poc-blind/` (fork tests on deployed mainnet USDC/WETH pair 0x728fd0a9…, sat-state singleton 0xAaC0fA3C…). OOS: on-chain fund loss only, NOT reliant on >33%↑/>25%↓ single-block price move; admin/rounding/design OOS.

**Verdict: NULL-COÛTEUX (executed-attack ledger), RE-SOURCE.** 4 executed theft-attempts, 0 survivors:
1. **Workflow-ranked #1 "highest-EV" seam (self-induced activeLiquidity shrink → free saturation-liquidation of every borrower) = REFUTED BY EXECUTION.** Synthesis premise: `resetSaturation` premium = `(newSat/oldSat−1)/MAG1`, newSat at current activeLiquidity vs stored oldSat → burn LP to shrink liquidity → seize premium×deposits, no repay. Source facts confirmed (Liquidation.sol:436 has NO over-saturation check, only `ratioBips<BIPS→0`; resetSaturation seizes with `badDebt=false` no-repay). BUT fork sweep (victim borrow 8–40% × burn 10–97%, max shrink 7.0×→1.9×): `calcSatChangeRatioBips` returned **0 in every cell**. The ratio is INVARIANT to activeLiquidity — `satInLAssets` (absolute net-debt) is fixed and `scaleAndSumSaturation` tranche re-anchoring cancels the liquidity dependence. Independently, burn utilization cap bounds shrink ≤50%.
2. **Interest-drift (SEAM 2)** = the INTENDED reset trigger: warp test fires the premium only via debt-growth, tiny (<1%/3yr) maintenance penalty. Not extraction.
3. **ownerTransfer/withdraw (SEAM 3)** = `onlyOwner` (pair-internal), unreachable by untrusted actor; `_update` validates the collateral-losing party at transfer-in.
4. **Flash-callback composition (operator-chosen drill)** = benign: mid-`borrow`-callback `liquidate(victim)` gives ZERO extra borrow ceiling (171,676 USDC no-cb == over-ceiling+20% reverts). Terminal `validateSolvency` reads live consistent `totalAssetsAndShares(true)` → no transient cache to corrupt.

5. **T1 fragile-liquidity freeze — CONFIRMED (my first "refuted" was NOVEL-READING; operator forced the re-hunt "lire le code comme un roman ne revele rien").** My 6 disconfirmers only tested the collapse paths I could imagine from understanding the dev's guard — all guarded → I falsely concluded "airtight/refuted." The un-imagined state: NOT fragile-drift alone (self-corrects; mint updates fragile immediately; high-fragile+time alone = no freeze), but **high-fragile × high-borrow_L × time**: (1) dominant LP + tiny 1-USDC borrow → whole L fragile; (2) `borrowLiquidity ≈85%` of deposit_L → activeLiquidity collapses (1.18e16→1.77e15); (3) after ≥1 day, saturation penalties accrue vs collapsed effective liquidity → penalty-mint path hits `require(totalDepositLAssets==0,'TS')` (TokenController:537, defensive assert). OBSERVED (poc-blind/TSFreeze.t.sol): fresh deposit reverts TS, repayLiquidity reverts TS (UNHEALABLE), pre-existing honest lender can't transfer/withdraw DEPOSIT_X (FUNDS TRAPPED), persists 1/30/365d. `fragileLiquidityAssets=0.95×(fragileShares/totalShares_L)×deposit_L` grows with interest (shares fixed) — the ratio the per-op guards never bounded. Partially vindicates prior [[ammalgam-dlex-saturation-liq-immunity]] T1.

**STATUS: confirmed reachable freeze but LOW severity (economics/reachability resolved, FreezeEconomics.t.sol):** (A) pure griefing/self-DoS — attacker's OWN capital fully trapped after freeze (~$1M dominant-LP + ~$13M borrowLiquidity collateral, every recovery path reverts); cost ≥ damage. (B) needs the attacker to be the DOMINANT LP with full un-borrowed reserves; on an organic pool the borrowLiquidity reverts AmmalgamMaxBorrowReached (util cap) → not reachable; interest-drift+high-fragile alone doesn't freeze. Net: the TS invariant IS violable (real correctness bug) but only via irrational griefing, guarded on organic pools → LOW floor. Situational lift: attacker facing liquidation freezes to block it. Likely dup of prior T1; EV marginal in fund-loss-only scope. Report to Ammalgam as Low correctness bug, don't expect payout.

**KEY LESSON (the big one):** READING CODE LIKE A NOVEL REVEALS NOTHING. Understanding the dev's guard = adopting their mental model = the state of MAXIMUM blindness. My "airtight guard / 6 disconfirmers / refuted" was reconstructing-and-agreeing, not hunting. The bug lived exactly in the state the guard's author didn't imagine (high-fragile × high-borrow_L). When I catch myself concluding "guard holds / self-prevented / refuted" from tracing the code, that IS the tell to STOP and ask "what input/state did the author NOT imagine here?" and build THAT. The reset-premium liquidity-manip class WAS correctly dead (executed); but the fragile+penalty freeze was alive and I mislabeled it dead by novel-reading. Relates to [[feedback-workflow-agents-coverage-not-verdict]], [[kill-poc-must-sweep-param-space]], [[feedback-audited-target-hunt-invariant-not-class]].
