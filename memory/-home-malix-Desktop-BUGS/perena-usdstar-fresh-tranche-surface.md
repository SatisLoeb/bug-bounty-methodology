---
name: perena-usdstar-fresh-tranche-surface
description: "Perena USD* (Cantina, Solana) intake — the deployed Bankineco binary hides an un-audited senior/junior tranche system that is the real EV; workflow mapped only the audited surface"
metadata: 
  node_type: memory
  type: project
  originSessionId: da62bebf-40e5-4359-9199-2c7c25fae4d1
---

Perena USD* — Cantina bug bounty, Solana stablecoin (~$9.9M USD* supply, ~$10.66M reserves → funded, EV-gate PASS). Intake 2026-07-04.

**Fortress = LOW/MED**: ONE audit, Hashlock (mid-tier), Oct 2025, `programs/bankineco` @ commit `97b69ae` (fix `59d9578`). Not a top-tier multi-audit fortress → catalogued classes still live.

**Deployed program identity (execution-verified):** Bankineco = `save8RQVPMWNTzU18t3GBvBkN9hT7jsGjiCQ28FpD9H` (BPF-upgradeable). USD* mint = `star9agSpjiFe3M49B3RniVU4CMBBEK3Qnaqn3RGiFM`. Numeraire AMM = `NUMERUNsFCP3kuNmWZuXtm1AaQCPj9uw6Guv2Ekoi5P` (SEPARATE program, scope-uncertain — confirm in Cantina list before spending).

**THE KEY LESSON — deployed ≫ audited.** `solana program dump` of save8RQ revealed the DEPLOYED binary added an entire UN-AUDITED senior/junior TRANCHE system + Kamino/MarginFi atomic-lending that Hashlock never saw (StakeJunior, Instant/Request/Fulfill/CancelUnstakeJunior, ApplyCapitalLoss, RefreshAtomicLendingAccounting, DepositInLp/WithdrawFromLp, CreateTrancheState). The 22-agent surface-map workflow MISSED it entirely because it reasoned from the audit's instruction list (pre-tranche). Only the deployed binary strings exposed it. This is [[feedback-verify-before-working-no-theater]] + the deployed-is-ground-truth discipline: on a closed-source Solana target, `solana program dump` + `strings` beats doc/SDK/IDL recon for surface discovery. The tranche system is in-scope-solid, post-audit (NOT dup), untrusted-reachable, most bug-dense class → Tier-1.

**Guards the binary revealed (IDL hides them):** "Rounding impacts the protocol" (rounding-direction guarded), "synthetically incremented"+"Insufficient pending yield" (update_yield_amount synthetic reserve push exists but capped by oracle pending yield → workflow lead #1 throttled), "Stale oracle"+"Price change too large" (M-01 fixed on deployed), account-parent binding present.

Full recon: `~/Desktop/BUGS/perena-usdstar/{recon/RECON-VERIFIED.md, recon/ROSETTA-account-layouts.md, RECONCILED-VERDICT.md, DECOMPILE-FINDINGS.md, poc/RESULTS.md}`. Binary at /tmp/bankineco.so.

**OUTCOME 2026-07-04: EARNED NULL (high confidence) on the junior-tranche surface.** Deep-decompiled (SBF via llvm-objdump-18, address model label*8=vaddr, code_offset=IDL_offset-8), rebuilt exact struct layouts from the SDK's Anchor IDL (bankineco.json — ground truth, replaced the agents' guessed offsets), and built a FORK POC (solana-test-validator + real program bytecode /tmp/bankineco.so + 13 cloned mainnet accounts, anchor+IDL driver, ZERO mocks). Empirically PROVEN the two realization legs CONSERVE: stake credits exactly staked×mint_price (no leverage on deposit, senior untouched); instant_unstake preserves senior backing ratio to the unit (mint_price Δ=0), pays escrowed_USD*×price−1%fee (junior LOSES on immediate round-trip), payout is escrow-bounded (can't burn more USD* than escrowed → can't extract senior principal). The "junior loss-dodge / leverage double-count" thesis is EMPIRICALLY FALSE on extraction. RESIDUAL: the accrual leg (does an inflated vv extract) not drivable without source — oracle yield mechanism is multi-stage (post→pending→consensus→distribute); update_yielding_amount hit a team_state ConstraintRaw (NOT the clean-permissionless path M-06 implied). Convergent evidence (static netting r9-=r5 + live vv<staked×mint_price = no accumulated unbacked premium + escrow-bounded exit) → high-confidence null. RE-OPEN only if a new tranche version ships or the accrual becomes untrusted-drivable. LESSON: deployed binary >> audit for surface discovery; agent SBF offset-reads unreliable (fix via IDL layouts); fork PoC settles what static SBF can't; "probably rounding" dismissal hid the RoundingImpact guard (found only by tracing to the op + executing the disconfirmer). Deep methodology validated but produced an honest null — target diet: heavily-structured code that conserves. [[feedback-target-diet-is-the-binding-constraint]] [[feedback-audited-target-hunt-invariant-not-class]]

**SCOPE CONFIRMED + CLOSED 2026-07-04.** Cantina bounty page (4bbe54f6-c8ea-4704-a756-0066689d7371) embedded JSON: in-scope = ONLY Bankineco IDL (solscan save8RQ) + bankineco-sdk. **Numeraire AMM (NUMERUN…) is OUT OF SCOPE** (zero mentions; third-party integrations OOS) → the swap↔oracle seam is not payable here, do NOT pursue. Critical cap $25k, 82 findings already submitted (saturated). All three in-scope on-chain value seams traced hypothesis-free by executed artifact → conserve (mint/burn↔price guarded; tranche↔vault conserves; refresh_atomic↔tvl↔backing faithful/conservative w/ real MarginFi decode). CLOSED earned-null (OUTCOMES: perena-usdstar-bankineco-tranche-2026-07-04). Re-open triggers ONLY: new deployed tranche version, or a Numeraire-in-scope bounty. Fork harness banked at ~/Desktop/BUGS/perena-usdstar/poc/ (launch.sh + lib/stake/unstake/coreflow.mjs).
