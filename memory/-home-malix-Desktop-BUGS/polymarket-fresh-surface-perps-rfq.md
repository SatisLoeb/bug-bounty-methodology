---
name: polymarket-fresh-surface-perps-rfq
description: Polymarket — the live in-scope surface is NOT the labored SC core; expand-voies poke found two fresh products (Perps API + combos-RFQ) the prior null-closures never touched
metadata: 
  node_type: memory
  type: project
  originSessionId: c826d761-990e-40ee-b65c-e98750544d13
---

Polymarket (Cantina bounty, scope = polymarket.com + SC). Many local dossiers (polymarket-darkside ×2, -firmgrade, -immortal-audit) are **null closures on ONE voie**: the SC core (Ctf-Exchange-v2 / Gnosis ConditionalTokens). Operator 2026-06-30: "on attaque par une seule voie et on s'obstine à prouver qu'il n'y a rien" + "NE TE FIE PAS AUX ENGAGEMENTS PRECEDENTS". A cold expand-voies manual poke (apparatus OFF, [[feedback-apparatus-is-packaging-not-discovery]]) surfaced two FRESH in-scope web surfaces the SC closures never saw:

- **Perpetuals** — `https://api.perpetuals.polymarket.com/v1` (WS `wss://ws.perpetuals.polymarket.com/v1/ws`). Brand-new leveraged product: SP500-USD / GOLD-USD / crypto perps, quote/collateral = **pUSD**, real volume. Routes: public `/info/{book,trades,funding,klines,instruments,tickers,statistics,fees}` (all UNAUTH, rich), gated `/account/{config,credentials,deposits,open-orders,portfolio,proxy,withdraw,withdrawals}` + `/trade/{deposit,leverage,orders}`.
- **combos-RFQ** — `https://combos-rfq-api.polymarket.sh` (`.sh` sibling baked in prod CSP `connect-src`). Go service, guessed routes 404; real routes live in a lazy perp/combos chunk not loaded on homepage.

**Perp auth model (observed live):** static credential pair `polymarket-proxy: <proxy>` + `polymarket-secret: <secret>` sent in PLAIN headers on every request — NO HMAC/timestamp/per-request signature. Stored in `localStorage` key `polymarket.perps.creds.v2:<addr>`. ACAO `*` WITHOUT allow-credentials (custom-header auth → wildcard not itself a leak). GET with proxy-only (no secret) → 401, so both required. `POST /account/credentials` → 404 ⇒ creds minted ELSEWHERE (onboarding flow on the perp web-app origin, not reachable via curl — needs browser). Untested high-EV seams (need a valid perp credential = wallet onboarding): (1) does server BIND proxy↔secret or is proxy a spoofable IDOR scoping param; (2) **pUSD CLOB↔Perp deposit/withdraw accounting seam** (shared collateral between two subsystems = double-count surface); (3) perp WS auth (unauth private-channel subscribe?); (4) cred-mint binding (wallet-light ATO if loose).

**Relayer** `relayer-v2.polymarket.com`: `/submit` `/transactions` `/nonce` `/address` `/relay-payload?address=&type=` `/relayer/api/auth`; type ∈ {SAFE,PROXY}. Auth = POLY_* EIP-712 signature headers (NOT cookie). CORS reflects arbitrary Origin + allow-credentials:true BUT auth isn't cookie-borne → **impact-less, closed null (no over-claim)**; OOS "CORS w/o demonstrated impact" anyway.

Other custom hosts: `geo-ip-api` (geo-enforcement; VPN-bypass is OOS but a LOGIC flaw isn't), `integrity`, `user-pnl-api`, `data-api`, `gamma-api`, `clob`. Re-enter via the Perps/pUSD seam, NOT the SC core.

**Browser-driven authed session (2026-06-30, operator's TEST account, full authorization "vas y je t'authorise a tout faire"):** perp app = `polymarket.com/perps` (NOT /perpetuals which 404s; `perpetuals.polymarket.com` host doesn't serve). Perps gated by (a) early-access code validated CLIENT-SIDE ("Incorrect code" with ZERO backend call) + (b) `/api/geoblock/perps-allow`→`{"whitelisted":false}`. Session has real CLOB creds (`poly_clob_api_key_map`: key/secret/passphrase/baseAddress) + JWTs; `/api/geo`→DZ. Bespoke `/api/*` Next.js routes (~70) reachable authed via in-page fetch: migrate-deposit-wallet, deposit, keys, account/verify-emails/*, intercom/jwt, geoblock/*, rewards/*, account/{mark-registered,mark-traded,track-trade}. Perp mint = `POST /account/proxy` (EIP-712 envelope, field `op`); deposit=POST /trade/deposit, withdraw=POST /account/withdraw, leverage=PATCH /trade/leverage. Perp cred object = {proxy, privateKey(local-gen), secret(server), walletAddress, expiresAt}.

**STATIC ON-CHAIN AUDIT of the fresh perp custody (2026-06-30, operator chose "static audit, no funds"):** `PERPS_DEPOSIT_CONTRACT 0xDCa4af75705dbB50f62437045afF9921947917d2` = EIP-1967 proxy → impl `0x5c5A0860f0b7d13318D72e4Bb8eA89713242f865` = **`ExchangeV1`** (verified, Solc 0.8.30, UUPS). Validium-style: operator commits `stateRoot` (++epoch), keeper calls `processWithdrawal(account,token,amount,fee,to,salt,ts,sig,int256 balance,proof)` — EIP-712 sig from `account` (SignatureChecker: EOA+ERC-1271) + Merkle proof of leaf `keccak256(keccak256(account,token,balance))` against current root; `remaining = provenBalance − withdrawn[epoch][token][account]`; `usedDigests[digest]` global replay-guard; `_settleWithdrawal` does `withdrawn[epoch]+=amount; transfer(to, amount−fee)`. **VERDICT: well-built, NO untrusted-reachable on-chain drain** (proof binds balance, digest replay-guarded, keeper-gated, fee/accounting correct, no rescue/sweep backdoor). Residual systemic risk = epoch-boundary double-withdraw IF the off-chain engine commits root N+1 without reflecting epoch-N on-chain withdrawals (`withdrawn[epoch]` resets per epoch → all cross-epoch accounting delegated to the off-chain engine) — but that's OFF-CHAIN (unreadable) + keeper-gated + **DORMANT: contract TVL ~$111 (only collateral-adapter 0xAdA100Db holds 111 native USDC; proxy+impl hold $0)**. **Likely OOS** (perp contracts not in the listed SC scope: Ctf-Exchange-v2/ConditionalTokens/UMA). owner(proxy)=0x47EbFAC3353314C788B96CDCbf41daadfE03629C. **RE-CHECK TRIGGER: perp GA / TVL climbs → deep-audit ExchangeV1 + the off-chain engine's root-computation WITH the live flow (the epoch-boundary double-withdraw becomes testable + payable then).**

**SESSION HONEST OUTCOME: 0 submittable findings, high discovery value.** No over-claim, no conceded-unexhausted gate. The fresh-surface payable EV is GATED behind perp GA (on-chain TVL) OR a funded account to exercise the off-chain RFQ/perp money-logic live (stale-quote, quote↔signed_order binding, pUSD CLOB↔Perp seam) — the resource the operator declined this session. Static audit confirmed the on-chain fresh contract is clean+dormant. This is the CORRECT verdict per the artifacts (vs the prior one-voie SC null-closures, the value here = the mapped fresh surface + the named GA re-check target).

**GATES PIERCED → NULL with executed artifact (1st Maxim, no over-claim):** (1) relayer CORS reflected+credentials = impact-less (auth is POLY_* signature, not cookie). (2) `/api/intercom/jwt?user_id=X` = `403 user_id does not match authenticated user` (session-bound, no impersonation). (3) perp mint `/account/proxy` = owner-signature-bound (no IDOR-of-mint). (4) perp geo/early-access bypass = DEPRIORITIZED (no loss=$X, borderline OOS "geo bypass" → dies at triage on this fund-theft program). `mark-traded`/`track-trade` trust an `address` body param but are analytics (attribution-spoof only, ~$0). **HIGH-EV UNTESTED money-logic:** CLOB authed IDOR/authz (have creds), pUSD CLOB↔Perp deposit/withdraw double-count seam (needs minted proxy), `/api/account/migrate-deposit-wallet` authz (deposit-redirect = direct fund-theft if client-identifier-trusting).
