---
name: sei-giga-executor-engagement
description: "Sei Immunefi $500K — the only fresh in-scope surface is giga/executor; fee/surplus voie closed by executed null, 4 voies still open"
metadata: 
  node_type: memory
  type: project
  originSessionId: c442e7ec-5cfa-4063-970f-f1c671671f14
  modified: 2026-08-11T10:47:12.373Z
---

Sei (Immunefi, $500K, triaged, KYC+arbitration) engaged 2026-08-11. Clones at `~/Desktop/BUGS/sei-audit/{sei-chain,go-ethereum}` (HEAD ab08efb5, 2026-08-10).

**Why this target is a GO.** Sei-chain core is 2.5yr live + audited (p_bounty≈0), BUT the program carves ONE fresh surface into scope while excluding nearly everything around it: `giga/executor` (~1181 LOC), Sei's new parallel-EVM executor, **enabled by default**. Fresh code where competitors stopped.

**Scope resolution (non-obvious, cost hours to derive):**
- IN: `giga/executor/**`, `app/app.go` Giga orchestration, `x/evm/**`, the go-ethereum fork, sei-cosmos OCC scheduler.
- OUT: the whole evmone bridge (`giga/executor/lib/**`, `internal/host_context.go`, `internal/interpreter.go`) — **production uses `NewGethExecutor` (app.go:2103), NOT evmone**; and ALL `giga/deps/**`, `giga/evmonly/**` ("giga packages other than executor").
- **THE killer clause:** "transaction result differences (incl. LastResultsHash divergence) BETWEEN Giga and V2" is EXPLICITLY OOS + known-accepted. So "Giga computes X differently than V2" is DEAD. Only ABSOLUTE impacts survive: theft / freeze / supply-inflation / Giga-node-vs-Giga-node chain split / crash. Fallback-induced block delay also OOS.
- Root cause must be in an in-scope file, but a bug where in-scope code MISUSES a correct `giga/deps` primitive is in scope.

**VOIE 1 — fee/surplus insolvency: CLOSED, executed null.** Ledger (each killed by execution, not inspection):
1. Coinbase double-credit → REFUTED: EndBlock (x/evm/keeper/abci.go:143-147) only *sweeps* the per-tx synthetic coinbase into the fee collector, never re-credits.
2. User overcharged / refund lost → REFUTED by exact integer arithmetic on the real path: pre-charge `gasLimit·P` (app.go:2093), `returnGas` `remaining·P` (state_transition.go:549), coinbase `gasUsed·(baseFee+effectiveTip)` (572); since `P = baseFee + effectiveTip`, **surplus = 0 exactly**.
3. Wei-dust rounding → REFUTED: `SplitUseiWeiAmount` + `AddWei/SubWei` carry the sub-usei remainder; surplus tracked in exact wei.
4. Missing surplus accumulation (app.go:2173 lacks the `GetEVMTxDeferredInfo` fold that V2 has at x/evm/keeper/evm.go:145 and msg_server.go:127) → REFUTED as unreachable: that fold only fires under `ctx.EVMEntryViaWasmdPrecompile()`, and giga runs `AllCustomPrecompilesFailFast` so ALL Cosmos precompiles abort→fallback. Coherent design intent.
5. TxIndex misalignment stranding fees → REFUTED: `evmTxs[i]` (app.go:1952-1962) ↔ `ctx.WithTxIndex(i)` (1544) ↔ `SetMsgs` (1978) ↔ `GetAllEVMTxDeferredInfo` iterating `k.msgs` — aligned.
6. Block-gas double-consume on fallback → REFUTED: `GetGasPool()` returns `MaxUint64` (keeper.go:496).
7. `feeCharged=true` skips geth `StatelessChecks` → REFUTED: `validateGigaEVMTx` (app.go:3012+) replicates nonce-high/low/overflow + fee-cap + minimum-fee.
8. Three base-fee sources (`validation.baseFee` 2088 / `cache.baseFee` 2106 / `blockCtx.BaseFee` for the coinbase credit) → all collapse to `GetNextBaseFeePerGas` on live blocks; divergent branches are height-gated historical forks (pacific-1 `< 114945913`; nil below v6.2.0 upgrade). REFUTED.
9. **Only surviving mechanism, killed on REACHABILITY:** `Finalize()` (app.go:2131) flushes balances into the block store, but `AppendToEvmTxDeferredInfo` (2173) — which makes EndBlock sweep the synthetic coinbase — runs only AFTER `WriteReceipt` (2162). If WriteReceipt failed, the fee would be flushed but never swept → permanently stranded in a keyless derived address (`CoinbaseAddressPrefix||txIdx`). BUT `WriteReceipt` returns only `SetTransientReceipt`'s proto-marshal error — not attacker-controllable. Per [[feedback-reachability-is-kill-gate-not-severity-modifier]] → NOT a finding. **Re-open if a reachable WriteReceipt/Finalize error path ever appears.**

**VOIE 2 — fallback double-exec: CLOSED, executed PoCs.** `giga/tests/zz_fallback_double_exec_test.go` (3 tests × GigaSequential/GigaOCC/V2withOCC, real app via ProcessBlock, no mocks). All 6 abort sentinels return BEFORE the only commit point (`Finalize()` app.go:2131); `Cleanup()` drops the branch layers. Selfdestruct payout applied exactly once, nonce bumped exactly once, debit exactly `value + gasUsed*1e11`. **Agent-authored, and its 3rd test was VACUOUS (asserted only NoError+Len, reported log output as if enforced) — I added the real assertions.** Always re-run agent PoCs and audit their assertion density.

**VOIE 3 — OCC determinism / chain split: CLOSED, structural ledger.** Giga stores ARE version-wrapped (`SetGigaKVStores`, giga/deps/tasks/scheduler.go:539) · every store key gets a multiversion store incl. transient (`tryInitMultiVersionStore` 228-242) · engine selection is a pure fn of block txs (`shouldProcessSingleRecipientEVMTransfersSynchronously` app.go:1648) · block partition requires strictly [EVM…][Cosmos…] else whole-block V2 (`firstCosmosSeen` 1796-1812) with `evmCache.Write()` before the V2 batch · coinbase + deferred-info are keyed PER TX INDEX by design, so not shared hot keys · conflict-storm bounded by `maximumIterations=10` then sequential (dev-modelled, same code as 2yr-old V2 OCC → dup+design-intent).

**VOIE 4 — sender-recovery/authz: CLOSED, executed PoC that refuted MY OWN hypothesis.** Real gap found: `helpers.RecoverAddressesFromTx` (utils/helpers/address.go:109) reimplements only step 4 of geth's `modernSigner.Sender` (fork transaction_signing.go:243-259) and **omits the line-251 `tx.ChainId().Cmp(s.chainID)` check**; since `Hash()` = `sigHash(s.chainID)`, the ChainId field is not covered by the signature. Recovery DOES return the true sender for a mutated-ChainId typed tx (proven). BUT execution is blocked by a COMPENSATING CONTROL: `EvmStatelessChecks` (app/ante/evm_checktx.go:139-153) validates chain-id for every non-legacy type and runs on BOTH giga drivers (app.go:1591, 2302) → `code=28 "invalid chain-id"`, no value moved, nonce not consumed. Pinned in `giga/tests/zz_chainid_malleability_test.go`. Also refuted: low-S malleability (`RecoverPubkey` calls `ValidateSignatureValues`) · legacy cross-chain (AdjustV binds chainID: V-2*chainID-8 ∉ {0,1} if mismatched) · unprotected txs (rejected outright) · feeCharged skipping `StatelessChecks` (validateGigaEVMTx replicates nonce high/low/overflow AND the EOA check). **Lesson: a compelling code-level gap was NOT a bug — only execution settled it.**

**VOIE 5 — crash/DoS: CLOSED, executed probe.** `giga/tests/zz_hostile_block_test.go` — one block mixing hostile shapes (gas=MaxUint64/MaxInt64/0, value=max-u256, Cosmos-precompile target, 60KB initcode, empty create, nonce=MaxUint64, unfunded+unassociated sender) × 3 engines. **No crash: ProcessBlock completes, every tx gets a result, EndBlock digests the heterogeneous leftovers** (txs with NO deferred info → the `Error!=""` branch — the case that could have hit `surplus.Add(nil sdk.Int)` at abci.go:148). Each shape dies on a NAMED guard: `tx gas exceeds max` (evm_checktx.go:155 `txGas > MaxInt64`, which protects the int64 casts at app.go:2183-2184) · `intrinsic gas too low` · `max initcode size exceeded` · `insufficient funds` · `nonce too high`. Cosmos-precompile tx → failfast → v2 fallback → clean revert (code 45). Honest tx in the same hostile block settles exactly. Also refuted by code: `deferred.go:28` nil-etx panic (GetEVMMsg excludes associate txs via `!emsg.IsAssociateTx()`, and `AsTransaction()` returns nil only when UnpackTxData fails, which already failed at decode). **Harness lesson: my first version shared one sender across cases — rejected txs don't bump the nonce, so everything cascaded to "nonce too high" and I was measuring my harness, not the chain. One sender per case.**
Voie 3 additionally confirmed by execution: `TestProbe_GigaOCC_WorkerCountDeterminism` varies the WORKER COUNT (the node-local config that differs across validators) and gets identical results.

**NOT covered (honest scope of the null):** sustained-load block-delay measurement against the 2.5s bar (needs real validator hardware) · sei-tendermint P2P/consensus message fuzzing (different surface) · deep contract recursion · EIP-7702 authority pre-association (`setCodeTxRequiresAuthorityAssociation`), though address.go:143-154 shows the devs modelled cross-chain auth replay deliberately. Recorded residual (reachability-killed): app.go:1826/1841 return nil txResults on cache/sched error → EndBlock indexes `k.txResults[txIdx]` unguarded (deferred.go:16) → out-of-range → halt-class; dead because `evmSchedErr` is structurally always nil and `cacheErr` only comes from GetFeeCollectorAddress/time.MarshalBinary. **Re-open if either becomes attacker-reachable.**

**COURTESY PR OPEN (awaiting review, 2026-08-11):** https://github.com/sei-protocol/sei-chain/pull/3895 — `test(evm): pin chain-id enforcement for typed transactions`, opened from GitHub account **SatisLoeb** (fork SatisLoeb/sei-chain, branch `test/chainid-enforcement-typed-tx`, +144/-0, single file `giga/tests/chainid_enforcement_test.go`). Explicitly framed as NOT a vulnerability report. **OPERATOR RULE (2026-08-11, standing): NO Claude/AI attribution on his public contributions — no `Co-Authored-By`, no "generated with", nothing. Overrides the default commit-trailer convention. Applies to every outward-facing artifact (commits, PRs, reports, advisories).** Commit was amended from aba4f40 → 43c9e0c to strip the trailer. Mutation-verified before shipping: disabling the non-legacy branch of `EvmStatelessChecks` makes the mismatched-chain-id tx EXECUTE (code 0) on both giga drivers, so the test genuinely detects the regression. Local branch in the sei-chain clone is still `test/chainid-enforcement-typed-tx` with 4 UNTRACKED audit files (`zz_*.go`, `occ_determinism_probe_test.go`) — never `git add -A` there.

**DARKSIDE PASS (Door A, 2026-08-11) — the strategic insight worth keeping.** Mined the devs' own suite: `giga/tests/giga_test.go` = 127KB, **48 tests, and essentially ALL of them assert PARITY** (Giga-vs-Geth, GigaOCC-vs-GigaSequential, LastResultsHash, receipt parity, fee-validation-order phases). **Their entire adversarial mental model is "does Giga match V2?" — which is exactly the class the bounty declares OUT OF SCOPE.** So their QA gives near-zero evidence about the ABSOLUTE invariants the program actually pays for. That is where residual risk concentrates on this target.
Highest-signal N-cell = **supply conservation**, selected on a Door-A × Door-B convergence: (a) zero tests anywhere assert conservation; (b) `x/evm/module.go:250 RegisterInvariants(_ sdk.InvariantRegistry) {}` is an EMPTY body — the module that mints via surplus registers no SDK invariant; (c) `x/evm/migrations/fix_total_supply.go` exists with the comment *"fix total supply mismatch caused by mishandled ante surplus"* — the failure mode ALREADY happened in prod, was patched by a one-off recomputation, and Giga then introduced a NEW surplus writer that bypasses the ante entirely (app.go:2030/2093/2173).
Attacked it: `giga/tests/zz_supply_conservation_test.go` asserts `GetSupply(usei) == Σ(account balances) + wei→usei` (the migration's own computation) across a 6-path adversarial block (success · create · precompile-failfast→v2 · value transfer · unassoc+unfunded → balance-migration fallback · pre-execution reject) × 3 engines. **HOLDS, drift = 0.** Sensitivity PROVEN by mutation (§0.5.7): injecting 1 usei in EndBlock → `drift=1` → test fails correctly, so the null is not vacuous.
Honest caveat + how it was closed: the run showed **`surplus = 0` throughout**, so the reconciliation itself was NOT exercised — claiming "the surplus path is sound" off that would be a biased null. Closed structurally instead: in Giga the fee `SubBalance` (app.go:2093) and the EVM's `returnGas`+coinbase `AddBalance` occur in the SAME stateDB so they cancel (V2 differs — its ante charges separately via `AddAnteSurplus`, the historical bug's source); and the `abci.go:150 if surplus.IsPositive()` asymmetry (a NEGATIVE surplus is silently dropped → would be un-reconciled inflation) is unreachable because it needs the three base-fee sources to disagree, and **`SetNextBaseFeePerGas` has exactly one non-migration caller (`AdjustDynamicBaseFeePerGas`, fee.go:56) called from exactly one place: `abci.go:100` EndBlock** — the base fee cannot move mid-block. `handleResidualFundsInDestructedAccounts` is symmetric and carries its own fossil comment ("we don't want to really burn the token since it will mess up total supply calculation").
**ALL 5 remaining Door-A N-cells ATTACKED and closed with executed artifacts (operator pushed: "pourquoi tu les as laissé" — I had listed them and stopped to report, the §0.5.4 sin):**
- **N1 EIP-7702 SetCode** (`zz_setcode_association_test.go`) — the untested sibling of `TestGigaVsGeth_SelfDestruct`; both are defer-to-v2 gates in the SAME function (2080 vs 2109), selfdestruct tested, SetCode zero tests. Found a REAL off-by-one: `setCodeTxRequiresAuthorityAssociation` (app.go:2242) evaluates `AuthorityToPreAssociate` against PRE-execution state, but the fork increments the SENDER nonce at state_transition.go:512 BEFORE applying authorizations at 516 — so for the canonical self-sponsored flow (authority==sender, auth.Nonce must be N+1) the pre-check always mismatches by one and returns "no association needed". **REFUTED on impact by execution: `associated=true` on all 3 engines.** app.go:**2069** `if !isAssociated → ErrBalanceMigrationRequired` fires FIRST and defers every unassociated sender to V2, before the SetCode gate is consulted. Associated sender → no pre-association needed; third-party authority → victim's nonce untouched, pre-check correct. Prague IS active (`x/evm/types/config.go:11-12 CancunTime=0, PragueTime=0`) so the cell was genuinely reachable — the kill was the earlier guard, not unreachability.
- **N2 ≥64 single-recipient engine heuristic** (app.go:1648) — both branches driven, 70 txs × 2 engines: 70/70 succeed, sink receives exactly per-tx value, supply integrity holds. Quantified the DoS angle: forcing the maximally-conflicting set through OCC by adding ONE odd tx costs ~2.5x OCC wall-clock (44ms→110ms at 70 txs) — real but bounded by `maximumIterations=10`, nowhere near the 2.5s bar.
- **N3 Blob txs** — `code=44 "unsupported transaction type"`, rejected cleanly, supply integrity holds (BlobTxType is in AllowedTxTypes but rejected downstream).
- **N4 associate-tx interleaved** — both transfers settle, supply integrity holds.
- **N5 store-iterator panic fallback** — structurally dead in production: `app.go:802-803 UseRegularStore = true` unconditionally, so state flows through the regular KVStore, not the giga cachekv that panics (`giga/deps/store/cachekv.go:239,244`); and both drivers recover+fallback anyway (1575, 2276).

**VERDICT: NULL-COÛTEUX, RE-SOURCE.** 5/5 voies closed with executed artifacts. This team guards its seams well and twice placed the guard somewhere non-obvious (chain-id in `EvmStatelessChecks` not the signer; per-tx-index synthetic coinbase instead of a shared fee collector) — code-reading alone would have produced TWO false positives here. Re-open triggers: a new `giga/executor` subpackage · the executor gaining its own receipt/deferred-info write path · any reachable `WriteReceipt`/`Finalize` error · Giga storage or Autobahn entering scope.

Related: [[feedback-depth-is-an-edge-only-where-ore-remains]], [[feedback-audited-target-hunt-invariant-not-class]].
