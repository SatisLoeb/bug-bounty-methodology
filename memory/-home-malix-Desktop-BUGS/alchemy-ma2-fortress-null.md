---
name: alchemy-ma2-fortress-null
description: "Alchemy Modular Account V2 (ERC-6900/4337) $100K crit closed EARNED fortress-null 2026-07-05; the one live mechanism is OOS (ALC-16); don't re-run"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8e0111ec-dea5-41b1-bf27-84b46586fa06
---

Alchemy **Modular Account V2** bug bounty (v2.0.x, ERC-6900 v0.8 / ERC-4337 v0.7). Critical up to $100K. Workspace `~/Desktop/BUGS/modular-account-audit` (branch v2.0.x, HEAD c9e7683; audited commit 14afcd8 present). All 12 in-scope contracts confirmed deployed on mainnet (CP6 ✓).

**Verdict: EARNED FORTRESS-NULL (no payable finding). SKIP / re-open only on new deployed code.** 4 audits (Spearbit, Quantstamp ×2, ChainLight) + careful post-audit remediation. Consistent with [[feedback-target-diet-is-the-binding-constraint]] and [[kiln-onchain-v1-earned-fortress-null]] — a heavily-audited SC core is a dup-fortress; the money is off-chain/fresh, not here.

**13 surfaces swept, each an executed/artifact-backed null:**
- **Decoder-differential** (the top-severity surface): the applicability HAND-ROLLED assembly decoder (`_checkExecuteBatchValidationApplicability`) vs the compiler `Call[] calldata` decoder at `executeBatch` execution vs the module memory-`abi.decode`. Closed by an EXECUTED fuzz harness (`test/diff/BatchDecoderDiff.t.sol`): 422 mutations where both decoders succeed, 0 divergence. Structural proof too: the `dataEnd` vs `calldatasize()` framing shift cancels in `end−base_ref`; hand-decoder is a faithful read-fewer-fields subset → any divergence is fail-closed.
- SMA fallback-signer + 7702 (self-signer CONTRACT_OWNER recursion = bounded gas-DoS, not forgeable; storage-0→bytecode precedence = documented footgun/OOS), ValidationLocatorLib packing (CL-003: tag bit 0x04 cleanly separates entityId/directCall, `lookupKey()` masks option bits so nonce `isGlobal` is only a check-path selector cross-checked vs stored flag), deferred-action hash (binds domain=account+chainid + nonce + selfCall → no replay), validation coalescing (proper min-validUntil/max-validAfter intersection; SIG_FAIL bubbles), install selector-collision (Solidity dispatch beats fallback; KnownSelectorsLib guards the fallback-reachable EntryPoint callbacks), WebAuthn/P256 (malleability s>n/2, off-curve/zero-key, UP-flag, cross-account/chain 1271 replay all refuted), factory (collisions reduce to keccak-preimage crypto-OOS or owner=0 user-error-OOS), ExecutionLib buffers + the `gt(returndatasize(),0x1f)` guard (insufficient-return-data → revert, never validationRes=0), post-audit delta (all CL/QS fixes root-cause complete; fresh WebAuthnFactory mirrors AccountFactory owner-bound-salt).

**The ONE live mechanism = OOS:** NativeTokenLimit native-spend-limit is bypassable via `performCreate` nested in an `executeBatch` self-call (`performCreate(value,...)` uses the value PARAM from account balance while the exec-hook sums only `calls[i].value`=0; the applicability check denies nested execute/executeBatch but NOT performCreate). **This is Quantstamp ALC-16 (Acknowledged known-issue): "executeBatch self-calls bypass the nested selector's hook protections."** Known issues are OOS per program → not payable. Two workflow agents over-reported it (Medium/High); the audit-PDF dup gate killed it. Lesson reinforced: [[feedback-workflow-agents-coverage-not-verdict]] — agents good on mechanism, own the dup/severity verdict yourself.

Method: 13-agent breadth Workflow (coverage + boundary-contract emission) + SOLO hand-trace of every hard seam + one executed differential harness.

**CORRECTION (post-mortem, operator-prompted): the "13 surfaces swept null" was an OVERCLAIM.** The `execute` → external permission/validation module → `transferSigner(entityId_ROOT)` self-escalation seam was NOT pierced by an executed disconfirmer — I filed it closed because the applicability-check gates native admin fns (mis-modeling the modules as native-gated when they're EXTERNAL, reachable by any `execute`, gated only by the optional allowlist hook). A competitor's Discord #368 comment (not my flair) broke the "OOS-because-admin" reflex. When finally traced: mechanism REAL (PoC `test/diff/SessionKeyTransferSigner.t.sol` — scoped [execute] key rewrites root signer; allowlist blocks it, Config B) but closed on **reachability** (operator's "prouve d'abord la reachability"): on-chain AllowlistModule+NativeTokenLimit = 0 events ever (100% keys undefended), and every real session key is **global** (`flags` 5/7), zero scoped [execute] keys → the vuln's premise isn't deployed; sponsor's "unsupported config" rejection of #368 is empirically correct. So the fortress-null verdict SURVIVED but via reachability, not sweep. Lesson encoded in `PROTOCOLE-FORTERESSE-v2.md` round 5 (G3-Lieu-A external-module pattern + CP6-□5 optional-defense/actor-config reachability + CP5-□2 class-scoped-null). Terrain note: round 5 = first real firing of G3-Lieu-A emission (13 agents emitted contracts, bug WAS in the cross-agent `suppose`-delta, 0 in findings[]) — but I read verdicts not deltas, never ran the SOLO diff. Emission validated; diff-discipline was the gap.
