---
name: ammalgam-dlex-saturation-liq-immunity
description: Ammalgam DLEX (Cantina $25K SC-only) firmaudit — CLOSED executed NULL-COÛTEUX; 11 theft-hyps all killed with fork PoCs; sole survivor H9 sat-liq-immunity real but un-monetizable
metadata: 
  node_type: memory
  type: project
  originSessionId: 63486a67-ff61-4d99-9761-e2e44c03509c
---

**STATUS 2026-07-11 — CLOSED, executed NULL-COÛTEUX / NO-GO (re-source). Every theft-path killed with an executed or traced artifact: H1-H9 (fork PoCs), T1 (REFUTED by the faithful multi-tx harness — freeze is a single-warp over-accrual artifact, unreachable on any active pool), and the mirror-invariant sweep below. Live EV on this target is off-chain = OOS for this SC-only program → RE-SOURCE. composition_skills_applied: firmaudit.**

**🟡 MIRROR-INVARIANT SWEEP (2026-07-11, operator-directed, apparatus OFF) — found ONE real asymmetry, killed under the SAME exclusion as F1.** The liquidation-premium cap: HARD caps premium (`checkHardPremiums`→`LiquidationPremiumTooHigh`, LTV-derived ≤11111 bips); LEVERAGE (F1) uncapped but premium≤netDeposit + debt repaid + badDebt burned = borrower-harm; **SATURATION uncapped AND distinct** — `resetSaturation` (AmmalgamPair.sol:841) repays NO debt, hardcodes `badDebt=false`, and `finalizeLiquidation` (:972) re-checks solvency ONLY on `to` (the liquidator, :988), never the borrower (comment :995-998 explicitly accepts the borrower may be left hard-liquidatable). Premium = `(ratioBips−BIPS)/MAG1` where `ratioBips = newSat·BIPS/oldSat` (Saturation.sol:2071), uncapped for the `oldSat>0` path; the team CAPS the sibling straddle branch (`oldSat==0`) at `MAX_SAT_RESET_FOR_STRADDLE_PREMIUM_BIPS=2000` (:2170) = the present-on-one-branch/absent-on-sibling oversight signature (Rule 8). MECHANISM (bytecode-verified, hand-traced): a SOLVENT-but-over-saturated position (C>D, not hard-liquidatable — over-saturation≠over-leverage) seized for premium>solvency-margin → `C_remaining<D` → bad debt `resetSaturation` never burns → socialized to LPs on the follow-up HARD liq = LP-loss CREATED from a solvent position (payable class, unlike F1's borrower-harm). **KILLED — operator verdict (load-bearing, holds F1 too):** the material LP-loss impact is CONDITIONAL on a deep saturation-drift (`newSat/oldSat≈3.5` for a 25% seize, reachable within OOS price-bounds AND before a rational liquidator resets at a smaller premium) that CANNOT be guaranteed in production; scope demands a CONFIRMED on-chain loss of funds to LPs → an unresolved-reachability guard-asymmetry fails the exclusion. Same [[feedback-reachability-is-kill-gate-not-severity-modifier]] wall as F1. Real LATENT defect (catalogue-worthy for a direct team note, activates if the sat-premium ever needs to bound a solvent seizure), NOT a bounty finding on this program. The sweep was the correct last thread AND the correct NO-GO.

**🔴 T1 FINDING (fortress-to-thief, HARNESS_SUSPENDED, mechanism hand-verified):** MaxTrancheOverSaturated gate erosion via UNGATED `fragileLiquidityAssets`. Gate reads `AL_gate=(DEPOSIT_L−BORROW_L)−fragileLiquidityAssets`; `fragileLiquidityAssets=0.95×fragileLiquidityShares×(DEPOSIT_L asset:share ratio)` (TokenController.sol:308-313). fragileLiquidityShares=DEPOSIT_L of straddlers (LP+X/Y-borrower), FIXED; the ratio inflates with every interest/penalty BORROW_L mint; `mintPenalties` NEVER calls Saturation.update → AL_gate drifts down ungated → DELTA(maxLeaf−hsl) 8→2 over ~550d@31%util (AL_reserve flat) → `liq(SAT)` reverts 0x4ea97c63 (gate rejects the liquidations it protects) → escalates to `fragileAssets>AL_reserve` = 0xbd3279df where even mint() reverts = PERMANENT UNHEALABLE FREEZE. Single-warp accrual is CONSERVATIVE (real faster: satPercentage→90-95% near brick). Executed+hand-verified: poc_t1/test/T1_Decisive.t.sol (brick+heal), T1_Heal.t.sol (freeze), T1_SelfLimit.t.sol. OPEN LEGS (subagent completing): (1) loss=bad-debt from a HARD-insolvent victim un-liquidatable during the brick; (2) freeze reachability/speed + maximal-fragile-straddle attack recipe; (3) which ops the freeze freezes (withdraw/burn frozen → Critical). Severity if completed: permanent freeze=Critical, temp-brick>1wk=High, bricked-liq→bad-debt=High/Critical.

---
(Superseded first-close text below, kept for the executed disconfirmers on H1-H9/LEAD1-2 which remain valid:)

**Target:** Ammalgam DLEX — Cantina bug bounty, Ethereum mainnet. Uniswap-V2 fork FUSED with lending, oracle-free (geometric TWAP + novel Saturation.sol tranche liquidation, 6 tokens/pair, mid-call IAmmalgamCallee callback). Critical=$25K / High=$10K. HIGH saturation: ChainSecurity + 0xMacro + 476-submission Cantina competition (Jul 2025). Impact bar: **on-chain LOSS OF FUNDS TO LPs ONLY** (Critical excludes unclaimed yield); OOS = price move >33% up / >25% down single block. Was a WEAK-GO at intake; deep executed audit confirmed the fortress.

**Workspace:** `~/Desktop/BUGS/ammalgam-audit/` (FINDINGS-LEDGER.md = full executed ledger). Source recovered from mainnet Etherscan (core-v1 repo private): Factory `0x1a411b0fd1f368d2f413a8cbb6aad425c923015b`, live pair `0x728fD0A966B993fe518B00122D51e494F99aBd6a` (USDC/WETH ~$50k). **Reusable Foundry fork harness** at `poc/`,`poc2/`,`poc3/`: `createPair` on the deployed factory → real deployed logic, clean state, via_ir=true, ~60-75s/test. Keep — the harness is the asset.

**Method that worked:** 16-agent workflow (map 7 subsystems + adversarially disconfirm 9 thief-hyps + coverage-critic) → I traced the fusion↔saturation seam SOLO → 4 fork-PoC subagents (poc/poc2/poc3) executed the survivors → I hand-verified every decisive test by re-running. The coverage-critic earned its keep (found the "liquidation-DoS→bad-debt" class + 2 leads the seam-first dig walked past). Key: agents MAP, I decide by EXECUTION; I caught the critic's #1 lead reading a divergence but not its SIGN — killed only by the 38,962-config sweep, not reasoning.

**Why it's a fortress (the two invariants that defend the whole AMM↔lending↔saturation seam, both EXECUTED):**
1. **LTV `increaseForSlippage`** re-checked against POST-burn liquidity via `getInputParams.stagedBurnAssets` (AmmalgamPair.sol:1150-1157) — defeats self-concentration; forces over-collateralization on any large position.
2. **Global `MaxTrancheOverSaturated` gate** (Saturation.sol:678-684, buffer=3) enforced on EVERY AL-reducing path incl. the mandatory burn-staging transfer (ERC20Base._update→validateOnUpdate) — caps liquidity-shrink ~7.5%, self-heals via permissionless mint().
Over-concentration REQUIRES over-collateralization; over-collateralized positions don't produce bad debt from ≤25% moves. **Structural closer:** saturation penalties are minted as BORROW_L to the pair = **LP REVENUE not principal** → saturation mis-accounting = unclaimed yield, OUTSIDE the loss-of-principal bar.

**11 theft-hyps, all killed:** H1-H7 agent-traced; H8 (borrowL/repayL conserves), LEAD 2 (liquidation-brick unreachable — gate self-guards on burn-transfer), LEAD 1 (leverage-badDebt only on insolvent — 38,962-config sweep, 0 violations), H9 all by EXECUTED fork PoC + my hand-verify. **H9 = the one real primitive:** uncapped `scaleDesiredSaturation` (SatState:272 capAtPenaltyStart=false vs :172 true) grants a manufactured over-concentrated position immunity to saturation-liquidation (LiquidationZeroPremium, executed; honest twin can't open it). But un-monetizable (the 3 backstops above). **Honest residual (flagged, NOT a finding):** interest/penalty accrual mints BORROW_L → lowers AL ungated = the ONLY un-gated AL reducer; negligible/self-limiting/healable-via-mint → would need a dedicated multi-month accrual engagement.

**Lessons:** [[feedback-audited-target-hunt-invariant-not-class]], [[doctrine-defense-shadow-confession]] (H9/LEAD2 were siblings-of-a-fix OV-6-1), [[feedback-depth-is-an-edge-only-where-ore-remains]] (fortress-null is the CORRECT verdict on a 3-review SC core; depth pays where ore remains, not here), [[feedback-workflow-agents-coverage-not-verdict]] (critic mis-read LEAD 1 sign; execution decides), [[feedback-verify-before-working-no-theater]] (operator drilled: NO "likely" — every survivor to unbiased fork PoC).


**T1 REACHABILITY REFUTED BY EXECUTION (2026-07-11):** faithful multi-tx harness (persistent BORROW_L supply read, immune to transient cache; `forge test --isolate`) proved the freeze-drift is a single-large-interval accrual artifact: single 730d accrual = +85.8% BORROW_L (reaches freeze), same 730d as 24 monthly accruals = +1.9% (never approached) => single-warp OVER-accrues penalty ~43x. On any ACTIVE pool the freeze is UNREACHABLE; only total-dormancy(~2yr)+one-interaction triggers it. T1 High draft RETRACTED (built on biased single-warp). Operator's insistence on the faithful harness killed a biased finding pre-submission = the Monad epoch-jump lesson exactly. The 2nd subagent's 'cubic over-accrual' was right; my transient-cache counter-theory was wrong. LESSON: single-warp accrual is NOT conservative for over-saturation penalties; ALWAYS validate accrual-driven reachability with a faithful multi-tx (persistent-read + --isolate) harness before a severity commit. Only clean payable finding on this target = F1 (leverage uncapped premium, parallel engagement).