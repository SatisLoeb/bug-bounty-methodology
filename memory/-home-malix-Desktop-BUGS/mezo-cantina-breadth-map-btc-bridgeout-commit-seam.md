---
name: mezo-cantina-breadth-map-btc-bridgeout-commit-seam
description: "Mezo (Cantina /code/acca29a4) breadth-map — one surviving untrusted candidate = BTC bridge-out dual-ledger commit-machinery desync under nested precompile calls; PoC-pending (needs faithful app/system harness, the fakes can't test it)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5f9bd21d-c8bc-4eec-a7f7-94bbaef41a31
---

**Mezo** Cantina engagement `cantina.xyz/code/acca29a4-d405-4405-a3b3-8c3feb10d1e3` (likely a competition). Scope CONFIRMED by operator = ONLY 2 assets: **musd** (Solidity, Liquity/thUSD-fork CDP; github.com/mezo-org/musd) + **mezod** (Go, Cosmos-SDK+EVM validator client incl. x/bridge, x/evm, precompiles, vote-extensions; github.com/mezo-org/mezod). validator-kit + docs + tigris(veBTC/veMEZO/gauge) + ntt-bridge-* repos = OOS-source. Workspace `~/Desktop/BUGS/mezo-audit/` (cloned mezod@v11.0.1 + musd + audits + ntt repos; STAGE1-INVENTORY.md + STAGE1-RAW.json + STAGE2 outputs).

**OOS kill-gate (critical calibration):** untrusted-actor-ONLY (privileged-access = OOS: PoA owner, gov multisig, allowlisted triparty controller, validator privilege), NOT-a-known-issue (DeltaXV = the ERC20 burn-erased-at-commit fixed v8/2026-04 = OOS; a genuinely-distinct sibling counts), NOT-rounding (explicitly OOS), not-market-turmoil, not-design/info. Special-focus areas: MUSD issuance, Wormhole-NTT, veBTC/veMEZO+pools+vault, validator/consensus config.

**Method run (operator-directed, exactly): /intake targeted corpus backstop → SOLO manual poke (apparatus off) → breadth-map Stage 1 (9-surface individual dissection, workflow) → Stage 2 (7 terminal-impact intersection agents, each gets the ENTIRE inventory) → Stage 3 solo synthesis.** Doctrine applied: agent-per-component recreates the audit blind spot; the surviving bug is a cross-module composition nobody owns; observe-don't-confirm; forget prior engagements.

**Stage-2 verdicts:** sig-delegation NONE (msg.value/recipient/nonce all bound), NTT NONE (in-scope mezotoken transferFrom = plain bank Send, conservation rests on OOS Wormhole transceiver), collateral↔bridge NONE (triparty = PoA-gated, attestation = accepted-trust, EVM↔Cosmos-balance = Halborn Clean-Room CRITICAL already SOLVED), unbacked-MUSD-interest PARK (accounting consistent by construction; only route = Cantina 3.4.7 accepted + market-crash), redemption/liquidation PARK (market-driven / accepted 3.2.6), bridge-out PARK, supply-invariant/halt PARK. **Every NONE/PARK killed by an EXECUTED code-read, not reflex.**

**THE ONE SURVIVING CANDIDATE (Stage-3 convergence — exits 5 AND 7 independently landed on it):** BTC **bridge-out dual-ledger commit-machinery desync under nested/repeated precompile calls in one EVM tx.** Mechanism hand-verified: `bridgeOut` (permissionless, precompile 0x7b7c..0012) writes `SaveAssetsUnlocked` (release-auth) to the cachedCtx BEFORE the burn (bridge_out.go:98); the BTC burn is split — `BurnBTC` (bank supply + IncreaseBTCBurnt) → cachedCtx, `journal.SubBalance` → outer statedb. `flushCache` is a SINGLE pointer on StateDB (statedb.go:66) that `CachedCtxCheckpoint.Revert` (statedb.go:854-868) REPLACES with a closure writing that checkpoint's whole-multistore CLONE (`ccc.ms.Write()`). Checkpoints are journal entries (state_object.go:148) reverted in reverse by RevertToSnapshot. So the cachedCtx (SaveAssetsUnlocked+BurnBTC) is restored WHOLESALE to a clone snapshot, while the outer statedb (journal.SubBalance) is reverted per-journal-entry to the exact snapshot — **two different revert mechanisms, two capture points.** A revert landing between two checkpoints (nested/repeated bridgeOut via a caller contract) could persist cachedCtx to one point + outer statedb to another → desync: (a) release-auth persists without net burn = unbacked L1/BTC release, or (b) balance↓ but bank supply unchanged → `verifyBTCSupply` EndBlock PANIC = chain-halt (special-focus #4), or inverse = free abtc → phantom MUSD collateral (the archetype seam). **Untrusted, distinct from DeltaXV (BTC path journal-based, explicitly untouched by the v8 fix), in-scope.**

**STATUS (2026-07-08 close): BOTH SURVIVORS CLOSED → on-chain breadth-map = NULL-COÛTEUX.**
- **Primary (bridge-out commit desync) — LEANING-NULL by traced mechanism (sharpen chosen over PoC-build).** Traced the actual machinery: (1) `CacheMultiStore.Clone()` in the mezo-fork store (`github.com/mezo-org/cosmos-sdk/store@v1.1.1-mezo` cachekv/store.go:415) is a TRUE deep clone (each `*cValue` copied by value) → each checkpoint is a faithful isolated snapshot; (2) go-ethereum's EVM.Call brackets each precompile CALL with snapshot/RevertToSnapshot, and the checkpoint is a journal entry reverted ALIGNED with the CALL frame → cachedCtx AND outer statedb roll back to the same point; (3) `journal.SubBalance` exists precisely so `commit(ctx)`'s SetAccount writes the SAME post-burn balance as `BurnBTC` (dual-ledger nets to ONE reduction). `WithMultiStore(ccc.ms)` aliases subsequent writes into the same ms flushCache writes. The naive nested-call-revert desync is not reachable. Only the subtlest multi-checkpoint-ordering edge is unclosed 100% (would need the app/system fork-PoC); EV dropped → not built. Faithful PoC (if ever) = app-level real keepers + Solidity caller nested bridgeOut(BTC)+revert, OR Hardhat + bridge-capable localnet (needs ETH_SIDECAR_RPC_PROVIDER).
- **Secondary (redeem system-rate hint) — NULL by trace.** `redeemCollateralVars.interestRate` (system rate) drives ONLY `vars.upperBoundNICR` (TroveManager.sol:1276-1285), a "10 min of interest" hint-tolerance band used only to bail a stale caller hint (`cancelledPartial`, 1299-1306). Value-moving amounts use the trove's own rate via `_updateTroveInterest` (line 366). Worst case = spuriously cancelled partial redemption (retry) = Cantina 3.3.4-class (accepted) + rounding-scale = OOS. No theft.

**OFF-CHAIN SWEPT TOO (2026-07-09) → WHOLE ENGAGEMENT = COMPREHENSIVE EXECUTED NULL-COÛTEUX. STOP MEZO, RE-SOURCE OFF-TARGET.** Re-sourced to the off-chain in-scope code (bridge-worker + ethereum/sidecar in the mezod repo) — 5 sub-surfaces dissected (workflow wwqs7r2xk), ALL NONE with code-grounded executed disconfirmers:
- **sidecar mint-inbound (highest EV = unbacked mint): NONE.** The mint is gated by (real FINALIZED canonical-address Ethereum AssetsLocked event: chain.FinalizedBlock "finalized" tag + bound-address FilterLogs on hardcoded MezoBridgeAddress + ChainID check) × (dual 2/3 quorum — 2/3+ of BRIDGE vals AND 2/3+ of NON-BRIDGE vals must independently observe the BYTE-IDENTICAL (seq,recipient,token,amount) tuple, proposal.go:646-693) × (tip-monotonicity anti-replay). A single/MITM'd feed can't reach quorum; forgery needs a systemic deterministic-transform bug (none — transform is deterministic across honest nodes).
- **sidecar outbound-attestation: NONE.** Batch/aggregate-sig apparatus is DEAD in the deployed build (bridgeWorker=nil, TODO server.go:227); chain-id encoding intentional (golden-hash tests); validator-ID contiguity = liveness/gas only (Halborn F7.1-adjacent).
- **worker BTC-withdrawal: NONE.** Keeper relaying on-chain PendingBTCWithdrawals; recipient hash-bound (computeAttestationKey, airtight — all 4 fields on-chain); reorg-double blocked by sequential processing + on-chain dedup; tBTC signs (OOS).
- **worker bitcoin-construction: NONE.** Attacker-bytes recipient parser (NewScriptFromVarLenData) canonical-enforced + round-trip identity; scriptPubKey = single output (no extra-output encodable); computeMainUtxoHash pins the UTXO.
- **worker config/key/endpoint: NONE.** Unauth/non-TLS gRPC = real hardening gap (their TODO) but mint quorum-gated on exact content; :7500 = reads only; ethPrivateKey privileged/OOS.

**FINAL: the full in-scope surface (on-chain: 9 surfaces × 7 impacts × 2 traced survivors; off-chain: 5 sub-surfaces) = executed NULL-COÛTEUX. Theft-primitives GENERATED and each killed with a code-grounded executed disconfirmer (not a reflex fortress). Effort-cap far exceeded, 0 survivors. Ore exhausted — RE-SOURCE to a DIFFERENT payable target, not deeper here.** Only re-open Mezo on a NEW deployed commit / new precompile / a live authed off-chain portal not in these repos. The single unclosed-100% item (bridge-out commit multi-checkpoint-ordering edge) reads sound and is not worth the PoC build EV.

Related: [[feedback-agent-fanout-recreates-audit-blindspot]] (Stage 2 impact-orientation + Stage 3 solo counter it) · [[feedback-default-posture-thief-not-fortress-prover]] · [[feedback-trace-the-flow-not-confirm-the-template]] (traced the real flow at the seam, let the anomaly reveal itself). Composition skills: intake + Workflow-breadth-3stage + solo-poke.
