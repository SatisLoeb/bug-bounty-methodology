---
name: rootstock-flyover-offchain-null-couteux
description: "Rootstock Flyover off-chain stack (Immunefi, rsksmart 2wp/LPS/SDK) — resume of July work; #1 fixed, #2 marginal; value-flow + on-chain↔bridge diff executed-null; latent Go↔Java segwit-encoding LP-drain (reachability-null, re-open trigger recorded)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6dd4914e-d9f0-4119-8e41-83e3abae0e75
  modified: 2026-08-12T22:04:45.704Z
---

**Rootstock (RSK) Flyover 2-way-peg, Immunefi (rsksmart). In-scope: 2wp-app, 2wp-api, liquidity-provider-server (LPS, Go), flyover-sdk, bridges-core-sdk + Primacy-of-Impact. rskj/rsk-powhsm/liquidity-bridge-contract are OOS carve-outs (but their RULES are usable to find in-scope LP bugs). Workspace `~/Desktop/BUGS/rootstock-flyover/` (repos cloned incl. rskj shallow). Continuation of [[rootstock-powhsm-recount-gate-defeat-not-theft]] whose re-open trigger was exactly this off-chain surface.**

**Prior July-04 findings, re-checked 2026-08-12 — largely spent:**
- **#1 topics-panic DoS (parsing.go `log.Topics[0]`) = DEAD.** Fixed 2026-07-06 under branch `fix/inmunefi-74136-74748` (`len(log.Topics)>0` guard, commit e3e41ec). Beaten to it (Immunefi #74136/#74748); my July finding was never submitted. Signal: LPS is ACTIVELY bug-bountied → live p_bounty but real dup-risk.
- **#2 captcha-free `acceptAuthenticatedQuote` = MARGINAL.** Still live (`RequiresCaptcha:false` + empty-sig `handleTrustedAccountSignature` early-return), BUT the empty-sig path was added by commit `feat: add functionality to accept pegout quote without captcha` = **design-intent tombstone** → griefing/Medium, high dismissal. Not worth submitting.

**Method correction from operator (the important lesson):** stop hypothesis/class-hunting fanned across agents (killed a 6-surface breadth workflow mid-run — it was confirming templates = predetermined null on 4-audit code). Instead TRACE THE REAL DATA FLOW at the seam nobody owns and let the anomaly reveal itself. `P(class-bug survives 5 audits)≈0`; the survivor is a composition/design-invariant/off-chain-component, never a class.

**Applied it — LPS Go core conserves value at every hop (executed-observed, not asserted):** pegin release (`callForUser`: amount `AmountToAddress` sums per-address correctly, low-conf double-spend refuted by `ForValue` tier edge-math monotonic+cap-40, callForUser↔registerPegin gap = designed recoverable delay, derivation collision refuted — QuoteHash leads the buffer); pegout release (`SendPegout`: `LbcAddress=contracts.PegOut.GetAddress()` not attacker-set → forged-event refuted; quoteHash+amount+`IsPegOutQuoteCompleted`+mutex+revalidate = double-send refuted; dest=`request.to` user's own); solvency (`AvailablePegoutLiquidity` = BTC bal − Σ RequiredLiquidity, mutex-serialized). 2wp-api = display + pass-through `/broadcast` (client builds tx). 2wp-app pegin: `areValidOutputs` (powPeg amt + OP_RETURN dest + change, fee-bounded) then all wallet builders (Reown/Xverse/Leather/Enkrypt/Trezor/Ledger) build the PSBT from the SAME validated `normalizedTx` = validate-A-sign-A. SDK `acceptQuote` validates on-chain (`validatePeginDepositAddress`). Two observed smells, both dead: `createPeginTx` reject-without-return (neutralized by JS promise single-settle); builders don't re-verify `getTxHex` id for SegWit witnessUtxo value (→ funds-to-fee, needs attacker==miner).

**On-chain↔bridge composition diff (operator-requested, rskj `BridgeSupport.registerFlyoverBtcTransaction` vs LP pre-checks) = MATCHED on every axis:** min-value identical (`Bridge.getMinimumLockTxValue()` literally `return getMinimumPeginTxValue(activations)`, same `>=` as `allUTXOsToFedAreAboveMinimumPeginValue`); LP amount `≥Total` stricter than bridge `>0`; locking-cap surpass refunds to **LP** (shouldTransferToContract=true in LP flow → REFUNDED_LP → no loss); already-processed LBC-gated; **derivation hash byte-identical** — Java `getFlyoverDerivationHash` (PegUtils) declares params `(…,lpBtc,lbc)` but `System.arraycopy`s `[quoteHash,userRefund,lbc,lpBtc]`, matching Go `GetDerivationValueHash` `buf.Write(QuoteHash,UserBtcRefundAddress,LbcAdress,LpBtcAddress)` exactly (the param-vs-copy order is a Java readability trap).

**THE LATENT FINDING (real divergence, reachability-null — the re-open trigger):** Go `DecodeAddress` (bitcoin/common.go) for segwit returns `[witver‖program]` → **P2WSH/P2TR = 33 bytes**; the bridge's `deserializeBtcAddressWithVersion` (entry for `userRefundBtcAddress` in `registerFastBridgeBtcTransaction`) **requires exactly 21 bytes and throws**, and `serializeBtcAddressWithVersion` is legacy-only (`getHash160()`). If a **segwit refund address** reached the pegin derivation: LP computes/watches deposit A, advances via `callForUser`, then `registerPegin` throws on the 33-byte refund → revert → **LP loses the advanced Value, attacker loses only fees → repeatable RBTC-liquidity drain.** DEAD because reachability-null: pegin `PeginQuoteRequest` has **no BtcRefundAddress field**; `get_pegin_quote.go:172/174` hardcodes it to `BitcoinMainnetP2PKHZeroAddress`/testnet-P2PKH-zero (21-byte legacy). Pegout `BtcRefundAddress=request.to` IS user-controlled + segwit-accepted, but pegout reimburses via LBC `refundPegOut` (RBTC), never the bridge 21-byte path. LP's own `LpBtcAddress`=trusted config. (P2WPKH accidentally survives: 21 bytes round-trips as version-0 legacy.)

**RE-OPEN TRIGGER (armed):** the day the pegin refund becomes user-controlled (a feature/SDK/API exposing `BtcRefundAddress`, or a bech32 refund reaching the flyover derivation) → live repeatable LP-drain, in-scope (defect is the Go LP's un-restricted `DecodeAddress` feeding a hash the bridge can't round-trip). Grep tell: any new writer of `peginQuote.BtcRefundAddress` other than the hardcoded zero-address in get_pegin_quote.go.

**Residual live payable surface (un-closed): 2wp-app connected-wallet manipulation** — scope lists "Modifying transaction arguments on an already-connected wallet" = CRITICAL; it's the runtime/threat-model surface not settleable from source alone. See [[feedback-default-posture-thief-not-fortress-prover]], [[feedback-audited-target-hunt-invariant-not-class]], [[reachability-is-kill-gate-not-severity-modifier]].

**WEB/APP surface (2wp-app + 2wp-api, live recon 2026-08-12) = also hardened NULL-COÛTEUX.** Deployed: mainnet `powpeg.rootstock.io`→`api.2wp.rootstock.io`, testnet `powpeg.testnet.rootstock.io`, staging `powpeg.staging.rootstock.io` (all S3+CloudFront, all live — no subdomain takeover). App headers: X-Frame-Options:DENY, nosniff, HSTS; CSP is meta-only (JS-appended) and WEAK (`script-src-elem 'self' 'unsafe-inline'` no nonce → injected inline `<script>` WOULD run) — but there is NO injection point: the ONLY `v-html` sink is `TermsContent.vue` rendering `md.render(terms_and_conditions feature-flag)` from `GET /features`, and features are GET-only with no public write path (backoffice write was REVERTED at HEAD) → not attacker-writable; all route params (txId/type/amount/network/wallet) render via Vue auto-escaped interpolation/`:attr` (no reflected XSS); txId hex-validated. 2wp-api = LoopBack, NO auth middleware (stateless public utility, no protected resource → authz/IDOR N/A), OpenAPI/explorer disabled, inputs OpenAPI-schema-validated (`{"$ne":null}`→422, NoSQL-injection blocked), an edge WAF 403s XSS-pattern payloads, clean error shape (no stack traces). CORS `ACAO:*`+`ACAC:true` = real misconfig but INERT (browsers reject *+creds, origin not reflected, no credentialed data). Public client IDs only (clarity/hotjar/walletconnect = OOS non-sensitive). Verdict: whole in-scope surface (SC + off-chain + cross-chain + crypto + web + API) is robust or reachability-null with executed artifacts → RE-SOURCE.

**CORRECTION (2026-08-13) — the "whole surface NULL-COÛTEUX" above was OVER-CLAIMED.** Operator found a real captcha-MIDDLEWARE fail-open I missed: `captcha.go` `!validCaptcha` branch writes 500 but has NO `return` → `next.ServeHTTP` runs → unauthenticated captcha-free `/pegin|pegout/acceptQuote` (both RequiresCaptcha:true, public) → free liquidity-reservation griefing across all LPs (captcha default-ON: `Disabled` zero-value false; no rate-limiter). VERIFIED at HEAD, unchanged since 2024-06-10, in v2.5.1 — released, un-fixed. Distinct from #2 (regular endpoint middleware defect, NOT the acceptAuthenticatedQuote design-intent path) and STRONGER (unambiguous bug). Griefing/Medium ceiling; real dismissal risks = web-asset-DoS-vs-SC-griefing categorization, "add rate-limiting" absorption, self-heal + native-peg fallback. I traced money-flow but treated the middleware (captcha/session/csrf) as plumbing and only greped it → the coverage gap. Lesson: [[feedback-audit-the-guard-not-just-the-money-path]].
