---
name: rootstock-powhsm-recount-gate-defeat-not-theft
description: RootstockLabs PowHSM (Immunefi $200K) — crypto core fortress-null; post-audit brothers re-count defeats found_best_block gate but does NOT commit best_block (theft refuted by executed disconfirmer)
metadata: 
  node_type: memory
  type: project
  originSessionId: db826c26-60c5-49a1-b4e6-7a6128825a57
---

**RootstockLabs PowHSM firmware (Immunefi, $200K remote-key tier), audited 2026-07-04 via /intake→/firmaudit→/darkside + a 7-cluster fan-out. Workspace `~/Desktop/BUGS/rsk-powhsm`.**

**Crypto core = earned fortress-null.** Read all of it by hand: difficulty↔PoW (`bc_diff.check_difficulty`, target=2^256/claimed-diff, capped accum), receipt↔txid (`auth_receipt.c` topic_2==auth.tx_hash), trie↔root (`auth_trie.c` bottom-up keccak chain to ancestor_receipt_root), sighash↔tx (`auth_tx.c` same byte stream). Every binding conserves — a class bug won't survive here (AXIOM held). Only public audit = **NCC Group v3.0.0 (2022), 0 Crit/High/Med, 5 Low all fixed**. The current crown-jewel code (brothers/uncles difficulty, auth parsers **rewritten from scratch**, segwit signing, N-of-M signer auth) was ALL added AFTER → un-audited by NCC = the fresh surface (Rule 4). NCC 2GR ("block numbers not validated") was "fixed" in docs only → block.number still drives SET_NETWORK_UPGRADE but the only differential (Iris-only merkle-proof cap) is DoS-at-most.

**F-01 (bc_advance.c) = real defect, theft REFUTED.** The brothers difficulty accumulation re-counts ONE brother N times: `HEADER_VALID` is set at coinbase (:944) and **never cleared within a brother**; every subsequent `OP_ADVANCE_BROTHER_CHUNK` re-enters `bc_adv_accum_diff()` (:952) which has no already-counted guard (only `found_best_block` short-circuit); `block.size` (brother RLP list length) is attacker-declared and only checked at list-close. Over-declare the list + feed inert phantom chunks → same capped difficulty credited per round-trip → up to ~819×/brother. **OBSERVED on the official tcpsigner (x86 build of shared in-scope bc_advance.c): one real merge-mined brother re-counted flips `found_best_block` False→True (MIN_REQUIRED gate crossed).**

**BUT the escalation to peg-out theft is REFUTED by an executed disconfirmer** (`findings/F-01-brother-difficulty-recount/poc_commit_test.py`): built a valid merge-mined fork block W (parent==regtest checkpoint) via the dev's `mine.py`, re-counted its brother → found_best_block True → **committed `best_block` NEVER moved off the checkpoint.** Airtight state-machine reason: the re-count REQUIRES an over-declared brother, which TRAPS the invocation (never reaches `block.size==block.recv` → `handle_end_of_block` — the only writer of best_block — never runs); finishing cleanly closes the RLP list → `list_end` FAILs (field!=coinbase); the 2-phase escape (abandon→INIT→re-send W) is rejected by `bc_adv_prologue` continuity (`next_expected_block` mismatch, firmware `0x6b9a`). `found_best_block` is transient RAM, self-heals on reset. Sibling merkle-skip defect [0] (already_validated brothers skip merkle, :512/574/696) commits only to REAL main-chain blocks. **Net: difficulty-accounting correctness bug + self-healing DoS, NO fraudulent best_block, NO loss=$X → Low/hardening, not payable. Logged OUTCOMES no_go_theft_refuted.**

**META-LESSON (the important one):** I framed this Critical off a real *gate defeat* (`found_best_block` flip), then the operator's "show best_block actually moves" + the executed disconfirmer caught the over-claim. **`found_best_block` (transient accumulator flag) ≠ `best_block` commit (NVM, the security-relevant state).** On any state-machine finding, the writer of the persistent state + its trigger condition is the gate to test — a flipped intermediate flag is not the impact. The trapped-brother/handle_end_of_block mutual-exclusivity is the exact "did the protocol let this, or my setup" that only execution answers. See [[feedback-verify-before-working-no-theater]], [[protocol-fortress-null-hunt]].

**Re-open triggers:** a genuine commit path for the re-count (would make it Critical); or the untouched in-scope OFF-CHAIN surface (powpeg-node Java, liquidity-provider-server, 2wp-api) — the crypto firmware is worked. Tooling built + reusable: tcpsigner builds native (libsecp256k1 from source to a local prefix, `C_INCLUDE_PATH`/`LIBRARY_PATH`); venv `powhsm-venv` has ledgerblue+rlp+python-bitcoinlib; PoC drivers speak the raw advanceBlockchain APDU protocol via the middleware.
