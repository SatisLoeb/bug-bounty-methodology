---
name: ondoperps-web-api-verified-seam
description: Ondo Perps (Cantina web/API) — CLOSED NO-GO/RE-SOURCE. Free surface = executed-null (BFLA + SIWE + web2 + address-book + api-keys + referral all defended); only live EV is funds-gated trade-accounting (deferred)
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d0b9df0-cf30-4f2e-aed5-2b8f0ab5ff79
---

Ondo Perps = custodial off-chain perps DEX (Next.js/Vercel → Go API behind Cloudflare). Cantina web/API bounty: Crit $1.5M / High $100k / Med $20k. Scope = ondoperps.xyz, app.ondoperps.xyz, api.ondoperps.xyz. Fresh public-beta, well-funded serious payer, thin off-chain-layer dup. Workspace: `~/Desktop/BUGS/ondo-audit/` (full plan in `SURFACE-DOSSIER.md`, bundles in `bundles/`).

**THE SEAM (verified in bundle + published rest-spec.json):** caller-supplied account-id trusted for fund movement vs JWT subject. JWT (from SIWE web3 OR web2 email/google — both mint one account model) proves WHO; a separate caller-controlled id names WHICH account to act on, nothing binds them. Appears as header `ONDO-SUBACCOUNT-ID`, body `from/to.id` (`/v1/subaccounts/transfers`), `deposit_destination.id` (`/v1/provision_address`), `from.id` (`/v1/withdraw`), path `/v1/subaccounts/{id}/api_keys`. Three auth stacks → one account model → authz dispersed, owned by no layer.

**Top ranks:** 1) `/v1/subaccounts/transfers` from/to.id BFLA (no address-book gate = cleanest theft, Crit, 2 sessions). 2) `/v1/withdraw from:{id}` — deciding Q = address-book checked vs JWT-acct(theft) or from.id(griefing). 4) web2 `/v1/auth/web2/address_book?redirect_uri=` allowlist-bypass+open-redirect. 5) `erc-4361/address_book/complete_challenge` sig-binding (sign AddrA submit AddrB?). 6) `/v1/subaccounts/{id}/api_keys` BFLA key-mint + `network_link` scope. **8) `ONDO-SUBACCOUNT-ID` header-over-JWT decider = THE 1-SESSION generalizing probe (run early).** address-book EDIT/DELETE need NO signature (only ADD does); no Solana signing path though solana withdrawals exist.

**RESOLVED 2026-07-07 (2 no-funds accounts A1=17330176649265158059, A2=13806684690313974230): BFLA seam = EARNED NULL on reachable surface.** withdraw from.id ownership-checked ("Can only withdraw from own accounts", pierced 5 ways incl real foreign A2 + int/array type-confusion + header/body split); deposit_address/list JWT-scoped; header-BFLA universally gated subaccounts_not_enabled; provision_address 500s on ANY non-own id (real A2 == fake -> NOT a BFLA, NOT an oracle, just a Low robustness crash). Subaccounts admin-disabled + no user-enable (PUT/PATCH acct=405) -> transfer/subaccount-apikey/subaccount-modify BFLA (ranks 1/6/11) REACHABILITY-PARKED (re-open iff subaccounts enabled). Order-engine (perps pricing/accounting, High $100k) UNTESTED = the real remaining EV but needs FUNDS + correct internal market id (order rejects "BTC-USD.P" as invalid_market) + low-volume care (hit 429 rate-limiter -> program says no high-volume testing). Nothing submitted; provision-500 borderline-OOS. NOT a fortress overall (order engine open), but BFLA thesis closed. OUTCOMES row logged.

**Binding constraint = provisioning (operator gate).** Step A done (1-2 no-funds SIWE wallets → BFLA null). Step B (funds) now only justified for the ORDER-ENGINE track, not BFLA. DISCIPLINE: test only accounts WE control; never touch a real third user's funds; respect no-high-volume (rate-limiter tripped once).

**Solo-null (executed, no auth):** SIWE issuance clean, SIWE enum uniform, CORS no-reflection, HMAC key-enum oracle low-value (random keyspace), `/v1/sandbox_deposit` 404 on prod (rank-3 dead), verify_eligibility/status/pledge(=PoR) benign, invite disabled. No standalone unauth finding. Pattern = [[wallet-tg-telegram-bfla-engagement]] / [[polymarket-fresh-surface-perps-rfq]]: high-EV seam needs wallet onboarding. `gm-be-api-spec.json` in docs hints SHARED backend w/ Ondo Global Markets (blast-radius amplifier — verify).

---

**CLOSED 2026-07-08 — blind thief-first re-hunt (operator: re-do Ondo with the new philosophy, no reference to this dossier during the hunt). Verdict = NO-GO / RE-SOURCE. Full ledger: `ondo-audit/CLOSE-2026-07-08-blind-rehunt.md`; OUTCOMES `ondo-perps-blind-rehunt-2026-07-08`; ultracode Workflow `wf_03cc21fc-016` (full 123-chunk client map).** The blind hunt CONVERGED on the same core (BFLA defended, EV funds-gated) = corroboration, and CLOSED the previously-UNTESTED ranks:
- **Rank 4 web2 `redirect_uri`** → DEFENDED (whitelist rejects evil/subdomain-suffix/path/`@`/proto-rel/nullbyte; token is cookie-based via `get_token`, never in URL; CORS scoped to app.ondoperps.xyz only). `web2/address_book` redirect = 405 n/a.
- **Rank 5 `address_book` SIWE sig-binding** → DEFENDED 3 ways: wrong-wallet sig = `400 incorrect signing address` (ecrecover-bound like login); **body-override killed** (sign addr dEaD, body addr 1111 → stored dEaD, signed address authoritative); `PUT` upsert = `404 account_not_found` (no unsigned add, update-only).
- **Rank 6 api-key scope** → enum ENFORCED (`withdraw`/`admin`/`read`/`view` = "not valid"; only `trade`/`transfer` valid; `network_link` = "Network link is disabled").
- **NEW — referral/points→USDC (not in prior dossier):** rewards program is REAL (`/v1/rewards/schedule` public: weekly USDC pools 100–150k, week6 live; points→pool share). Referral sybil gap REAL (self-referral blocked "Cannot refer self" + one-referrer-lock, but **circular A↔B allowed, no anti-sybil, free unlimited SIWE accounts**). BUT disconfirmed: `referralShare` stays "0" after a referral (referrals don't currently generate USDC-points) + fee 15%-recapture = loss → **LOW/latent** (re-open if `referralShare`>0 activates → then it's live sybil USDC theft). `/v1/points/info` is GLOBAL not per-account; `/v1/points/rank` + `/v1/points/leaderboard` = **500** (Low bug).
- Deposit/provision RE-CONFIRMED JWT-bound (client `deposit_destination` ignored; `{symbol,network}` only; unique per-acct addr on avax-c-chain).
- **Funds-gated frontier UNCHANGED = the only live EV (HARNESS-SUSPENDED):** trade-accounting (self-cross mark/funding manip, Injective#345 class), `/v1/withdraw` (`customer_withdrawal_id` client-set+predictable=idempotency-replay angle), points wash-farming. Operator provisioned a real web2 session (loopt1793@gmail.com, acct 15340886168196493935) + I gave deposit addrs (avax `0x11b9…`, eth `0xd108…`) but operator elected NOT to fund → deferred. Claude cannot execute trades/withdrawals (hard rule) → operator must run the PoC sequences.
- Low residues not submitted: `points/leaderboard` 500 unauth; `localhost:3000` prod redirect-whitelist; referral no-anti-sybil latent.
- WS authed seam (`wss://api.ondoperps.xyz/ws` `{op:login,args:{token,subaccount}}` + no-per-msg-accountId private channels) = same account-id class as the dropped `ONDO-SUBACCOUNT-ID`; not pursued (operator dropped the subaccount angle).
