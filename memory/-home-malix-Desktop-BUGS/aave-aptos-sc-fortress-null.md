---
name: aave-aptos-sc-fortress-null
description: "Aave V3 on Aptos (Cantina, $100k-$1M Critical GHO) — SC Critical surface earned fortress-null 2026-06-29; faithful Move port of canonical V3.3; the untouched in-scope off-chain frontend/API is the next move"
metadata: 
  node_type: memory
  type: project
  originSessionId: f12acb52-984f-4c4b-91ce-9b64dd53c682
---

**Aave V3 on Aptos** (Cantina bounty, repo github.com/aave/aptos-aave-v3, Move 2.3). Workspace `~/Desktop/BUGS/aave-aptos-audit/` (TARGET-DOSSIER.md w/ full corpus map + poke table). Reward binary: Critical $100k-$1M (10% funds-at-risk, min $100k), High capped $50k, Med $5-10k → only a Critical (theft/insolvency/permanent-freeze) pays.

**STATUS 2026-06-29: SC Critical surface = EARNED fortress-null (proven by execution, not reflex).** Flow = `/intake` (corpus patterns: lending shape, accounting/liquidation/reentrancy/arithmetic top) → manual-impro poke. 13 Critical-seam deep-checks, ALL faithful/gated/closed: time-unit (now_seconds+SECONDS_PER_YEAR, no μs — P-LEND-017 refuted), ray/wad math (half-up+ceil+overflow guards), withdraw-reentrancy (Aptos dispatch-reentrancy-guard + governance-listed underlyings, the "Move prevents reentrancy" belief is moot because the hook isn't attacker-controlled), liquidation seize-calc (faithful `_calculateAvailableCollateralToLiquidate`), u256→u64 casts (Move `as` is CHECKED-abort not truncate, + FA-supply-bounded), mint/burn authz (`public(friend)`-gated, all `_for_testing` properly `#[test_only]`), **aToken transfer HF-bypass** (the sharpest Move-specific seam: an Aptos FA's native `primary_fungible_store::transfer` could bypass `finalize_transfer`+HF-check — but CLOSED because user aToken stores are FROZEN via `set_frozen_flag(...true)`, so only the pool's `transfer_ref` moves them → routes through finalize_transfer which validates HF when borrowing), onBehalf borrow (restrictive: `assert user==on_behalf_of`, delegation removed), oracle staleness/price (validate_oracle_timestamp: age<=max_price_age + not-future + >0 + <=I192_MAX-for-negative), virtual-balance (V3.1 +added/−taken; flashloan nets +premium correctly), capped-price sUSDe/stable (linear upper-bound min(price,cap), admin-validated params, protective), deficit/bad-debt + liquidation orchestration (close-factor 50%@HF>0.95, dust-prevention, has_no_collateral_left, gross/net fee split = V3.3 faithful).

**LESSON: the deviation/port-gap thesis did NOT pan out — the port is genuinely faithful + Move-correct (every footgun handled), + 4 audits.** This is the heavily-audited-faithful-core wrong-target pattern ([[feedback-target-diet-is-the-binding-constraint]]) — but PROVEN by execution across 13 seams, not conceded ([[feedback-closure-bias-expand-voies-hunt-seams]]). A faithful port of a canonical, audited protocol is dup-dead on the SC core; the deviation edge only pays when the port actually deviates.

**RE-OPEN / NEXT (the live EV on this target):** the **in-scope off-chain frontend/API is UNTOUCHED** — the upshift gap (DeFi audits the contracts to death, treats the backend as a normal web project; scope explicitly includes "official web interface + API endpoints + deployment scripts/config"). That is the highest-EV remaining move on this $1M fresh target → route `/upshift` or gravedigger web. SC-side un-swept (lower-prob): e-mode/isolation/supply-borrow-caps edges, aave-rate curve math, flashloan-complex debt-open. Re-open SC only on a new post-audit commit delta.
