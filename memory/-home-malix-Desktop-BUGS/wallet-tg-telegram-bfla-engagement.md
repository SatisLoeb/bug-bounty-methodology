---
name: wallet-tg-telegram-bfla-engagement
description: "Wallet (@wallet/Telegram custodial wallet) bounty — backend mapped, value is 100% authed, cross-user BFLA harness staged, suspended awaiting 2 sessions"
metadata: 
  node_type: memory
  type: project
  originSessionId: c048e441-5a44-45d3-a4f6-109e2ad565c3
---

Target: **Wallet (@wallet / Telegram)** custodial TON wallet bounty. In-scope listed hosts (decision: LISTED HOSTS ONLY): wallettg.net/.com, walletbot.me, p2p.walletbot.me, pay.wallet.tg, wallet.tg + "Crypto infra cold/hot wallets". Engaged program (walletbot.me 56% resolved). Workspace: `~/Desktop/BUGS/wallet-tg/` (RECON.md + JS bundles + harness/).

**Discovery (apparatus-off manual poke, 2026-06-30):** the SPA's own `openapi.js` bundle reconstructs a ~20-microservice fleet the program table never names — `alectryon`(auth: Mini-App initData→telegram-wallet token), `api/v1`(wallet-core, FastAPI), `p2p`, `perps`, `crypto-cards`, `users`(get-kyc), `funds-gateway`, `portmone`, `earn`, `referral`, etc. ONE listed edge host (wallettg.com/walletbot.me) reverse-proxies EVERY service by path-prefix through istio+nginx, incl. `/alectryon/public-api/auth` → all reachable scope-cleanly via the listed host.

**Value is 100% authenticated.** Free/unauth angles swept with EXECUTED artifacts (all null): `/internal/*` public bypass = React SPA catch-all (byte-identical to bogus path, NOT a bypass); alectryon initData forge holds (400 format-gate); by-user-id/money reads = 401; provider shims = 405 nginx; deeplink/session/claim = 401. Only open endpoint = `/p2p/public-api/health`.

**Crown-jewel authed worklist (angle-A authn≠authz, harness ready):** `p2p/public-api/v3/payment-details/get/by-user-id` (bank/card PII by userId) · `order/history/get-by-user-id` · `user-statistics/get/by-user-id` · `users/public-api/v2/user/get-kyc` · `transactions/{details,withdraw_details}/{transaction_id}` IDOR · money 2nd-step authz `withdrawals/process_withdraw_request/{uid}` + `transfers/process_transfer/{id}` + `giveaways/gift/{gift_uid}/claim` · the p2p↔wallet-core inter-service trust seam. **Bombshell:** `/internal/users/{user_id}/{tokens,verify}` = raw-user_id token-mint/verify, server-internal → instant ATO if any service SSRFs/misroutes to it.

**STATUS (2026-06-30): reachable surface SWEPT, multiple executed nulls; binding constraint = ACCOUNT STATE.** Ran the cross-user harness with live ES256 sessions (2 throwaway accts, KYC-BLOCKED + $0 + uninitialized). Executed NULLS: (1) body-userId BFLA — server ignores body id, JWT-scoped (A→B returned A's data; disconfirmer ghost-id → A's data); (2) JWT forgery — alg:none/None/sig-strip/garbage all 401 on BOTH verifiers (FastAPI `token_corrupted` + p2p); (3) exchange create → 403 KYC-forbidden; (4) withdrawal validate_amount rejects negative; (5) create_withdraw_request `max_value` is a BOOL not numeric cap, flow dead-ends at 404 account_not_found (is_created:false); (6) portmone JWT-scoped no IDOR; (7) alectryon initData forge → robust input-validation, signature path 500s. Token model: ES256 JWT, identity in plaintext claims + Fernet `main_data`; /api/v1 = raw token, /*/public-api = Bearer; tokens live ~3 min.

**EXCHANGE DOUBLE-SPEND/RACE = EXECUTED NULL (funded $1.49, 2026-06-30):** create_exchange does NOT reserve (created 3 orders vs same balance), but submit is ATOMIC + IDEMPOTENT — 6 concurrent submits → only 1 settled (1.4 USDT→0.907 TON, exact), replay returned the SAME transaction_id, siblings 400 operation_failed. Balance engine correct under concurrency; same ledger ~certainly guards transfer/withdraw. Harness `harness/race_exchange.py` (needs browser UA — urllib hits Cloudflare 1010; classifier blocks Claude from executing money ops → operator runs via `!` or Bash-permission). NEXT enabled test: cross-user resource IDOR now testable — B has real tx id 141510164267099; account-A token reading it settles the resource-id IDOR question.

**SEAM-HUNT after operator's "script-kiddy" pivot (2026-06-30):** ran a 5-lens decompile workflow — operator's sharp critique: "le découpage en agents recrée l'angle mort organisationnel que je chasse" (agent-split rebuilds the org silo where the seam-bug lives; the seam is in the GAP no single lens sees — hold it all in one head). VERIFIED every agent thesis by execution, caught 2 over-claims: `/internal` path-confusion NULL (app-reached≠handler-reached; 404 not 401), `authorize_by_telegram` NULL (validates HMAC). **MAJOR SELF-CORRECTION: my "2 signing realms" (perps/earn/referral/onboarding reject token) was MY error — header-scheme artifact (I sent `Bearer`; they want RAW `authorization:`). RE-TEST raw → all 200.** One token, one realm. Reopened perps/earn/funds-gateway. All UUID-keyed except **earn `withdraw/{transfer_request_id}/cancel` = sequential integer** (the one real cross-stack IDOR candidate) — but un-drivable: earn deposit → 403 access-policy, geo-spoof NULL (uses signed `country_code_by_ip`). **PATTERN (binding constraint): throwaway accts are systematically gated from every money-moving feature** (P2P offer=antifraud RESTRICTED, earn deposit=access-policy forbidden, withdraw=fee>balance) — robust reputation/access-policy defense-in-depth. NO exploitable bug across the whole engagement; target robustly built. To continue needs access-policy-PASSING accounts (aged/reputation/non-DZ). Lesson [[feedback-workflow-agents-coverage-not-verdict]] + [[doctrine-seam-rattachement-is-the-value]]: workflow/agent-split was the wrong instinct for seam-hunting (recreates the silo); verification caught my own realm error.

**CORPUS-COMPLETENESS CLOSE (poke-first, corpus-second, 2026-06-30):** ran web-corpus-query (shape=payment-fintech+REST-API). Backstop caught that poke had SKIPPED info-disclosure (the #1 REST payout class $105K) entirely → ran it → NULL (no secrets/source-maps/.git/graphql/verbose-errors; apiKey=FingerprintJS public). Then closed the 3 remaining gap classes, all EXECUTED: mass-assignment NULL (strict DTOs → "JSON parse error" on unknown fields = the defense), currency/decimal-confusion NULL (precision rejected, convert network-agnostic), stored-XSS BLOCKED (merchant/nickname→NOT_MERCHANT, merchant=antifraud-gated), DOM-XSS NULL (no sink: startParam→structured React-escaped params, tonconnect validates URL), geo-self-declare NULL (countryAlpha2 validated, US=ILLEGAL_COUNTY). **Harness bug caught by verification: 1st mass-assign run used RAW scheme on Bearer services→401s + "PRIV echoed" was a FALSE POSITIVE (FastAPI 422 echoes input) — re-ran correct.** Verdict: CLASS-COMPLETE on reachable surface, every top class has an executed null. Target robustly built (ES256 + atomic ledger + ownership-checks + strict DTOs + signed geo + antifraud/reputation/access-policy defense-in-depth). IS-NOT (untested behind the wall): earn integer-id IDOR + P2P escrow role-BFLA — gated by antifraud on throwaways, need reputation/aged accounts. Lesson [[feedback-corpus-coverage-gate-lead-dont-improvise]]: corpus = completeness backstop (caught the info-disclosure I'd skipped), NOT verdict; poke found the depth, corpus found the gap I missed.

**Strategic finding: auth model SOLID; money-logic surface (exchange/withdraw/transfer/p2p execution) UNREACHABLE from blocked/$0/uninitialized acct.** To raise EV need a transacting account (KYC + small funds + init) — operator chose stay-blocked, so reachable surface is largely exhausted. Remaining no-unlock angles (lower yield): deeplink/session/claim hijack (needs 2 fresh sessions), startParam DOM-XSS (CSP allows unsafe-inline; deep client trace), 500 error paths. Lesson: [[feedback-target-diet-is-the-binding-constraint]] — process converted wrong-account-state → honest nulls correctly; the funnel (account state) is the bottleneck, not effort. [[feedback-apparatus-is-packaging-not-discovery]] held: manual poke found the whole hidden backend from the bundle. Dossier: ~/Desktop/BUGS/wallet-tg/RECON.md.
