---
name: polymarket-v2-onchain-fortress
description: "Polymarket V2 on-chain module system (PositionManager/modules/Exchange/DepositWallet/migration) deeply re-read 2026-07-01 = fortress; the live EV is off-chain, not the SC core"
metadata: 
  node_type: memory
  type: project
  originSessionId: cefd795d-d46a-4cfc-8ba3-63e0d8d04ec7
---

Polymarket (Cantina bounty ff945ca2, Polygon chainid=137). The V2 module system was deep-read as a machine 2026-07-01 (MANIFEST 4-voie immortal protocol), extending the 2026-06-20 Combinatorial-only closure ([[polymarket-fresh-surface-perps-rfq]] is the OFF-CHAIN sibling).

**Sources:** `~/Desktop/BUGS/polymarket-audit/v2-src/` (Etherscan-verified DEPLOYED sources, 150 .sol, NO test files → darkside Door A impossible, Door C only) + `scope-src/` (audited ctf-exchange-v2). Cantina repo is auth-gated (WebFetch 307 login) → could NOT diff Cantina-scope-commit vs deployed = a Rule-5 (source≠deployed) delta gap left OPEN.

**VERDICT: fortress.** Read across all 4 voies:
- DepositWallet+Factory: session-signer (ERC-6492-layout-compatible wrapper), ERC1271 (Solady nested), UUPS factory-gated, 2-step handover (pendingOwner only settable by owner's own onlySelf batch → no permissionless takeover). CLEAN. **1 park-latent (OOS):** `execute()` is NOT pause-gated + `revokeSessionSignerEmergency` is itself onlyPaused(timelock) → a paused wallet is still drainable by the operator/session-signer path; the "emergency recovery" invariant the user believes is false. But harm needs rogue-operator (centralization OOS) or leaked-session-key (OOS) → not payable.
- PositionManager: moduleId in top-8-bits of positionId; auth = `moduleById[posId.moduleId()]==msg.sender || crossModuleAuth[msg.sender]`; baseHash=keccak(moduleId,data)&128bits binds module. burn always `_burn(msg.sender)`. crossModuleAuth[Combinatorial]=true widens MINT not burn.
- Combinatorial (11 ops), NegRisk (convert=identity NO(i)=Σ_{j≠i}YES(j), resultsSum≤DENOM, synthetic Other fallback), Binary, migration mixins (legacy CTF/NegRiskAdapter→V2): all conservation-sound + role-gated. Migration guards result.length==0 + backing reaches vault atomically before any V2 redeem.
- All pUSD/position mints role-gated: CollateralToken MINTER_ROLE (modules only), onlyModuleByPositionId, onlyResolver (BRIDGE|RESOLVER). No ex-nihilo mint.
- Exchange: fully onlyOperator + AssetAccountingMismatch/crossing/fee≤MAX_FEE_RATE(immutable) guards. Router atomizes transfer+consume (mitigates pre-transfer theft).

**Hypothesis-free trace of the value↔oracle seam (2026-07-01, operator's anti-template directive):** migration result-normalization `result_[0]=legacyPayout0*DENOM/(p0+p1)` handles partial/degenerate legacy payouts correctly (Σ YES payouts + Other = DENOM = 1 collateral, conserved even under legacy [1,1] split resolutions). Neg-risk resultsSum/fallback state machine is double-count-free (fallback SETS resultsSum=DENOM, doesn't +=). **Only surfaced anomaly:** two legacy conditions of one neg-risk event both resolved YES (legacy CTF conditions are independent; app-layer "one winner" not enforced on-chain) → 2nd `_resolveMigrationCondition` hits `require(resultsSum_≤DENOM)` → REVERTS → that condition's YES holders can't resolve/redeem = liveness/DoS, requires oracle to report two YES = thin (borderline OOS "incorrect oracle data"; oracle-manipulation IS in scope but no extraction, no loss=$X).

**GAPS closed 2026-07-01 pass:** (1) **AutoRedeemer** source FOUND in `polymarket-darkside-2026-06-11-audit/build-pm/` + hypothesis-free trace = CLEAN (snapshot-once accounting self-corrects: every path drains delta fully — V2 `transfer`, legacy `onramp.wrap`→`safeTransferFrom` pulls exactly payout; payout hardcoded to `from` → rogue-operator can only force-redeem, never redirect). (2) **The 2026-06-11 session already PROVED the fortress with ~46 executed fork PoCs** (T2 all-false migration, T3 recursive rounding, T4 stranded, D DepositWallet sig-confusion, M module-trust, AutoRedeemer) — my whole session re-derived it by reading. Full dev repo + 23 audit reports live in that dossier's `build-pm/` + `audits-v2/txt/`.

**AXIOME re-derivation (2026-07-01) — every Acknowledged money-path across Cantina-Apr/Quantstamp-May/Pashov-May/Cantina-Jun-diff, mapped:** the convergent class = migration→neg-risk→unbacked-pUSD. All variants die: Cantina#6 all-false = PRIOR-PROVEN-NULL (T2: horizontalMerge needs synthetic-Other YES, migrant never holds it→reverts); Cantina#5/Pashov-L04 post-prepare-append = SAME synthetic-Other structural block + untrusted-reachability COLLAPSES (prepareMigrationEvent is onlyCreator=Polymarket, migrates its OWN UMA-resolved markets; the "third-party oracle appends a question" needs Polymarket to migrate an attacker-oracle market — operational, dev-acknowledged "we won't append"); Quantstamp OC-3/OC-5 + Cantina#1/#4 + Pashov-L11 = cross-chain-bridge (hub/spoke, NOT Polygon-deployed value path) OR trusted-role (module-replace/divergent-resolver=centralization OOS); Pashov L-18/L-30, Cantina#14 = dust (OOS rounding<$1). Sharp variants already FIXED (POL-V2-1/2, June 3.1.4 spoke-round-trip, M-03/M-04). **No Acknowledged finding produces an untrusted-reachable loss=$X on deployed Polygon scope.** (3) Cantina-scope-commit vs deployed delta still unverified (repo auth-gated) — the ONLY real re-open trigger = a NEW post-deploy module/commit.

**Where the live EV actually is:** OFF-CHAIN operator layer (Perps/pUSD seam, needs funded account) per [[polymarket-fresh-surface-perps-rfq]], NOT the SC core. Doing another SC pass = the exact "one-voie obstination" the operator flagged. Re-open SC only on a new post-deploy commit or if AutoRedeemer source reveals an authz gap.
