---
name: grunt-3flabs-intake
description: "3F Labs \"grunt\" Cantina bounty intake — 14K LOC SC fortress, 4 audits, OOS=confession-map, in-scope adversary=untrusted-only"
metadata: 
  node_type: memory
  type: project
  originSessionId: af28bdab-90ca-43be-9873-ebc5d8d803bf
---

**Target:** 3F Labs `grunt` (github.com/3FLabs/grunt), Cantina bug bounty. Scope = all Solidity in `/src` on `main`, EXCLUDING `facility/IntentDescriptor.sol`. Off-chain OOS. Clone at `~/Desktop/BUGS/grunt-audit/grunt`; audit PDFs → text at `~/Desktop/BUGS/grunt-audit/audit-txt/`.

**Architecture:** Facility (central hub, ERC-6909 LP tokens, state machine DEPOSITING→RESOLVING→RESOLVED) orchestrates: Request (dual-token PT/YT vault) · PositionManager (multi-position aggregator over Morpho) · Funds (Centrifuge/Pareto/USCC RWA adapters + WrappedAsset) · MorphoBorrowPosition · TransferGuard (compliance). All beacon-proxy factories.

**Fortress level: HIGH.** 4 audits — ChainSecurity main (v1→v5, 34 findings), ChainSecurity Funds, Cantina main (10 H/M + 28 Low + 10 info), Cantina FeeReview. 115 test files incl invariant/fuzz/fork/reentrancy. Single-module class bugs = predetermined-null; the picked fruit is gone.

**THE KEY FRAME (why this bounty is winnable at all):** The OOS list is a 1:1 map of the audits' Risk-Accepted/Acknowledged findings — it is the devs' *confession map* of where they stopped. Scope says a report is rejected unless it shows a **NEW INDEPENDENT IMPACT outside these assumptions.** All privileged roles (owner/facilitator/operator/guardian/rebalancer/curator/consumer/executor/deployer) are TRUSTED. Therefore **the in-scope adversary is an UNTRUSTED actor only** — an ordinary LP, an external caller, a cross-intent participant, a Morpho/Centrifuge counterparty acting within documented behavior. Any candidate whose trigger REQUIRES a trusted role to misbehave = OOS. Living bug = reachable by no-special-role OR an impact a trusted role cannot prevent/reverse.

**In-scope escape hatches (explicitly carved out of the OOS text):** (1) component address introduced WITHOUT violating operational assumption; (2) impact on a NON-zero-quorum intent or that bypasses a separate protection; (3) new impact BEFORE bad-debt state is detectable; (4) untrusted party creates an unbounded token set under supported-token assumptions; (5) production bootstrap bypass on a fresh PM.

**Deviation surface:** `lib/morpho-blue` = 3FLabs' OWN fork, not upstream — canonical-deviation candidate (darkside Door C).

**Method chosen (per operator warning that agent decomposition recreates the intersection blind-spot):** breadth-map workflow with (a) shared baseline+OOS injected into every agent so nobody re-reports known issues, (b) a dedicated SEAM phase whose unit-of-work IS the handoff (not the module), (c) a completeness-critic that re-stitches every module-pair/3-module value-path the split may have missed. Artifacts → `~/Desktop/BUGS/grunt-audit/breadth-map/`. See [[feedback-audited-target-hunt-invariant-not-class]], [[doctrine-seam-rattachement-is-the-value]], [[apparatus-is-packaging-not-discovery]].

**STATUS 2026-07-05 — SC = EARNED FORTRESS-NULL (executed, not reflexive).** THREE independent methods converged on ONE un-executed region (the leverage-core collateral leg, which the shipped invariant fuzzer FAC-5 omits — it tracks only PM-shares+debtToken, never collateral): (1) solo hand-trace = conservation holds by construction; every donation/sweep seam is negative-EV because the injected value DILUTES to the whole shared pool (`amounts.add` credits all; a thief spends $X, pool gains ~$X spread over everyone incl. himself → net≤0); (2) completeness-critic named the same leverage core; (3) impact-decomposed chain-hunt (8 agents, adversarial refute) = ZERO chains survived, killed by the same two structural facts. Built the executed disconfirmer `grunt/test/facility/LeverageCore.invariant.t.sol` — collateral-deposit intents driving the REAL depositManager/withdrawManager/burnManager+swap across 2 intents sharing 1 PM, asserting the collateral/debt/share solvency mirrors FAC-5 omits. **Result: 3 invariants PASS, 0 fail, at BOTH ~32.8k calls AND ~512k calls (2000×256, 4.2 CPU-hrs), provably NON-VACUOUS (each selector ~42k calls; last-run depositMgrOk=22/withdrawMgrOk=9/burnMgrOk=9/swapOk=4/claimOk=21).** Bias traps caught+closed: ghost-state desync (fixed → derive phase on-chain) + afterInvariant-runs-post-shrink measurement artifact (fixed → reachability proven by deterministic `test_leverageCycleExecutes`). IrmMock confirmed non-zero-rate (interest is a live stressor). Larger 512k-call run launched to strengthen. **Only residual sliver:** async exogenous-delivery misattribution (MockFund settling ≠ order.output) — but this = the OOS "per-order balance-sweep attribution" clause, operator-recoverable → likely dead. **Scope is /src Solidity ONLY (no off-chain pivot exists).** Verdict: grunt SC closes as fortress-null; re-open only on a new /src commit or a new-impact angle on the async sliver. Reports: `04-LEVERAGE-CORE-TRACE.md`, `03-CHAIN-HUNT.md`, `01-RESIDUAL-BLINDSPOTS.md`. See [[feedback-trace-the-flow-not-confirm-the-template]] (the harness must not be biased — vacuity+config-parity checks are mandatory).
