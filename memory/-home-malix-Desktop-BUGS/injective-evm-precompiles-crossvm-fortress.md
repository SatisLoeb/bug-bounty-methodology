---
name: injective-evm-precompiles-crossvm-fortress
description: Injective native-EVM precompiles (bank/exchange/staking) + cross-VM atomicity AUDITED 2026-06-29 at mainnet commit 3ade14d/v1.20.0 = earned executed fortress-null; EVM/erc20 were OOS in the only public C4 contest so this surface was genuinely un-audited; one dormant cross-margin lead
metadata: 
  node_type: memory
  type: project
  originSessionId: 10b90010-ed37-424c-8329-685db3b11d96
---

Audited 2026-06-29 (operator pushed: expand voie off Peggy.sol → the in-scope `injective-core` native EVM). Repo `injective-core-v120-audit/repo` git `3ade14d` == mainnet `node_info` v1.20.0 (LCD `enable_call:true`, erc20 module live with ~100 `factory/` pairs). **The EVM (`modules/evm/**`) + erc20 (`modules/erc20/**`) modules were EXPLICITLY OOS in the Feb-2026 C4 "Injective Peggy Bridge" contest** (scope.txt = peggo + peggy only) — so this surface is genuinely un-audited by the public contest, NOT a C4 dup. It IS in-scope for the `injective-core` Cantina bounty (operator-confirmed; v1.20.0 is an official release, not a beta).

**EXECUTED verdicts (gates PIERCED, not blessed — the operator's 2 novel-reading warnings forced execution over agreement):**
- **bank precompile** (`evm/precompiles/bank/bank.go`): `transfer(from,to,amt)` takes an arbitrary `from` arg, authorized ONLY by `denom=GetBankDenom(caller)`. Looked like a Critical (register attacker contract C↔valuable denom via permissionless `CreateTokenPair`, then `transfer(victim,attacker)` drains). **NULL — over-claim caught by the unbiased simnet PoC.** The internal `erc20/keeper/keeper.go createTokenPair` (NOT the msg_server.go wrapper I first read) type+authority-dispatches: native `inj`→unregisterable; **peggy/IBC→permissionless BUT force the owner-less FixedSupplyBankERC20, custom erc20 forbidden** (so USDC/USDT/bridged un-drainable); tokenfactory→admin-only (self-rug of own denom = not a vuln). mint/burn are self-scoped (`MintERC20` hardcodes `denom="erc20:"+caller`). See [[feedback-authz-split-across-layers]].
- **exchange precompile** (`evm/precompiles/exchange/exchange.go`): DOUBLE-gated. `validateAndDispatchMsg`→`authzKeeper.DispatchActions(caller,msg)` binds `caller==msg.Sender`-or-authz-grant; AND `MsgWithdraw/etc ValidateBasic`→`CheckValidSubaccountIDOrNonce` binds `subaccountID.owner==sender` (common_utils.go:77 `bytes.Equal`). `withdraw(sender=attacker,subaccountID=victim)` fails ValidateBasic; `sender=victim` fails DispatchActions. `GenericExchangeAuthorization.Accept` enforces `hold<=SpendLimit` and decrements (orders compute hold via castCreateSpotOrderParams). The #134 act-as-another is genuinely closed here.
- **cross-VM atomicity**: store-atomic. inj-fork `cosmossdk.io/store => github.com/InjectiveLabs/cosmos-sdk/store v0.50.14-inj.10` cachemulti `Clone()/Restore()` iterate EVERY substore via `BranchStore` (KV+ObjKVStore+transient+mem all mounted in the multistore → all in `cacheMS`). `ExecuteNativeAction` journals a `nativeChange{snapshot}`; `nativeChange.Revert→revertNativeStateToSnapshot→cacheMS.Restore`. EVM revert restores the precompile's Cosmos store writes. NOT the Evmos cross-VM desync bug.

**DORMANT LEAD (re-run WHEN cross-margin enables — same trigger as [[injective-exchange-firmaudit-parked]]):** the ONLY keeper state outside `cacheMS` is the exchange `riskEngine *risk.Engine` in-memory **cross-pool snapshot cache** (`Build/EvictCrossPoolSnapshotCache`). It escapes the EVM revert (in-memory, not a store) → a precompile action that populates/evicts it, then reverts the EVM tx, could leave a stale snapshot for a later same-block op. Mechanism is real, BUT cross-pool = **cross-margin**, which is OOS + not enabled on mainnet (single-margin uses `EffectiveProfile→default`, never builds a cross-pool snapshot) → no mainnet subaccount populates the cache → not production-reachable → instant-kill NOW. When cross-margin goes live: re-examine the cross-pool cache for an EVM-revert (and tx-revert / ante-revert) desync.

VERDICT: the whole Injective SC + native-EVM-precompile + cross-VM surface is an earned executed null. Per [[feedback-target-diet-is-the-binding-constraint]], Injective is exhaustively worked (this + Peggy ×5 + [[injective-exchange-firmaudit-parked]] + web) → EV is elsewhere. Related: [[apparatus-is-packaging-not-discovery]] (the manual poke off Peggy found this whole un-audited surface), [[injective-cantina-web-bounty-rugpull-signal]] (weight p_bounty down).
