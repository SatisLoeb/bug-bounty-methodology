---
name: monad-node-bounty-triage-go
description: Monad Consensus&Execution Cantina bounty ($1M) triaged GO; 2 P1 survivors (parallel-exec relaxed-merge determinism + JIT↔interpreter under Monad pricing); deep pass deferred by operator
metadata: 
  node_type: memory
  type: project
  originSessionId: 61439117-954c-497d-af2e-838e1dfc23d9
---

**Monad Consensus & Execution Bug Bounty** (Cantina Public Bounty, LIVE) — https://cantina.xyz/code/31f19cea-dda2-4568-8f99-27dafd120c97. Ceiling Critical $1M / High $100k / Medium $35k. **$100 deposit/submit; operator Cantina balance was $60 → MUST top up before any submission** (rejects burn the slot). Workspace: `~/Desktop/BUGS/monad-node-audit/` (repos cloned: monad-bft Rust + monad C++ @ v0.15.0).

**2026-07-04: firmaudit Phase 0→S→T triage = GO** (see `TRIAGE-CARD.md`). Operator chose **STOP at triage**; deep pass (Pass 1-4) deferred (`STOP-AT-TRIAGE.md`). Not a fortress. 7 audits + Code4rena + 248 findings + 44 researchers ~8mo live → dup-risk high on any NAMED class, so the living bug is a whole-system determinism seam (the AXIOM). No AI-bot. Last published audit = 202602; v0.14→v0.15 = 763 files ALL post-audit.

**Program's governing seam rule:** monad C++ is only valid via monad-bft Rust — a C++ bug counts only if input first passes monad-bft. Kills raw-malformed-RLP-to-C++ findings.

**Two P1 survivors (both Critical-ceiling, ORDINARY-USER-reachable = dodge every validator/>1/3/DDoS OOS carve-out, both confirmed-fresh + confirmed dup-gap, hand-verified by me):**
- **P-01 parallel-exec relaxed-merge determinism** — optimistic-concurrency (Block-STM-like) `try_fix_account_mismatch`+`min_balance` balance-relaxation. Confirmed **zero test coverage** (grep of test dirs = 0). Path: `execute_block.cpp:224`→`execute_transaction.cpp:441-481`→`block_state.cpp:149-185` can_merge→`state.cpp:641-701`. First-Q: find a balance-observing/branching read that escapes BOTH `validate_exact_balance` and `record_balance_constraint_for_debit` min_balance → schedule-dependent state root.
- **P-02 JIT↔interpreter divergence under Monad pricing** — `vm.cpp:112-160` execute_raw picks JIT-vs-interpreter on PER-NODE cache/compile state (incl compile-fail→interpreter fallback) → 2 honest nodes run different paths same tx. Devs' differential fuzzer oracle = evmone over **EVM revisions ONLY**, never MonadTraits pricing (pricing-v1@MONAD_SEVEN, MIP-3, 128KB code, higher cold costs). Compiler whole-block static gas (`basic_blocks.hpp:415`) vs interpreter per-instruction; devs accept OOG↔FAILURE divergence (`assertions.cpp:98-112`). First-Q: differential harness parameterized on MonadTraits (not evm_revision=covered), target dynamic-gas/OOG boundaries.

**SHARED ROOT:** P-01+P-02+P-04 converge on the **Monad reserve-balance/pricing subsystem** (freshly added post-audit, consensus-critical). **Recommended first open (deferred):** ONE differential harness over fresh Monad-priced code diffing the MonadDB state root across both axes → observed state-root divergence = ideal triager-proof PoC. PoC bar: full-node/monad-solonet mandatory for High/Crit.

**2026-07-04 Pass-1 (POSTURE-faithful, trace-the-flow):** execution/staking region = FORTRESS-NULL, 4 deep traces closed-with-artifact (relaxed-merge INVARIANT R; infinite-mint doubly-guarded Rust-exact-pin+C++-cap=honest-25; staking reward-acc composition; accumulator refcount fails-closed via calculate_rewards checked_sub). Dev hardened every accounting path. **P-02 (operator chose to go all-in, treat as THE target) read-only structural discovery DONE (`analysis/p02/P02-SEAM-AND-DISCOVERY.md`):** obvious MonadTraits divergences CLOSED by reading — static+storage gas tables SHARED (opcode_table<traits>), MIP-3 memory hand-asm `_raw_mip3` (context.S:96-154) MATCHES C++ `expand_memory<MIP3>` line-for-line (`word_count>>1` linear cost, 8MB bound, order). MIP-3 = only Monad-version-specific hand-asm. Residual surface needs the DIFFERENTIAL HARNESS (bloc-6 observe): code-size 24KB→128KB (evmone caps 24KB, structurally un-fuzzed), multi-frame MIP-3 8MB boundary, SSTORE refund under Monad pricing, OOG boundary. Harness = interp-vs-compiler under MONAD_NINE+ trait (NOT evm_revision — that's evmone-covered). BLOCKED on C++ build — root cause = DISK: single fs 233G **100% full (was 1.2G free)**; a monad build needs several GB. Attempted build (clang-18, gcc-15 absent; Make generator, ninja unavailable via pip/PEP668; submodules init'd then ENOSPC). 2026-07-04 operator chose "free my footprint": deinited submodules + rm evmone + rm .git/modules → freed 3.7G, disk now 4.9G free, third_party 7.8M. To RESUME harness: re-`git submodule update --init --recursive --depth 1` in monad/ + re-clone third_party/evmone (category-labs/evmone branch v0.18.0-category) + init its submodules, then `CC=clang-18 CXX=clang++-18 cmake -G "Unix Makefiles" -B build -DMONAD_COMPILER_TESTING=ON`, build target `monad-compiler-fuzzer` (or minimal `monad-vm` first to validate clang-18 on C++23). **P-02 DIFFERENTIAL BUILT + RUN 2026-07-04 → EARNED NULL on deployed surface.** Minimal target `vm-diff-test` (monad-vm leaf libs + monad_core + evmc::mocked_host + gtest, NO monad_execution/evmone). Equivalence GROUNDED in execute_final (success_bit + gas_left + gas_refund + state-on-mutual-success; raw status OOG≠FAILURE is NOT consensus-visible). First-run divergences = CONFIRMED harness artifacts (raw-status compare + non-rollback state); after fix, ALL 90 tests PASS. Clean corpus: MIP-3 8MB boundary, LargeCodeSweep 24-128KB (evmone-uncoverable), SstoreRefundSequences, ~5000 fuzzed programs. Interp≡compiler. One LOW anomaly: MONAD_NEXT (rev10, UNDEPLOYED) segfaults in interpreter sstore MIP-8 path (musttail+clang-19 or dev-incompleteness, not production-reachable). RESIDUAL not-swept: CREATE/large-initcode, nested-CALL cross-frame memory, precompiles, exact-gas-boundary sweep. Full detail in analysis/p02/P02-SEAM-AND-DISCOVERY.md. **Sibling intersection = parallel-exec↔state-merge RELAXED-MERGE determinism (execute_transaction.cpp:335-338, triage P1, zero-test-coverage) — higher value, NOT yet probed.**

**2026-07-05 RELAXED-MERGE DIVE (analysis/p02/RELAXED-MERGE-RESERVE-DIVE.md):** core relaxed-merge invariant VERIFIED SOUND — every balance READ forces exact via State::get_balance (state.cpp:227), only debits go relaxed (record_balance_constraint_for_debit), min_balance math correct (=debit+debits_so_far−credits_so_far), SELFDESTRUCT uses get_balance (exact, hypothesis refuted). Core = earned null. **LIVE seam = Monad reserve-balance (MONAD_NINE+ precompile 0x1001 DEPLOYED):** TWO impls of the dip predicate — ENFORCEMENT = full-scan dipped_into_reserve (evm.cpp:275,335), PRECOMPILE 0x1001 = incremental cached rb_.has_violation(); flags (use_recent_code_hash_/selfdestruct-exemption) set separately in init_from_tx → can diverge. OPEN THREADS (no proven bug yet): (1) full-scan EXEMPTION bypass = spend locked reserve — selfdestruct+current-incarnation exemption (reserve_balance.cpp:96-100, MONAD_NINE) + post-tx effective_code_hash (72-75, MONAD_EIGHT); (2) full-scan-vs-cached divergence impact bounded unless a value contract consumes 0x1001 (staking? unverified). Next = thread 1 reachability + get_max_reserve/incarnation trace + full-node PoC. **2026-07-05 RESOLVED via flow-trace (no-hypothesis) = EARNED NULL.** get_max_reserve ignores addr → flat 10 MON constant (per-account UNIMPLEMENTED TODO). Reserve×relaxed-merge SAFE: full-scan reads get_original_balance+get_balance → both force set_validate_exact_balance → every reserve-subject account touched becomes EXACT → can't relaxed-merge → concurrent change forces RE-EXECUTION → reserve re-validated on real balances (stale-check hole closed by design). Relaxed survives only for non-subject contracts (no reserve). Two-impl divergence (selfdestruct-exemption order full-scan vs incremental) INERT: divergent state {destructed+current-incarnation+delegated-code} unreachable (EIP-3541 blocks 0xEF code on CREATEd/current-incarnation accts; 7702 delegation IS 0xef0100). Thread-1 reserve-escape: no bypass (EOA can't become non-delegated contract; exemption needs current-incarnation). Precompile 0x1001 read-only, no in-scope consumer. Residual only-if: get_max_reserve wired per-account (the TODO) OR a value contract consumes 0x1001. METHOD LESSON confirmed: tracing the value surfaced the exact-forcing safety + EIP-3541 kill; hypothesis-hunting would have mis-declared a bypass.

**2026-07-05 STAKING DIVE (analysis/p02/STAKING-DIVE.md, no-hypothesis flow-trace):** deep trace of the staking precompile (STAKING_CA, delegate/undelegate/withdraw/claim/reward). Synthetix reward-per-token accumulator + Monad refcounted per-epoch accumulator snapshots + delta-epoch activation. Every candidate anomaly the trace surfaced was REFUTED by a discovered safety mechanism: (1) delegate inflates val.stake immediately but reward denominator = consensus_view.stake (snapshot) not val.stake; (2) increment-time accumulator placeholder FINALIZED at epoch boundary (syscall_on_epoch_change:1648 sets acc.value = boundary val.acc for next & next_next epoch); (3) finalization domain (valset_snapshot) aligns with earning set (this_epoch_view = snapshot during delay/consensus during main; syscall_snapshot moves earning consensus→snapshot); (4) reward_invariant per-validator solvency guard (unclaimed_rewards>=rewards); (5) external MON↔internal stake: stake=msg_value, transferred to STAKING_CA by pre_call, DELEGATECALL/CALLCODE/STATIC REJECTED (monad_precompiles.cpp:36 kind!=EVMC_CALL||flags!=0). Well-built subsystem, NO CONFIRMED BUG. **2026-07-05 EXECUTED INDEPENDENT ORACLE:** the devs' model-based fuzzer computes expected rewards by re-reading the contract's OWN accumulator + same x*(q-p)/UNIT_BIAS formula (model.cpp:235) -> verifies conservation + impl-consistency but is STRUCTURALLY BLIND to design-level mis-attribution (A earns B's share, total conserves). Built 6 independent-fair-share TEST_F oracles in test_staking_contract.cpp (target `test_staking_contract`); patched 8 clang-19-vs-gcc-15-libstdc++ gaps (value_or({}), append_range, insert_range, delegation `<span>`). **ALL 6 PASS:** symmetry (equal stake->equal reward), no-earn-before-activation (+1 main & +2 delay), withdrawal-unbonding-fair (the "keeps earning" mechanism earns exactly fair share not double), undelegate+redelegate-no-double-earn, compound-no-early-earn. Attribution EXECUTED-verified robust — independent judge not sharing their accumulator model. Run: `./build/category/execution/test_staking_contract --gtest_filter='*oracle_*'`. Residual = full random fair-share fuzz.

**2026-07-05 PARALLEL-EXEC STATE-MERGE DETERMINISM (P1, analysis/p02/PARALLEL-MERGE-DETERMINISM.md):** pivoted to the P1 "zero-test-coverage" seam. Block-STM shape: parallel execute + strict tx-index-ordered merge (boost::fibers promise chain, execute_block.cpp:215). Determinism enforced by a HARD crash-assert `MONAD_ASSERT(block_state_.can_merge(state))` on retry (execute_transaction.cpp:474) — re-execute-with-correct-state must reconcile. Failure mode = SEVERE: any untracked read (state read not recorded into original_) → crafted 2-tx block diverges optimistic-vs-retry → assert fires on every node → CHAIN HALT. But read-set VERIFIED COMPLETE: recent_account_state→original_account_state records any account read (nonce/code_hash/balance/exists); storage via State::get_storage→original_storage INCL. staking/reserve precompile reads (StorageVariable::load→get_storage); code via code_hash; balance relaxed w/ min_balance; can_merge (block_state.cpp:149) validates all. EARNED NULL by reading. Only residual = serial-vs-parallel block-level state-root differential (reading can't fully close a subtle non-det re-execution). No untracked-read hole found. **2026-07-05 EXECUTED the parallel-vs-serial differential** (the coverage gap: EVERY block test + Ethereum suite runner = PriorityPool{1,1} single-thread → multi-thread merge determinism UNTESTED). Built `TraitsTest.parallel_vs_serial_determinism_differential` in execute_block_test.cpp: 16-tx block all CALLing one shared SLOAD/+1/SSTORE(slot0) contract (max read-modify-write conflict); no signing needed (execute_block trusts passed senders, static_validate only checks EIP-2 r/s → set r=s=1, chain_id=nullopt); digest=FNV(gas_used,status) — gas encodes SSTORE cold/warm order. 1×serial(1thr)+40×parallel(8thr), assert identical. **PASS all 19 revisions**, and GENUINELY stresses it: num_retries=14-15/16 per run, VARYING run-to-run (14 vs 15) → scheduling non-deterministic, yet receipts IDENTICAL → non-det SCHEDULING + deterministic OUTPUT. Executed-null on the P1 seam under real concurrency. Residual: balance/selfdestruct/cross-account conflict patterns, more threads, TSAN. Harness patches: 8 clang-19 stdlib gaps + the differential test. Run: `execute_block_test --gtest_filter='*parallel_vs_serial_determinism*'`. Added balance-conflict variant (relaxed-merge path, reads recipient balance == M*V) — PASS all 19. **TSAN EXECUTED 2026-07-05:** apt Boost.Context is fcontext-only (uninstrumentable); built Boost.Context 1.83 from source with `context-impl=ucontext`+TSAN → `/tmp/boost_1_83_0/stage/lib/libboost_context.a`; TSAN build via `local-tsan.cmake` toolchain + a patch to `category/core/CMakeLists.txt` check_if_boost_fiber_needs_ucontext_macro overriding Boost::context to the ucontext .a. Needs `sudo sysctl vm.overcommit_memory=1` at binary STARTUP (reverts to 2, only needed for the shadow-mmap). Result: pool-churn (create/destroy per run) gives 6 fiber-lifecycle races (NOT the merge); refactored to reuse ONE pool → **0 data races in the merge** (consensus-critical part race-clean, 15 retries exercised). Only residual = `Compiler::compile_loop` cv "unlock unlocked mutex" = DEV-ACKNOWLEDGED benign (compiler.cpp:137-139, JIT not merge, missed-notify→interpreted→same result) + a TSAN nested-bug SEGV at fiber teardown (TSAN instability). Parallel-exec seam = earned-null verified at output-determinism AND data-race levels. SHELL NOTE: shell snapshot has `set -e` — guard pkill/grep/pgrep with `|| true`. BUILD NOTE: multiple concurrent `make` in one build-dir corrupt each other → stale binaries; `pkill -f 'make -C build'` misses re-parented children — use `killall -9 make cc1plus clang++-19`. The TSAN CMake patch (Boost::context→ucontext override in category/core/CMakeLists.txt) is GLOBAL and breaks the non-TSAN `build/` (undefined `__tsan_*` at link) — revert it (`git checkout`) before rebuilding `build/`.
**2026-07-05 CONFIRMED FINDING (first concrete of the dig): epoch-jump finalization gap.** `syscall_on_epoch_change` (staking_contract.cpp:1629) validates next_epoch but too weakly (`<= last_epoch` reject, NOT `== last_epoch+1`); finalization only does acc(next_epoch)+acc(next_epoch+1) (look-ahead-2, assumes +1). A jump E→E+50 never finalizes acc(E+1..E+49) → they keep the delegate-time placeholder → a delta activating in the skipped range earns PRE-ACTIVATION rewards (theft-of-yield / or SolvencyError DoS). EXECUTED PoC `StakeLatest.poc_epoch_jump_skips_accumulator_finalization` in test_staking_contract.cpp: disconfirmer (oracle_no_earning_before_activation_main, +1) → LATE rewards==0 PASS; PoC (jump) → LATE rewards NON-ZERO, FAIL = demonstration. Doc: analysis/p02/FINDING-epoch-jump-finalization-gap.md. REACHABILITY = open leg: next_epoch comes from a SYSTEM_SENDER-signed system tx (key = consensus, out of repo); static_validate_monad_body also doesn't check next_epoch. Medium floor (proven incomplete-validation + fund consequence), High ceiling IF key validator-held + consensus doesn't enforce consecutiveness. Fix: enforce ==+1 or finalize all epochs in (last,next]. **REACHABILITY CLOSED 2026-07-05 (dug monad-bft, present locally at ../monad-bft): NOT exploitable → defense-in-depth Low, DO NOT submit as Med/High.** Chain: on_epoch_change.new_epoch = block_header.epoch (monad-system-calls/lib.rs:173); every proposal validated `proposal_epoch == epoch_manager.get_epoch(proposal_round)` (signing.rs:433,512 InvalidEpoch reject); get_epoch returns from epoch_starts built ONLY by schedule_epoch_start = current+1 at boundary blocks (epoch_manager.rs:75); schedule runs on every committed block (consecutive seq_nums → no gap); PLUS validate_system_transactions_input re-generates expected syscalls from header.epoch and compares (validator.rs:222) so a forged tx (SYSTEM_SENDER_PRIV_KEY is hardcoded/public) is rejected unless epoch matches consecutive value. Consensus guarantees strictly +1; genesis 0→N jump carries no pending state. The exec-layer incomplete-validation is real but upstream-mitigated → hardening note only. LESSON: the reachability dig did its job — avoided over-claiming a Medium that a monad-bft-tracing triager would dismiss (credibility preserved). Both maxims satisfied (didn't over-claim bypass, didn't concede un-exhausted — traced the full consensus chain). META: darkside/kill-gate reachability discipline works; execution-layer "missing invariant enforcement" findings on a node MUST be traced into the consensus repo before severity-commit. UNTRACED residual (ranked): (1) decrement_accumulator_refcount returns 0 on refcount==0 w/ only LOG (588) — devs anticipate it; hunt increment/decrement imbalance in compound-boundary-double-apply / redelegate-after-full-undelegate / withdrawal_id-reuse; (2) syscall_snapshot std::partial_sort determinism on equal-stake ties + Nth-stake cutoff (consensus-critical); (3) addValidator BLS/secp key-proof crypto; (4) externalReward griefing; (5) changeCommission timing; (6) dust accumulation stranding unclaimed_rewards.

**Harness now WRITTEN** (`monad/test/vm/unit/monad_interp_vs_compiler_diff_test.cpp`, registered in that dir's CMakeLists under `if(MONAD_COMPILER_TESTING)`): derived `InterpVsCompiler<T>` fixture reusing evm_fixture.hpp, `run_diff` compares Compiler vs Interpreter (status/gas_left/gas_refund/output/storage) under `MonadEvmRevisionTypes` (auto-runs MonadTraits<MONAD_NINE>); cases Mip3MemoryBoundary + FuzzDynamicGas(2000 seeds). Full resume recipe in `analysis/p02/P02-SEAM-AND-DISCOVERY.md`. Confirmed the existing fixture `execute_and_compare` only does Compiler-vs-Evmone-at-EVM-rev (EVM pricing) = why the gap is real. Harness = instantiate Monad VM with MonadTraits<MONAD_NINE> in BOTH interp+compiler mode, compare directly (fuzzer's fuzz_iteration compares vs evmone keyed on monad_eth_revision = EVM-only, that's why it can't reach MonadTraits). Needs ~5-10G free (or minimal VM-only build ~2-3G). monad-bft clone kept.
DROP: mempool race (architecturally neutralized). P2 also-rans: RLP-blockval (7702 accept/reject gap, dup-max), TFM-reserve (dev TODO at 7702 seam), MonadDB ring-shrink, FFI triedb prefix_key OOB (Med cap), consensus-core optimistic-Voted-commit reorg (formally verified). Relates to [[feedback-audited-target-hunt-invariant-not-class]], [[protocol-fortress-null-hunt]].
