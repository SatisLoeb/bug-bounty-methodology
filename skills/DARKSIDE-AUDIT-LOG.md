# DARKSIDE-AUDIT-LOG — darkside applied to the skills themselves (the tooling)

Record of which skills have been darkside-audited (target = the skill, not code), so it is not redone.
A future "did we darkside the skills?" check finds THIS file.

| skill | date | verdict | findings | fixes applied |
|-------|------|---------|----------|---------------|
| darkside (SELF-audit) | 2026-06-23 | 2 findings (1 ironic completeness-hole + 1 Door-C-on-itself) + F-3 already done | DS-3 (the ironic one) §0.5.6 mandates "run COVERAGE-LEDGER-PLAYBOOK.md" + §0.5.3d "run SURFACE-INVENTORY-PLAYBOOK.md" — BOTH files MISSING (the completeness-discipline skill has a completeness hole in its OWN refs; the method survives inline + in DARKSIDE-HUNT.md which exists, 514L) · DS-1 Door C's deviation enumeration had no completeness ledger (≠ §0.5.6 for files) and forbids fan-out → coverage rested on one reader noticing the deviations · DS-2 corpus correctly absent (F-3 done). darkside is the most self-aware skill (§0.5 = 10 self-guards; §5 re-run-§0.5-before-null). | applied: DS-3 redirect both broken refs to the inline discipline + DARKSIDE-HUNT.md · DS-1 canonical-form-as-deviation-ledger (no fan-out — listing canonical components ≠ pattern-matching-for-bugs, so it doesn't suppress the surprise). **DS-3 CORRECTED 2026-06-23 (§0.5.3d caught MY OWN coverage error): the playbooks were NOT missing — they EXIST in methodology-backup/skills/ (7169o/7590o); my "missing" read only checked ~/.claude/skills/, a SUB-SPACE. Real cause = SYNC GAP (sync-skills.sh installed validated skill-DIRS to LOAD but never the root .md). Fixed properly: cp'd both to ~/.claude/skills/ + patched sync-skills.sh to `find -maxdepth 1 -name '*.md' -exec cp` into LOAD. Reverted the false "NOT materialized" note to a clean pointer. The completeness skill's completeness hole was REAL (refs broke) but my diagnosis ("never written") was the §0.5.3d failure it warns about.** **RE-PASS 2026-06-23 (operator nudge "tu as audité darkside?" — I had CLOSED TOO EARLY A SECOND TIME, EXACTLY LIKE EPM): bottom-of-surface (which I should have run the first time) found DS-4 + DS-5 I missed by checking only the 2 playbook refs, not ALL refs — the §0.5.3d failure AGAIN, on darkside itself. DS-4: 3 `feedback_*` refs (§0.5.8/0.5.9/0.5.10 "Full rule:") TRULY MISSING (verified wide across ~). DS-5: the `[[capability-vs-view-the-paying-vein]]` memory (§2.2 "the authority") MISSING. Both part of a PROJECT-WIDE gap (below). Numbering §0.5.1-10 consecutive ✓, no duplicate headers ✓, firmaudit §🌑 pointer consistent ✓. NOT fabricated (these are the operator's own lesson files) — the rules survive inline; real fix = machine-2 sync.** |
| firmaudit | 2026-06-23 | 3 findings (2 root-shared + 1 self-inflicted) | F-1 prose-MANDATORY≠enforced · F-2 rule-accretion dilution · F-3 catalogue-hook-in-thief-mode | F-1+F-2: `firmaudit-spine.sh` (mechanical process-artifact coverage) + SPINE skeleton at top + wired into §7/NO-GO/First-action. F-3: corpus hook moved top→Pass-2 (2F), reframed. |
| gravedigger | 2026-06-23 | 4 findings (1 structural + 1 latent-refuted + 2 minor/self-inflicted) | G-1 no seam-thesis/discovery-door (pure catalogue-mode; web path lost firmaudit's Phase-S fix) · G-2 auto-execute vs hardcoded tools, no existence-check (REFUTED-live by execution: all tools exist here; LATENT on machine-2/fresh-install) · G-3 non-linear phase numbering vs `--phase N` doc · G-4 corpus hook domain-mismatched (SC class-density on a web skill) | ALL 4 applied: G-1 Phase 1.5 Web Seam Thesis + darkside pointer · G-2 tool-existence guard · G-3 `--phase N` doc fixed · G-4 hook→H1-HUNTING+precedent-scan. |
| mrrobbot | 2026-06-23 | 4 findings (1 structural + 1 reframe + 1 latent-refuted + 1 minor) | M-1 no Door-C/darkside pointer on the 14-audit-survivor class (only Door-A check-matrix+mirror, both need a sibling; misses the no-sibling composition bug) · M-2 corpus/pattern-scan opening contradicts its own "not surface patterns" principle · M-3 "MANDATORY run these" × 8 tools, no existence-check (REFUTED-live: all exist; LATENT) · M-4 Pattern-DB table lists both stale C4-HUNTING(128) + current corpus(163), same lineage | ALL 4 applied: M-1 Door-A-vs-Door-C + /darkside pointer · M-2 reframe (catalogue=surfacer, check-matrix=aim) · M-3 tool-existence guard · M-4 supersede note. |
| power, extract, invfuzz (the 3 dictated new veins) | 2026-06-23 | CLEAN (no fix) — operator-dictated, self-honest | power: corpus correctly absent (execution-decides thesis like darkside — its whole rule is "never infer authz from a modifier"; a catalogue prime would undercut it); SELF-NOTES its own grep-completeness hole ([trou NoOnes]) — observation surfaced to operator (a mechanical "enumerate all storage-writers then score" would close it), NOT auto-edited (dictated method). extract: corpus absent correct (it IS a math-method/5-gate chain, not the generic class-selector EPM-2 is). invfuzz: corpus absent correct (diff-harness method). All 3 carry honest DETTE-DE-VALIDATION notes. No fix — don't rewrite a dictated, self-honest vein. |
| wide, orca (2 dictated new veins) | 2026-06-23 | corpus-fixed during the systematic check (no full darkside finding) | wide: CAVEATED (corpus=SC-surfaces only; multi-surface web/mobile uses H1-density). orca: corpus REMOVED (package-router, not protocol-shape density). Both carry honest validation-debt + the verify-agent-output discipline (orca P0). No other finding. |
| disclose (security-disclosure) | 2026-06-23 | 1 system-rule violation + 1 minor + corpus-OK | DIS-1 outcome schema lacks `composition_skills_applied` (writes to the CORRECT OUTCOMES.jsonl, unlike immunefi-submit, but still violates SKILL-SELF-ATTRIBUTION — the mandatory field absent) · DIS-2 two overlapping quality gates (22-pt rubric + 24-pt PREFLIGHT, ~90% same criteria) · corpus correctly absent (formatter) | applied: DIS-1 add composition_skills_applied:["disclose"] + mandate · DIS-2 cross-ref note (24-pt PREFLIGHT is canonical). |
| immunefi-submit | 2026-06-23 | 1 HIGH provenance-hole + 1 minor + corpus-OK | IMM-1 (HEADLINE) outcome logging writes to `BUGS/SKL/outcomes.jsonl` (4 rows, self-labelled "canonical") which is NOT the pipeline-canonical `BUGS/OUTCOMES.jsonl` (38 rows), AND 0 `composition_skills_applied` → violates the SKILL-SELF-ATTRIBUTION system rule (the exact provenance-hole that rule exists to close, worse: a separate file/schema, invisible to `grep OUTCOMES.jsonl`) · IMM-2 "21-Point Rubric" scored /22 · corpus correctly absent (formatter, doesn't hunt) | applied: IMM-1 redirect→OUTCOMES.jsonl + schema aligned (program→protocol, payout→reward, +channel, +composition_skills_applied:["immunefi-submit"]) · IMM-2 →22-Point. ⚠ 4 legacy SKL rows left for operator to migrate (no silent write to canonical track-record). |
| screenshot | 2026-06-23 | 1 mild (staleness, chill-class) + SS-2 stale-ref + corpus-OK | SS-1 the "anti-AI classifiers can't flag a .png" premise (purpose #2) is outdated — 2026 multimodal triage OCRs the image + re-runs the text-classifier, AND a consistent terminal prompt/theme/font is a cross-submission fingerprint (chill C-1 in pixels). The robust value (independent verification + effort signal, purposes #1/#3) holds regardless. SS-2 (§0.5.3d caught my own miss) line 292 "rare per user's boycott rule" was STALE (boycott LIFTED 2026-06-01). corpus correctly absent. | applied: SS-1 caveat (lean on verification+effort, vary the terminal surface) · SS-2 boycott-lifted correction. |
| exploit-primitive-mindset | 2026-06-23 | 2 findings (1 Door-C consistency + 1 corpus-GAP) — EPM-3 self-refuted | EPM-1 the HARDENED path (MR-1..5) is Door-A only (catalogue + check-matrix + prior-audit), no Door-C/darkside pointer — same gap as gravedigger G-1/mrrobbot M-1, on the class-selection front · EPM-2 (the INVERSE corpus finding) the class-SELECTION skill (Step1 type→Step2 primitive = class-density-by-shape) is the skill that MOST needs the corpus, yet had NO hook — my earlier systematic-check MIS-classified it "correctly NO corpus" (§0.5.3d caught my own coverage error) · EPM-3 (Step-4 difficulty-filter vs Rule 40) SELF-REFUTED: Step 4 skips the COMMODITIZED/easy class + cadences the long-cycle = Rule-40-compliant | applied: EPM-1 MR-6 /darkside (Door C) pointer · EPM-2 corpus class-density backing + --methods in Step 2 (data for the type→primitive pick, before code). **RE-PASS 2026-06-23 (operator nudge "tu as fait le tour?" = §0.5.4 'operator pushing once = you closed too early' — I HAD): EPM-4 L63 "four specific places" but 6 MR classes (my MR-6 worsened it) · EPM-5 DUPLICATE "Integration with existing workflow" section (L536 superseded by L581 "(updated)") — missed entirely first pass · EPM-6 no tool-existence guard (inconsistent w/ gravedigger/mrrobbot/upshift). All 3 fixed. Lesson: a bottom-of-surface pass is mandatory PER skill, not just the headline finding.** |
| chill | 2026-06-23 | 1 moderate Door-C + 1 mild staleness + corpus-OK | C-1 (Door C, the interesting one) chill defeats the anti-AI classifier by ADDING human tells, but the P1-P9 set is FINITE+REPEATABLE → applied identically across reports it becomes its OWN cross-document fingerprint (the "chill template"); the skill handles INTRA-doc over-use (F5) but not CROSS-doc signature · C-2 F1-F12 calibrated to the 2026-05 classifier, no recalibration cadence as classifiers adapt (may flag the over-roughened style itself) · corpus correctly absent | applied: C-1 Principle 6 (vary the SET across reports, not just within) · C-2 recalibration-cadence note (moving target). |
| report-nerve | 2026-06-23 | 1 mild hardening + corpus-OK | RN-1 the closing-gate grep is framed as the completion condition ("draft not complete until all 3 greps clean") but a syntactic grep catches only LISTED phrasings — same self-flagellation/concession/overclaim survives in unlisted wording (the D9/Superform lesson: syntactic-clean ≠ semantic-clean). The chill auto-invoke is a BENIGN internal styling composition (not outward — NOT the U-1 bug). Corpus correctly absent (writing skill). | applied: RN-1 reframe grep as necessary-not-sufficient BACKSTOP + point to semantic Rule 1-4 read + D9 as the real gates. |
| expand-surface | 2026-06-23 | CLEAN (no fix) — E-1 REFUTED by execution | E-1 (the dead-dispatcher hypothesis): all 11 destination playbooks EXIST + substantial (110-208 LOC each), the "skeleton" tools exist, the "not built" ones are HONESTLY marked → not a dead dispatcher. E-3 corpus correctly absent (novel ZK/MPC/compiler surfaces not in corpus). It is the MODEL: it marks tool status, which gravedigger/mrrobbot did NOT. No fix applied (don't touch what's clean). |
| upshift2 | 2026-06-23 | CLEANEST so far — 2 minor consistency fixes only | U2-1 (mild) /disclose+immunefi+security-disclosure at Phase 6 not explicitly operator-gated (but already says "does not auto-orchestrate mid-hunt" — no U-1 cascade bug) · U2-2 companions/vector-packs no existence-check (REFUTED-live: all 7+4 exist; LATENT) · U2-3 corpus correctly ABSENT (multi-surface, SC delegated to mrrobbot) | applied: U2-1 disclosure-channels OPERATOR-GATED · U2-2 tool-existence guard. upshift2 is discovery-PURE (the seam-thesis generalized) + already had the no-auto-orchestration fix upshift lacked. |
| upshift | 2026-06-23 | 2 findings (1 HIGH cross-skill conflict + 1 latent-refuted) + corpus-OK | U-1 (HEADLINE, the strongest of the series) auto-orchestrates /gravedigger+/mrrobbot+/disclose AND auto-activates on conversation mention — DIRECTLY contradicts firmaudit's NON-NEGOTIABLE no-auto-orchestration rule ("NOT spawn their disclosure flows, NOT let one skill's auto-orchestrates-X line fire X"); runaway-cascade risk = a mention of "executor wallet" → upshift fires → auto-spawns the outward-facing /disclose flow · U-2 companion files/playbooks no existence-check (REFUTED-live: all 9+3 exist; LATENT) · U-3 corpus correctly ABSENT (off-chain surface; SC-layer delegated to /mrrobbot which has it) | applied: U-1 /disclose OPERATOR-GATED (never auto-spawned) + auto-activation→SUGGEST-not-fire + firmaudit reconciliation note · U-2 tool-existence guard. upshift HAS the discovery door (seam-gap thesis + mirror + gate-pierce) — no G-1/M-1. |

## firmaudit darkside (2026-06-23) — detail

**Gate:** FULL-DESCENT (never externally darkside-audited; verified no prior audit document existed — the only prior tooling-darkside was the OUTCOMES self-attribution traceability audit, CLAUDE.md:246, a different finding).

**F-1 (Door A coverage-gap, root) — PROSE-MANDATORY ≠ ENFORCED.** firmaudit calls Phase S / 4-PASS / Door C / COVERAGE-LEDGER / AUDIT-CONDITIONS "MANDATORY / gating / illegal until", but `preflight-mechanical.sh` + `on-finding.sh` enforce ZERO of them (only the per-finding submission layer D0/D8b/D9). Executed artifact: grep of both scripts = 0 hits for any process-phase artifact. Confirmed: a NO-GO/null/submission can skip the discovery phases and pass every mechanical gate. Same "noted ≠ run / encoded ≠ applied" failure the skill documents in others, turned on itself.

**F-2 (Door C, shares F-1's root) — RULE-ACCRETION DILUTION.** Unwritten invariant: "more MANDATORY rules = more coverage." Disproven by darkside's OWN preamble (firmaudit's §🌑 volet "generalized the germe into a contemplative abstraction that lost the bounded door + the validator → 5 nulls vs the germe's 1 Critical on injective"). 519 lines + darkside 519 + floor + target exceed the attention budget; firmaudit had no dilution-detection. Shared root with F-1: loading/asserting a rule ≠ applying it (the skill's own §0.5.4).

**F-3 (self-inflicted, this session 2026-06-23) — CATALOGUE-HOOK-IN-THIEF-MODE.** The `corpus-query` PATTERN-BANK hook (added 2026-06-23 when wiring the corpus into the skills) sat at the top of firmaudit AND darkside — the two skills whose thesis is that the wins are OUTSIDE the catalogue (darkside: "Catalogue-mode = completeness; Thief-mode = discovery; Door C does NOT fan-out because pattern-matching suppresses the surprise"). Priming catalogue pattern-matching at the entry primed the vector-first failure firmaudit forbids.

**Anti-inflation (disconfirmer on the findings):** F-1/F-2 survive ("the operator catches it" = exactly the failure mode the skill exists to remove, §0.5.4) — honest severity = reliability/hardening, NOT "firmaudit is broken" (it works; the wins prove it). F-3 survives but fix = relocate/reframe, not remove (corpus is legitimately useful in Pass-2 completeness).

## gravedigger darkside (2026-06-23) — detail
- **G-1 (Door C, headline):** gravedigger is catalogue-mode-dominant (11 phases + 37 rules + H1 11-step scan + JWT/OAuth/Container arsenals). It has discovery PRIMITIVES scattered as rules (R14/R19/R31/R34) but NO seam-thesis PHASE that aims the hunt before the catalogue sprays, and no darkside pointer — the exact "vector-first noise on hardened targets" failure firmaudit fixed with Phase S. firmaudit Path C delegates web→gravedigger, so the seam-discovery is LOST in the web path. Helix #134 (authz-grant-as-identity ATO) was a SEAM, no P-H1-* names it. FIX: Phase 1.5 Web Seam Thesis + "/darkside for Door C" pointer.
- **G-2 (REFUTED-live → LATENT):** anti-inflation win — almost claimed live; `ls`-checked all hardcoded auto-execute tools (injection-proxy, redis-scanner, jwt-arsenal, reverse-lookup) → ALL EXIST here → not a live finding. Latent on machine-2/fresh-install (no existence-check before "auto-execute never-ask"). FIX: tool-existence guard (ls-first, note-and-continue, never crash).
- **G-3 (minor):** phases run -1,0,0.5,1,2,3,4,5,**9,7,6** (numbered historically not by run order) while `--phase N` doc said "1-6". FIX: doc corrected.
- **G-4 (self-inflicted this session):** the corpus hook (added when wiring the SC corpus) is domain-mismatched — `corpus-query` has no web/api shape; gravedigger's real bank is H1-HUNTING-PATTERNS. FIX: hook→H1-HUNTING + precedent-scan.

## 🧩 META-FINDING (across firmaudit + gravedigger + mrrobbot)
**firmaudit is the ONLY skill with a discovery door (Phase S seam-thesis + the DARKSIDE volet).** The two skills it DELEGATES to — web→gravedigger, SC>$50K→mrrobbot — both LACKED Door C (gravedigger G-1, mrrobbot M-1). So whenever firmaudit hands off, the seam/Door-C discovery was structurally lost — on exactly the hardened targets where Door C is the only remaining win. NOW FIXED on both (gravedigger Phase 1.5 + /darkside pointer; mrrobbot Door-A-vs-Door-C + /darkside pointer).

## 🔌 CORPUS-INTEGRATION STATUS (checked at EVERY audit, per operator directive 2026-06-23)
The corpus is SC-protocol class-density (amm/lending/bridge shapes). At each skill audit, ALSO check: does the skill have the corpus hook, is it domain-matched, well-placed (not before the discovery aim)?

| skill | domain | corpus verdict |
|-------|--------|----------------|
| firmaudit | SC | matched, RE-PLACED top→Pass-2 (F-3) — catalogue is completeness backstop, not the aim |
| mrrobbot | SC | matched, REFRAMED top→surfacer (M-2) — check-matrix is the aim |
| darkside | discovery | REMOVED (catalogue suppresses the Door-C surprise) |
| gravedigger | WEB | MISMATCH→REPLACED with H1-HUNTING+precedent-scan (G-4) — corpus has no web shape |
| orca | routing-npm | REMOVED (package-router uses deployed-vs-SDK, not protocol-shape density; downstream SC skill pulls it) |
| wide | allocation-multisurface | CAVEATED (corpus=SC-surfaces only; web/mobile surfaces use H1-density) |
| power, extract, invfuzz, upshift, upshift2, expand-surface, report-nerve, chill, exploit-primitive-mindset, screenshot, immunefi-submit, disclose | authz/math/diff/offchain/writing/web | correctly NO corpus (SC-class-density does not apply) |

**Standing check for each future audit:** verify the corpus hook's domain-match + placement (after the discovery aim, never before) as part of the darkside pass.

## ✅ ALL 18 SKILLS DARKSIDE-AUDITED (series complete 2026-06-23)
firmaudit · gravedigger · mrrobbot · upshift · upshift2 · expand-surface · report-nerve · chill · exploit-primitive-mindset · screenshot · immunefi-submit · disclose · power · extract · invfuzz · wide · orca · darkside(self). Every skill: corpus-integration verified per the standing directive (the table above).

**Recurring roots found across the series:**
1. **No discovery-door on the delegated skills** — firmaudit was the only one with Phase-S/Door-C; gravedigger (G-1), mrrobbot (M-1), exploit-primitive-mindset (EPM-1) all lacked it → all now have a /darkside pointer.
2. **Corpus mis-integration from this morning's wiring** — F-3/G-4/M-2 (mis-placed), EPM-2 (missing where it best fits), orca/wide (domain-fixed). All resolved.
3. **Auto-orchestration vs operator-in-the-loop** — upshift U-1 (the strongest finding: auto-spawned /disclose contradicts firmaudit's no-auto-orchestration); upshift2 already correct.
4. **SKILL-SELF-ATTRIBUTION violated by the loggers themselves** — immunefi-submit (IMM-1, wrong file + no field) + disclose (DIS-1, no field). Both fixed.
5. **Tool/ref existence assumed** — G-2/M-3/U-2/U2-2 (latent, tools exist here) + DS-3 (two darkside playbook refs were a SYNC GAP, fixed) + **DS-4/DS-5 PROJECT-WIDE (found only on the operator's 2nd nudge — I'd closed too early on darkside exactly as on EPM): 13 unique `feedback_*.md` "Full rule:" refs across chill/darkside/EPM/firmaudit/gravedigger/mrrobbot/upshift + CLAUDE.md are ALL DANGLING (0/13 present), plus the `capability-vs-view-the-paying-vein` memory. The entire `feedback_` corpus + that memory are a machine-2-sync gap (same class as the disappeared skills). The rules SURVIVE inline (the refs are elaborations); NOT fabricated — fix = sync from machine-2. This means the feedback_/memory ref-axis was UNDER-CHECKED on ALL skills, not just darkside — a known incompleteness in this audit series.**
6. **Anti-AI-detection staleness** — chill C-1/C-2 + screenshot SS-1 (the classifier adapts; the human-signal set is itself a fingerprint).

**Operator follow-ups (not skill-darkside):**
- migrate the 4 legacy `BUGS/SKL/outcomes.jsonl` rows into `OUTCOMES.jsonl` (left un-touched — your call on the canonical track-record).
- commit `methodology-backup` git (all session work: 18 audited skills + corpus --methods + 3149 solodit discovery_how).
- optionally materialize `COVERAGE-LEDGER-PLAYBOOK.md` + `SURFACE-INVENTORY-PLAYBOOK.md` (DS-3), or keep the inline discipline as source of truth.
