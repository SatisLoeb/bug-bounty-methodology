37. **MANDATORY — Weight Card before submission of any non-Informational finding.** This rule applies to every finding claiming severity ≥ Low with a dollar impact. Before submitting, populate the Weight Card template in `REPORT-STANDARD.md` (section "Weight Accounting") and verify the hard gate via `WEIGHT-CARD.md` Stage 2. The hard gate is **at least one numerical anchor**: either **W1** (computed dollar loss with formula and inputs from on-chain reads, API counts, or documented limits) OR **W5** (≥1 paid precedent from H1/C4/Cantina/Sherlock with a dollar figure). Both slots cannot be qualitative. An unpaid "related H1 #xxx" does NOT satisfy W5 — only paid references with dollar amounts count. **DoS findings** may use the W1 alternate anchor (form b): `downtime duration × req/s × affected users` measured from a reproducible PoC counts as numerical anchor. **Smart contract findings** with forge-test state delta assertions auto-pass W1 (the asserted delta IS the computed loss). Dashboard slots **W2** (stakeholder map), **W3** (reversibility audit), **W4** (live conditions proof) are optional but their absence surfaces argumentation weakness pre-submission. **D8 in `PREFLIGHT-CHECK.md` is gating** — D8 fail = submission blocked at claimed severity. Use `arsenal/tools/precedent-scan.sh <class>` to populate W5 fast — its output includes a copy-paste-ready W5 block directly formatted for `REPORT-STANDARD.md`. **Why this rule exists:** Phemex R2 (submitted 2026-04-10, marked Informative 2026-04-13) had a proven chain and would have passed D7, but the draft said *"I have not confirmed whether this enables profitable extraction via margin manipulation, because doing so would require opening leveraged positions with real capital"* — weight was narrated, not computed. Rule #15 existed (honest impact quantification) but fired reactively, post-oversell. Weight Card forces computation proactively. **Chain Proof Gate (rule #36) answers "does the exploit execute?" — Weight Card (rule #37) answers "is the exploit profitable, for whom, irreversibly, and market-priced at what?".** Both gates are complementary; a finding must pass both before submission. **For pure information disclosure findings already claimed at Informational/Low, this rule does not apply** — the severity floor already reflects the absent weight. For every other finding making a dollar claim at severity ≥ Low, this gate is mandatory. **Anti-pattern phrases that fire this rule** (if any appear in the draft without a numerical anchor backing them): "could drain", "could extract", "significant funds", "substantial losses", "large number of users", "many users", "potentially affects", "widespread impact", "catastrophic", "devastating", "severe financial damage". The test: replace "significant funds" with "$3.47" in the draft — if the sentence becomes absurd, the number is doing the work of the argument and must be real.

