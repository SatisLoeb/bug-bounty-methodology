**MANDATORY — Weight Card (all non-Informational findings):** Before any PoC report claiming severity ≥ Low with a dollar impact, populate the Weight Accounting section of REPORT-STANDARD.md per `WEIGHT-CARD.md`. Hard gate: W1 (computed dollar loss with formula + cited inputs) OR W5 (≥1 paid precedent from H1/C4/Cantina/Sherlock with $ figure). Both cannot be qualitative. DoS findings use W1 form (b): `downtime × req/s × users` from a reproducible PoC. Run `arsenal/tools/precedent-scan.sh "<class>"` to populate W5 fast — the script emits a copy-paste-ready W5 block directly formatted for REPORT-STANDARD. **D8 in PREFLIGHT-CHECK.md is gating** — fail blocks submission at claimed severity. See CLAUDE.md rule #37. Complementary to the Chain Proof Gate: Chain Proof = "does the exploit execute?", Weight Card = "is it profitable, market-priced at what?". Both must pass.

```
