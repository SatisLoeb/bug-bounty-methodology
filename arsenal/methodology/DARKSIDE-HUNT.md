# DARKSIDE-HUNT — MOVED

> **This methodology is now the standalone `darkside` skill.** The tactical doc that used to
> live here has moved to **`~/.claude/skills/darkside/DARKSIDE-HUNT.md`**, and the procedure
> (germe, two-door priority rule A>B, thief-inventory admission gate, 5-axes analyzer, manual
> loop, exit gate) lives in **`~/.claude/skills/darkside/SKILL.md`**.
>
> Invoke it with `/darkside`, or it is referenced automatically by firmaudit at Pass-1 on
> audited targets.
>
> This file is kept only as a redirect so older references do not 404. Do NOT edit darkside
> methodology here — edit the skill. A copy maintained in two places drifts silently; the skill
> is the single source of truth.

**Why it moved (2026-06-11):** the volet was re-founded back onto its germe — *mine what the
devs TESTED, list the untested sibling they FORGOT, with the mentality of a thief storing
primitives toward a theft.* The prior version had generalized that bounded, executable idea into
a contemplative abstraction ("places attention fled / max-trust × min-verification / 5 axes")
that lost the finite entry point and the chainability admission gate — and accordingly produced
noise (5 nulls on injective-core where the germe found the only Critical). The skill restores
Door A (defensive-coverage matrix) as PRIMARY, Door B (dev-paranoia-map) as FALLBACK, the 5 axes
as a per-candidate ANALYZER, and the thief inventory as the validator.

See:
- `~/.claude/skills/darkside/SKILL.md` — the procedure
- `~/.claude/skills/darkside/DARKSIDE-HUNT.md` — the tactical catalogue
- `~/.claude/skills/darkside/DEPENDENCIES.md` — external dependency manifest
